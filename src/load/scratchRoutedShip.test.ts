import { describe, expect, it } from "vitest";

// Throwaway: a deliberately failing test for the live routed-ship check (agent-ship item 16).
describe("routed ship live check", () => {
  it("adds two numbers", () => {
    expect(1 + 1).toBe(2);
  });
});
