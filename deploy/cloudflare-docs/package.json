{
  "name": "@coreplane/switchboard-docs-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm --prefix ../../docs run build",
    "deploy": "npm run build && wrangler deploy",
    "verify": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "tail": "wrangler tail switchboard-docs"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
