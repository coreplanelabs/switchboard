# Authorization

One decision, `authorize(actor, action, resource) → allow | deny(reason)`, over a policy table of data rows, replaces every mechanism Switchboard used to decide what a caller may do: command `chatGate`s resolved from `permissions.*` lists, machine-token `scopes`, the machine-caller channel pin, and the `canRunAgent` / `canUseRepo` / `canManageRepos` / `canEditChannelConfig` / `isOperator` helpers ([plan](../docs/plans/2026-09-03-001-feat-authorization-model-plan.md), [#395](https://github.com/coreplanelabs/switchboard/issues/395)). Identity is resolved **once per surface** into a typed `Actor`; every gate is a **policy row**; channel visibility is the **relation `member-of(actor, channel)`** and holds on every surface — Slack, HTTP ingress, MCP, the Access-gated `/api`, the CLI, the schedule shim, and memory reflection. It is the successor of two earlier shapes built for real reasons whose intent it keeps and whose form it retires: KD6 of the golden plan (machines get their own gated identities) and KTD10 of the run-history plan (a token must not read another team's runs, and a denied read is `not_found`). The weekly self-improvement cron seeing zero runs, an Access operator reading a private-channel run, and a private-conversation fact becoming `org` memory are the three leaks the model closes — as consequences of one table, not as special cases.

- **Code**: [`src/core/authz/types.ts`](../src/core/authz/types.ts) (the shared contract: `Actor`, `ActorKind`, `Grants`/`GrantSet`/`NO_GRANTS`, `Action`, `Resource`/`ResourceType`, `ChannelVisibility`, `Condition`, `Rule`, `Decision`, `Predicate`, `ChannelDirectory`); [`src/core/authz/policy.ts`](../src/core/authz/policy.ts) (the table + module-load validation); [`src/core/authz/authorize.ts`](../src/core/authz/authorize.ts) (the one public decision); [`src/core/authz/predicate.ts`](../src/core/authz/predicate.ts) (`predicateFor`, store-side only); [`src/core/authz/grants.ts`](../src/core/authz/grants.ts) (`grantsFor`, the native `grants` block, `translateLegacyConfig`); [`src/core/authz/actor.ts`](../src/core/authz/actor.ts) (per-surface actor resolution, `effectiveGrants`); [`src/core/authz/channelDirectory.ts`](../src/core/authz/channelDirectory.ts) (the seam, `InMemoryChannelDirectory`, the TTL cache). Adapter touch points: [`src/core/commandChat.ts`](../src/core/commandChat.ts) (chat actor), [`src/channels/mcp.ts`](../src/channels/mcp.ts) (token actor), [`src/channels/commandHttp.ts`](../src/channels/commandHttp.ts) (Access browser / service-token actor), [`src/channels/slack.ts`](../src/channels/slack.ts) (`SlackChannelDirectory`, visibility stamped at dispatch), [`src/core/schedules.ts`](../src/core/schedules.ts) (schedule actors declared in the registry), [`src/config.ts`](../src/config.ts) (`grants` schema, `validateConfig` rules, the legacy keys until their removal).
- **Tests**: [`src/core/authz/policy.test.ts`](../src/core/authz/policy.test.ts), [`src/core/authz/authorize.test.ts`](../src/core/authz/authorize.test.ts), [`src/core/authz/predicate.test.ts`](../src/core/authz/predicate.test.ts), [`src/core/authz/grants.test.ts`](../src/core/authz/grants.test.ts), [`src/core/authz/actor.test.ts`](../src/core/authz/actor.test.ts), [`src/core/authz/channelDirectory.test.ts`](../src/core/authz/channelDirectory.test.ts); the adapter tests [`src/core/commandChat.test.ts`](../src/core/commandChat.test.ts), [`src/channels/mcp.test.ts`](../src/channels/mcp.test.ts), [`src/channels/commandHttp.test.ts`](../src/channels/commandHttp.test.ts), [`src/channels/slack.test.ts`](../src/channels/slack.test.ts), [`src/core/schedules.test.ts`](../src/core/schedules.test.ts), [`src/config.test.ts`](../src/config.test.ts); the store side [`src/core/commands/runs.test.ts`](../src/core/commands/runs.test.ts), [`src/core/commands/friction.test.ts`](../src/core/commands/friction.test.ts), [`src/core/frictionLedger.test.ts`](../src/core/frictionLedger.test.ts), [`src/core/memory/reflection.test.ts`](../src/core/memory/reflection.test.ts); the regression net [`src/core/commandConformance.test.ts`](../src/core/commandConformance.test.ts) (actor kind × action × resource enumerated from the table).
- **Receipts**: tracked on the `Receipts: features/authorization.md` issue in the [project tracker](https://github.com/orgs/coreplanelabs/projects/1).

## Behavior

1. **Actors — resolved by the surface, never trusted from the request (KTD3).** An `Actor` is `{ kind, id, grants, onBehalfOf?, origin? }`. `kind ∈ { user, service, schedule, agent }`; `id` is platform-namespaced (invariant 4) and is the ONE identity every later decision keys on: `slack:U…` (a Slack user), `http:<subject>` (an ingress token's subject), `mcp:<subject>` (the same token over MCP), `access:<sub>` (a Cloudflare Access browser session), `access:svc:<common_name>` (an Access service token), `cli:local`, `schedule:<name>`, `agent:<name>`. Each adapter returns exactly what it can prove and nothing more: Slack the user id of the event; HTTP/MCP the token entry the bearer matched; the Access gate the verified JWT's `sub` or `common_name`; the CLI the fixed local identity; the schedule shim the registry entry that fired. **Grants are never attached by the adapter**: one resolver, `grantsFor(actor, config)`, reads them from configuration (item 9), so two surfaces resolving the same id get the same grants. `origin` (a chat actor's `{ channelId, threadKey }`) is context for the channel-scoped commands and the channel memory scope — never authority ([command-registry.md](command-registry.md) item 21). An `agent` actor carries `onBehalfOf`, the principal whose run it is; its **effective grants are the intersection** of its own and the principal's, computed per `GrantSet` (`"all" ∩ S = S`, `S ∩ T` set-wise) — an agent never exceeds the person it acts for (R2). An actor the adapter cannot resolve is `NO_GRANTS` on every axis. Adapters make **no authorization decision**: no scope comparison, no channel pin, no gate resolver — building the `Actor` is the whole of their part.
2. **Actions — today's vocabulary, no new words (R3).** An `Action` is a command's effect class `<group>:read | <group>:write | <group>:exec` (`runs:read`, `friction:write`, `repo:exec`, …), or one of the three non-command actions: `agent:run:<name>` (start the named agent — decided in `dispatch()`, invariant 3; `agent:run:*` = every agent), `repo:use` (act inside a repo's resident environment), `memory:write` (reflection writing a fact to a scope). A command declares its `action` once in `defineCommand`; `write` never implies `exec`, and no action authorizes a run (KTD16).
3. **Resources — typed, with the attributes rules read.** `run { id, channelId, userId, repo?, channelVisibility? }`; `channel { id, visibility }`; `memory-scope { key, kind: org | user | repo | channel, originChannelVisibility? }`; `repo { owner, name }`; `config-scope { kind: channel | user, id }`; `agent { name }`; and `command { id }` for list-shaped actions with no single resource (`runs.list`, `friction.report`). `ChannelVisibility ∈ { public, private, dm, machine, unknown }`; `machine` is every `http:*` / `mcp:*` channel, whose members are the tokens granted it. A handler names the resource it is about to touch; nothing about a resource is inferred from the request.
4. **The policy table — data, closed vocabulary, validated at load (KTD1).** A `Rule` is `{ action, resource, actorKinds?, resourceKind, originVisibility?, when: Condition[] }`. Within a row `when` is **ANDed**; rows for the same `(action, resource)` are **ORed**; **no matching row is a deny** (R7). A row also carries one-sided **selectors** — `actorKinds` (the actor's kind), `resourceKind` (a `memory-scope`'s `org | user | repo | channel` or a `config-scope`'s `channel | user` — REQUIRED on a row for a kinded resource type and FORBIDDEN on every other, typed per resource as `KindOf<T>` and enforced by the `Rule` type: a `memory-scope` row without a kind or a `run` row with one does not compile), `originVisibility` (the resource's origin channel visibility) — that pick WHICH rows apply to a request and read one side only, as distinct from the five **conditions** below, which relate actor to resource; R11's org-write rule is an `originVisibility: [public]` selector on the `memory:write` × `memory-scope { kind: org }` row — a point-check-only row, never compiled into a list predicate. The condition vocabulary is CLOSED: `has-grant(g)` (`actor.grants.actions` contains `g` or is `"all"`), `member-of` — ONE definition, used by both evaluators: it holds when EITHER `actor.grants.channels` contains the resource's channel id (or is `"all"`) OR the channel's visibility is `public` (`resource.channelVisibility`, stamped on the run at dispatch, KTD7; `unknown` is never public); a later cut adds directory-confirmed membership for private channels behind the same seam without changing the definition's shape — `is-self` (`resource.userId` equals the actor's id, or the `onBehalfOf` principal's), `owner-of` (`actor.grants.repos` contains the resource's `owner/name`, or is `"all"`), `all-channels` (`actor.grants.channels === "all"`). Every member is both evaluable against one resource and compilable to a store predicate — that is the admission test for a condition; a need for an arbitrary predicate is a plan-level stop, not a local addition. The table is validated when the module loads: a rule naming an unknown condition kind, an unknown resource type, or an action no command declares throws at import, so a malformed table can never serve a request. Today's gates are rows, one vocabulary for humans and machines (KTD5): `open` → a row with no condition; `operator` → `has-grant(<group>:write)` (or `:read` for the read commands) — the grant the admins' `all` translation and an operator's grants carry; `repoManager` → `has-grant(repo:write)`; `channelConfig` → `has-grant(config:write) ∧ member-of(channel)` — the added `member-of` denies only a case that cannot occur (a chat actor is trivially a member of the channel it speaks in; a machine actor's target is the channel its token grants), so it is behavior-preserving; `agentRun` → `has-grant(agent:run:<name>)` on resource `agent { name }` (which agents an actor may run is an action, not a grant axis — `Grants` has exactly three: `actions`, `channels`, `repos`). A run read is `runs:read` ∧ (`member-of` ∨ `all-channels`); a `run` resource with `is-self` is how a DM run belongs to its user.
5. **`authorize` — the one public decision (KTD2, KTD8).** `authorize(actor, action, resource)` is the ONLY export command code, `dispatch()`, and reflection call; it evaluates the rows for `(action, resource.type)` against the actor's effective grants and returns `{ allow: true }` or `{ allow: false, reason }`. The `reason` names the failed condition (`no rule for runs:read on run`, `not a member of slack:C…`, `missing grant friction:write`) and goes to the **audit line only** ([command-registry.md](command-registry.md) item 7); a reply never carries it. A denied **point read of a resource** (`runs get <id>`, `runs events`, `runs friction`, `runs stop`, the run page's history read) renders as `not_found`, byte-identical to a run that does not exist — existence is never revealed (R5). A denied write or list is `unauthorized` with the shared 🚫 wording, whose golden texts are unchanged. Handlers never compare ids or channels by hand (R1): `grep -rn "caller.channel\|chatGate" src/` outside `src/core/authz/` is empty.
6. **List reads — the same rules, pushed into the store (R6).** `predicateFor(actor, action, resourceType)` compiles the rows for a list-shaped action into a `Predicate` — `none`, `all`, `channels-in(ids)`, `repos-in(repos)`, `user-is(id)`, `visibility-in(visibilities)`, or an `and` / `or` of those (a row's ANDed conditions → `and`, the ORed rows → `or`) — from the SAME table `authorize` reads: `all-channels` → `all`; `member-of` → `or(channels-in(actor.grants.channels), visibility-in(["public"]))` — the same two-sided definition as item 4, so the run store filters on the stamped `channelVisibility` column and a `public` run is listed for everyone (the `visibility-in` leaf is the one addition to the contract's `Predicate`, landing with the store wiring); `owner-of` → `repos-in(actor.grants.repos)`; `is-self` → `user-is`; no matching row → `none`. It is callable only from the store adapters (`RunsService`, `RunStoreFrictionLedger`, the memory stores), which translate it into their own filter (`channelId IN (...)` on the run-history DO, indexed — never a full scan; `none` never touches the store), so a list returns only what a point read would have allowed and **no surface loads records and filters afterwards**. A denied list is an empty page, not an error. The invariant that makes this safe is the **`predicate ⇔ authorize` differential**: for any actor and any fixture of records, filtering by the store predicate equals filtering by point `authorize` on each record — the load-bearing test of the module. `runs list` without a store predicate (`predicateFor` returns `none` for an unknown actor) lists nothing.
7. **Channel visibility — a relation, on every surface (R5, R10, KTD4, KTD7).** `member-of(actor, channel)` and the `all-channels` grant are the two ways a run is readable; the Access API is bound like every other surface (an operator without `all-channels` gets `not_found` on a private-channel run they are not a member of). `ChannelDirectory { info(channelId) → { visibility }; isMember(actorId, channelId) → boolean | "unknown" }` is the adapter seam that supplies channel facts: it is the SOURCE of the `channelVisibility` stamp (dispatch asks it once per run) and, later, of membership facts — it is **not consulted at `authorize` time**, which reads only the stamped resource and the actor's grants; `unknown` — a channel the directory has never seen, an API failure, a timeout — is **never public and never a member** (R7). The first cut is **visibility-only**, a consequence of item 4's `member-of`: `public` → every actor of the workspace is a member; `private` and `dm` → the run's own `userId` (`is-self`) and holders of `all-channels`; `machine` → the tokens granted that channel (`grants.channels`). Real Slack membership (`conversations.members`, TTL-cached, one page per channel per TTL) lands behind the SAME seam without a handler change ([gap] below); a stale-allow window of minutes after a member leaves is accepted and documented, a stale-deny is impossible because `unknown` denies. Every run is **stamped with `channelVisibility` at dispatch** (from the adapter: Slack `conversations.info` `is_private` / `is_im` / `is_mpim`; `machine` for `http:*` / `mcp:*`), so neither read-side membership nor the memory write gate needs a Slack call at read time; a record without the stamp is `unknown` → private for writes, membership-gated for reads. A machine token's pinned channel is the same relation: `channels: {"http:ops"}` sees `http:ops` and nothing else (KTD10 preserved).
8. **Memory — reflection writes are gated (R11).** For every candidate fact, reflection calls `authorize(runActor, "memory:write", memory-scope { key, kind, originChannelVisibility })`. A fact whose run originated in a `private` or `dm` channel (or `unknown`) may be written to the `user`, `channel`, or `repo` scope, **never to `org`**; the rows narrow the model's `audience` to the narrowest allowed scope (`org` → `user` for a DM, `org` → `channel` for a private channel with no repo) and never widen it. A fact from a `public` channel keeps today's routing. Reads are unchanged ([memory.md](memory.md)).
9. **Configuration — one `grants` shape, legacy keys translated (R8, KTD6).** The native block is `grants: { <actor id>: { actions?: string[] | "all", channels?: string[] | "all", repos?: string[] | "all" } }` keyed by platform-namespaced actor id — three axes, no more (agents are actions, item 2); an absent axis is the empty set, `"all"` is explicit and never a default (the `repos` axis is `"all"` only where today's open-when-absent rule holds and the translation says so). `grantsFor(actor, config)` merges the native block with `translateLegacyConfig(config)`, a unit-tested pure function of the legacy keys; the same identity named by both shapes takes the **native grants** and `validateConfig` warns naming the id and both keys. Schedule actors are declared in the schedule registry (`actor: { kind: "schedule", id: "schedule:<name>", grants }`) — a firing is authorized by the same table as everything else and no permissions list hand-names `http:cron` (R9). The legacy keys are accepted for **one release** after the native shape ships, then removed; after removal `validateConfig` names any leftover legacy key with its replacement. The translation:

   | Legacy key | Resolves to (per named actor) |
   |---|---|
   | `permissions.admins: [id…]` | `{ actions: "all", channels: "all", repos: "all" }` (`"all"` actions covers `agent:run:*`) |
   | `permissions.operators: [id…]` (Access ids) | `{ actions: every <group>:read + every <group>:write (never :exec), channels: "all" }` — today's fleet-wide operator, fleet-wide stays |
   | Access browser session (any `access:<sub>`) | `{ actions: every <group>:read }` implicitly; `channels` from `grants` / operators; nothing else |
   | `permissions.repoManagement: [id…]` | `{ actions: + repo:write, friction:write }` |
   | `permissions.channelConfig: [id…]` | `{ actions: + config:write }`; key absent → every user holds `config:write` (open-when-absent preserved); key present and empty → admins only |
   | `permissions.agents.<name>: [id…]` | listed ids get `actions: + agent:run:<name>`; an agent with no allowlist gives every `user` actor `agent:run:<name>` |
   | `permissions.repos.<owner/name>: [id…]` | listed ids get `repos: + owner/name`; a repo (or the whole map) with no allowlist is in every allowed user's `repos` (`"all"` when the map is absent) |
   | `permissions.serviceTokens.<cn>: [scope…]` | `access:svc:<cn>` gets `{ actions: the listed scopes }`; unlisted → nothing |
   | `SWITCHBOARD_INGRESS_TOKENS` entry `scopes: [s…]` (default `["dispatch"]`) | `http:<subject>` / `mcp:<subject>` get `{ actions: the scopes }` — `dispatch` stays the ingress run permission |
   | `SWITCHBOARD_INGRESS_TOKENS` entry `channel: <c>` | `{ channels: {"http:<c>"} }` (and `mcp:<c>` over MCP) — the namespace pin unchanged; no `channel` → no channel membership |
   | `cli:local` | `{ actions: "all", channels: "all", repos: "all" }` — the local operator, unchanged |

10. **Fail-closed defaults (R7) — all preserved, enumerated.** An unresolvable actor has `NO_GRANTS`. A missing grant denies. No `(action, resource)` row denies. No admins configured means no operators (the shared 🚫 text names nobody to ask). `unknown` membership or visibility is not a member. A `channelVisibility`-less record is `unknown`. A schedule whose actor has no token sends nothing and records `misconfigured`. A malformed `scopes` or `grants` entry skips the identity rather than widening it. A `ChannelDirectory` failure yields `unknown`, never `allow`. An `agent` actor without `onBehalfOf` has its own grants only; with one, never more than the principal.
11. **Resolved questions, stated as behavior.** (OQ1) Admins hold `all-channels`; identities translated from `permissions.operators` hold `all-channels` too (today's fleet view is preserved by the translation), and an operator configured **natively** is membership-bound unless its `channels` is `"all"` — the fleet view is one grant away, never implicit. (OQ2) `friction report` computes over the runs the actor can see: `RunStoreFrictionLedger.recent()` takes the actor's predicate, so a Slack user's report reflects their memberships, a pinned token's its channel, an `all-channels` holder's the fleet; the aggregate is per-actor by construction and a fleet view is the `all-channels` grant. (OQ3) Visibility-only membership first (item 7), real membership later behind the same seam. The `self-improvement` schedule actor is declared with `{ actions: {friction:write, friction:read}, channels: "all" }`, so the weekly pass analyzes every channel's runs (closes [#395](https://github.com/coreplanelabs/switchboard/issues/395)).

## Validation criteria

| Criterion | Evidence |
|-----------|----------|
| Every surface resolves an `Actor` with the platform-namespaced id and kind — Slack admin and plain user (`user`, `slack:U…`), token with and without `channel` (`service`, `http:`/`mcp:<subject>`), Access browser (`access:<sub>`) and service token (`access:svc:<cn>`), `cli:local`, the `self-improvement` schedule (`schedule:self-improvement`); grants come from `grantsFor`, never the adapter; an unresolvable identity is `NO_GRANTS` | `[unit]` `src/core/authz/actor.test.ts::resolveActor::*`, `src/core/commandChat.test.ts::chatActorFor::*`, `src/channels/mcp.test.ts::actor resolution::*`, `src/channels/commandHttp.test.ts::actor resolution::*`, `src/core/schedules.test.ts::schedule registry::declares the self-improvement actor` |
| `agent` actors: effective grants are the intersection with `onBehalfOf` on every axis (`"all" ∩ S = S`); an agent never exceeds its principal; no principal → own grants only | `[unit]` `src/core/authz/actor.test.ts::effectiveGrants::intersects the agent's grants with the principal's`, `::never exceeds the principal` |
| Every row of the table has a positive and a negative test; AND within a row, OR across rows; no row → deny with a reason naming the gap | `[unit]` `src/core/authz/policy.test.ts::policy table::every rule allows its positive fixture and denies its negative`, `src/core/authz/authorize.test.ts::authorize::ANDs conditions within a row`, `::ORs rows for the same action and resource`, `::denies when no rule matches` |
| The condition vocabulary is closed: a rule with an unknown condition kind, unknown resource type, or an action no command declares fails at module load | `[unit]` `src/core/authz/policy.test.ts::validatePolicy::throws on an unknown condition kind`, `::throws on an unknown resource type`, `::throws on an action no command declares` |
| An unknown actor kind denies everything; `NO_GRANTS` denies everything but the open rows | `[unit]` `src/core/authz/authorize.test.ts::authorize::denies every action for an unknown actor kind`, `::NO_GRANTS passes only open rows` |
| Deny reasons reach the audit line and never the reply; a denied point read is `not_found`, byte-identical to a missing run; a denied write is `unauthorized` with the unchanged golden 🚫 text | `[unit]` `src/core/commands/runs.test.ts::runs.get::a run in a channel the actor is not a member of is not_found, identical to a missing run`, `src/core/commandRegistry.test.ts::CommandRegistry audit line::carries the deny reason`, `src/core/commandChat.test.ts::handleChatCommand::restricted wording unchanged` |
| `predicateFor` ⇔ `authorize` differential: for every actor fixture (admin, operator, member, non-member, pinned token, schedule, `NO_GRANTS`) over a fixture of runs across public / private / dm / machine channels, filtering by the predicate equals filtering by point `authorize` | `[unit]` `src/core/authz/predicate.test.ts::predicateFor ⇔ authorize differential::filtering by the predicate equals filtering by authorize for every actor fixture` |
| `predicateFor` compiles `all-channels` → `all`, `member-of` → `or(channels-in, visibility-in(["public"]))`, `owner-of` → `repos-in`, `is-self` → `user-is`, no row → `none`; `unknown` visibility never matches `visibility-in(["public"])`; it is not importable from handlers (only store adapters) | `[unit]` `src/core/authz/predicate.test.ts::predicateFor::compiles each condition to its predicate`, `::returns none when no rule matches`, `src/core/authz/predicate.test.ts::module boundary::no handler module imports predicateFor` |
| Stores push the predicate down: `RunsService.list` and `RunStoreFrictionLedger.recent` pass it to `store.list`, never filter after loading; `none` never touches the store; the run-history DO answers `channels-in` via `channelId IN (...)` without a full scan | `[unit]` `src/core/commands/runs.test.ts::runs.list::passes the actor's predicate to the store and never filters after`, `src/core/frictionLedger.test.ts::RunStoreFrictionLedger::recent() takes the actor's predicate`, `deploy/cloudflare-memory/runHistory.test.ts::list::channels-in compiles to an indexed IN filter` |
| Deliberate change (a): the `schedule:self-improvement` actor sees runs from every channel | `[unit]` `src/core/commands/friction.test.ts::friction.propose::the self-improvement schedule actor analyzes every channel's runs` (red-first) |
| Deliberate change (b): an Access operator without `all-channels` gets `not_found` on a run from a channel they are not a member of; a Slack user is bound the same way; a pinned token still sees only its channel | `[unit]` `src/core/commands/runs.test.ts::channel visibility::an Access operator without all-channels gets not_found outside their channels` (red-first), `::a Slack user sees only member channels`, `::a pinned token sees only its channel` |
| Visibility-only membership: `public` → every workspace actor; `private` / `dm` → the run's `userId` and `all-channels` holders; `machine` → the tokens granted the channel; `unknown` → nobody | `[unit]` `src/core/authz/channelDirectory.test.ts::visibility-only membership::*` |
| `ChannelDirectory`: `unknown` is never a member; a directory failure or timeout yields `unknown`, never allow; the TTL cache serves within the window and refetches after | `[unit]` `src/core/authz/channelDirectory.test.ts::InMemoryChannelDirectory::unknown is not a member`, `::a failing lookup is unknown`, `::TTL cache::*`, `src/channels/slack.test.ts::SlackChannelDirectory::maps is_private / is_im / is_mpim to visibility`, `::an API failure is unknown` |
| Runs are stamped with `channelVisibility` at dispatch (Slack from `conversations.info`, `machine` for `http:*`/`mcp:*`); a record without the stamp reads as `unknown` | `[unit]` `src/core/dispatcher.test.ts::channel visibility stamp::*`, `src/core/runRecord.test.ts::channelVisibility defaults to unknown` |
| Deliberate change (c): a reflection from a `dm` channel with an `org`-audience fact writes to `user`, never `org`; a private channel narrows `org` → `channel` (or `repo`); a public channel keeps today's routing; the audience is never widened | `[unit]` `src/core/memory/reflection.test.ts::write gate::a dm-origin org fact is written to the user scope, never org` (red-first), `::a private-channel org fact narrows to channel`, `::a public-channel fact routes as before`, `::never widens the audience` |
| Every legacy key translates per the table: `admins` → all three axes `"all"`; `operators` → every read + write, no exec, `all-channels`; browser session → reads only; `repoManagement` → `repo:write` + `friction:write`; `channelConfig` absent → open, empty → admins; `agents.<name>` → `agent:run:<name>` actions and `repos.<slug>` allowlists, both open-when-absent; `serviceTokens`; token `scopes` and `channel`; `cli:local` all | `[unit]` `src/core/authz/grants.test.ts::translateLegacyConfig::*` (one test per row of the table) |
| A native `grants` block and its legacy equivalent resolve to the same `Actor.grants` (differential); an identity named by both takes the native grants and `validateConfig` warns naming it; a malformed entry skips the identity, never widens | `[unit]` `src/core/authz/grants.test.ts::grantsFor::native and translated legacy config resolve to identical grants`, `::native wins over legacy for the same identity`, `src/config.test.ts::validateConfig::warns when grants and a legacy key name the same identity`, `::a malformed grants entry is skipped` |
| Schedule actors are declared in the registry with their grants; a firing is authorized by the table (no `http:cron` in any permissions list); a schedule whose actor has no token sends nothing and records `misconfigured` | `[unit]` `src/core/schedules.test.ts::schedule registry::self-improvement declares actor + grants`, `::planScheduledFiring::no token → nothing sent, misconfigured` |
| Today's chat gates are rows with unchanged decisions: `open`, `operator`, `repoManager`, `channelConfig` (open-when-absent; `member-of` never denies a chat actor's own channel), `agentRun`; `defineCommand` carries no `chatGate`; every command in `commands/all.ts` has a rule and a command without one fails the suite loudly | `[unit]` `src/core/commandConformance.test.ts::authorization::every command has a policy rule`, `::a command without a rule fails loudly`, `::actor kind × action × resource decisions match the table on every surface`, `src/core/commands/config.test.ts::config.set channel::member-of never denies the origin channel` |
| Fail-closed defaults hold: no admins → nobody is an operator; `dispatch`-only token refused on every registry command; `runs:write` refused on `friction:write`; `write` never implies `exec`; a browser session never holds `exec` | `[unit]` `src/core/authz/authorize.test.ts::fail-closed::*`, `src/core/commandConformance.test.ts::authorization::a caller without the grant is refused before parse` |
| `friction report` computes over the actor's visible runs: a member's report differs from the fleet's; `all-channels` yields the fleet | `[unit]` `src/core/commands/friction.test.ts::friction.report::analyzes only the runs the actor can see` |
| The weekly cron analyzes the fleet | `[agent]` With the bot deployed and the `cron` token present: temporarily reschedule the `self-improvement` registry entry + `wrangler.jsonc` to fire within the hour, deploy, then `wrangler tail switchboard` shows `[schedule] self-improvement → completed run <id> — 🔍 N runs analyzed` with **N > 0** while other channels have finished runs in the retention window; the run page's Answer lists patterns across channels. Revert the schedule after. |
| An Access operator is bound by membership on the Access API | `[agent]` As an Access identity configured natively WITHOUT `channels: "all"`, `GET /api/runs.get?id=<id of a finished run from a private Slack channel the identity is not a member of>` → `404 {code:"not_found"}`, byte-identical to `?id=<nonexistent>`; `GET /api/runs.list?status=all` omits the run; the same request as an admin returns it. |
| A DM run is visible only to its user | `[agent]` DM the bot from account A and let the run finish; in Slack as A, `runs list --status all` (as an operator) lists it; as account B (an operator, not admin), `runs list --status all` omits it and `npx tsx src/cli.ts runs get <id>` from an Access session as B is `not_found`; as an admin it is present. |
| `friction report` reflects the caller's memberships | `[agent]` In a private channel with recent finished runs, `@switchboard friction report` from a member shows those runs in "runs analyzed"; the same command from a channel the caller is not a member of (public channel, non-admin) shows a smaller count that excludes them; an admin's count is the fleet's. |

## Roadmap (gaps)

| Criterion | Evidence |
|-----------|----------|
| Real Slack membership behind the seam: `SlackChannelDirectory.isMember` from `conversations.members` (TTL-cached, one page per channel per TTL); a member of a private channel who is not the run's user sees its runs; leaving the channel denies within the TTL | `[gap]` visibility-only is the shipped first cut (item 7); real membership is the plan's U5 |
| Agent actors gate tool-level actions: a coding agent's `repo:use` / push target decided by `authorize(agent actor, "repo:use", repo)` instead of the resident's compound gate | `[gap]` the `agent` kind and `onBehalfOf` exist (item 1); the resident gating has not moved |
| Legacy keys removed: `permissions.admins\|operators\|repoManagement\|channelConfig\|agents\|repos` and token `scopes` / `channel` translation deleted; `config.example.yaml` shows only `grants`; `validateConfig` names any leftover key with its replacement | `[gap]` the plan's U7, one release after the native shape ships |
