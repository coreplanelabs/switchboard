# Self-improvement proposals

Switchboard turns the run-friction **analysis** ([run-friction.md](run-friction.md), #105) into **action**: it keeps every finished run's diagnosis in a ledger, clusters the friction that recurs *across* recent runs, and files the top recurring patterns as labeled GitHub issues — each with the evidence (the friction data, the affected runs) and a concrete suggested fix — deduped against the proposals already open. This is the second piece of Area 7b ([#84](https://github.com/coreplanelabs/switchboard/issues/84)): *observe → diagnose → **propose***. It is **human-gated by construction**: the only side effect is opening a labeled issue for a person to triage. It never opens PRs and never merges anything.

**Built on the analyzer, not beside it (locked design).** Every pattern is made from the `FrictionFinding`s `analyzeRunFriction` already emits; the proposer adds no per-run detection. The one signal it adds — `long_run`, a run whose wall time is ≥2× the median of recent runs — cannot exist per run by definition, and stands in for **cost spikes** until per-run token accounting exists (AGENTS.md known gap). The clustering, ranking, rendering, and dedupe step is a pure function of the ledger records (no clock, no I/O), so it is tested against recorded runs exactly as #105 intended.

**Where recent runs live.** The live registry evicts a finished run 60s after it ends, so "friction across recent runs" needs a ledger: after every run the dispatcher analyzes the run's own event stream and records `{ runId, label, agent, finishedAt, diagnosis }` to the `FrictionLedger`. Three implementations (AGENTS.md invariant 2): **`WorkerFrictionLedger` — the production choice** — an HTTPS client to the `FrictionDO` on the state Worker (`deploy/cloudflare-memory/`, the same Worker and `MEMORY_TOKEN` bearer as memory; one SQLite Durable Object per ledger key `friction:<repo>`, so the ledger survives bot restarts and redeploys — AGENTS.md invariant 6); the JSONL `FileFrictionLedger` under `data/` (used when no `selfImprovement.worker` is configured — the same durability as `data/overrides.json`, i.e. **ephemeral on Cloudflare Containers**; startup warns loudly); and `InMemoryFrictionLedger` (tests/dev). The record carries the run id (not the view token), so a ledger row or an issue body can never grant live-view access.

- **Code**: [`src/core/frictionProposals.ts`](../src/core/frictionProposals.ts) (pure: `normalizeCommand`, `patternSignature`, `clusterFriction`, `proposeImprovements`, `dedupeProposals`, the `FrictionRunRecord`/`FrictionPattern`/`ImprovementProposal` types, the marker helpers); [`src/core/frictionLedger.ts`](../src/core/frictionLedger.ts) (`FrictionLedger` seam; `InMemoryFrictionLedger`, `FileFrictionLedger`); [`src/core/frictionLedgerWorker.ts`](../src/core/frictionLedgerWorker.ts) (`WorkerFrictionLedger`, `buildFrictionLedger`); [`deploy/cloudflare-memory/worker.ts`](../deploy/cloudflare-memory/worker.ts) (`FrictionDO`, `POST /friction/record`, `POST /friction/recent`); [`src/execution/githubIssues.ts`](../src/execution/githubIssues.ts) (`IssueTracker` seam; `GithubIssueTracker`, `InMemoryIssueTracker`); [`src/core/selfImprovement.ts`](../src/core/selfImprovement.ts) (`runSelfImprovement`, `formatSelfImprovementReport`, `SelfImprovementConfig`); [`src/core/frictionCommands.ts`](../src/core/frictionCommands.ts) (`friction report|propose`); [`src/frictionProposeCli.ts`](../src/frictionProposeCli.ts); [`src/channels/frictionTrigger.ts`](../src/channels/frictionTrigger.ts) (`POST /friction/propose`) + the weekly cron in [`deploy/cloudflare/worker.ts`](../deploy/cloudflare/worker.ts); the dispatcher's post-run ledger write and command routing ([`src/core/dispatcher.ts`](../src/core/dispatcher.ts)); wiring in [`src/index.ts`](../src/index.ts); config in `config/config.example.yaml` (`selfImprovement`).
- **Tests**: [`src/core/frictionProposals.test.ts`](../src/core/frictionProposals.test.ts), [`src/core/frictionLedger.test.ts`](../src/core/frictionLedger.test.ts), [`src/core/frictionLedgerWorker.test.ts`](../src/core/frictionLedgerWorker.test.ts), [`deploy/cloudflare-memory/friction.test.ts`](../deploy/cloudflare-memory/friction.test.ts) (runs inside workerd against the real SQLite DO), [`src/execution/githubIssues.test.ts`](../src/execution/githubIssues.test.ts), [`src/core/selfImprovement.test.ts`](../src/core/selfImprovement.test.ts), [`src/core/frictionCommands.test.ts`](../src/core/frictionCommands.test.ts), [`src/frictionProposeCli.test.ts`](../src/frictionProposeCli.test.ts), [`src/channels/frictionTrigger.test.ts`](../src/channels/frictionTrigger.test.ts), [`src/core/dispatcher.test.ts`](../src/core/dispatcher.test.ts) (`self-improvement wiring (Area 7b / #84)`).

## Behavior

1. **Ledger.** After every agent run (fast paths excluded — they never run an agent) the dispatcher records `analyzeRunFriction(events)` over the run's own event copy (a ring of the newest 5000 events, so a pathological run's terminal notes are never the part dropped), with the registry run id, the runs-index label, the agent name, and the finish time. Best-effort: a ledger failure is a `[friction]` warning line, never a failed run or a delayed reply. `recent({ limit?, sinceMs? })` returns runs oldest-first as copies; the ledger is bounded (500 runs — the oldest fall off; the file compacts at 2× that, the DO trims on every write in one transaction). **Every implementation upserts by run id** — a retried or duplicated `record` replaces, never double-counts (the in-memory store replaces in place, the append-only file resolves a repeated id to its last line on read and materializes that at compaction, the DO uses `ON CONFLICT(run_id) DO UPDATE`); an empty run id is rejected as a record everywhere. Corrupt or foreign rows/lines are skipped, never fatal. **Durable ledger** (`WorkerFrictionLedger`): `record` → `POST /friction/record {ledgerKey, record}` (upsert by run id, so a retried write is idempotent; 400 on a malformed/oversized record — the bot logs it), `recent` → `POST /friction/recent {ledgerKey, limit?, sinceMs?}` (records shape-checked on the way back; any failure throws → the friction command replies ⚠️). `buildFrictionLedger` (startup): `selfImprovement.worker.baseUrl` + its bearer (`tokenEnv`, default `MEMORY_TOKEN`) → Worker ledger keyed `friction:<selfImprovement.repo>`; otherwise the file ledger with a warning naming the path (and the missing env var, when a worker was configured). Runs are recorded whether or not `selfImprovement` is configured.
2. **Signatures.** Each finding maps to a stable key `<kind>:<signature>`. Tool findings are keyed **shell-aware**, because real agents chain the same work differently every run (`cd /tmp/ws/repo && npm ci --silent 2>&1 | tail -2 && …` vs `cd ~/switchboard && npm ci --silent >/dev/null 2>&1; (npm test …)` — the first real capture proved an exact-command key never recurs): the analyzer's label prefix and the ` → result` tail are dropped, the command is split into shell segments (`&&`, `||`, `;`, `|`), and each segment reduces to its `program [subcommand [script]]` head (`npm ci`, `git checkout`, `npm run typecheck`, `npx vitest`) with navigation/echo/pipe-filter programs (`cd`, `echo`, `tail`, `grep`, …), redirections, env assignments, wrappers (`sudo`), quoted strings, and volatile tokens (URLs → `<url>`, hashes → `<sha>`, numbers → `<n>`) removed; the ordered, de-duplicated heads (≤4) form the signature (`slow_tool:git checkout, npm test`). `setup_install` keys on the install segment alone plus the flags that change what is installed (`pnpm install --frozen-lockfile`; `--silent`/`--no-audit`-style flags dropped), so every `npm ci` run is one pattern. An unknown-tool failure (tool misuse) keys on the tool name. Note findings key on their kind, never their free text: `budget_hit:time|turns`, `wrap_up:wrap_up`, `infra_failure:sandbox_dead`, `infra_failure:mid-tool <cmd>`, `infra_failure:<cmd>`.
3. **Clustering.** A pattern is a key seen in ≥ `minRuns` **distinct** runs (default 2) — a finding repeated within one run counts once toward recurrence (recurring means a process problem, not one bad run) but every occurrence is counted and its time summed. Ranking: distinct runs ↓, peak severity ↓, attributed time ↓, key — a total order, so the output is deterministic. Each pattern keeps up to 5 examples, most recent first, each anchored to its run. `long_run`: among runs with `runMs`, those ≥ max(2× median, 10 min) are outliers, grouped per agent (`long_run:<agent>`); a fleet where every run is long has no outliers.
4. **Proposals.** The top `top` patterns (default 3) become issue proposals: title `[friction] <kind> recurs in N of M runs: <signature>` (≤120 chars), body = the dedupe marker `<!-- switchboard-friction-pattern: <key> -->`, a "proposal for a human to triage" preamble linking #84, **Pattern** (kind, signature, recurrence with share, occurrences, attributed time, peak severity), **Evidence** (a table of the example runs: run id + label, finish time, the redacted finding, its time; plus the remaining run ids), **Suggested fix** (kind-specific and sharpened by the evidence — e.g. a failing `--frozen-lockfile` install → regenerate the lockfile / document the install command / onboard as a resident; `command not found` → add to the sandbox image; unknown tool → align the agent's toolset; `budget_hit:turns` → `maxTurns` or batch calls; `sandbox_dead` → container sizing), and **Provenance**. One label, `selfImprovement.label` (default `self-improvement`), created in the repo when missing.
5. **Dedupe.** Open issues carrying the label are listed (PRs excluded, paginated) **plus the 30 newest open issues unfiltered** — GitHub's label-filtered list was observed to lag creation by a few seconds during this feature's validation, and an immediate re-run refiled a pattern — and matched by **marker**, never title (titles change with run counts) or label. A matched proposal is reported as already open and not refiled; a closed proposal's pattern can be proposed again. When no pattern recurs, GitHub is never consulted.
6. **Filing and failure.** Fresh proposals are filed one by one through the `IssueTracker` (REST, App installation token — App `issues:write`; never `gh`; bodies clipped to GitHub's limit). A create failure is reported per proposal and later proposals still file. `dryRun` computes and reports everything (including what would be filed) and files nothing; it still lists open issues for the dedupe. With **no repo** at all (the CLI without `--repo`) the pass is a pure dry run that never consults GitHub — no credential needed.
7. **Triggers.** (a) Chat, channel-agnostic, inline, never a model turn: `friction report` (read-only, open to everyone — like `repo list`) shows the ranked recurring patterns; `friction propose [--dry-run] [--top <n>] [--min-runs <n>]` files — gated by the same fail-closed rule as repo management (`permissions.repoManagement`, admins only when unset), refuses by name, and refuses with a hint when `selfImprovement.repo` is unset. Unknown flags and non-positive numbers are named parse errors. (b) CLI, no bot process: `npx tsx src/frictionProposeCli.ts <file|dir>... [--repo owner/name] [--label L] [--top N] [--min-runs N] [--file] [--json]` over saved material — `/runs/:id/friction` JSON captures, ledger JSONL, or raw run-event captures (analyzed on the spot) — auto-detected per file, deduped by run id, unreadable files skipped by name. Dry run unless `--file`, which requires `--repo`. (c) **Scheduled**: the Worker shim (`deploy/cloudflare/worker.ts`) runs a second cron, Mondays 14:00 UTC (`0 14 * * 1`), that `POST`s the bot's `/friction/propose` with a dedicated bearer (`FRICTION_TRIGGER_TOKEN`, a shim secret forwarded to the container); the route (`src/channels/frictionTrigger.ts`) runs exactly the chat command's step and answers the report as JSON (`{ok, dryRun, runsAnalyzed, patterns, filed, duplicates, failed, text}`), which the shim logs. Fail-closed: no token → 503 and nothing runs; wrong/missing bearer → 401 before any body is read (constant-time compare); non-POST → 405; optional body `{dryRun:true}`; malformed body → 400; unset `selfImprovement.repo` or no ledger → 503 with the reason; a ledger/tracker failure → 500 with the error. The issues are the notification — no channel post.

## Validation criteria

| Criterion | Evidence |
|-----------|----------|
| Command normalization: prefix/tail stripped, volatile tokens blanked, length capped | `[unit]` `src/core/frictionProposals.test.ts::normalizeCommand::*` |
| Shell-aware signatures: segment heads, noise/redirections/env/wrappers dropped, capped; the two real chainings of `npm ci` share one install signature; install flags kept minus quiet flags | `[unit]` `::commandSignature::*`, `::installSignature::*`, `::patternSignature::clusters the SAME install chained differently across real runs…` |
| Signatures: tool findings by category + command (label prefixes dropped), unknown tool by tool name, notes by kind | `[unit]` `::patternSignature::*` |
| No patterns for no/clean runs; a one-off (one run) never becomes a pattern; minRuns honored | `[unit]` `::clusterFriction::returns no patterns…`, `::clusters the same finding across DISTINCT runs and ignores one-offs…`, `::honors minRuns` (red-verified: disabling the minRuns filter fails these) |
| Within-run repeats count once toward recurrence but all occurrences are counted | `[unit]` `::counts a finding repeated within ONE run as one run…` |
| Ranking total order (runs, severity, time, key) | `[unit]` `::ranks by distinct runs, then peak severity, then attributed time` |
| Examples most-recent-first, capped, run-anchored | `[unit]` `::keeps the most recent examples first, capped, each anchored to its run` |
| `long_run` outliers per agent; none when all long or only one | `[unit]` `::detects cross-run duration outliers…`, `::does not flag long runs when every run is long…` |
| Deterministic; input not mutated | `[unit]` `::is deterministic and does not mutate its input` |
| Proposals: ≤ top, rank order, marker embedded; title/labels/evidence/fix/#84 rendered; a fix template for every kind | `[unit]` `::proposeImprovements::*` |
| Dedupe by marker, not title; closed → re-proposable; marker parsing tolerant | `[unit]` `::dedupeProposals::*`, `src/core/selfImprovement.test.ts::dedupes only against OPEN issues…` (red-verified: disabling the marker match fails these) |
| Ledger contract (empty, oldest-first, limit/since, bounded, upsert by run id, copies) for the in-memory and file impls; an empty run id is not a record; file persists across instances, skips corrupt lines, compacts, missing file = empty | `[unit]` `src/core/frictionLedger.test.ts::*` |
| Worker ledger client: record POSTs under the ledger key with the bearer; non-2xx/non-JSON/transport failures throw with detail; recent shape-checks and orders | `[unit]` `src/core/frictionLedgerWorker.test.ts::WorkerFrictionLedger::*` |
| Startup selection: worker + bearer → durable; no worker / no bearer → file with a warning naming path and env var; no config → still a ledger | `[unit]` `src/core/frictionLedgerWorker.test.ts::buildFrictionLedger::*` |
| FrictionDO: record→recent round-trip oldest-first per ledger key; upsert by run id; limit/sinceMs; bounded at 500 (oldest fall off); 400 on bad key/record/oversize/limit/sinceMs; 401 without bearer; 405 non-POST | `[unit]` `deploy/cloudflare-memory/friction.test.ts::*` (inside workerd, real SQLite DO) |
| Given a fixture of runs with a recurring pattern, ONE deduped proposal is filed with the evidence; a second pass files nothing; dry run files nothing; no repo → pure dry run, GitHub never consulted; nothing recurring → GitHub untouched; a create failure is reported, not thrown; minRuns respected | `[unit]` `src/core/selfImprovement.test.ts::runSelfImprovement::*` |
| Report text: runs analyzed, ranked patterns, filed / already open / failed / dry run | `[unit]` `src/core/selfImprovement.test.ts::formatSelfImprovementReport::*` |
| GitHub tracker: label-filtered paginated list + newest unfiltered page, deduped, drops PRs; label ensured once (422 race tolerated); bearer + user-agent; body clipped; HTTP errors and missing credential thrown with detail | `[unit]` `src/execution/githubIssues.test.ts::GithubIssueTracker.*` |
| In-memory tracker: numbering, open+label filtering, close, call log | `[unit]` `src/execution/githubIssues.test.ts::InMemoryIssueTracker` |
| Chat commands: parse (flags, named errors, prose passes through); `report` open + GitHub-free; `propose` refused by name for non-admins; admin propose files then dedupes; `--dry-run`; unset repo hint; missing ledger message | `[unit]` `src/core/frictionCommands.test.ts::*` |
| Dispatcher records every run to the ledger (id/agent/label/diagnosis); a ledger failure is a warning, never surfaced; `friction` commands are inline (no model, no executor) and gated | `[unit]` `src/core/dispatcher.test.ts::self-improvement wiring (Area 7b / #84)::*` |
| CLI: arg parsing (dry-run default, `--file` needs `--repo`, slug check, bad flags); loads friction JSON, ledger JSONL, raw captures; expands dirs, skips unrecognized by name, dedupes run ids | `[unit]` `src/frictionProposeCli.test.ts::*` |
| Over real recent Switchboard runs, the proposer surfaces recurring patterns and files actionable, deduped `self-improvement` issues | `[agent]` Capture the `/runs/:id/friction` JSON of ≥3 finished production runs (live view, within the TTL) into a directory, then `npx tsx src/frictionProposeCli.ts <dir> --repo coreplanelabs/switchboard` (dry run: expect a ranked pattern list), then `--file` → the top proposals open as labeled issues, and a second `--file` run reports them as already open. **Validated 2026-08-28** on 3 real review runs (captured from the live view): 1 recurring pattern `setup_install:npm ci` (2 of 3 runs, 1m 3s) → filed [#139](https://github.com/coreplanelabs/switchboard/issues/139); re-run → "already open, not refiled". The first re-run had refiled ([#140](https://github.com/coreplanelabs/switchboard/issues/140), closed as duplicate) because GitHub's label-filtered listing lagged creation — fixed by also listing the newest unfiltered issues (behavior 5). Details in [PR #138](https://github.com/coreplanelabs/switchboard/pull/138). |
| `friction report` / `friction propose` from Slack on the deployed bot read the durable ledger | `[agent]` In #switchboard-prompting: `@switchboard friction report` → the report (ranked patterns, or "no recurring friction pattern"); `@switchboard friction propose --dry-run` → the same plus a "would file" list when patterns exist. **Validated 2026-08-29** on bot `9dcca375`: [report](https://coreplanelabs.slack.com/archives/C0BQS7KPJHK/p1788035479509019) and [propose --dry-run](https://coreplanelabs.slack.com/archives/C0BQS7KPJHK/p1788035490952459) both → `🔍 8 runs analyzed — no recurring friction pattern found`. (Both had failed with `Unknown option \`*Sent\`` until #156 — the plugin footer arrives on the same line as the command; see slack-channel.md.) A live `friction propose` that files remains to be observed the first time a pattern recurs. |
| Durable ledger survives a bot redeploy: runs recorded before a container redeploy are still listed by `friction report` after it | `[agent]` With prod on the Worker ledger: note `friction report`'s run count, redeploy the bot Worker (`deploy/cloudflare`), then `friction report` again → the count is unchanged or higher, never reset. Also `wrangler tail switchboard-memory` shows `[friction/record] friction:coreplanelabs/switchboard <- <runId>` after each run. **Wiring validated 2026-08-29**: state Worker `3d9aeeda` (`env.FRICTION (FrictionDO)`), bot `f56a92e6`; a trigger dry run produced `[friction/recent] friction:coreplanelabs/switchboard -> 0 runs` in the state Worker tail ([receipt](https://github.com/coreplanelabs/switchboard/pull/143#issuecomment-5464244216)). **Survival validated 2026-08-29**: count 5 before the `aa4a1835` bot redeploy, 8 after, 8 after the `9dcca375` redeploy — never reset ([receipt](https://github.com/coreplanelabs/switchboard/issues/84#issuecomment-5464729889)). |
| Trigger route: fail-closed without a token (503); 401 wrong/missing bearer; 405 non-POST; runs the step and files through the tracker, dedupes on a second call; `{dryRun:true}` files nothing; malformed body 400; no repo / no ledger 503 with reason; ledger failure 500; node adapter never reads an unauthorized body | `[unit]` `src/channels/frictionTrigger.test.ts::*` |
| Weekly cron fires the pass without a human | `[agent]` After deploying the shim with `FRICTION_TRIGGER_TOKEN` set: `curl -s -X POST https://switchboard.coreplanelabs.dev/friction/propose -H "authorization: Bearer $FRICTION_TRIGGER_TOKEN" -d '{"dryRun":true}'` → 200 with the report JSON (and 401 without the header); then the first Monday 14:00 UTC, `wrangler tail switchboard` shows `[friction] weekly propose → HTTP 200 …`. **Route validated 2026-08-29** on bot Worker `f56a92e6`: no bearer → 401, GET → 405, dry run → 200 `{"ok":true,"dryRun":true,"runsAnalyzed":0,…}` ([receipt](https://github.com/coreplanelabs/switchboard/pull/146#issuecomment-5464244123)). First cron firing: pending (Mon 2026-08-31 14:00 UTC). |
