// The default VitePress theme, plus the product's own accent colour.
// Nothing else is overridden: the default theme is the reason this site is
// fast and legible, and every component we do not touch is one we do not own.
import DefaultTheme from "vitepress/theme";
import "./product.css";

export default DefaultTheme;
