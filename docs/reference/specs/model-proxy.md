# Model proxy: a run's bearer buys model calls through the bot, and no container ever holds a key

The credential boundary ([record 0016](../../decisions/0016-long-lived-process-not-serverless.md)): no Worker and no execution container holds a model key; the bot process does. A harness that runs inside an execution container and calls the provider itself ([record 0032](../../decisions/0032-pi-is-the-harness-the-native-loop-retires.md)) must therefore believe it holds a key while holding a token the bot minted for that [run](../vocabulary.md#run) alone. The **model proxy** is that seam: three routes on the bot's HTTP server shaped like the three provider APIs a harness speaks natively, a **run bearer** minted per run and presented as the API key, and a **meter** that records every proxied call as the run's own `model.turn` span with the attrs the native runner sets — so the run page, the friction analyzer and the costs page keep one vocabulary and never learn the turn happened in another process. Its first client is the pi harness ([harness-pi.md](harness-pi.md)): a run whose preset is on pi hands pi the bearer as its provider key; every other run is unchanged.

- **Code**: `src/core/modelProxy/runBearers.ts` (`RunBearerStore` — `mint`, `leaseStarted`, `markLoopEnded`, `markTurn`, `clearTurn`, `marksOf`, `issue`, `adopt`, `rotate`, `verify`, `consumeTurn`, `reparent`, `revoke`, `grantOf`, `sweep`; `bearerHashOf`; `RunBearerGrant`, `RunBearerFacts`, `RotateVerdict`), `src/core/budgets.ts` (`bearerExpiresAt`, `provisionalBearerExpiresAt`, `ALLOWANCES.bearerGrace`), `src/channels/modelProxy.ts` (`decideDoor` the header-only door, `handleAdmitted` the call, `handleModelProxyRequest` both, `createModelProxyHandler` the node:http adapter; `pinRequest`, `shapeTools`, `upstreamFor`, `SseMeter`, `meterAnthropicMessage`, `meterOpenAiCompletion`, `meterResponses`, `turnAttrs`, `refusalResponse`; the paths and forwarded-header lists), `src/core/modelProxy/usage.ts` (`usageFromAnthropic`, `usageFromOpenAI`, `usageFromResponses`), `src/channels/adminModelProxy.ts` (`POST /admin/model-proxy/bearer`, the operator's probe bearer), `src/core/provider.ts` (item 11: the block's declaration — `WIRES`/`Wire`, `WIRE_ALIASES`, `ProviderModelOverride`, `wireOf`, `vendorOf`), `src/core/modelCard.ts` (item 11: `resolveModelCard`, `decideControls`, `ModelCard`, `CardRegistry`), `src/core/modelRegistry.ts` (item 11: the catalog seam's types, Node-free for the Workers' typechecks), `src/core/installedModelRegistry.ts` (item 11: the installed pi registry as the catalog — the bot's Node reader), `src/config/validate.ts` (`validateProviders`, `validateModelOverride`), `src/core/testing/providerConformance.ts` (item 11: the matrix scaffold — rows, drivers, the lint, the printer), `src/core/dispatch/resolve.ts` (item 11: the [card](../vocabulary.md#card) resolved in `resolveTarget`, the refused control thrown), `src/core/dispatch/provision.ts` (item 11: `run_meta.card`, the `control_degraded` notes; `mintRunBearer`, `ProvisionDeps.runBearers`), `src/core/dispatcher.ts` (the mint after the attach, the revoke in the outer finally), `src/core/runEnding.ts` (`RunEndingDeps.onFinished` — the revoke at the run's finish), `src/core/trace/workerTrace.ts` (`shimRoute`: the `model-proxy` route word), `src/index.ts` (the store, the three routes and the mint route wired beside `/ingress` and `/mcp`), `src/core/commands/providers.ts` (item 12: `providers.check`, `compareCardToEndpoints`, `endpointsFromJson`, `configuredModelRefs`, `registryDrift`), `scripts/registry-drift.ts` (item 12: the `check:registry-drift` gate over the example config), `src/core/harness/pi/harness.ts` (item 12a: `isParkedProviderError`, the held turn and its reissue), `src/core/plane/decide.ts` (item 12a: `reissueSteerSentence`, `isReissueSteerText`).
- **Tests**: `src/core/modelProxy/runBearers.test.ts`, `src/core/modelCard.test.ts` (item 11: `vendorOf`, the three layers with provenance, `decideControls`), `src/core/modelRegistry.test.ts` (item 11: the installed registry as a catalog), `src/core/providerConformance.test.ts` (item 11: the matrix rows over pi and OpenCode, the lint, the printer, the one-ref-parser scan), `src/channels/modelProxy.test.ts`, `src/channels/adminModelProxy.test.ts`, `src/core/dispatch/provision.test.ts` (`mintRunBearer`), `src/core/dispatcher.test.ts` (the bearer through a dispatch), `src/core/runEnding.test.ts` (the finish hook), `src/core/trace/workerTrace.test.ts` (the route word), `src/core/commands/providers.test.ts` (item 12: the comparison, the command, the drift gate), `deploy/cloudflare/modelProxyForwarding.test.ts` (the shim forwards the paths blind; the worker-bot project), `src/core/harness/pi/harness.test.ts` (item 12a: the held turn and the reissue steer).
- **Docs**: [record 0032](../../decisions/0032-pi-is-the-harness-the-native-loop-retires.md) (the proxy, the run bearer and the meter), [record 0016](../../decisions/0016-long-lived-process-not-serverless.md), [http-ingress.md](http-ingress.md) item 10 (the shim's part), [tracing.md](tracing.md) item 17 (the `model.turn` emitters), [harness-pi.md](harness-pi.md) item 15 (the turn guard the proxy enforces, derived from the wall clock), [How-to: operate production](../../how-to/operate-production.md) (the probe).

## Behavior

1. **Three routes, the shapes a harness speaks natively.** `POST /v1/messages` is Anthropic-shaped (`anthropic-messages`), `POST /v1/chat/completions` Chat-Completions-shaped (`openai-chat`) and `POST /v1/responses` Responses-shaped (`openai-responses`) — the same words a `providers:` block's `wire` declares (item 11; the legacy `type` words load as their aliases), so a run's route is its provider's wire and nothing is ever translated between shapes: a run whose provider speaks another shape is refused `400 wrong_shape` naming the path it should have used. A preset on OpenCode with a Responses block is refused at dispatch by name (`resolveTarget`) until OpenCode's bundled `@ai-sdk/openai` is measured against the logging fake — the conformance matrix's declared `cannot` carries the same reason — so the Responses route's clients are pi's runs. The shim Worker forwards the three paths to the container the way it forwards `/ingress` — it answers none itself, reads nothing, and its route table names them with the one word `model-proxy` — and the Access application does not cover them; the run bearer is the whole door, decided in the bot. Transparent means: the proxy touches exactly two fields of the body (item 4) and re-serializes the rest as it came — `cache_control` markers, tool definitions and `tool_choice`, `thinking`, `output_config.effort`, `reasoning.effort`, `stop_sequences`, `metadata`, `stream` all reach the provider byte-for-byte — and hands the provider's answer back with its status, `content-type` and request id, a streamed answer chunk for chunk in order.
2. **The run bearer.** One is minted the moment a run's executor is provisioned (the dispatch's attach, `mintRunBearer`), bound to the run's id and pinned to the resolved `<provider>/<model>`, the preset's `maxTokens` and `maxTurns`, expiring provisionally at the provisioning allowance plus the run's effective lease plus the grace (`provisionalBearerExpiresAt`, [decision 0046](../../decisions/0046-a-budget-is-a-lease-carved-from-its-parent-and-one-module-proves-the-leases-fit.md)); the moment the harness starts the lease it moves the expiry to the lease's end plus the grace of one minute (`leaseStarted`, `bearerExpiresAt`, `ALLOWANCES.bearerGrace`) — the write-up and the post-step run inside the lease ([harness-pi.md](harness-pi.md) item 15), so a minute is all a late call needs, and the grace is measured from the lease, never from the attach. The token is `sbr_<runId>.<secret>` — the run named in the clear, 32 random bytes as the secret — so the proxy tells an unknown run from a wrong secret without a second lookup, and secrets compare in constant time. It is **revoked** the moment the run is reported finished (`RunEnding.finished` runs `onFinished` before the seal) and again by the dispatch's outer finally for a run that never reached its loop; a revoked entry stays until its expiry so a late call is answered `revoked`, then is swept. The store is in-process: a bot restart drops every bearer with the runs that held them, and the generation that resumes a run mints its own. A second mint for the same run replaces the first — except that a pi which outlived the bot still holds the bearer the previous generation revealed to it ([harness-pi.md](harness-pi.md) item 8): the run's harness facts carry that bearer's secret hash (`bearerHashOf`, SHA-256 hex — the store keeps hashes, never secrets, and a hash buys no call), and the generation that re-attaches adopts it onto the run's fresh entry (`adopt`) so pi's calls verify under this generation's grant and turn counter beside the new bearer; a row without a hash cannot make that pi's calls verify, so the harness ends it and restarts pi with the new bearer instead. Without a store (the CLI, tests) nothing is minted and the run is byte-identical to before. The token returns to the caller alone: never logged, never on the record, never in a file. **Rotation.** A process relaunched under a living bot — a replaced container, the ceiling of [record 0038](../../decisions/0038-the-harness-is-a-contract-pi-is-its-one-implementation.md)'s survival clause over [harness-pi.md](harness-pi.md) item 16's floor — gets a new secret on the run's own entry (`rotate`), never a second mint: the turns spent, the expiry and the span stay exactly what they were, where `mint` would reset them and hand each relaunch a fresh budget. The order is for a bot death: the new hash joins the entry and is handed to the caller's write of the run's row BEFORE every earlier hash — the mint's, an operator's, an adopted one — is dropped, so a generation that dies between the two steps leaves a row naming the secret the relaunch was minted and never the orphan's; a row write that throws propagates with both secrets verifying and nothing dropped. The write is issued synchronously inside `rotate` (the run's `saveFacts`, queued on the ledger's write-through, which flushes in order): what the store orders is that write's place in the queue, so no facts write may land between the callback and `rotate`'s return. A re-attach adopts and never rotates: a live process cannot be handed a new secret. `rotate` refuses by name the runs `issue` mints nothing for — unknown, ended, expired — and hands the row nothing then; `grantOf` counts the secrets that buy the run's calls (`bearers`): the mint's, one more per `issue` and `adopt`, exactly one once a rotation completes. The rotation's one caller is the run loop's relaunch (`prepareRelaunch`, [harness.md](harness.md) item 6): the row's facts — the new hash and the relaunch count — are written inside `record`, the harness is opened on the rotated bearer only after `rotate` returned, and a rotation refused by name refuses the relaunch, so no process ever starts on a secret the row does not name.
3. **The door, from the headers alone, in order.** A path that is not one of the three is `404`; a non-POST `405`; no bearer `401 missing_bearer` (the bearer rides `Authorization: Bearer <bearer>`, the OpenAI shapes' header, or `x-api-key: <bearer>`, the Anthropic SDK's — either is accepted on every route); a token that is not a run bearer `401 malformed_bearer`; a run this bot never minted `404 unknown_run`; the right run with a wrong secret `401 unknown_bearer`; a bearer past its expiry `403 expired`; one whose run ended `403 revoked`. The node adapter decides this before it reads a byte of the body and destroys a refused request. Past the door: a body over the cap (32 MB, the Messages API's own) is `413`; a body that is not a JSON object `400 invalid_body`. Every refusal is written in the route's shape so the client's SDK reads it as the provider's own error: `{ type: "error", error: { type: <code>, message } }` on the Anthropic route, `{ error: { type: <code>, message } }` on the two OpenAI ones. A refusal never echoes what the caller sent: an unknown path is refused without repeating it, and the wrong-shape message names the route hit from the proxy's own table.
4. **Pinning and the upstream.** `model` on the wire is the grant's bare model id whatever the body named; the output cap is the grant's `maxTokens` — as `max_tokens`, on the chat shape as `max_completion_tokens` when the body capped with that key (a stray `max_tokens` beside it is dropped, so no second cap survives), on the Responses shape as `max_output_tokens`, never below 16 (the API's own floor: a smaller pin would be refused upstream). The upstream is the run's `providers:` entry: the Anthropic shape goes to `<baseUrl ?? https://api.anthropic.com>/v1/messages` with the real key as `x-api-key` and the client's `anthropic-version` (the SDK's default when the client sent none) and `anthropic-beta`; the chat shape to `<baseUrl>/chat/completions` and the Responses shape to `<baseUrl>/responses`, each with `Authorization: Bearer <key>` when the entry names an `apiKeyEnv`, and no authorization header at all when it does not (a local endpoint). Only `accept`, `anthropic-version` and `anthropic-beta` are forwarded from the request; the run bearer's own headers never are. A provider the config does not name, or one without a `baseUrl` on an OpenAI shape, is `503 provider_unconfigured`; a key variable that is unset `503 provider_key_missing` naming the variable — both decided before a turn is spent. The key is revealed into the upstream request and nowhere else. The `providers:` block is read at each call, so a config reload reaches the proxy as it reaches the mint.
5. **The turn guard is enforced here, as a typed run event.** The grant's `maxTurns` is the preset's runaway guard — `runawayTurnCap(maxMinutes)`, six turns a minute over the wall clock ([harness-pi.md](harness-pi.md) item 15; 540 for a coding run) — not a [budget](../vocabulary.md#budget) a working run reaches. A turn is counted before the call is forwarded (so concurrent calls cannot overrun) and the call past it is refused `403 turn_budget_exhausted`, forwarding nothing and opening no span; the refusal publishes one `run_note` of kind `turn_budget_exhausted` on the run's stream naming the guard and the counts (`model proxy refused a call past the run's 540-turn guard (540 turns used)`; the response body says `the run is past its 270-turn guard (270 turns used)`) — the same kind the harness notes when it ends the run on the guard ([harness-pi.md](harness-pi.md) item 6), so the run page and the friction analyzer read one vocabulary. A `403` rather than a `429`: a rate-limit status is one an SDK retries, and this is a refusal. An upstream failure spends the turn. A run that ended between the door and the turn — its bearer verified a moment ago, the run revoked since — is `403 revoked` with no note: the store answers `ended`, never a zero-turn budget.
6. **The meter: one `model.turn` per proxied call, the loop's attrs.** Every forwarded call opens one `model.turn` span under the span the grant carries — the request root at the mint, the harness's `run.agent` once `reparent` moved it there ([harness-pi.md](harness-pi.md) item 5), so a proxied turn lands where the native loop's did — with `model` (the `<provider>/<model>` ref, as `run_meta` carries it) and the tools the request offered at start — `tools`, their count; `toolNames`, sorted and comma-joined; `toolChoice`, the request's word as `auto` (the default when tools came), `none` (no tool came — whatever the choice said, an explicit `auto` included — or the request said so), `any` (the OpenAI dialects' `required`) or `tool` (a named tool — the chat shape's function object, the Responses shape's flat `{type: "function", name}`; the Responses tool table is flat too, its names read at the top like Anthropic's) — so a record says whether a write-up turn still carried the workspace tools ([harness-pi.md](harness-pi.md) item 15, [decision 0046](../../decisions/0046-a-budget-is-a-lease-carved-from-its-parent-and-one-module-proves-the-leases-fit.md)) and a call that never answers still says what it offered. **The harness's marks shape what goes upstream** (`shapeTools`, beside `pinRequest`; the record's amendment): the harness marks the run's bearer entry when it steers the write-up (`markLoopEnded`), and every request after it goes upstream with `tool_choice: none` — the model is shown its tools and may call none, so the checkpoint is a text turn, the cached tool and system prefix stand, and only the conversation's cached blocks are re-read for that turn; a [follow-up](../vocabulary.md#follow-up) turn on the session ([harness-pi.md](harness-pi.md) item 14) is marked for its duration with the tools it may call (`markTurn`, cleared by `clearTurn`): the request's tool list goes upstream trimmed to them with the choice left to the model (the description turn reads the [pull request](../vocabulary.md#pull-request) and the change and submits; the [verdict](../vocabulary.md#verdict) turn reads the head and submits), or, marked without a list, as it came (the re-review turn). Nothing is refused for the tools it carries; `toolChoice` on the span is the word that went upstream, `tools` and `toolNames` what the run offered. The marks live on the run's entry with its bearer, so a rotation or an adopt keeps them; and ends it with the attrs the native runner set: `stopReason` mapped as the deleted adapters mapped it (Anthropic `end_turn` and `stop_sequence` → `end_turn`, `tool_use`, `max_tokens`, anything else `other`; chat `stop` → `end_turn`, `tool_calls` → `tool_use`, `length` → `max_tokens`; the Responses shape has no finish reason, so its status is mapped — `completed` with a `function_call` item in the output → `tool_use`, `completed` otherwise → `end_turn`, `incomplete` at `max_output_tokens` → `max_tokens`, anything else `other`), `inputTokens`, `outputTokens`, `cacheReadTokens` and `cacheWriteTokens` when the provider reported them (`usageFromAnthropic`, `usageFromOpenAI`, `usageFromResponses` in `src/core/modelProxy/usage.ts` — the Responses usage shape is `input_tokens`, `output_tokens`, `input_tokens_details.cached_tokens` and `cache_write_tokens` as the cache counts, with `output_tokens_details.reasoning_tokens` riding inside the output count, never a fifth counter), and `ttftMs` (the first chunk's arrival) on a streamed answer. **The meter row** (record 0052): every `model.turn` also says who bills it and what it cost — `biller` (the block the call was forwarded through, the grant's provider name) and `vendor` (the card's, when the grant carries one) from the span's start, and at its end `usd`, `priceSource` and, on a BYOK turn, `feeUsd` (`priceTurn` in `src/core/modelPricing.ts`, over the operator's `costs.prices` and the card the grant carries). The precedence is `provider` > `operator` > `registry` > `none`: a provider-reported cost is stored as reported — the meter reads it beside the counters off the same usage object (`reportedCostOf`: OpenRouter's final chunk's `cost`, `is_byok` and `cost_details.upstream_inference_cost`; on BYOK `usd` is the fee plus the upstream cost with the fee beside as `feeUsd`; a BYOK chunk without the upstream charge keeps the fee as `feeUsd` beside the layer below that prices the tokens, never as `usd`) — else the operator's layer prices the counted tokens (`costs.prices` for the exact ref, [costs.md](costs.md) item 4b, or the block's own `models.<id>.price`), else the registry layer (the card's rate, its long-context tiers by pi's rule — the whole request re-rates at the highest tier whose `inputTokensAbove` the input side, input plus cache reads plus cache writes, exceeds — or the Anthropic family list, folded into this layer), else the turn is `none`: unpriced, never $0, and a stream broken before its final chunk is unmetered and `none`. A streamed answer is forwarded chunk for chunk as it arrives and the span ends only after the last chunk; the `SseMeter` reads the counts as the bytes pass — Anthropic's `message_start` carries the input and cache counts, `message_delta` the stop reason and the final output count; a chat stream's `finish_reason` and its `usage` frame when the client asked for one (`stream_options.include_usage`); a Responses stream's closing `response.completed` / `response.incomplete` / `response.failed` event, which carries the whole response with its usage and status — a `data:` line may span chunks and CRLF framing is accepted. A buffered JSON answer is metered whole. An upstream status outside 2xx is forwarded verbatim (status, headers, body) and ends the span `error` with `httpStatus`; an upstream that cannot be reached is `502 upstream_unreachable`; a stream the upstream breaks ends the span `error` and errors the forwarded stream. The span's parent is the request root, so it streams onto the run's record through the same sink every setup span uses and is dropped, like any late span, once the run is sealed.
7. **What the proxy never says.** One log line per call — `[model-proxy] run=<id> turn=<n>/<max> <shape> → <status> in=<bytes> out=<bytes> <ms>ms` — and one per refusal naming the status, the code and the run id; never a body, a bearer, a secret or a key. The bearer store's verdicts carry a reason and a run id, never token material.
8. **The operator's probe bearer.** `POST /admin/model-proxy/bearer` with `{ "runId": "<id>" }` mints one more bearer on a live run's own entry — same expiry, same turn counter, same pinned model — and answers `201 { ok, runId, bearer, expiresAt, model, path, turns: { used, max } }`, the bearer written to that response once and never to a log (the line names the operator and the run). The door is the restart's and the crash's: an ingress bearer whose `http:<subject>` actor holds `deploy:write` — the operator who may stop the bot may mint a probe for one of its runs — `401` without a bearer, `403` without the grant naming it, `503` without a token map, `405` for a non-POST, all from the headers. A run this bot never minted is `404 unknown_run`; a run that ended or whose bearer expired `409 run_ended`; a body that is not `{ runId }` with a run id in the run-id alphabet `400`. Its calls are the run's: they spend its turns and land on its record as `model.turn` spans, and they stop at its end.
9. **The node adapter.** The door from the headers, then the call; a buffered answer is written whole; a streamed one has its headers flushed first and is written chunk by chunk under back-pressure, then ended; a caller that goes away mid-stream aborts the upstream call through the request's signal. Every response's content type is one of three literals chosen by kind — the provider's JSON or event stream as such, anything else as `text/plain` — with `X-Content-Type-Options: nosniff`, so nothing this route writes, a refusal or a provider's error page, is ever rendered by a browser as a document; the other forwarded headers ride as they came.
10. **Every run is the proxy's client.** The run's pi is handed the bearer as its provider key through the process environment ([harness-pi.md](harness-pi.md) item 4), and every model call it makes is one proxied turn here — pinned, guarded and metered as above; no code path in the bot calls a provider with the deployment's key for a run's turn. The bearer exists on every run — minted and revoked around each — and buys nothing unless presented.
11. **The card: one description of `<block>/<model>` per run, decided before the first call** ([record 0052](../../decisions/0052-a-run-resolves-one-model-card-and-every-control-is-decided-against-it-before-the-first-call.md); `src/core/modelCard.ts`, `src/core/modelRegistry.ts`, `src/core/installedModelRegistry.ts`). The `providers:` block declares what the card is resolved from (`src/core/provider.ts`): `wire` — `anthropic-messages`, `openai-chat` or `openai-responses` — with the legacy `type` (`anthropic`, `openai-compatible`) loading as its alias for one release, a block declaring both refused by name; `vendor` — a name, or `model` for an aggregator whose vendor is the model id's first segment; `catalog` — the pi registry file the block's cards are read from, defaulting to the block's own name when such a file exists and `none` for no catalog; `models.<id>` — the operator's override (`levels`, `capField`, `window`, `inputs`, `cache`, `price`, and `answers` — the answer shapes the card takes, `tool` and/or `text`, read by the intake gate's load-time check, [routing-and-config.md](routing-and-config.md) item 27); `passthrough` — extra body fields merged into every request, never a control. `vendorOf` is the one vendor parse beside `parseModelRef` (`catalog` never enters it: a block named unlike its catalog still serves the vendor the declaration or the id names), and the validator refuses a malformed declaration by name (`validateProviders`: a `catalog` the installed library does not ship, a `vendor: model` block whose ids carry no prefix, a `models.<id>.levels` word outside our tiers or `null`). `resolveModelCard` layers the operator's override over the registry card over the wire's structural defaults — the cap field each wire spells the cap with (`max_tokens`, `max_completion_tokens`, `max_output_tokens`), pi's 128000 window when no layer names one, the vendor-keyed cache table — carrying per field the layer that named it; the registry is the installed `@earendil-works/pi-ai`'s `providers/data/*.json`, read once per process, a card looked up under the block's wire first and then under any wire, our `openai-chat` spelled pi's `openai-completions`. `decideControls` then answers **native**, **degraded** or **refused** for each control the request asks about: the effort tier (a tier the card names is native under the card's own wire word, whatever its spelling; a tier the card marks `null` is refused; `xhigh`/`max` a card does not name degrade to the highest named tier below, and only that fallback degrades; a card naming no levels sends the asked tier unvouched), the output cap and the window (always decided — every run sends one and compacts somewhere), images and documents (a document degrades to a text stub until a harness carries files), and the cache rule. The dispatcher resolves the card in `resolveTarget` where it checks the block, before any ack card or span: a refused control ends the run with the reply naming the model and what it takes (`Model "<ref>" refuses effort "max": <model> does not take effort "max"`, the refusal code `model_card_refused`), the same shape as `Unknown provider`, and never a call that fails mid-run; the card rides the record as `run_meta.card` and each degraded control publishes one `run_note` of kind `control_degraded` (`control`, `asked`, `applied`, `vouched`, `why`) before the first turn. The harnesses run on the card: it rides `HarnessRun.card` into pi's `models.json` and OpenCode's configuration ([harness-pi.md](harness-pi.md) item 4, [harness.md](harness.md) item 12), so the word on the wire is the card's, never one a harness chose; the harness write also names the biller's own provider for the block (record 0052's amendment): OpenCode's `package` is a table on the wire and the biller (`openCodeProviderPackage`; `billerHarnessProvider`, `src/core/provider.ts`) — `@ai-sdk/anthropic` on the Anthropic wire, `@ai-sdk/openai` on Responses, the aggregator's own `@openrouter/ai-sdk-provider` for a biller the table names (it asks for `usage.cost` on the final chunk and spells reasoning `reasoning: { effort }` in the tier variants), and `@ai-sdk/openai-compatible` only for a block that is generic in fact — a `markers` cache rule on a biller the table names caches the aggregator's own way: the pinned binary exempts the openrouter route from per-block `cache_control` placement (measured, item below), so OpenCode's write carries OpenRouter's top-level `cache_control: { type: "ephemeral" }` (its "automatic caching") as the provider's `settings.extraBody`, merged into every request body — and a `markers` rule on a chat-wire biller with no harness-side provider is decided `degraded` with a `control_degraded` note true on both harnesses ("no harness-side provider vouches for the markers"), never a vouched `native` (`decideControls`); the meter still records four counters and the proxy serves the wire's route (item 1); `modelPricing`'s list fallback still drops the provider prefix alone through `parseModelRef` ([costs.md](costs.md) item 4b — an aggregator's vendor-prefixed ref stays a list miss, reported unpriced), so a ref is split in one place. The matrix that proves the card is a scaffold (`src/core/testing/providerConformance.ts`): rows are controls × outcomes, columns are harnesses, each cell a card resolution and control decision asserted against the record the card produces and the request body derived from that harness's own configuration write (`piCapturedBody` reading `piModelsJson`, `openCodeCapturedBody` reading `openCodeConfig`), so a green cell proves the file carries the wire's word; the lint names a wire, an example-config block or a control with no row, and a row a harness cannot pass is declared `cannot` with the reason — one remains on OpenCode: the Responses block, refused at dispatch until `@ai-sdk/openai` is measured (the aggregator's marker payload is green: measured with the real binary through the real proxy, the pinned 2.0.3 exempts the openrouter route from per-block cache placement and no configuration key reaches the request's own `cache` option, so the markers ride as the top-level `cache_control` through `settings.extraBody`, measured beside the usage ask and the reasoning spelling in `opencode/testing/realDriver.test.ts`).
12a. **The provider level and the held turn (record 0064).** The proxy is the one place a provider's health is observed, so it reports the level to the plane through the optional `plane` seam (`ModelProxyDeps.plane`, wired in the bot process to the write-through's `planeLevel`/`planePark` — `POST /plane/level` with `{name: "provider", provider, side: up|down}` and `POST /plane/park`; the CLI's `ask` loopback has no ledger and wires none): a transport failure or a 5xx gets ONE retry in all — the two shapes share it, so a turn makes at most two upstream calls; a client abort is neither retried nor reported (the caller going away says nothing about the provider); a failure past the retry reports the provider `down` and **parks the run** on `provider_up(provider)` (the park keyed `<provider>#<runId>`, so a second failing turn of the same run is the same wait), and the error is still relayed in the route's shape; a relayed success reports `up` — the handler dedupes to changes, so a healthy provider is not re-reported every turn, and the first success after a `down`, from ANY run, is what flips the condition. On the plane, the `up` writes one typed reissue message per parked live run into its durable inbox (sender `plane`) and offers a `steer` carrying the same message and durable sequence in that transaction, then deletes the park row. The state Worker pushes the committed offer through `/plane/effects`; the active generation atomically verifies the open effect and its durable ownership while renewing the lease before it puts the row into the live harness inbox, acknowledging `done` after delivery, `skipped` when that sequence already arrived, and `deferred` while draining or on a non-owner. Deferred offers ride the owner's heartbeat, sequence dedupe makes push/heartbeat/reclaim one release, and the durable row remains the restart fallback; a run whose provider never answers ends on its lease as today. **The harness's half** (`src/core/harness/pi/harness.ts`; [harness-pi.md](harness-pi.md) item 6): on a ledger-tracked run (`HarnessRun.providerPark`, set by the run loop only where a ledger run exists — the park and the reissue steer both land on the run's live row, so without one no steer would ever release a hold) a relayed failure of the shape the proxy parks on (`isParkedProviderError`: the proxy's own 502, a relayed 5xx — never a stream cut after a relayed success, which parked nothing and keeps the retry ladder, and never a 429, which the proxy relays without parking) holds the failed turn with **no `harness_error` event and no retry ladder**: pi stays settled on the failed call while the loop keeps ticking its budgets and draining the inbox, the lease still counting. The plane's reissue steer — the inbox row with sender `plane`, its text `reissueSteerSentence` read back whole by `isReissueSteerText` (`src/core/plane/decide.ts`) — is recognized as it drains whether or not the hold exists yet (the plane's `up` can beat the harness's read of pi's errored `message_end`) and **buffered, never steered into pi as a follow-up**: it releases the hold and **re-issues exactly that held turn once**, as a prompt under its own id like the retry ladder's (pi reads a steer only at a turn boundary that is not coming), sent only once pi has settled on the held turn — one prompt per release however many reissue rows the buffer carried, the rows recorded as `input` events and their seqs consumed on pi's echo like any follow-up's, and a refused release prompt keeps the hold for the next settle (a `harness_error` note names the refusal) rather than clearing it with no steer ever coming again; a follow-up drained beside them rides the usual steer and is read at the re-issued turn's boundary. A run without the capability keeps the ladder; a wind-down already begun owns the ending as before.

12. **`providers check` and the registry drift gate** ([record 0052](../../decisions/0052-a-run-resolves-one-model-card-and-every-control-is-decided-against-it-before-the-first-call.md); `src/core/commands/providers.ts`, `scripts/registry-drift.ts`). The card's live half: `providers.check`, a typed command on every surface (action `providers:read` — a browser session's read baseline, a grant everywhere else; gated on no capability), reads the provider's own `GET <baseUrl>/models/<vendor>/<model>/endpoints` for each AGGREGATOR model the configuration names — a block declaring `vendor: model`, the refs from `configuredModelRefs` (the defaults' per-agent models and every block's `models.<id>` override key) — through the deps' injectable fetch, never a global one. `compareCardToEndpoints` is the pure comparison over the card the dispatcher would resolve (item 11) and the endpoint's facts (`endpointsFromJson`: `architecture.input_modalities`, each endpoint's `context_length` and `supported_parameters`): it reports where the two disagree, each drift with the `models.<id>` override that would pin the endpoint's fact — a window differing from the card's (a wire default is called unvouched) pins `window: <n>`; a modality list contradicting or unvouched by the card pins `inputs: {image: …}`; a parameter list without the card's cap field pins the wires' spelling it does list (`capField: max_tokens`); a `reasoning` parameter against a card naming no levels is reported as "levels not stated" — the endpoints API lists parameter names, never effort values — with the pin naming the wire's own words for the tiers pi's rule would otherwise refuse, confirmed against the vendor's documentation before pinning; a card naming levels against a parameter list without `reasoning` pins the refusal. A failing endpoints read is that model's own error line, never the whole check's failure; a configuration naming no aggregator model answers that plainly. The drift gate is the same module's offline half: `scripts/registry-drift.ts` (`npm run check:registry-drift`, under `check:consistency`) runs `registryDrift` over the example config's refs and the installed pi registry, naming every model whose block reads a catalog the registry ships but whose card the registry no longer carries — a pi bump that drops a card fails `verify` by name instead of surfacing on a run; a block without a catalog has no registry to disagree with and is never drift.

## Validation criteria

| Criterion | Proof |
|---|---|
| 2: a mint names its run and verifies to the grant with zero turns; secrets never collide and verify only for their own run; a malformed token, an unknown run and a wrong secret are refused by reason, garbage never throws; a second mint replaces the first | `[unit]` `src/core/modelProxy/runBearers.test.ts::RunBearerStore — mint and verify::*` |
| 6: the marks on the run's entry — the loop's end read back, a turn with tools, a turn without, the turn cleared; they survive `rotate` and `adopt`; a run never minted or one that ended takes none | `[unit]` `src/core/modelProxy/runBearers.test.ts::RunBearerStore — the loop's end and a turn's tools, marked by the harness for the proxy (model-proxy item 6)::*` |
| 2: the lease's start resets the expiry to the lease's end plus the grace, replacing the mint's provisional expiry; `issue` hands out the new one; a run never minted or one that ended takes no lease | `[unit]` `src/core/modelProxy/runBearers.test.ts::RunBearerStore — expiry and revocation::the lease's start resets the expiry to the lease's end plus the grace…` |
| 2: valid to the instant before expiry and `expired` from it; `revoke` ends every bearer of the run and a later call is `revoked`, not unknown; a revoked entry is swept past its expiry and becomes an unknown run; the sweep runs on every mint | `[unit]` `src/core/modelProxy/runBearers.test.ts::RunBearerStore — expiry and revocation::*` |
| 2: a pi that outlived the bot keeps the previous generation's bearer: `bearerHashOf` names a token's secret as 64 hex without being one; `adopt` lets that bearer verify on the next generation's store under its grant and turn counter beside the new bearer; the hash presented as a secret buys nothing; `adopt` refuses an unminted run, one past its expiry, an ended run and a hash of another shape | `[unit]` `src/core/modelProxy/runBearers.test.ts::RunBearerStore — a bearer across generations (docs/reference/specs/harness-pi.md item 8)::*` |
| 2: `rotate` mints a new bearer on the run's own entry and drops the old — the old answers `unknown_bearer`, the new verifies to the same grant with the same turns, expiry and span, and the row is handed the new secret's hash once; an unknown, ended or expired run is refused by name with nothing handed to the row; a row write that fails leaves both secrets verifying and the old never dropped; `grantOf` counts the run's secrets | `[unit]` `src/core/modelProxy/runBearers.test.ts::RunBearerStore — rotate: a relaunch's new bearer on the run's own meter::*` |
| 2: the relaunch rotates before the harness rebuilds — the row written once inside the rotation with the new hash and the count, the old bearer refused, the turns kept, the new bearer in the relaunched process's environment; a rotation refused by name refuses the relaunch with nothing written | `[unit]` `src/core/dispatch/relaunch.test.ts::prepareRelaunch — the relaunch decided and prepared::relaunches: the bearer rotated on the run's own meter…`, `::refuses by name, writing nothing…`, `src/core/dispatch/runLoop.test.ts::the pi harness — the container replaced under a living bot: the relaunch ceiling::a living bot, the typed error from the log read…` |
| 2, 5, 8: `consumeTurn` counts to `maxTurns` and refuses past it naming the counts, never past the cap; unknown and revoked runs are refused as `ended`; `issue` mints another bearer on a live entry with the same expiry and counter and nothing for an unknown, revoked or expired run; `grantOf` reads the facts and never a secret | `[unit]` `src/core/modelProxy/runBearers.test.ts::RunBearerStore — turns and the operator's extra bearer::*` |
| 1: the three paths name the three shapes and nothing else | `[unit]` `src/channels/modelProxy.test.ts::the model proxy's paths::names the three shapes a harness speaks natively, and nothing else` |
| 1: a preset on OpenCode with a Responses block is refused at dispatch by name; the same block on pi resolves | `[unit]` `src/core/dispatch/resolve.test.ts::resolveTarget — the provider checked, and the target repo/ref/PR started::a preset on OpenCode with a Responses block is refused at dispatch by name…` |
| 3: the door — no or malformed bearer 401 with the body unread; unknown run 404, wrong secret 401; expired and revoked 403 by name; non-POST 405 and a non-proxy path 404 before any bearer; either header spelling; wrong shape 400 naming the right path, the Responses route both ways; the same refusals on the Responses path in the OpenAI error shape — none forwards; a refusal never echoes the path | `[unit]` `src/channels/modelProxy.test.ts::the door — decided from the headers, before the body is read::*` |
| 3: a body over the cap 413, non-JSON 400, an array 400 — none forwarded, no turn spent | `[unit]` `src/channels/modelProxy.test.ts::the body — bounded, JSON, an object::*` |
| 1, 4: the Anthropic shape — the preset's model and `max_tokens` on the wire, every other field byte-for-byte, the real key as `x-api-key` with the client's version and beta headers, the bearer nowhere in the upstream request; the version defaulted and the config's `baseUrl` honoured; `pinRequest` is pure | `[unit]` `src/channels/modelProxy.test.ts::pinning and pass-through — the Anthropic shape::*` |
| 1, 4: the OpenAI shape — the provider's `baseUrl` and bearer, model and cap pinned, the rest kept; `max_completion_tokens` pinned on its own key with no second cap; a keyless provider sent no authorization; an unset key variable 503 by name and an unnamed provider 503, both before a turn is spent | `[unit]` `src/channels/modelProxy.test.ts::pinning and pass-through — the OpenAI shape::*` |
| 1, 4: the Responses shape — `<baseUrl>/responses` with the provider's bearer, model and `max_output_tokens` pinned at or above the floor of 16, `reasoning.effort` and the flat tool table byte-for-byte; `pinRequest` is pure | `[unit]` `src/channels/modelProxy.test.ts::pinning and pass-through — the Responses shape::*` |
| 6: a streamed Anthropic answer forwarded chunk for chunk in order with its headers, the span open until the last chunk and ended with model, stop reason, the four token counts and the time to first token under the grant's span; the tools offered — count, sorted names and the `tool_choice` word on both dialects, `none` when no tools came — on the span from its start; after `markLoopEnded` the checkpoint request goes upstream with `tool_choice: none` on both dialects with its tools untouched, a turn marked with tools goes upstream trimmed to them, a turn marked without tools goes as it came, an unmarked run is untouched, and the span's `toolChoice` is the upstream word; a buffered answer metered whole with the adapter's stop-reason mapping; a streamed OpenAI answer's `tool_calls`, cached tokens and the usage-less case; the SSE meter across chunk boundaries and CRLF | `[unit]` `src/channels/modelProxy.test.ts::the meter — one model.turn span per proxied call, the runner's attrs::*` |
| 6: the Responses route metered — a coding preset on pi offers tools with `reasoning.effort` and gets a tool call back from the fake, the stream forwarded chunk for chunk and the span ended `tool_use` with the usage shape's four counts and the ttft; a buffered answer metered whole with the status mapping; a `tool_choice` in the flat Responses shape recorded as the span's word; the marks shape the flat table as they shape the other two dialects' | `[unit]` `src/channels/modelProxy.test.ts::the meter — the Responses route (model-proxy item 6)::*`, `src/core/modelProxy/usage.test.ts::usageFromResponses (token usage → TokenUsage)::*` |
| 5: the call past `maxTurns` is 403 `turn_budget_exhausted`, publishes the `run_note` naming the guard and the counts, the body says the run is past its guard, forwards nothing and opens no span; a run that ended between the door and the turn is 403 `revoked` with no note | `[unit]` `src/channels/modelProxy.test.ts::the turn guard — a refusal is a typed run event::*`, `::the turn guard — a run that ended between the door and the turn::*` |
| 6: an upstream error forwarded verbatim with the span ended error and `httpStatus`, the turn spent; an unreachable upstream 502; a broken stream errors the forwarded stream and the span, unmetered and `priceSource: none` | `[unit]` `src/channels/modelProxy.test.ts::upstream failures::*` |
| 12a: the harness half — a parked-shape failure on a park-capable run holds the turn with no `harness_error` and no retry ladder; the plane's reissue steer re-issues exactly the held turn once as a prompt (two steers in one drain are one prompt, their seqs consumed on pi's echo); a reissue steer drained before the hold exists, or before pi settles on it, is buffered — never an ordinary steer, never a mid-turn prompt — and still releases the hold; a refused release keeps the hold for the next settle; a stream cut keeps the ladder even when park-capable; the classifier takes the proxy's 502 and relayed 5xx, never a stream cut or a sub-5xx status | `[unit]` `src/core/harness/pi/harness.test.ts::runPiHarness — a run on pi from the first file to the answer::a parked provider failure on a park-capable run holds the turn with no harness_error and no ladder; the plane's reissue steer re-issues the held turn once…`, `…::a reissue steer drained before the hold exists is buffered, never an ordinary steer…`, `…::a reissue steer drained between the errored message_end and pi's settle waits for the settle…`, `…::a refused reissue prompt is a failed release…`, `…::a provider-up effect pushed into the owning live inbox reissues the parked turn immediately and acks done before any heartbeat or reclaim`, `…::a stream cut after a relayed success keeps the retry ladder even on a park-capable run…`, `…::isParkedProviderError matches the failures the proxy parks on and never a stream cut or a sub-5xx status` |
| 12a: a failure past the proxy's one retry reports the provider `down` and parks the run; a relayed success reports `up` once per change; the plane's `up` atomically persists and offers each release, live-pushes it only after an atomic effect/owner fence and lease renewal, preserves it on failure/defer or a stale-generation ack, and deduplicates by inbox sequence | `[unit]` `src/channels/modelProxy.test.ts::the provider level and the park (record 0064)::*`, `deploy/cloudflare-memory/runLedger.test.ts::the plane's checkpoint steers and the provider condition — the heartbeat body, /plane/park, /plane/level provider (record 0064)::a provider down and parked live run recover atomically as one durable row and one pushed offered steer; repeat up writes nothing`, `::fences a pushed steer and renews its owner's lease atomically before registry delivery`, `::a stale generation cannot close a steer offer after reclaim; the durable owner heartbeat receives and closes it`, `::a failed provider-up transaction writes neither the durable row nor its steer effect`, `::a failed live push leaves both the durable row and steer offer for an owner heartbeat; sealing first removes the park and produces no steer`, `src/channels/planeEffects.test.ts::POST /plane/effects — the push transport (record 0064)::the stale generation defers a pushed steer, then the durable owner heartbeat delivers and acks it`, `::renews the durable owner fence before registry delivery, so reclaim cannot cross the local wake`, `::rechecks drain after the durable fence returns, so handoff cannot wake stale registry state`, `src/core/runLedger/writeThrough.test.ts::events, state, heartbeat::an owner heartbeat delivers a live steer once; duplicate heartbeat and reclaim copies of its durable sequence are skipped` |
| 6: the meter row on the span — a provider-priced turn stores the figure as reported with `priceSource: provider`, the biller and the card's vendor; a BYOK turn sums the fee and the upstream cost with `feeUsd` beside (without the upstream charge the fee rides beside the layer that prices the tokens, never as the turn's dollars); a turn with no wire cost and an operator entry prices `operator`; a registry card's tier above 272,000 input tokens prices the whole request at the tier | `[unit]` `src/channels/modelProxy.test.ts::the meter row — biller, vendor, usd, priceSource (model-proxy item 6)::*` |
| 6: the precedence as a pure function — provider over operator over registry over none, the BYOK sum, the tier rule counting cache reads and writes on the input side, the Anthropic family list as the registry layer's fallback, no usage → `none` whatever the card says | `[unit]` `src/core/modelPricing.test.ts::priceTurn — the meter row's precedence (model-proxy item 6)::*` |
| 6: the cost fields read beside the counters — OpenRouter's `cost`, `is_byok` and `cost_details.upstream_inference_cost`; a usage without a finite cost reports none | `[unit]` `src/core/modelProxy/usage.test.ts::reportedCostOf (the wire's cost fields beside the counters)::*` |
| 7: no log line carries the bearer, the key or any request or reply text | `[unit]` `src/channels/modelProxy.test.ts::what the proxy never says::*` |
| 9: the adapter refuses from the headers with the body unread and the request destroyed, pipes a stream chunk by chunk after flushing the headers, and a caller's close aborts the upstream call; every content type from the closed literal table with `nosniff`, an HTML-claiming upstream body written as plain text | `[unit]` `src/channels/modelProxy.test.ts::createModelProxyHandler — the node adapter::*` |
| 8: the probe bearer — 201 with the bearer, expiry, model, path and turns for a live run and a log that names the operator and the run but never the bearer; unknown run 404, ended or expired 409; the door's 401 / 403 naming the grant / 503 / 405 from the headers; a malformed body 400; nothing minted on a refusal | `[unit]` `src/channels/adminModelProxy.test.ts::POST /admin/model-proxy/bearer — an operator's bearer for a live run::*` |
| 2: `mintRunBearer` pins the resolved provider and model and the preset's caps, expires at the profile's minutes plus the margin, hangs under the request root and publishes on the run's stream; nothing without a store or for an unnamed provider | `[unit]` `src/core/dispatch/provision.test.ts::mintRunBearer — the run's model-proxy bearer::*` |
| 2, 10: through `dispatch()` the bearer exists and verifies during the model turn with no proxied turn counted, carries the resolved model and the preset's caps, and is revoked once the run finishes; a refused dispatch mints nothing and a process without a store runs as before | `[unit]` `src/core/dispatcher.test.ts::the model proxy's run bearer through dispatch()::*` |
| 2: `RunEnding.finished` runs `onFinished` at once, before any seal, once per finish; a throwing hook is logged with the run id | `[unit]` `src/core/runEnding.test.ts::createRunEnding — seal after the reply, records after the seal::finished(id) runs the onFinished hook at once…` |
| 1: the shim's route table names the three paths `model-proxy` and no other `/v1` path | `[unit]` `src/core/trace/workerTrace.test.ts::shimRoute::names the model proxy's three paths as one route word, and no other /v1 path…` |
| 1: the shim answers three paths itself and forwards every other to the container, knows nothing of the proxy's paths, and holds the model keys only to hand them into the container's environment — never in a header, a URL or a fetch | `[unit]` `deploy/cloudflare/modelProxyForwarding.test.ts::the shim forwards the model proxy's paths to the container blind::*` |
| 12: the record's trace — an unknown aggregator model's three unknowns (window, image input, levels) reported with their pins beside the cap field's; an endpoint lacking `max_completion_tokens` reported with `capField: max_tokens`; an endpoint naming `reasoning` without values reported as levels not stated; agreement is no drift; a card naming levels against no `reasoning` parameter pins the refusal | `[unit]` `src/core/commands/providers.test.ts::compareCardToEndpoints — the pure comparison over the card and the endpoint::*`, `src/core/commands/providers.test.ts::endpointsFromJson — the provider's answer parsed to the fields the comparison reads::*` |
| 12: the command reads each aggregator model the configuration names through the injectable fetch — never a direct block's — and reports the drift with its pins; a failing read is that model's error line; no aggregator model is answered plainly | `[unit]` `src/core/commands/providers.test.ts::providers.check — the command over the registry with an injectable fetch::*` |
| 12: the drift gate names a model the bumped registry dropped, by ref; a block without a catalog is never drift; `configuredModelRefs` collects the defaults' models and every block's override keys once each | `[unit]` `src/core/commands/providers.test.ts::registryDrift — the drift gate between the pinned registry and the config's models::*` |
| 12, live: `providers check` against the example config's aggregator models, its output posted on the tracking issue | `[agent]` Run `npm run cli -- providers check` on a checkout whose `config/config.yaml` names an aggregator block (or ask `providers check` in chat with the grant) and post the drift lines with their pins. |
| 11: `vendorOf` names a direct block's vendor off the block name, an aggregator's off the id's first segment and a block named unlike its catalog by its declaration or its id — never its catalog; `type: openai-compatible` loads as `wire: openai-chat`; a malformed `models.<id>.levels` is refused at load by name, as is a `models.<id>.answers` that is not a list or names a word outside `tool` and `text`; the card layers operator over registry over wire with provenance per field, `decideControls` answers native/degraded/refused, and the record's trace rows pass on both drivers with the request body rendered from each harness's own write — the harness-write rows green, the write naming the biller's own provider (the aggregator package with `usage.include`, `reasoning: { effort }` and the top-level `cache_control` via `settings.extraBody`, the generic package only for a biller with no harness-side provider, whose markers card degrades with a note true on both harnesses), OpenCode's Responses row the declared `cannot`; no by-hand split of a ref survives outside `parseModelRef` and `vendorOf` | `[unit]` `src/core/modelCard.test.ts::vendorOf — the one vendor parse::*`, `::resolveModelCard — operator over registry over wire::*`, `::decideControls — native, degraded or refused before the first call::*`, `src/core/modelRegistry.test.ts::the pi registry as a catalog::*`, `src/core/providerConformance.test.ts::the provider table::*`, `src/config/validate.test.ts::validateProviders — the block's declaration::*` |
| 1, 6, 8, live: a bearer minted for a real run buys one model call through the deployed bot, metered on that run's page | `[agent]` (needs an ingress bearer whose subject holds `deploy:write`.) Start any run and read its id from the card's Live run link or `runs list`. `POST $SWITCHBOARD_BASE_URL/admin/model-proxy/bearer` with `authorization: Bearer $SWITCHBOARD_INGRESS_TOKEN` and `{"runId":"<id>"}` → `201` with `bearer`, `expiresAt`, `model`, `path`. `POST $SWITCHBOARD_BASE_URL<path>` with `x-api-key: <bearer>` (the Anthropic shape; `authorization: Bearer <bearer>` on the OpenAI one) and `{"model":"ignored","max_tokens":1,"stream":true,"messages":[{"role":"user","content":"Say OK."}]}` → the provider's event stream, and the run page shows one more model turn on the preset's model with its token counts; the same request without the header → `401`; with the bearer after the run has ended → `403 revoked`; the bot's log carries `[model-proxy] run=<id> turn=…` and no body. |
