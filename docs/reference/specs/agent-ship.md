# Agent: ship

The coding → review → fix loop to LGTM as one pipeline: `agent:ship` implements a task, gets the PR reviewed, hands the findings back, and repeats until the review approves — one dispatch, one card, one run record, with every side effect (branch create, PR open/edit, review post) executed by the bot process from typed artifacts.

- **Code**: `src/core/dispatch/ship.ts` (the ship branch: the preflight, the one run record and card, the round loop over `runShipPipeline`, the report — over `ShipDeps`), `src/core/dispatcher.ts` (the fork into the branch), `src/core/shipPipeline.ts` (the caps, the interrupted note, the round header, and `runShipPipeline` — the round loop and its endings), `src/core/ship/` (the pipeline's stages as files: `preflight.ts` — the entry checks, `shipTaskText`, `shipBranchName`; `codingChild.ts` — one coding round, round 0 and every fix round, with the branch contract and the fix turn; `reviewChild.ts` — one pinned-head review round with the review turn; `childRound.ts` — what the child rounds share; `contract.ts` — the child contract, `contractFromPlan`, `contractFromTask`, `renderContract`, `GUARDS`; `handoff.ts` — the handoff a coding child hands back, pure: `Handoff`, `parseHandoff`, `isHandoffShape`, `redactHandoff`, `renderHandoffComment`, `renderHandoffLedgerRows`; `coordinator.ts` — the plan runner's state machine, pure over the coordinator's step returns: the plan graph and cursor (`parsePlanGraph`, `openPlanCursor`, `readyUnits`, `startUnit`, `settleUnit`, `unitBranch`, `parsePlanBranch`, `parseShipPlanRequest`) and the unit pipeline (`openUnitPipeline`, `nextAction`, `applyReturn`, `renderUnitReport`)), `src/core/commands/contract.ts` (`contract render`, the by-hand render), `src/core/reviewRound.ts` + `src/core/codingPrPostStep.ts` (the per-round units), `src/core/reviewVerdict.ts` (findings), `src/tools/workspace.ts` (`submit_dispositions`, `submit_handoff`), `src/agents/registry.ts` (`AGENTS["ship"]`), `src/config.ts` (`ship` caps block), `src/config/validate.ts` (`validateShip`)
- **Docs**: [pr-description.md](pr-description.md) (the PR gate ship builds on), [agent-coding.md](agent-coding.md), [agent-review.md](agent-review.md), [resident-repos.md](resident-repos.md)
- **Budgets**: the ship preset declares the pipeline's wall clock — `AGENTS["ship"].maxMinutes`, 120, which a deployment's `ship.maxMinutes` replaces (`shipPresetFor`) — as its budget, a profile field like any preset's ([routing-and-config.md](routing-and-config.md) item 2): a scope's boundary clips it, a `budget:` directive narrows it, the card and the record say so, and the pipeline runs on the **effective** minutes; `ship.maxRounds` (default 3) is the pipeline's own cap. Child rounds run on their own agent budgets (coding 60 turns/45 min, review 30 turns/25 min) **clipped to the pipeline's remaining wall clock** — the wall clock is authoritative and whichever cap hits first ends the loop

## Behavior

1. **A directive, never a registry command.** `agent:ship in <owner>/<repo>: <task>` resolves through `parseDirectives`/`AGENTS["ship"]` (`machine: "repo-resident"`, `toolset: "full"`) and forks inside `dispatch()` after agent resolution — a `CommandRegistry` handler may never start an agent run (AGENTS.md invariant 3). Slack and CLI only: HTTP `/ingress` and the MCP dispatch tool refuse `agent:ship` with a pointer to the run page — both adapters are single-shot request/response and cannot hold a pipeline-length connection. Thread follow-ups are sticky like every agent.
2. **Compound permission gate.** Running ship requires `canRunAgent` for `ship` AND `coding` AND `review`, plus the repo allowlist for the target repo; the ship branch forks after the dispatcher's repo gate so `canUseRepo` always runs. Child rounds never re-enter `dispatch()` — without the compound gate a user denied `coding` (or the repo) would gain push+PR capability through ship. The refusal names the missing grant.
3. **Ship owns the pipeline branch.** The resident binds one ref per thread at first attach and ignores later hints ([resident-repos.md](resident-repos.md) item 16); a thread bound to the base branch would hand every review round the base tree. Ship names the branch itself, creates it on origin from the PR base via the GitHub REST API BEFORE the first attach (`createBranchRef`: the base tip's sha → `refs/heads/<branch>`; a 422 already-exists is success, so a restarted pipeline reuses its own deterministic branch) — the resident refuses to bind a ref origin does not have — and binds the thread to it at round 0's attach; the round-0 coding child is told it is already on that branch. A creation failure (or an unknown base) aborts with the reason before any round starts. Every coding round's attach must land on the pipeline branch — a thread already bound to another ref refuses before any model call, naming both refs — and every review round's attach must land at the pinned head (the extracted attach-head guard).
4. **Round 0 = the PR gate end to end.** The coding child implements, pushes, and submits its `PrDescription`; the pipeline reuses the coding PR post-step ([pr-description.md](pr-description.md) item 5) so ship holds `{repo, prNumber, headSha}` from code. Round-0 terminals other than "PR opened" — clarifying question, budget write-up, no description, unproven push — end the pipeline with a report naming the terminal; ship never retries a child round. The question passthrough keeps the thread alive: the user answers and re-enters.
5. **Review rounds run on a pinned head through the extracted units.** Head pinned before dispatch (readonly attach, same threadKey), the run submits the structured verdict with findings, the reviewed-head gate + repush-void machinery apply unchanged, and the review posts pinned to the head it read. A verdict always covers the full diff against base — the `re-review-delta` skill narrows *reading*, never verdict scope. **No verdict = abort**: a review child that ends without `submit_verdict` (budget, refusal, stop) ends the pipeline with a report naming that terminal — it is never converted into a `request_changes` the reviewer did not make.
6. **Findings and dispositions are typed artifacts.** `ReviewVerdict.findings[]`: `{ id, severity: blocking|major|minor|nit, file, line?, title }`, validated fail-closed per finding; `buildReviewPostBody` renders them under the verdict line, and the body starts with the exact `LGTM:` token only for `approve` — an `approve` carrying a `blocking` finding is downgraded to `request_changes` in `parseVerdictInput`, so the auto-approve workflow can never fire over a self-declared blocking defect. Fix rounds record one disposition per finding through `submit_dispositions` (`fixed|declined` + note; unknown ids rejected by name). Dispositions are kept per round: finding ids are only unique within one review round, so a later round reusing an id for a new finding inherits nothing, while a finding carried forward unchanged (same id, severity, file, title) keeps its recorded disposition. Outside a ship fix round the tool has no sink and answers with an honest no-op ("dispositions were not recorded"), never a false "recorded" ack.
7. **Fix rounds address everything and repush.** The findings payload rides the synthesized user turn (the `address-review-findings` skill carries the craft): every severity including nits gets a disposition, commits are squashed coherent, the description is resubmitted (the bot re-renders and edits the PR at the new head), and the branch repushed. A coding round — round 0 or a fix round — that pushed the pipeline branch onto an open PR without resubmitting gets the same bounded description turn a plain coding run gets ([pr-description.md](pr-description.md) item 5), on a copy of the round's messages while its workspace is still attached, before the round's post-step; only a turn that still submits nothing leaves the post-step's warning. The fix round's PR post-step note reaches the thread exactly like round 0's, and a fix round that opened a NEW PR (the old one closed out from under the pipeline) hands its number to every later round and report. A fix round that ends with the branch still at the previously reviewed head repushed nothing — the pipeline aborts with that reason (plus the post-step's note) instead of burning a review round on the same diff, with ONE exception: a round that declined EVERY finding on the record changes no code on purpose, and the re-review still runs over the same head to verify those arguments and possibly concede (the decline path's designed resolution — `maxRounds` still bounds a decline stalemate). Rounds are strictly serial — the orchestrator awaits each child before dispatching the next, and checks the run's `RunControl` between rounds, so an operator stop short-circuits to the final report; the stop is honored between outcomes, never over one: a review verdict that already posted settles first — an approve that posted is merge-ready, stop or no stop, and a stopped report names a posted changes-requested review.
8. **Caps are ceilings, honestly reported.** The pipeline's wall clock is the ship run's effective profile ([routing-and-config.md](routing-and-config.md) items 2 and 4): the ship preset's declared budget — `ship.maxMinutes`, one number the profile and `resolveShipCaps` both read, the registry's 120 by default — as a boundary on the path or the request's `budget:` directive clipped it (`caps.maxMinutes` is `profile.minutes`; `maxRounds` stays the config block's). The card names the clip before the fork like any run's (`budget 10 min (channel boundary; preset asks 120)`), and the ship run's ledger row and record carry the profile under the preset's name. The child rounds run under that profile: each attaches on its own preset's class and identity — within the parent's, which the gate judged once before the fork, because `coding` and `review` declare ship's class and an identity at or under ship's `write` — and dispatches with its budget clipped to the remaining pipeline time; a round starts only when the reservation check passes. Worst-case rounds exceed the 120-min default by design (three full worst-case rounds ≈ 255 min): the wall clock, not `maxRounds`, ends most worst-case pipelines, while typical rounds run far below their ceilings. A cap report distinguishes **declined** findings (disposition recorded) from **unaddressed** ones (no disposition), computed against the LAST review round's findings with only the dispositions recorded for them — a carried-forward finding keeps its disposition, a reused id inherits nothing.
9. **LGTM → merge-ready, never merged.** Merge-ready stands on the POSTED approval: `runReviewPostStep` returns a typed outcome, and an approving verdict whose post failed or was refused by the reviewed-head guard ends the pipeline with an honest report naming the reason — the PR carries no approving review; re-run ship in the thread with the PR URL to retry. On a posted `approve` the pipeline re-checks the PR and reports merge-ready: PR link, rounds used, declined findings, and the pending human merge as the remaining gate; a re-check that cannot fetch the PR says its state could not be re-verified — never "closed". Ship's bot-process GitHub writes are exactly the round-0 branch create, the PR open/edit, and the pinned review post — no merge endpoint is reachable from any ship code path, and the coding prompts + fix skill carry never-merge/never-approve. Because the approving verdict's `LGTM:` line triggers the auto-approve workflow in a repository that has opted in, ship refuses a target repo with auto-merge enabled up front.
10. **Entry checks make restarts safe.** Ship never opens a duplicate PR (open-or-edit by head branch). A ship invocation resumes at review — skipping round 0 — only when ALL hold: a user turn names the PR (the thread→PR inference reads user turns only), it is open, authored by this bot's own GitHub identity (the App's bot user — `resolveGithubIdentity`, resolved from GitHub once per process and matched by login AND immutable id; the code names no bot), same-repo head, and the invocation carries no new task text. An identity that cannot be resolved refuses fail-closed: an open PR's authorship cannot be judged. A PR reference binds the entry checks only when the invocation carries no other task text (a resume request) or the PR is ship's own; a PR number quoted as evidence inside new task text does not bind — it stays in the task as context and round 0 starts from the repo's default branch (or a user-named `on <ref>`), never from the quoted PR's head branch. This holds even when the cited PR's facts cannot be fetched: a PR named in the CURRENT message (`repoContext.prFromMessage`) beside new task text falls through to round 0 rather than refusing on the transient failure, and the round-0 base drops any PR-derived ref (`repoContext.refFromPr`, flagged at the resolver) so the failed fetch cannot leak the stranger's head branch. Kept fail-closed on a fetch failure: an INHERITED thread PR (not in-message), and a BARE in-message reference with no task text (a resume attempt must verify the PR first). A new task over ship's own still-open thread PR is refused naming the PR; a bare reference to a human-authored PR is refused as not ship's to drive. Terminal reports tell the user to include the PR URL when re-issuing. No mid-loop round state persists — recreatability, not resumption (AGENTS.md invariant 6). When the bot dies under a pipeline, the next generation closes its run `interrupted` and tells the thread ([run-history item 36](run-history.md)): the PR it had opened, if any, and the exact re-issue that continues the loop — `agent:ship` with only the PR URL (this item's resume-at-review), or with the task when no PR existed (round 0 again on the same deterministic branch); the same note closes its card. Nothing restarts a pipeline unattended.
11. **Resident-only v1.** No resident worktree → a plain report, never per-round cold clones. Each round attaches its own executor with the child agent's toolset (readonly review attach / writable coding attach — the mode-switch wipe is the accepted cost, bounded by clipped budgets).
12. **Rounds are legible.** A typed `ship_round` run event (index, agent, outcome) marks every boundary on the one stream — per-round cost is derivable by slicing `model.turn` spans between them, and every round is also a `ship.round` span (`index`, `agent` attrs) with the child run's `run.agent` under it ([tracing.md](tracing.md) item 17) — and the card carries an orchestrator-owned round header (its own variable composed into the frame, so a child's `update_status` cannot erase it). `ship` joins `NO_REFLECT_AGENTS`: its report is per-PR findings ephemera. Endings are truthful: the outcome statuses distinguish completed / aborted / capped / stopped, and the card closes ✅ with a checked-off checklist only for a completed pipeline — an abort or cap closes ⚠️ over the un-rewritten checklist, stops keep ⏹/⛔ — while a final reply that throws writes the run record `failed` (the main path's invariant), never a completed record the thread never saw.
13. **The child contract is a typed object the pipeline renders, never one the child assembles** (`src/core/ship/contract.ts`): what a coding child is handed for a plan unit, and what the review child checks the diff against — the unit's own text, the spec rows it names, the repository's agent rules and the guards it may not weaken, because the instructions handed to a worker decide the outcome and the plan already knows more than a task string carries. `contractFromPlan({ planMarkdown, unitId, readSpec })` is pure — the plan and the specs arrive as text, nothing reads a disk or the network — and builds `{ unit, specRows, agentRules, guards, rebase }`: the unit is its `### U<n>.` heading to the next `###`/`##` heading, verbatim (a closing thematic break dropped), with the plan's bullet keys (Goal, Requirements, Dependencies, Files, Approach, Test scenarios, Verification) parsed whole; the spec rows are the items the section names as `<spec>.md item(s) <n>…` — path-prefixed, backticked, linked or bare, comma/and lists and ranges, a parenthetical after the number, and a bare `spec item <n>` attributed to the nearest preceding spec named in the section (to nothing when another file such as `AGENTS.md` is nearer) — each once, in order, resolved to the spec's numbered item verbatim plus the validation-table rows whose criterion names that item (a leading `<n>:`, `item <n>`, a list, a range) with their proof bindings; a spec that is not readable or has no such item is said in the render, never invented. The agent rules are the text the caller read (`AGENTS.md`, else `CLAUDE.md`, else none); the guards are `specs:check`, `specs:coverage --test-guard`, `hygiene:check` and `decisions:check`, one line each on what they refuse; a unit the plan lacks throws naming the units it has. `contractFromTask(task)` is the compatibility path: a task string is a plan of one unit — the task as its whole section — with no spec rows, so the ship directive on a task string keeps working unchanged. `renderContract(contract, { maxChars })` renders one block under `## Contract` with the fixed sub-headings `### First instruction` (rebase the unit's branch onto the merged parent, both named when known; a conflict ends the unit), `### Unit <id> — <title>` (the section's body verbatim), `### Spec rows`, `### Agent rules` (with its source file) and `### Guards`, in that order and to the same bytes for the same object. Over the budget (`DEFAULT_CONTRACT_MAX_CHARS`, the one both children render with, until real renders fix the number) it cuts in a fixed order — the unit's Approach text first, then the agent rules, then the spec items' behavior text (their proof rows stay), then the proof rows (the rows' names stay) — never spec rows before Approach, each cut leaving a placeholder saying where to read the dropped text, and a closing line saying what was dropped; a part that is not there is not reported dropped; the unit's other bullets (its test scenarios above all), the rows' names and the guards are never cut, and the render reports its length and whether it is still over. The coding child takes the contract as a round option and appends the rendered block to its FIRST user turn as its own text part after the request's text, on a copy (the description turn sees the same turn); the review child takes the same object on its round and renders it into its system prompt right after the REVIEW TARGET block ([agent-review.md](agent-review.md) item 17), so both children hold one object rather than two readings of a tree that may differ; without a contract both run exactly as before, and the pipeline's order (items 4–7) is unchanged. Until the plan runner exists, `contract render --plan <path> --unit U<n> [--root <path>] [--branch <name>] [--onto <ref>] [--max-chars n]` ([command-registry.md](command-registry.md) Catalogue; CLI-only under `contract:read`, because it reads host paths the caller names) renders a unit by hand: it reads the plan, the specs it names under the repository root the plan path implies (`docs/plans/<file>` → the directory above `docs`, else the plan's own directory, else `--root`) and the rules file, prints the block followed by a receipt line with the measured length, the budget and what was dropped, refuses a missing plan as `not_found` and an unknown unit as `invalid_input` naming the plan's units, and renders an unreadable spec as not found rather than failing. Two questions the design record leaves open are answered conservatively until decided: a unit naming no spec rows receives none (not the covering specs' rows by path), and a child reports a follow-up in its handoff (item 14) rather than proposing a unit.
14. **The handoff is data the parent posts, never prose a person copies** (`src/core/ship/handoff.ts`; [agent-coding.md](agent-coding.md) item 9): what a coding child hands back beside its description when it ran for a plan unit — `{ deviations: [{ from, to, why }], followUps: [{ what, where }], unproven: [{ criterion, why }] }` — so a deviation from the unit reaches the unit's board issue without a person writing it there. The module is pure and dependency-light (the run record's node-free contract imports its type and shape check, so nothing in it may pull in zod or a Node built-in): `parseHandoff` validates a tool call (the three lists present, each at most `HANDOFF_MAX_ITEMS` entries, every field a non-empty string of at most `HANDOFF_MAX_FIELD_CHARS` once trimmed, unknown keys dropped; a refusal is a string naming the path); `isHandoffShape` is the structural check a stored record runs — shape only, no bounds, because redaction may lengthen a stored string; `redactHandoff` puts every string leaf through the redaction seam; `renderHandoffComment` renders the board-issue comment — the unit id and the pull request in its first line, each non-empty list under its own heading, an empty list omitted with its heading, then the ledger rows in a fenced block a person pastes into the plan's follow-ups ledger while the plan is `proposed`, and nothing at all for an entirely empty handoff, the same bytes for the same object; `renderHandoffLedgerRows` renders one row per entry in the ledger's exact three-column shape (`HANDOFF_LEDGER_HEADER`), the source naming the unit's handoff and its pull request, the disposition `open` until a person decides, cells escaped so a pipe or a newline cannot break a row. The coding round (`codingChild.ts`) takes the handoff through the same `ToolContext` sink as the description and hands it back typed on its result (the last call wins; an affirmed empty handoff is three empty lists, distinguishable from none); when its contract names the unit's board issue (`ChildContract.issue`, set by the caller that knows it — the plan runner; never rendered, since the block is the child's brief and the post is the parent's) it posts the rendered comment there AFTER the PR post-step, so the comment names the PR, through the pipeline's `postIssueComment` seam — the bot's own issue-comment write on its GitHub identity, the same REST write the general agent's issue tools make — and reports where it landed, or why it did not, as a note the thread sees like the post-step's; a failed post is never a failed round, the record still has the handoff; an empty handoff posts nothing; nothing is posted after a hard stop; a round without a contract, or whose contract names no issue (a task-string pipeline; the by-hand receipt), hands the handoff back and posts nowhere. The pipeline records the LAST coding round's handoff on its outcome — decorated once at the loop's exit, so a stop, a cap or an abort after a round that handed one back still carries it — and the ship branch puts it on the run record, redacted by the one record assembly ([run-history.md](run-history.md) item 2). The plan's own follow-ups ledger is amended by a person at review points, each row `open` until decided: no bot path writes a plan record, and a plan's body freezes once accepted.
15. **The plan runner's state machine is pure over the coordinator's step returns** (`src/core/ship/coordinator.ts`; [record 0031](../../decisions/0031-the-coordinator-runs-a-plan-not-a-pull-request.md); the coordinator itself is [http-ingress.md](http-ingress.md) item 9). The ship coordinator — a Workflow instance in the bot's shim Worker — has no model turn and holds no credential: it asks the bot for one step at a time and feeds the answer back, and everything it decides is a pure function here. Nothing in the module reads a clock (every bot answer carries `at`, the machine's only time) or carries a task's text or a thread's contents (a spawn's `brief` names the unit and the run ids whose records the bot reads to compose the child's turn). Two machines. **The plan graph and its cursor.** `parsePlanGraph(planMarkdown, planId)` is every `### U<n>.` unit in the plan's order with the unit ids its Dependencies bullet names (lists and `U<a> to U<b>` ranges, itself excluded), its slug (`u<n>-` and at most 24 characters of its title) and its head branch `plan/<plan-id>/<unit-slug>`; the plan id is the plan file's name without its extension (`planIdOf`: lowercase letters, digits and hyphens, or a throw naming the path), `parsePlanBranch` reads a branch back and answers undefined for every other shape — the line the merge grant draws — and `planInstanceId` names the plan (`plan-<plan-id>`), so a second runner for one plan meets the engine's duplicate-id refusal; `parseShipPlanRequest` reads `plan <path>.md [units U<n>, U<m>]` and nothing else, so a task string stays a task string. `openPlanCursor(graph, selected?)` puts the selected units (every unit when none are named) in play in the plan's order and refuses an unknown unit naming the plan's units and a dependency cycle; `readyUnits` are the pending units whose in-play dependencies are done — a dependency outside the selection is the requester's assertion that it is done; `startUnit` refuses a unit outside the selection, one already started or settled and one not ready, each by name; `settleUnit(…, "done")` frees the dependents and `settleUnit(…, "failed")` — any ending but a merge: a conflict, a cap, a stop, a refused merge — blocks them transitively and leaves every other ready unit in play; `cursorFinished` when nothing is pending or running. **The unit pipeline.** `openUnitPipeline(input, at)` over `{ unit: { id, branch }, repo, base, caps, merge: runner | person, resume? }`; `nextAction(state)` names the step the machine is at — `branch` (the head branch from the base), `spawn` (the preset, the round, the child's budget, the brief), `wait` (`run finished:<runId>` with a timeout), `read-record`, `pr-check`, `merge`, `sleep`, `end` — under a step name `<unit>/<round>/<kind>` (`<unit>/0/coding`, `<unit>/1/review`, `<unit>/1/fix`; the waits, reads and busy waits numbered under it, `<unit>/branch`, `<unit>/merge/<n>`), so a spawn's idempotency key is unique across a plan's units and never carries a colon; `applyReturn(state, ret)` is the transition, and an answer for any step but the one the machine is at — a duplicate `run finished`, a replayed spawn — changes nothing, so every return is safe to apply twice. The rounds follow items 3 to 9 over the returns: the branch, then round 0 as a coding child briefed with the unit's contract and the rebase onto the base; a `spawned` or `alreadySpawned` answer starts the round's wait — one child, never a second — with a timeout of the child's budget plus five minutes, the budget being the preset's own clipped to the pipeline's remaining wall clock (the loop's clip, never under the two minutes a spawn accepts); a `busy` answer waits for the live run's end and asks again, or ends at the wall-clock cap when the clock ran out meanwhile; a `refused` answer ends the unit with the authorize stage's own name and a `failed` one as an abort; every wait — an event or a timeout alike — is confirmed by `read-record`: a run still live waits again under the next step name and advances nothing, `interrupted` ends the unit with the ship-restart note of item 10, a finished coding child is followed by `pr-check` — no open pull request heading the branch is the round-0 terminal at round 0 and an abort after a fix round; the open pull request's number and head are adopted, so one closed and reopened under the pipeline is followed — and a fix round whose head is the one the review read aborts unless every finding of that review was declined on the record (item 7); a review round is briefed with the pull request, the head to pin and, from the second round, the previous review's run and the fix run that answered it, and its verdict settles the round: none is the no-verdict abort (item 5), `request_changes` a fix round — or the round cap when the rounds are spent, the wall-clock cap when the reservation fails (item 8) — and `approve` merge-ready, unless the approval did not post, an honest abort (item 9); a stopped child ends the unit as an operator stop naming the mode, honored around a posted verdict and never over one (item 7); a failed child aborts the unit. On a plan branch (`merge: runner`) an approve is followed by the `merge` step at the approved head — `merged` ends the unit `merged`, `pending` polls every five minutes for at most an hour under `<unit>/merge/<n>`, `refused` ends it `merge_refused` naming the reason, and an approve with no known head to merge at is `merge_refused` too, never a person's merge-ready; off a plan branch (`merge: person`) the unit ends `merge_ready` and a person merges, as item 9 has it. Every transition reports the round boundaries the card and the run stream take (`CoordinatorNote`: `started`, `pr_opened`, `approve`, `request_changes`, `no_verdict`, `aborted`, `stopped` — item 12's vocabulary, drawn by `shipRoundHeader`) and the ending; `renderUnitReport(state)` renders the thread's report in the loop's own words — merge-ready with the verdict and the declined findings, the cap with its declined/unaddressed split, the stop, the abort, the interrupted note — plus the merge's (`merged`, `merge_refused`) and the refusal's. The Workflow that drives this machine, the bot routes it asks and the merge route are not built here: the machine changes nothing until they exist.

## Roadmap (gaps)

- `[gap]` Separate child `RunRecord`s with a parent/pipeline id — requires the node-free run-record contract shared with `deploy/cloudflare-memory/worker.ts`; round attribution rides `ship_round` events until then.
- `[gap]` Ship over HTTP/MCP — needs a job-shaped ingress (accept, return the run link, run detached), not a held socket.
- `[gap]` Cold-sandbox ship path (per-round clones); v1 refuses without a resident.
- `[gap]` Warm cross-mode worktrees — today every readonly↔writable round switch wipes and reclones ([resident-repos.md](resident-repos.md) item 50).
- `[gap]` Per-round memory reflection — `NO_REFLECT_AGENTS` drops the coding rounds' learnings along with the report's ephemera; revisit with child records.

## Validation criteria

| Criterion | Proof |
|---|---|
| `AGENTS["ship"]`: machine class `repo-resident`, full toolset, the pipeline's default wall clock (120) as its budget, turns and tokens placeholders; its child presets run within its profile (ship's class, an identity at or under `write`); directive resolves on Slack/CLI | `[unit]` `src/agents/registry.test.ts::ship agent…` |
| 8: `shipPresetFor` is the registry's def with `ship.maxMinutes` as its budget, the default the def's own and `SHIP_DEFAULT_MAX_MINUTES` that same number; always a copy | `[unit]` `src/config.test.ts::ship caps block (agent:ship pipeline)::shipPresetFor…` |
| 8: the branch runs the pipeline on the parent's effective budget — `caps.maxMinutes` is the profile's minutes, `maxRounds` the block's — and the record carries the ship profile with its clip | `[unit]` `src/core/dispatch/ship.test.ts::runShipBranch — the agent:ship fork::the pipeline runs on the parent's effective budget…` |
| 8: end to end — a channel boundary clips the pipeline's 120 to 10, every child is clipped to it, the card names the clip and the record carries the ship profile; `agent:ship budget:10` clips as the caller's own boundary while the card names `ship.maxMinutes` as what the preset asks | `[unit]` `src/core/dispatcher.test.ts::agent:ship (pipeline)::a channel boundary clips the ship pipeline's wall clock…`, `::\`agent:ship budget:10\` clips the pipeline's wall clock…` |
| HTTP/MCP refuse `agent:ship` with a run-page pointer, no pipeline | `[unit]` `src/core/dispatcher.test.ts::agent:ship (pipeline)::channel guard…` |
| Compound gate: allowed ship but denied coding → refused naming coding, no child run; denied repo → refused, no child run | `[unit]` `::permission: user allowed ship but not coding…`, `::allowed all three agents but denied the target repo…` |
| Auto-merge repo refused before round 0 | `[unit]` `::auto-merge repo → refused before round 0` |
| LGTM round 1: coding → PR → approve → merge-ready reply (PR URL, rounds, pending human merge) | `[unit]` `::LGTM round 1…` |
| Findings round trip: request_changes → fix child gets the payload verbatim → re-review → approve; dispositions in the final report | `[unit]` `::findings round trip…` |
| Approve + failed post → honest report, no merge-ready claim; approve + guard-refused post → same; the post step's typed outcome | `[unit]` `src/core/dispatcher.test.ts::agent:ship (pipeline)::approve whose post FAILED…`, `::approve whose post was REFUSED…`, `src/core/reviewRound.test.ts::runReviewPostStep…` (posted/skip/failure outcome rows) |
| Merge-ready re-check that cannot fetch the PR → "could not be re-verified", never "no longer open" | `[unit]` `::merge-ready re-check that cannot fetch…` |
| No verdict from a review child → abort report naming the terminal, no fix round | `[unit]` `::no verdict from review child…` |
| `maxRounds` cap → report splits declined vs unaddressed; a later round reusing a finding id inherits nothing (listed unaddressed) | `[unit]` `::maxRounds cap…`, `::a later round reusing a finding id…` |
| Child budgets clipped to remaining wall clock; reservation check refuses a round before the deadline passes | `[unit]` `::wall-clock: a child is dispatched with clipped maxMinutes…`, `::reservation check…` |
| Branch binding: round 0 creates the pipeline branch from base on origin BEFORE the first attach (422 already-exists tolerated; creation failure → abort, no attach; a resume never creates); every round's attach sha equals the pinned head; a thread bound to another ref → coding-round refusal naming both refs | `[unit]` `src/core/dispatcher.test.ts::agent:ship (pipeline)::branch binding…`, `::fresh pipeline: the bot creates…`, `::branch creation fails…`, `::a thread already bound to another ref…`, `src/execution/githubPulls.test.ts::createBranchRef…` |
| One coding round on its own (the stage): no resident → a plain result and no model turn; a thread bound elsewhere → refusal naming both refs; the child runs on a clipped copy of its def under the branch contract and the PR opens from the typed description (redacted `pr_description`, `pr_opened` published); a round that left the pipeline branch → no PR write; a fix round's LAST dispositions and a hard stop ride back typed | `[unit]` `src/core/ship/codingChild.test.ts::runShipCodingChild…::*` |
| One review round on its own (the stage): an unknown PR head → the pre-flight's refusal before any attach; no resident → a plain result and no model turn; the round attaches at the pinned head, runs a clipped copy of the review def on the synthesized turn (a later round's turn carries exactly the prior findings and dispositions it was handed) and hands back the verdict, the reviewed head and the POSTED outcome; a verdict-less child posts its prose with no LGTM line; a hard stop settles and posts nothing | `[unit]` `src/core/ship/reviewChild.test.ts::runShipReviewChild…::*` |
| Fix round with no new head → abort carrying the post-step's reason, no second review; a fix round's NEW PR adopted for later rounds + report | `[unit]` `::a fix round that repushed nothing…`, `::a fix round that opened a NEW PR…` |
| Entry checks: bot-authored user-named open PR + no new task → resume at review, no create; new task over open PR → refusal naming it; human-authored PR → refusal | `[unit]` `src/execution/githubIdentity.test.ts::resolveGithubIdentity::*`; `src/core/dispatcher.test.ts::agent:ship (pipeline)::thread with user-named, bot-authored open PR…`, `::thread PR open + new task…`, `::thread PR authored by a human…`, `::the bot's own GitHub identity unresolvable…` |
| Entry checks: a foreign PR cited in new task text falls through to round 0 off the default branch — even when its facts fetch fails — and drops the PR-derived ref (`refFromPr`); an INHERITED unreachable PR or a BARE in-message reference stays fail-closed; the resolver flags in-message PRs (`prFromMessage`) and PR-head refs (`refFromPr`) | `[unit]` `src/core/dispatcher.test.ts::agent:ship (pipeline)::new task text citing a human-authored open PR does NOT bind it…`, `::in-message cited PR + new task text + FAILING prFacts fetch…`, `::inherited unreachable PR (prUnpostable) + new task text…`, `::in-message cited PR + NO task text + failing prFacts fetch…`, `src/core/repoContext.test.ts::…PR-source flags for ship…::…prFromMessage…` |
| Resident unavailable → plain report, no cold clone | `[unit]` `::resident attach fails…` |
| Operator soft stop between rounds → no new round, stopped report; a stop during a round settles a posted verdict first (approve → merge-ready; request_changes → stopped report naming the posted review) | `[unit]` `::operator soft stop…`, `::a soft stop flagged during an approving round…`, `::a soft stop flagged during a changes-requested round…` |
| Endings close the card truthfully (completed → ✅ checked-off; abort/cap → ⚠️ un-rewritten; stops ⏹/⛔) and a thrown final reply records `failed` | `[unit]` `::card close is truthful…`, `::maxRounds cap…` (⚠️ close), `::a final reply that throws…` |
| No merge path: no `PUT …/merge` seen across every pipeline scenario | `[unit]` `::no merge path…` |
| Verdict findings validated fail-closed; approve+blocking downgraded; LGTM token contract holds with findings present | `[unit]` `src/core/reviewVerdict.test.ts` (findings + downgrade + body-shape rows) |
| `submit_dispositions`: valid set accepted, unknown id named, second call wins, full toolset only; no sink (no ship fix round) → honest no-op, never a false "recorded" ack | `[unit]` `src/tools/workspace.test.ts::submit_dispositions tool` |
| `ship_round` events in order with typed index/agent/outcome; round header survives child `update_status` | `[unit]` `src/core/dispatcher.test.ts::agent:ship (pipeline)::round events…`, `::round header…` |
| Skills scoped: `address-review-findings` → coding only, `re-review-delta` → review only; manifest check green | `[unit]` skill-store scoping test; `npm run skills:check` |
| Machinery run: `agent:ship` on a throwaway task reaches LGTM in ≤2 rounds — thread link, PR, round events, reviews pinned per head | `[agent]` `agent:ship in <owner>/<repo>: <throwaway task>` in a thread → the card closes ✅ with a merge-ready reply naming the PR; the run page shows one `ship_round` boundary per round and one review per head, pinned to it. |
| Cap run: `maxRounds=1` for the test thread → cap report with the declined/unaddressed split | `[agent]` set `ship.maxRounds=1` for the test channel, run a task the reviewer will not approve on the first pass → the card closes ⚠️ with the cap report listing declined and unaddressed findings separately. |
| Judgment run: a real-repo task — observed round count recorded; a human records agreement/disagreement with the approving verdict (human-gated) | `[agent]` run `agent:ship` on a real task; record the round count from the `ship_round` events and, once approved, have a human read the PR and record whether they agree with the approving verdict. |
| Contract (item 13): a unit is its heading to the next `###`/`##` heading, verbatim, the closing thematic break dropped, its bullets parsed whole; an absent unit is undefined | `[unit]` `src/core/ship/contract.test.ts::parsePlanUnit — a unit is its…::*` |
| Contract (item 13): the spec rows a unit names — path-prefixed, backticked, linked and bare spellings, comma/and lists, ranges, a parenthetical after the number, the bare `spec item <n>` attributed to the nearest preceding spec and to nothing past another file, each row once | `[unit]` `src/core/ship/contract.test.ts::specItemRefs — the spec rows a unit names…::*` |
| Contract (item 13): a spec's numbered item is resolved verbatim with the validation rows naming it (a leading `<n>:`, `item <n>`, a list, a range) and their proof bindings; an absent item or an unreadable spec is said | `[unit]` `src/core/ship/contract.test.ts::parseSpecItem / parseValidationRows / itemNumbersNamed…::*` |
| Contract (item 13): `contractFromPlan` carries exactly the named rows resolved, the rules, the guards and the rebase; a unit naming no rows receives none; an unknown unit throws naming the plan's units; `contractFromTask` is a plan of one unit with no rows, titled by the task's first line | `[unit]` `src/core/ship/contract.test.ts::contractFromPlan — the typed contract for one unit::*`, `::contractFromTask — a task string is a plan of one unit with no rows::*` |
| Contract (item 13): the render is one block under `## Contract` with the five sub-headings in a stable order and the same bytes for the same object; over the budget it drops Approach, then the agent rules, then the spec items' text, then the proof rows, each with a placeholder and a closing line, never a part that is not there; the rows' names, the unit's test scenarios and the guards survive every cut; the default budget holds a task contract | `[unit]` `src/core/ship/contract.test.ts::renderContract — one block under…::*` |
| `contract render` (item 13): the block from the plan, the specs it names and the rules file (AGENTS.md, else CLAUDE.md, else none) under the root the plan path implies, the measured length in the JSON and on the receipt line; `--root`, `--branch`, `--onto` and `--max-chars`; an unreadable spec rendered as not found; a missing plan `not_found`, an unknown unit `invalid_input` naming the units, a malformed id refused by the schema without echoing it; CLI-only under `contract:read`, no chat baseline | `[unit]` `src/core/commands/contract.test.ts::contract.render::*`, `::repoRootOfPlan — where the plan's specs and rules are read from::*` |
| Contract (item 13): a coding round handed a contract carries the rendered block as the last text part of its first user turn, the request's text first, the description turn on the same turn, the caller's messages untouched; a round without one runs its messages as given | `[unit]` `src/core/ship/codingChild.test.ts::runShipCodingChild — one coding round as a stage::a contract → its rendered block is the first user turn's last text part…`, `::withContractInFirstUserTurn: the first USER turn takes the block…`, `::runs the child on a CLIPPED copy of the coding def…` |
| Contract (item 13): a review round handed a contract carries the same rendered block in its system prompt right after the REVIEW TARGET block with its turn unchanged; a round without one carries no block | `[unit]` `src/core/ship/reviewChild.test.ts::runShipReviewChild — one review round as a stage::a contract → the same rendered block the coding child gets…`, `::round 1: attaches at the pinned head…` |
| One unit of this program rendered with a contract and run by hand, the number the design record lacks measured: the block pasted into the coding request, its character count and dropped parts posted as the receipt on the unit's tracker issue, the review read against the same block | `[agent]` `npx tsx src/cli.ts contract render --plan docs/plans/<program plan>.md --unit U<n>` → paste the block into the `agent:coding` (or `agent:ship`) request for that unit; post the receipt line (characters, budget, dropped) on the unit's tracker issue; when the review posts, confirm its findings name any test scenario the unit listed and the diff lacks. |
| Handoff (item 14): `parseHandoff` accepts the three lists trimmed, drops unknown keys and accepts empty lists; refuses a non-object, a missing or non-array list, a non-object entry, a blank, non-string or over-long field and too many entries, each as a string naming the path; `isHandoffShape` accepts a parsed handoff, its JSON round-trip and an over-long field and refuses a missing list, a non-array list, a non-object entry and a non-string field; `redactHandoff` redacts every string leaf and never mutates its input | `[unit]` `src/core/ship/handoff.test.ts::parseHandoff — the validator the submit_handoff tool answers with::*`, `::isHandoffShape — the structural check a stored record runs::*`, `::redactHandoff — every string leaf through the redaction seam::*` |
| Handoff (item 14): the ledger rows are one row per entry in the ledger's three-column shape with disposition `open` and the unit and PR as the source (the unit alone without a PR), none for an empty handoff, cells escaped against a pipe, a backslash and a newline | `[unit]` `src/core/ship/handoff.test.ts::renderHandoffLedgerRows — rows in the plan's follow-ups ledger shape::*` |
| Handoff (item 14): the board comment leads with the unit id and the PR link (or says there is no pull request), puts each non-empty list under its own heading, omits an empty list with its heading, ends with the ledger rows in a fenced block, renders the same bytes for the same object, flattens a newline inside a field, and renders nothing for an entirely empty handoff | `[unit]` `src/core/ship/handoff.test.ts::renderHandoffComment — the unit's board-issue comment::*` |
| Handoff (item 14): a coding round hands a submitted handoff back typed; a contract naming the unit's board issue → the rendered comment is posted there through the GitHub seam naming the unit and the PR, and the round's note says where; no contract, or a contract naming no issue → recorded, posted nowhere; an empty handoff → recorded as the empty object with nothing posted, and a round that submitted none carries no handoff; a failed post → the handoff still rides back, the note says the post failed and that the record has it, the round is not a failure | `[unit]` `src/core/ship/codingChild.test.ts::runShipCodingChild — one coding round as a stage::a handoff submitted beside the description rides back typed…`, `::a handoff on a round without a contract…`, `::an EMPTY handoff under a contract naming an issue…`, `::a board post that FAILS…` |
| Handoff (item 14): the ship run's record carries the handoff its pipeline's outcome returned, redacted, and the pipeline's GitHub seam carries the injected issue-comment write | `[unit]` `src/core/dispatch/ship.test.ts::runShipBranch — the agent:ship fork::a pipeline whose outcome carries a handoff…` |
| A child's handoff reaches its unit's board issue without a person writing it there | `[agent]` Awaits the plan runner — until it exists no live path hands a ship round a contract naming an issue. Then: run the program's next unit through the runner with the unit's board issue on its contract; the issue gains the handoff comment posted by the bot, and `runs get <id>` on the child's run shows the same object. Until then the by-hand receipt: an `agent:coding` run with the rendered block pasted into its request submits a handoff, `runs get <id>` shows it on the record, and no issue gains a comment. |
| Runner (item 15): the plan graph — every unit heading in order with the dependencies its bullet names (lists, `to` ranges, `none`), its slug and its branch; `unitSlug`, `unitBranch` and `parsePlanBranch` refusing every other shape; `planIdOf` from the file's name or a throw naming the path; `planInstanceId` names the plan; `parseShipPlanRequest` reads `plan <path> [units …]` and nothing else | `[unit]` `src/core/ship/coordinator.test.ts::the plan graph — units, their dependencies, their branches::*` |
| Runner (item 15): the cursor — ready units in the plan's order once their in-play dependencies are done; a selection narrowing the plan with a dependency outside it counted satisfied; a failed unit blocking its dependents transitively while every other ready unit stays in play; an unknown unit, a unit started twice or before it is ready, and a cycle refused by name | `[unit]` `src/core/ship/coordinator.test.ts::the plan cursor — ready units in dependency order, a failure blocking its dependents::*` |
| Runner (item 15): every ending on step returns — merge-ready on a plan branch through the merge step and off one for a person's merge; the findings round trip with the fix and re-review briefs and the declined disposition on the report; the round cap with its declined/unaddressed split; the wall-clock cap with the children's budgets clipped and the reservation; a stop naming its mode, honored around a posted verdict and never over one; the aborts (no pull request at round 0 with the child's words leading, a branch not created, a failed child, no new head unless every finding was declined); no verdict; an approval that did not post; a resume at review skipping round 0 | `[unit]` `src/core/ship/coordinator.test.ts::the unit pipeline — every ending the in-process loop has today, on step returns::*` |
| Runner (item 15): the durable half — a duplicate `run finished` advances the state once and a stray return changes nothing; a timeout is confirmed by `read-record` like an event; a live answer waits again under a new step name; `interrupted` ends with the ship-restart note naming the pull request when one exists; `alreadySpawned` proceeds to the wait without a second child; `busy` waits for the live run's end and asks the same step again; a refused spawn ends with the gate's name and a failed one as an abort; the merge's pending poll is bounded and a refused merge names its reason; an approve on a plan branch with no known head is a refused merge, never a person's merge-ready; a reopened pull request is adopted and none aborts; every step name is `<unit>/<round>/<kind>` without a colon | `[unit]` `src/core/ship/coordinator.test.ts::the unit pipeline — the event, the timeout and the confirmation (the durable half)::*` |
