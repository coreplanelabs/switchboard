# Execution & sandboxes

Tools never touch the bot host: every `bash`/`read_file`/`write_file` runs through an `Executor`. Production uses per-thread Cloudflare Sandboxes behind the proxy Worker; a thread's follow-ups reuse its workspace.

- **Code**: `src/artifacts/` (the artifact store: `store.ts` seam with the R2 and in-memory implementations, `keys.ts`, `contentType.ts`, `config.ts`, `buildStore.ts`, `agreement.ts`), `src/core/dispatch/staging.ts` (inbound staging: the pull and exclude commands, the turn's line, copy-then-pull, the run's workspace paths), `src/core/dispatch/threadAssets.ts` (the thread's files: the one catalogue read over the runs' `artifact` events, and the where-clause the prompt and `recall` share), `deploy/cloudflare/artifactsCopy.ts` (the Worker's copy route) + `deploy/cloudflare/ensure-bucket.mjs` (the bucket step of the bot's `npm run deploy`), `src/execution/` (executor, factory, cloudflareSandbox, sandboxErrors, sandboxEnv, sandboxIdle, sandboxStart, e2b, resident, githubApp, githubRepoProbe, sandboxLifecycle), `src/agents/registry.ts` (`MACHINE_CLASSES`, `AgentDef.machine`), `deploy/cloudflare-sandbox/worker.ts` + `wrangler.jsonc` + `Dockerfile` + `docker-wrapper.sh` + `runtime-supervisor.sh`, `src/deploy/artifactsBucket.ts` (the artifacts bucket's lifecycle rules and privacy verdict over the operator's Cloudflare token), `src/core/commands/artifacts.ts` (`artifacts lifecycle`, `artifacts check`), `src/execution/seedPlan.ts` (item 25: the seed handle's shape, the fix-up script, the missing-backup classification)
- **Docs**: [Execution and trust](../../explanation/execution-and-trust.md), [AGENTS.md invariants 5, 6](../../../AGENTS.md)
- **Tests**: `src/artifacts/*.test.ts` (the store seam, keys, types, config, the builder, the bucket agreement), `deploy/cloudflare/artifactsCopy.test.ts` + `deploy/cloudflare/ensure-bucket.test.mjs` (the copy route and the bucket step), `src/core/dispatch/staging.test.ts` (inbound staging), `src/core/dispatch/threadAssets.test.ts` (the thread's files: the catalogue read), `src/channels/slack/attachments.test.ts` (the by-reference classification), `src/execution/factory.test.ts` (per-agent provisioning + resident selection + the machine classes), `src/execution/githubRepoProbe.test.ts` (the `repo-cold` repository vet), `src/core/dispatch/resolve.test.ts` (which vet a class gets), `src/core/dispatch/authorize.test.ts` (the `repo-cold` refusals), `src/execution/resident.test.ts`, `src/execution/binaryRead.test.ts` + `src/execution/readBytesRemote.test.ts` (the binary read: contract, local, both remote clients), `src/artifacts/store.test.ts` + `src/artifacts/keys.test.ts` + `src/artifacts/contentType.test.ts` + `src/artifacts/config.test.ts` + `src/artifacts/buildStore.test.ts` (the artifact store), `src/execution/shellQuote.test.ts`, `src/execution/githubApp.test.ts`, `src/execution/githubIdentity.test.ts` (the identity the process acts as), `src/execution/bashTimeout.test.ts`, `src/execution/executor.test.ts`, `src/execution/e2b.test.ts`, `src/execution/cloudflareSandbox.test.ts`, `src/execution/sandboxErrors.test.ts`, `src/execution/sandboxEnv.test.ts` (the body-only env reader the Worker uses), `src/execution/sandboxLifecycle.test.ts` (the exec keepalive + static guards that the sandbox Worker wires it, renders every failure through `thrownText`, reads the env map through `envFromRequest` from the body alone while the executor sends only the body, names `setsid -f` in its timeout hint, and that `wrangler.jsonc` rolls out in one wave), `src/deploy/wranglerTemplate.test.ts` (the sandbox template's instance type), `src/deploy/imageNode.test.ts` (static: every image names one exact Node tag whose major is `.nvmrc`'s, and the execution images copy it over the base's and assert it at build time), `src/deploy/sandboxRuntimeSupervisor.test.ts` (the sandbox image's PID 1: the supervisor's shape and its behaviour under a fake runtime), `src/deploy/sandboxDocker.test.ts` (static: the sandbox image installs the Docker engine and the lazy-start wrapper, and the wrapper's shape), `src/deploy/imageToolchain.test.ts` (static: both execution images carry one toolchain — python3/make/g++, ffmpeg, Playwright's headless Chromium at one pin — and prove each piece at build time), `src/agents/registry.test.ts::the workspace prompts name the image toolchain` (every prompt with a workspace names it); the deeper sandbox behaviors are infrastructure-dominated — the live checks below are their proof. `src/deploy/artifactsBucket.test.ts`, `src/core/commands/artifacts.test.ts` (the bucket's rules, privacy and calls; the two operator commands)., `src/execution/sandboxIdle.test.ts` (item 22: the idle verdict, the ledger, and the guard against a fake host — arming, counting, the stuck cap, the bounded destroy and the platform kill), `src/execution/sandboxStart.test.ts` (item 23: the start gate against a fake host — one warm-up, the starting answer, the pass-through, the failure handed on, the restart after a stop), `src/execution/seedPlan.test.ts` (item 25: the handle's parse, the fix-up script, the constants, the classification, and a scan over the Worker, the image, the template and the secrets manifest for the wiring), `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor seed`

## Behavior

1. **Per-thread isolation**: one workspace/sandbox per `threadKey`; follow-ups reconnect. Losing a sandbox degrades gracefully — repos re-clone, nothing else is lost. A thread's container stays warm for **5 minutes of idle** (`SANDBOX_SLEEP_AFTER`, `src/execution/sandboxLifecycle.ts`; the SDK default is 10 on the 0.12 and 0.13 lines, was 20 on 0.3.x): a follow-up inside that window lands on the same workspace, a later one gets a fresh container and re-clones — legible, and otherwise unchanged behaviour. Idle means idle: a running command never counts toward it (item 2). The deadline is the Worker's own, not the SDK's (item 22): five minutes after the last request the thread's Durable Object served, the container is destroyed whatever runs inside — a detached pi orphaned by a bot death (item 21) has that window for the next bot generation to reattach, as it did on the 0.12 line.
2. **Command timeout is shell-level and structured**: every `/exec` runs under coreutils `timeout -k 10 <secs>` inside the sandbox — 280s by default, raised per call by the bash tool's `timeoutMs` (item 12) up to the shared 20-min ceiling; a deadline kill is a genuine **exit 124** (137 normalized to 124) with an actionable stderr line naming the limit, the `timeoutMs` knob and, for a job that must outlive the command, `setsid -f` with the wrapper's stdio redirected (`DETACH_HINT`, `src/execution/sandboxLifecycle.ts`; never `nohup`: the command's process group is reaped when `timeout` returns, so a plain background job dies with it — item 17's reason, and the recipe the `explore` preset carries, [agent-explore.md](agent-explore.md) item 5; the redirects are item 24's). Two SDK backstops sit above it and never win the race: the per-process `timeout` the Durable Object launches each command with (the shell limit plus a 40 s margin, `SDK_BACKSTOP_MARGIN_MS`; the Durable Object's own wait for the output to END is the command's deadline plus 20 s (`OUTPUT_AFTER_EXIT_MS`, item 24), after which it asks the runtime: a process that exited is answered with its code, one still running is killed and answered as the timeout), and the container's `COMMAND_TIMEOUT_MS` (1240s, Dockerfile — 40s above the ceiling; changing it needs a container rebuild). The SDK's own timeout surfaces only a `timedOut` flag, which is folded into the same exit 124 story. **Invariant — one in-flight exec never outlives the container's activity timeout**: the 0.13 SDK renews the activity timeout every second while a command's stream is open (its control connection's busy poll, `BUSY_POLL_INTERVAL_MS`), so `sleepAfter` (5 min) is idle time only and the shell-level `timeout` is the one deadline a command can hit. History: the 0.0.x `@cloudflare/containers` base renewed the clock once per proxied fetch, BEFORE the fetch, and its alarm loop SIGTERMed the container the moment the clock read expired — a command whose budget equalled `sleepAfter` died at the budget exactly and the next command found `/workspace` empty; the Worker carried its own once-a-minute keepalive from then until the 0.13 line made the SDK's own tracking the mechanism.
3. **Heartbeat streaming**: `/exec` responses send headers immediately, a whitespace heartbeat every 15s, then one JSON document — no hop ever sees an idle connection (idle drops lost results in transit). Failures arrive in-body in a dual shape (`error` + `exitCode: 127`/`stderr`) so old/new executors both render them; in-body errors are never blindly retried. **The failure text is never empty**: both Worker catches (the `/exec` stream and the `/read`/`/write` handler) render the thrown value through `thrownText` (`sandboxErrors.ts`) — the SDK's own message when it has one, else `sandbox exec failed with no message from the SDK (<name>[, code <code>] | no error name); the container may still be running a previous image while a Worker/image rollout is in progress — retry in a minute`; the classifiers (`isFleetBusyError`, `isRecycleError`, `recycledMidCommandMessage`) keep reading the raw shape. On the other side, a success body has no `error` key at all, so the executor treats a PRESENT-but-empty `error` as the Worker's failure shape with its text missing — `ExecInfraError`, item 9 — never as a command exit. The failure this closes: a `SandboxError` built from a body with no `message` has `message === ""`; a `shape.message ?? String(err)` fallback keeps it (`??` fires only on null/undefined), a truthy check lets `{error: ""}` through as a plain `exit 127`, the health tracker counts a success, and the model reports its shell "down". The document also carries `durationMs`, the command's wall time in the sandbox ([tracing.md](tracing.md) item 19); the bot does not read it yet.
4. **One command is one supervised process, and the Worker retries nothing of its own.** On the 0.13 SDK line there is no persistent shell session to lose: `SwitchboardSandbox.runCommand` starts each command as its own process (`createExtensionProcessSandbox(this).exec(argv, { env, timeout })`, `argv` = `timeout -k 10 <secs> bash -c 'mkdir -p /workspace && cd /workspace && <command>'`) and collects it (`output()`), inside the Durable Object, and the fetch handler calls that method over RPC. The SDK's `runWaking` starts a stopped container and waits for its control port before the process is spawned, so a cold or sleeping container is a slower first command, never an error the Worker has to re-send; a container that is still being placed is the fleet-busy answer (item 14). History: on 0.3.x a container restart orphaned the SDK's cached session and the Worker reset it and retried once; 0.12.x persisted the session id and recreated it itself, and still answered `Container is starting` during a boot, which the Worker re-sent after 3 s. Both fences are gone with the sessions. A failure whose command MAY have run — the runtime replaced under the call — is never re-run; item 9 names it a recycle instead.
5. **GitHub identity**: a GitHub App mints 1-hour installation tokens on demand, injected into sandboxes as `GH_TOKEN` — never stored on the bot host. **Least-privilege by identity**: the token is minted scoped to the run's effective profile's identity (`AgentDef.identity`, one of `none | read | write`, never inferred from the toolset name — [record 0026](../../decisions/0026-capability-profiles-and-request-routing.md)) — a `read` identity's sandbox (the review agent) gets a **read-scoped** token — exactly `contents:read`, `pull_requests:read`, `issues:read` (for the `github_issue_list/get` tools of [github-tools.md](github-tools.md)), `actions:read` (workflow runs, jobs, logs and the Actions cache list: `gh run list/view --log`, `gh cache list`), `checks:read` (check-runs and check-suites: `gh pr checks`) and `metadata:read` (GitHub requires it on every installation token), every value `read` — so it can clone a private repo and read its PRs, issues and CI results but physically cannot comment/review/push/re-run from inside, while a `write` identity (coding, ship) gets the write-scoped token it needs and a `none` identity mints nothing (`resolveGithubToken(scope)` in `githubApp.ts`, the scope chosen by `githubTokenScopeFor(identity)` and applied by `githubEnvs` in `factory.ts`). The read set is an explicit subset of the installation's grant, never the grant itself: GitHub refuses a mint that asks for a permission the App does not hold, so every name in it must appear in the required permissions, and `statuses` (the legacy commit-status API — Actions reports through check-runs, so an Actions-built repository never needs it; a repository whose CI posts commit statuses would need it added to the set for those rows to show in `gh pr checks`) and `workflows` (a write-only grant) are deliberately not requested. Required app permissions: Contents, Pull requests, Issues (all RW) + Actions/Checks/Statuses (read), Workflows (RW). PRs are authored as `<app>[bot]`. **The installation must cover every repo agents are asked about** — with `repository_selection: "selected"`, an uncovered repo 404s on every `gh` call (this repo itself included). Prefer "All repositories" on the org, or audit the selection when adding repos. **Never in the command text**: the sandbox Worker passes the credential through the SDK's per-exec `env` option (0.12.x applies it to that one command and restores the environment afterwards), so the command string the SDK logs never carries `GH_TOKEN` — 0.3.7 ignored the option and the Worker used an inline base64 `export` prefix that put the live token into every `Command executed` log line. **The credential rides ONLY in the request body**: `CloudflareSandboxExecutor.call` sends the resolved map as `env: { NAME: value }` in the JSON body on every route (`/exec`, `/read`, `/write` — one shape everywhere; the Worker uses it only for `/exec`), and the Worker reads it through `envFromRequest` (`src/execution/sandboxEnv.ts`, node-free, bundled into the Worker), which upper-cases names, keeps only shell identifiers (`^[A-Z_][A-Z0-9_]*$`, the resident's rule) with string values, and drops everything else — an `env` that is not a plain object yields nothing, never a throw; request headers are never read. Why: Workers Logs record every invocation's request headers and redact them by a NAME heuristic only; bodies are not recorded. Measured on a header transport: `x-env-gh_token` showed as `REDACTED` while a probe header `x-env-PROBE_VAR: hello-from-env-option` was visible in clear in the `POST /exec` invocation event — a variable with an innocent-looking name leaks. **There is no header fallback**: both ends use the body alone and no `x-env-*` header is sent or read (a body-only bot meeting a header-only Worker was a one-release transition — `deploy all` deploys the bot before the sandbox Worker, `src/deploy/plan.ts` — closed once every deployed sandbox Worker read the body). **Freshness — a command that starts on a token finishes on it**: the credential is resolved per COMMAND, never captured per run — `CloudflareSandboxExecutor` and `E2BExecutor` take `resolveEnvs()` and call it on every exec (the sandbox Worker already injects the env inline per command, so nothing persists in the sandbox) — and the mint cache reuses a token only while it has at least `TOKEN_REUSE_MARGIN_MS` (the 20-min command ceiling + 5 min) left, so the longest possible command cannot outlive the token it started with. Under a "5 minutes before expiry" rule the cache can hand a nearly spent token to a run whose first command then runs the full 20-min ceiling; the token expires under it, every later command in the run gets `401 Bad credentials`, and the run produces no verdict. **A caller's own variables for one command** ride the same channel: `ExecOptions.env` is sent as the body's `env` beside the credential (the credential winning a clash) and the resident Worker's `/exec` reads it through the same `envFromRequest` into the exec's env option under its own injected variables — the way the pi harness hands a run's bearer to pi ([harness-pi.md](harness-pi.md) item 4), never on a command line.
6. **A sandbox Worker/image rollout never looks like a dead or silent sandbox.** A bot deploy has a **~10–15 min deaf window** (container image rollout); Socket Mode events during it are lost, not redelivered. A sandbox-Worker deploy rolls containers too, and a Worker and its image deploy as two artifacts: until the rollout finishes, the NEW Worker code can be handed a container instance still running the PREVIOUS image (Cloudflare's own words). In-flight thread sandboxes on old instances are SIGTERMed in any rollout mode (the grace period is 0) — recovered by (4), disk lost, named by item 9. What is closed and named is the window a NEW thread could fall into: (a) `deploy/cloudflare-sandbox/wrangler.template.jsonc` sets `rollout_step_percentage: 100`, so old-image instances are replaced in ONE wave and the window shrinks from minutes (the platform default `[10, 100]`) to seconds; (b) the 0.13 SDK's control protocol is versioned, so a Worker on one line talking to a container on another fails its handshake by name rather than running commands against a server that misreads them — `RuntimeControlProtocolError` against a versioned peer, and against a 0.12 server, which predates the handshake, the missing call itself (`'utils.getRuntimeMetadata' is not a function`, the text the first 0.13 deploy's live probe answered with during its three-minute roll before going live) — the Worker never `destroy()`s a container of its own accord (a destroy from a Durable Object hook is unbounded, coalesced callers hang until eviction, and a fresh placement during a gradual wave can land on the old image again); (c) a command that reaches a skewed instance and fails without a message is named by `thrownText` (item 3) with the rollout hint. History: the 0.12.x Worker read the container's `/api/version` in `onStart` and logged `sandbox.version-skew` at warn; the 0.13 line exposes no such read from the Durable Object, and the handshake carries the fact instead. The shape this closes: a release deploys the sandbox Worker and its image together, a thread created seconds after the upload lands on a previous-image container, and every command fails with an empty message rendered as a silent `exit 127` until a rollout wave happens to replace the instance.
7. **Per-agent provisioning**: executor selection is context-aware — `makeExecutor(opts, { threadKey, agent, profile, repo?, ref? })` sees the resolved agent, the run's **effective profile** (`src/config/profile.ts`: the machine class provisioned and the identity minted are read from it and never from the agent's own fields, so nothing a boundary capped can leak back in) and the inferred repo/ref for resident environments, and returns `{ executor, note? }` (the note is the named fallback reason the dispatcher shows on the status card). Each agent declares the **machine class** its runs are provisioned on (`AgentDef.machine`, one of `MACHINE_CLASSES` in `src/agents/registry.ts` — item 18 has the four), and the factory branches on the class alone: an agent on `none` (general, research) gets a null executor — **no sandbox or workspace is created or reconnected, and no sandbox credential is required** — and a tool call reaching it surfaces as a legible tool error naming the class (a wiring bug: workspace tools on a machine-less class), never a crash. An agent on `repo-resident` (coding, review, ship) provisions the configured backend: the target repository's resident when it is serviceable (item 8), else the per-thread backend with the checkout and a named note. Repository resolution reads the same class: only a class that carries a checkout resolves or gates a repository ([resident-repos.md](resident-repos.md) item 29).
8. **Resident backend**: with `execution.resident {baseUrl, tokenEnv}` configured AND a resolved target repo (`ctx.repo` — populated by the repo resolver), the factory probes the resident's `/status` (short timeout; transport failures negative-cached 30s) and selects `ResidentExecutor` only on `warm`; any not-warm state falls back to the per-thread backend above with a named note (`resident <state> (<reason>) — using fresh sandbox` — a degraded resident is loud). `not-onboarded` (404) also falls back per-thread, but carries a note too — `repo not onboarded as a resident — running in a cold per-thread sandbox; onboard it (\`repo onboard <owner/name>\`) for a warm, deps-ready environment` — so the user can tell coding ran cold instead of on a warm resident (never a silent surprise). Full contract and criteria in [resident-repos.md](resident-repos.md).
9. **Infra-failure classification**: an `Executor` returns a normal nonzero command exit as ordinary output (`exit <n>: …`, never a throw), but throws `ExecInfraError` — carrying a typed `reason` (`ExecInfraReason`), named where the executor builds the failure and never inferred from its prose: `transport-lost` (the request failed on its transport), `deadline-passed` (the call's or the send's deadline passed with no answer), `empty-failure` (the Worker's failure shape with its text missing), `worker-unavailable` (an HTTP 5xx: the isolate rolling under a deploy, the resident mid-restore or its mirror mutex held), `answered` (the Worker named a failure in its body on a status that is not a 5xx — the SDK's words forwarded, a named refusal — whose meaning is in the words), `refused` (a refusal no wait clears, judged at once by the type whatever its words: a bare 4xx, a worktree still gone after the one re-attach, the deploy-storm streak guard, the strike after the wake wait — its words still the container's, the wait already spent — and the resident `down` or the resource unregistered behind a 5xx), `aborted` (the run's own stop aborted the request — the stop's signal decides before the error's name; nothing waits); the first four a wait can clear (`EXEC_INFRA_WAITABLE`, `infraMayClear`), which is what the harness's one more command waits on ([harness.md](harness.md) item 6). **The resident client types an answer by the fields the resident puts on it, never by its words or its status class alone** (`residentAnswerReason`, on `/exec`, `/read` and `/write` alike). A 5xx that carries the resident's lifecycle pair — `state` and `stateReason`, the Worker's `getStatus()` beside the answer, on the hydrate path's `not-serviceable` answers (`restore in progress`, a hydration failing mid-restore), on `mirror-busy` while a refresh holds the mirror, on `disk-pressure` and on attach's `image-stale` — is `worker-unavailable` when that pair says the resident is coming back and `refused` when it does not, by the ONE decision the wake path makes on the engine view (`isWakeable`, `src/execution/residentWake.ts`: `restoring`, a serviceable state, a `degraded` reason the engine retries; not `down`, not `onboarding` — the rebuild after a down transition —, not a repo failure), so one fact never gets two verdicts by route. The answer's own `reason` — `mirror-busy`, `disk-pressure`, `image-stale`, the `not-serviceable` detail, `runtime-replaced`, `control-reset` — is never read as the lifecycle reason: a busy mirror on a `degraded (github-unreachable)` resident is a wait, not a repo failure. The fields ride every route alike: the JSON routes' body, and the `/exec` stream's one document on both of its paths — a failure the Durable Object named (`execFailureDocument`: the pair, the answer's own word, its `status`) and a pending result that REJECTED, the stub and not the method, answered as the Durable Object answers the same fact when it catches it inside (`threadRejectionErr`, by route — the mapping `/read` and `/write` give a rejected stub too): the DO reset by a code update is the DO's own word, `control-reset` on its 409, so a write's unknown outcome is resolved by the client's control-reset rule and never waited on as the resident unavailable; a runtime replacement is answered as the methods answer it, the DO's gate applied as far as it reaches — on `/exec` the word is said where the SDK's own moved sentence vouches for it (`sdkVouchesRuntimeMoved`, the DO's rule applied to the text that survives the stub boundary) and withheld where only the DO's knowledge of a restore under way could (the unknown branch's bare 409 with the SDK's words, as `replacedExecAnswer` gives it; the seam's one more command decides), on `/read` and `/write` it is said either way (`runtimeReplacedErr`, unconditional there on purpose, so the client's one re-attach-and-retry fires); anything else is the 500 with its `transient`, decided once the typed predicates are settled, through the same document — and the client reads a streamed answer's status from the body on `/exec`, `/attach`, `/await-restore` and `/op` by one rule (`answeredStatus`: only an HTTP 200 carrying an `error` and a `status` is re-statused), so a streamed 503 is the 503 it is, a streamed rejection is the catch-all's 500 it is, and neither is a deterministic answer over HTTP 200; `stateReason` is remote text and is sanitized like `reason` ([resident-repos.md](resident-repos.md) item 62). A Worker predating `stateReason` put the lifecycle reason in `reason` on its not-serviceable answers, so where `stateReason` is absent the client reads `reason` as the lifecycle reason unless it is one of the answer's own words that can reach that reading (`mirror-busy`, `disk-pressure`, `image-stale`; `unregistered` is refused before it, `runtime-replaced` and `control-reset` are 409s with no state) — the skew window keeps its wait through a degraded-but-serviceable resident; only that Worker's mirror-busy on a degraded resident, carrying no lifecycle reason at all, refuses. `reason: "unregistered"` (no registry record or repo facts to serve from, said at both preamble sites) is `refused`. A 5xx with a body but no state is the resident's deterministic answer — `read-failed`, `attach-failed at <step>`, `op-failed at <step>` — and `answered`, so its words decide at the seam and the failure stands at once instead of being re-run for five minutes, unless it is the 500 for a throw no route named — one builder, `catchAllErr` (`deploy/cloudflare-resident/threadErr.ts`: the shapes and the rules, pure over the throw and the route with the SDK's predicates handed in, run under plain Node with the SDK's real sentences; the Worker's scan pins only the wiring), at whichever catch met the throw: the fetch handler's catch-all, the streamed `/attach` and `/await-restore` rejections, the thread data plane's last resort, a route's own catch around its body (`attach-failed`; `op-failed` with no step, at the op's catch and the `/op` stream's rejection) — and the Worker typed that throw `transient: true` (`isTransientPlatformThrow`: a control reset, a runtime replacement, or the transient no word names — `isUnnamedPlatformTransient`: the runtime unreachable — by the SDK's connect abort's `AbortError` name or its exact sentence (`RUNTIME_UNREACHABLE_WORDING`, anchored to the whole message, so a `TimeoutError`'s `The operation was aborted due to timeout`, the text a route's own `AbortSignal.timeout` on a slow GitHub raises, never matches; a wrapper is read by its `cause`), or by the resident's own renamed word `runtime-unreachable:` (`isRuntimeUnreachableReason`), which is what crosses the stub once `run()` has renamed the abort —, the pinned SDK's own platform-transient predicate (`isPlatformTransientError`: the code-update reset, a lost connection, the storage-startup reset, the typed `retryable` flag — the SDK's signal, not a string table of ours), and the sentences it does not name, each anchored to its sentence so an embedding never matches: the platform's bare `internal error` (the whole message, and the TOP-LEVEL throw alone, `PLATFORM_INTERNAL_ERROR_WORDING` — git's `fatal: internal error` is not it, and a Worker's own failed outbound `fetch()`, which carries exactly that message, wrapped by the route that made the GitHub or mirror subrequest as its `cause`, is that route's deterministic failure, never a blip a re-probe clears; the SDK types it only where workerd set its `retryable` flag, which nothing says it always does), the memory-limit reset (`exceeded its memory limit and was reset`), the storage-timeout reset (`storage operation exceeded timeout which caused object to be reset` — a route's own `storage operation failed` is not it), and the Durable Object overloaded, which the SDK excludes from its own tight retry and this client re-probes after the harness's bounded wait), which a re-probe clears (`worker-unavailable`); a bare 5xx with nothing is `worker-unavailable`; a body on any other status is `answered`; a bare 4xx is `refused`. Who reads `transient`: `/exec`, `/read` and `/write` through `residentAnswerReason`; `/attach` reads it alone, by one rule at every site (`isTransientRefusal`, the provenance rule `probeStatus` types `/status` by: a 5xx carrying the field, or a 5xx carrying no Worker document at all — the edge's own page — and never a flag on another status; so an edge 5xx on `/attach` enters the wake wait as the blip it is, never a plain error the selection falls cold on a minute after its probe waited, nor one the harness cannot wait on): a transient 500 on `attach()` — the run's first attach in the factory (a fresh run's, or a resumed run's re-attach to its recorded worktree) and every mid-run re-attach alike — enters the wake wait itself (a `/status` probe, then a re-attach as soon as the engine says it serves — at once when the first probe already says so, with no pause and no second probe (the engine was never told of an exit, so its serving view is current; after a container's exit the view lags, and the first re-attach follows the first pause, never a full `/attach` into a container still starting) —, the probing bounded by the caller's budget under the wake ceiling: `FIRST_ATTACH_WAIT_MS` (`src/core/budgets.ts`, a minute) for a dispatch's first attach — one budget across the factory's retry by default after a needs-ref — the retry gets what the first attach's WAIT left (`ResidentNeedsRefError.wokeAfterMs`), never what the first request's own latency took, a stale mirror fetched before the 409 being the attach's time and not the wake's — the card naming both waits' total; for a mid-run re-attach's PROBING what is left of the operation's ONE wake budget, which the wait for a rolling container and the recovery attach's wait draw on from the same clock — a clock that starts at the first wait, never at the call, so the call's own latency before the refusal is the command's and never the wake's — so an operation never probes twice its budget; the attach REQUESTS inside that wait are not the operation's to bound: a recovery attach, direct or as the wait's re-attach, runs under the attach's own default whatever the operation's budget, and the run's deadline and lease bound the whole; the wake budget bounds the PROBING alone, and ONE bound holds for every attach request, whoever opened it — the first request of an `attach()` and each re-attach inside its wake wait alike, a first attach's, a resume's, a head move's, a recovery attach's and the rolling wake's re-attach that recreates the worktree from the mirror: the attach's own default (`attachOnce`'s: the exec default, `BASH_TIMEOUT_MS`, five minutes — never the twenty-minute exec ceiling), clipped to the RUN's remaining wall clock less the write-up reserve where the executor carries the run's clock (`ResidentExecutorOptions.remainingMs`, built into the executor per dispatch from `ExecutorContext.remainingMs` — the run control's lease clock, `RunControl.remainingMs`, which the RUN LOOP starts (`startLease`) — on the harness's own `lease` event, whichever harness published it (pi and OpenCode both do, [harness-pi.md](harness-pi.md) item 15), and for a resume from the remainder the record kept, since a resume publishes no second `lease` — and the dispatch, the relaunch and a review round hand on; `attachBoundWithinRun`, `src/execution/bashTimeout.ts`), so every attach the executor opens for the run's life — under the harness's container operations, the tools, the post-steps — is clipped, never one caller's alone; inside the reserve, or with less than an attach's floor past it (`ATTACH_REQUEST_MIN_MS`, half a minute: a bound too short to clone would be cut mid-clone and struck as a rollout — the command's one-second floor is the model's to choose, an attach is opened on the run's behalf), no request is opened at all: the typed `refused` (`ResidentLeaseSpentError`), naming the run's clock, since an opened request would run the attach to its end in the resident for a run that is ending and its deadline would read as waitable — and the run's end is read from it where it is decided: a relaunch's re-attach asks for nothing (`WorkspaceReattachLeaseSpentError`, the decision `lease_spent`) — read at its entry, again after its probe's wait, off the executor's `ResidentLeaseSpentError` when the lease ran out under the attach's wake wait, and again before any refusal the attach's own failure would become — a wake budget struck out, a re-attach request cut short — so the lease's end is never handed to `workspace_lost`; the probe's wait and the wake budget are both clipped to the lease less the reserve (`leaseClippedBudget`), so no wait spends the run into the reserve and hands the lease's end to a refusal — and the run ends on its budget: the `time_budget_exhausted` note carrying the bound's own sentence — `attachBoundWithinRun`'s note, the one source, worded by the bound that refused: inside the reserve, or only N s past it under the attach floor — and `no write-up ran`, the budget's plain "without finishing" answer, the status `completed`, and none of the tail against the replaced container (no workspace observation, no push-before-abort salvage, no description turn, no work-left-behind note, no PR post-step; on a review, no head settle, no verdict turn, no review post: `tailSkipped`, the one predicate a hard stop skips them all by); never `workspace_lost` and never a new run from its request with a fresh lease; on a harness loop's own commands it cannot arise, since the loop ends before the reserve, and on the wind-down's or a post-step's command it is that command's own failure, never the container's verdict (`identity` answers no name on a `refused`); a bound the wake budget, the wake ceiling, an operation's call bound and the command's own `timeoutMs` never clip, since an attach may clone and install deps and a request cut short strikes a resident still attaching — a rollout strike the tracker counts — and provisions the run cold beside it; an executor built with no run (staging at dispatch, the CLI) and an attach before the lease starts (the dispatch's first attach) get the default alone; an operation's wall clock is its command's, and neither a recovery attach nor the wake's re-attach is the command) — and in that wait an unreachable Worker is the same blip: the Durable Object that reset or lost its connection under the attach loses `/status` too, so an unanswered probe after a transient refusal — `isUnansweredProbe`, the one rule the wait's decision, its strike and the selection probe's wait read the view by, typed by its provenance and never by the status class alone: the transport failing, the Worker answering `/status` with a 5xx it typed `transient`, or a 5xx carrying no Worker document at all (a non-JSON body — the edge's own error page, where the Worker never ran to type it); a 4xx there, an operator token no longer accepted, and a 5xx the Worker answered without typing it — its document carrying a string `error` and no `transient`, a throw in the status route — are the answers they always were and definite, `refused` — is waited through under the budget, never the definite "nothing says the container is coming back" a container's exit is judged by, and a wait spent with the Worker unreachable throughout ends in `worker-unavailable`, never `refused` — and binds when the resident answers, the binding carrying `wokeAfterMs` so the card names the wait (`· after waiting Ns for the resident`, as item 27's restore wait is named) and a hiccup a re-probe clears never sends a run cold; the run's hard stop rides in (the dispatcher hands its control's `hardSignal` to the attach stage as `stopSignal`) — into the first attach request itself as well as the wait's probes and re-attaches, into a resumed run's re-attach and a mid-run relaunch's (`RelaunchContext.stopSignal`, the run loop's `hardSignal`; the factory rethrows the stop by its typed shape, never as the re-attach refusal that would dispatch the stopped request again, `reattachWorkspace` answers `stopped`, and the relaunch ends the run as a hard-stopped run — the stop's answer, `stopped_hard`, nothing rotated, nothing relaunched, nothing restarted; a resumed run's row closes with the stop's status, never `interrupted`; a refusal beside a pending stop stays the refusal) and into a review round's mid-run head move (`moveTo`, carrying the round's `hardSignal` in `MoveOptions`) — so a stop at any point ends it at once with the stop's typed error and the factory never provisions a stopped run cold — and the dispatch reads that error as the stop it is (`attachWorkspace` answers `stopped` on the executor's typed `aborted` error — `isRunStopError`, the one shape a stop is read by, never the signal's state beside some other failure: a provisioning failure that coincided with a pending stop stays `setup_failed` with its error replied — and the settlement counts that stop as the loop's, so a follow-up queued during the wait is dropped with the ⛔ note, never run fresh on the stopped thread), closing the card `stopped before the run started` (⛔) with no error reply and ending the request `stopped`, never `setup_failed`; past the budget it is the wake's strike, `worker-unavailable`, which the factory folds into its cold-fallback reason and a longer clock may still wait on; a deterministic 500 stays the attach's own legible error, judged at once with no probe; the selection probe before the attach ([resident-repos.md](resident-repos.md) item 24's warm gate) — a fresh run's, a resumed run's (`reattachWorkspace`) and a mid-run relaunch's alike ([harness-pi.md](harness-pi.md) item 16) — meets the same blip a moment earlier and is waited through the same way when the Worker typed its 5xx transient — the fetch handler's catch-all 500 carries the field, `probeStatus` puts it on the unreachable view — re-probed at once and then every `WAKE_POLL_MS` under the first attach's budget on the wake path's ONE loop (`waitOnStatus`, which `awaitWake` runs on too — one pause, one stop check, one budget rule, each caller's probe under its own deadline: the wake's `WAKE_PROBE_TIMEOUT_MS`, the selection's configured `probeTimeoutMs` (8 s by default, resident-repos.md item 25) for its first probe and every re-probe alike; the factory's `probeThroughBlip` is the one function both the fresh selection and the resumed re-attach call), the first re-probe paying no pause as the wake wait's first view pays none, the run's stop riding into the first probe and every re-probe alike (a stop that aborts the first probe is the stop's typed error, never a cold fallback on the view it produced, and arms nothing), every re-probe through `probeResident` so a transport failure met inside the wait opens the outage breaker as the first probe's would — never the run's own stop aborting a probe, which the stop's signal decides before the error's name and which opens no window —, the wait drawn from that budget and named on the card with the attach's own — an attach that fails after it names it too; a probe still transient past it falls cold naming the wait (a resumed run's is refused naming it); and a blip never opens the probe's outage breaker while it is waited — the breaker stays the case of the platform not answering: a TRANSPORT failure, judged at once as before, and a wait SPENT on the edge's own page (more of the wait's views document-less than the Worker's typed blip — what the wait was spent on, never the last view alone: an edge wait that ends on one typed view is still an edge wait, and one edge page after a minute of typed blips is not), which arms the window at the wait's end so an edge outage costs one wait, not one per concurrent dispatch, while a wait spent on the Worker's own typed blip arms nothing; `/op`'s streamed failure document carries its `status` in the body as `/exec`'s does (`streamOp` sheds nothing), and `ResidentOperations` judges it by the same rule — `isTransientRefusal` over the status `answeredStatus` reads off the document, never the field on a 200 or on a named refusal — putting `transient: true` on the error outcome as the one signal (the message left the resident's words), and a real HTTP 5xx on `/op` with no Worker document at all — the edge's page — is read by the same rule and carries the signal too; the repo command's reader words it (`— the resident was unavailable for a moment; re-run the command`), so a platform blip is told apart from a failure in the op. **The strike after the wake wait** (`residentWakeStrike`, `residentWakeBudgetStrike`, [resident-repos.md](resident-repos.md) item 65) carries the wait's origin (`WakeOrigin`: a container's exit, or a refusal the Worker typed transient, where no container exited) as its classification code — `container-exited`, the one the tracker counts, or `transient-refusal` — and in its sentence (waited "to wake", or "to come back"), and the one decision on the last engine view (`wakeStrikeReason`): a budget that ran out while the resident was still coming back is `worker-unavailable` — the client's budget is the command's own, the harness's restore-window bound is a longer clock, and one restore gets one wait whatever route the resident answered by ([harness.md](harness.md) item 6) — while a definite view (`down`, `onboarding`, a Worker that did not answer `/status`) is `refused`; a re-attach that failed as infra inside the wait ran under that bound, and its transport verdict still says nothing about the resident: the strike reads the same engine view; the run's own stop is the exception — its signal rides into that re-attach and into every `/status` probe of the wait as into every send, and every stop point throws the stop's one typed shape, `ExecInfraError` with reason `aborted` classified as the transport: the re-attach's as the call built it, the pause's and the probe's as `wakeStopped` builds it — never a plain error one caller unwinds and another counts, never a strike on the unreachable view the stop itself produced. **The request-failed sentence is one builder for every client** (`requestFailedSentence(subject, host, route, err, recheck)`: the two executors through `requestFailedMessage(worker, route, err)`, the resident admin client, `src/core/residentAdmin.ts`, with its own nouns and a re-check that names `repo list` — nothing special-cases a string), and both executors' request-failure sites pass the run's signal, so a stop is `aborted` and never the transport lost — when the exec transport itself fails — the sandbox unreachable (network), an HTTP/worker error, the sandbox's in-body "Command execution failed" / exitCode-127 signal, a PRESENT-but-empty in-body `error` (`""` — the Worker's failure shape with its text missing, item 3; a success body carries no `error` key, and a bare exit 127 with no output and no `error` key stays an ordinary command result — `foo 2>/dev/null` is a legitimate silent 127), or a worktree still unavailable after a re-attach. When that in-body failure says the container went away under the command — a typed 0.12.x error (`SessionTerminatedError`, `OperationInterruptedError`, matched by NAME because the Durable Object RPC boundary keeps `name`/`message` and drops the prototype; `isRecycleError`) at any elapsed time, or a recycle-shaped text (0.12.x: `Session '…' shell exited (exit code: n)`, `The sandbox container stopped while the operation was pending.`, and `The sandbox was destroyed while the operation was pending.` — the disconnect a `destroy()` under a pending call produces; 0.3.x: `Command execution failed`, `Session terminated`, `Session '…' not found`) more than a minute into the attempt (per attempt — a retry gets its own clock) — the sandbox Worker rewords it as `sandbox recycled mid-command after <n>s — the container's runtime was replaced or restarted under the command, which did not finish; check /workspace before continuing: it is empty if the container was replaced (re-clone), intact if only its runtime restarted (<original>)` (`recycledMidCommandMessage`) — still exit 127, never a faked exit 124: it IS an infra failure and the command did not finish, so the model looks instead of shortening a command that was never the problem. Inside the Durable Object the decision is typed (`isRuntimeReplacement`: `StaleProcessHandleError`, `RuntimeIdentityInactiveError`, an `OperationInterruptedError` whose reason is a replacement — `runtime_replaced`, `container_stopped`, `sandbox_destroyed`, `sandbox_lifetime_changed` —, an `RPCTransportError` whose kind is a loss, or the SDK's replacement wording anywhere in the cause chain, the same list the resident Worker uses), so the wording is `certain`; the fetch handler's catch keeps the by-name and by-text fallback for anything the Durable Object threw unnamed. **A runtime that did not answer is named** (`runtime-unreachable`, `src/execution/sandboxErrors.ts`): every command and file operation reaches the container through the SDK's control connection to its port; when nothing answers there — the server exited and item 21's supervisor is starting it again, or the runtime is wedged — the SDK's connect aborts after 30 s with the bare `The operation was aborted` (`isRuntimeUnreachableSignal`, the same signal the resident Worker classifies, `src/execution/residentRefresh.ts`). The Durable Object catches it at the spawn and on the file operations (never after a typed replacement, which is item 9's recycle) and builds `SandboxRuntimeUnreachableError`, whose message starts with the token and carries the container id (the Durable Object's id, the same word the `containers` log dataset carries), the SDK pin, the platform's `running` flag and the SDK's text as the cause: `runCommand` answers `/exec` in-body with `reason: "runtime-unreachable"` in the item-3 dual shape; the file methods throw it, and the fetch handler matches it by name or token after the RPC boundary (`isRuntimeUnreachableError`) and answers `/read` and `/write` as HTTP 503 with the same reason. The executor never re-sends `/exec` and re-sends a 503 on its ordinary transport retry (3 s, 6 s, 12 s); both end as `ExecInfraError` carrying the text, so the card names the cause and the container. Both remote executors do this (`CloudflareSandboxExecutor`, `ResidentExecutor`); `ExecInfraError extends Error`, so message/`instanceof Error` callers are unchanged. This is the seam every caller reads a dead sandbox from: the pi harness's container operations and the relayed tools take an `ExecInfraError` as the failure of that one operation, named ([harness-pi.md](harness-pi.md) item 5 — the native loop's two-strikes fail-fast, `ExecHealthTracker`, went with the loop), so a wedged sandbox is told apart from a command the agent should keep handling. Two things are deliberately NOT infra: a resident's answer that the container under the thread is gone — the named `runtime-replaced` (a deploy swapped the runtime under one command — [resident-repos.md](resident-repos.md) item 43) or the preflight's `worktree-missing` (the container disk was recycled — item 27) — is, on `/exec`, the typed `ExecSandboxRestartedError` at once, the same word the item 65 wake ends in, so the pi harness ends the run for a restart from its request ([harness-pi.md](harness-pi.md) item 16); on the idempotent routes a `runtime-replaced` is re-attached and re-issued once, and only a second one there with no successful op between becomes `ExecInfraError`, so a single deploy can never read as a dead sandbox. And a full sandbox fleet (`fleet-busy`, item 14) is **capacity**: the executor waits for a slot and, when the wait is spent, throws `ExecCapacityError` — a separate class the tracker neither counts nor resets on — so a fleet at `max_instances` can never read as a dead sandbox. Every `ExecInfraError` the resident executor throws is classified for the spans around it ([tracing.md](tracing.md) item 2): a failed request `transport` (`timeout` when the deadline fired), a non-200 `http` with the status as the code, a worktree still gone after re-attach `infra`/`attach`, a repeated runtime replacement `infra`/`runtime-replaced`, an in-body exec error `infra`; a GitHub API failure is `http` with its status.
10. **Container toolchains**: the sandbox image (`deploy/cloudflare-sandbox/Dockerfile`) carries git + gh + Node + npm + **pnpm** + **bun** (installed globally with npm; image and SDK move together); the resident image (`deploy/cloudflare-resident/Dockerfile`) carries git + Node + npm + **pnpm** + **yarn** (classic; a Berry repo bootstraps through its committed `yarnPath` release) + **bun** — every package manager the onboard-time `detectCommands` ([resident-repos.md](resident-repos.md) item 52) can name, so a detected install command always exists where it runs. **Node is the repository's own line in every image, never the base's**: the `cloudflare/sandbox` bases ship Node 22 (the 0.12.9 and the 0.13.0-next lines alike; the 0.3.x base shipped 20, which could not load 15 suites), while the repository builds and tests on Node 24 (`.nvmrc`, CI's setup-node, the install script) and its published CLI requires `>=24` — so a suite run inside an image on the base's Node reads a different `verify` than CI does: `npm ci` warns on the CLI package's `engines`, and a test can pass on one major and fail on the other. Each execution image therefore copies Node from a stage of its own at an exact `node:<version>-slim` tag, pinned to `linux/amd64` (the one platform the base publishes, so the copied binary runs in it whatever the build host is) — the base's npm tree removed first, then the binary, npm's tree and the headers at the base's own paths, so its npm/npx symlinks keep resolving — before any global install, and asserts that exact version at build time, so a base or stage bump that moves Node fails the build, not a run; the bot image builds FROM the same tag, and `src/deploy/imageNode.test.ts` holds the three tags equal and their major equal to `.nvmrc`'s. The base's container server is a bun-compiled binary and does not run on that Node, so the swap changes only what a run's commands see. pnpm was added to both so coding/build runs in pnpm repos no longer hit `pnpm: command not found`. **Every toolchain version is pinned exactly and asserted at build time**, and `src/deploy/imagePins.ts` + `imagePins.test.ts` fail the suite if a floating tag returns ([resident-repos.md](resident-repos.md) item 53): both images installed `pnpm@latest`, so the pnpm major tracked the last image build rather than a commit — a routine rebuild moved it 10 → 11, pnpm 11 stopped reading `package.json`'s `pnpm` field, and a repo keeping its `overrides`/`patchedDependencies`/`onlyBuiltDependencies` there fails `--frozen-lockfile`. pnpm sits on the 10 line on purpose (10.x reads that field; pnpm ≥10 self-manages `packageManager`, so the pin is a floor a repo can raise for itself — which also means the version baked in is not necessarily the one that runs, and a repo with a `packageManager` field fetches its own at install time). bun is pinned for the opposite reason: it does not self-manage `packageManager`, so the baked version is the one that runs, and the bases' own bun (1.3.x, a real binary at `/usr/local/bin/bun`) rejects a `bun.lock` at lockfileVersion 3 — bun 1.4's stamp for scoped `overrides` — as `Unknown lockfile version`, so every install in such a repo failed; both images remove the base's binary and install bun at one exact pin, at or above what an onboarded repo pins ([resident-repos.md](resident-repos.md) item 53). **Both images carry one toolchain for what a run builds and looks at** (`src/deploy/imageToolchain.test.ts` holds the two Dockerfiles to one shape): `python3`, `make` and `g++` — node-gyp's needs, so a native module (node-pty) rebuilds from source in a cold `npm install` instead of dying on "no Python"; `ffmpeg` (Ubuntu's, with libx264), so an agent pulls frames out of a video to look at it (`ffmpeg -i in.mp4 -vf fps=1 frame_%03d.png`) and encodes video from frames or a screen recording; and a headless Chromium through Playwright at an exact pin — screenshots, PDFs and `recordVideo`, from `playwright screenshot` / `playwright pdf` / `require('playwright')`. The browser is Playwright's own build, never apt's: on Ubuntu 22.04 the `chromium` package is a snap stub that does not run in a container. Only the headless shell is installed (`--only-shell`; there is no display) with its system libraries (`--with-deps`) and the fonts pages render text with (`fonts-liberation`, `fonts-noto-color-emoji`); `PLAYWRIGHT_BROWSERS_PATH=/opt/ms-playwright` is set BEFORE the install so the browser lands outside root's home, and the tree is opened `a+rX`, because the resident runs every agent command as an unprivileged `workerN`; `NODE_PATH=/usr/local/lib/node_modules` makes `require('playwright')` resolve from any working directory; Chromium runs with `--no-sandbox` (Playwright's default `chromiumSandbox: false`), which is what a root process, or one without user namespaces, needs. **Every piece is proven by the layer itself, so the BUILD fails, not a run** (CI builds every image on every pull request): the compilers answer `--version`, ffmpeg encodes a one-second `testsrc` clip to h264 and decodes a non-empty frame back out of it, the `playwright` CLI is on PATH at exactly the pin, and a real headless screenshot of a `data:` page lands as a non-empty png — in the resident image a second time as `worker1`, after the user pool exists, so an unprivileged thread user is proven able to drive the browser. The proof artefacts, the apt lists, apt's `.deb` archive (the base has no docker-clean hook; 400 MB stayed behind without `apt-get clean`) and npm's cache are dropped in the same layer. The prompt of every preset with a workspace (coding, review, explore, and the resident variants) names the toolchain in one sentence — the model reaches only for what it has been told is there; the cold prompts offer Docker, the resident prompts say there is none. **Every image also carries pi** (`@earendil-works/pi-coding-agent`) at one exact pin, proven at build the same way — the coding harness a run's container can start in place of the native loop ([harness-pi.md](harness-pi.md) item 3), and the bot image's for the presets without a workspace, whose pi is a child of the bot ([harness-pi.md](harness-pi.md) item 12). **Every image also carries OpenCode** (`@opencode/cli`) at one exact pin beside pi, the wrapper's install script allowed by name and `opencode --version` proven at build — in the resident again as `worker1` — the second harness's process ([harness.md](harness.md) item 12), dark until a deployment names it. Changing any image requires a container rebuild + redeploy to take effect.
11. **Hard-stop cancellation**: `Executor.exec(command, { signal })` carries the run's hard-stop `AbortSignal`. `LocalExecutor` kills the child process (the abort surfaces as an `exit …` line, never a throw); `ResidentExecutor` and `CloudflareSandboxExecutor` join it with their per-call deadline (`execDeadline`) so the bot-side request drops at once — the command inside the container keeps running until the release: the resident's `/detach force` kills the thread user's processes before evicting ([resident-repos.md](resident-repos.md) item 16a); the thread-sandbox Worker has no kill route, so there the command runs to its own `timeout`. `E2BExecutor` ignores the signal (its command API cannot cancel) — safe because the runner races every tool await against the signal and stops waiting regardless — and gains `release(mode)`: `"always"` (a read-only run or a hard stop) **kills the micro-VM**, the only way to end an uncancellable command; `"if-idle"` keeps it for the thread (the idle timeout reclaims it). The dispatcher releases with `"always"` after any hard stop, so a hard stop always frees the resident pool user (`/detach force`) or the E2B sandbox. **Every remote exec has a bot-side deadline of budget + `EXEC_CALL_MARGIN_MS`** — `ResidentExecutor` and `CloudflareSandboxExecutor` run every send (headers AND the streamed body — `/exec` answers HTTP 200 at once and heartbeats until the outcome, so the body read is where a dead sandbox's wait sits) under `execDeadline(budget + 30 s, signal)`, the budget being the command's clamped `timeoutMs` for `/exec` and `BASH_TIMEOUT_MS` for a file op; a Worker that never answers is an infra failure — `ExecInfraError` naming both numbers (`sandbox worker /exec gave no answer within 330s (command budget 300s + 30s margin) — the sandbox may be gone; the command may still be running in it`), which fail-fast (item 9) counts — never an indefinite wait. The deadline is per send: a fleet-busy wait (item 14) does not eat into it. `execDeadline` is a plain timer, not `AbortSignal.timeout` (Node runs that one on an internal timer no test can drive). Without it a run can wait indefinitely on one `/exec`: the sandbox container gone (`There is no container instance…`), the Worker still heartbeating, a client that passes only the hard-stop signal and reads the body with no deadline, and a runner that checks its wall clock only between steps — nothing fires until an operator hard-stops it.
12. **Per-call bash timeout (`timeoutMs`)**: the bash tool's input schema carries an optional integer `timeoutMs`, Claude Code-style — the tool description names the default and the max, which is how the model learns to ask for more time (a long probe dies at a flat 5-min cap, and `npm test`/`npm run build` brush it). Policy lives in ONE module, `src/execution/bashTimeout.ts`, bundled into both deploy Workers: default `BASH_TIMEOUT_MS` (5 min, unchanged), ceiling `BASH_TIMEOUT_MAX_MS` (20 min), floor 1s. **Clipped to the run**: the ceiling alone does not stop one command from eating a run (20 min is 80% of the review agent's 25-min budget — one first command can take all of it and leave ~4 min), so the harness hands the relayed tools the run's remaining wall clock (`ToolContext.remainingMs`) and a tool that waits keeps `RUN_DEADLINE_RESERVE_MS` (60 s) back from it (`await_runs`, `attach_file`'s render — [agent-conductor.md](agent-conductor.md) item 8); the shell is pi's own tool now, run in the container under pi's per-call `timeout` (seconds, none by default), judged at the harness's gate by that explicit timeout — one reaching past the loop's end is refused before the command runs ([harness-pi.md](harness-pi.md) item 7) — and the loop's end cuts whatever runs without one ([harness-pi.md](harness-pi.md) item 15); the native bash tool's `timeoutMs` argument and its clip to the remaining wall clock went with the native tool table. **The documented clamp rule** (`clampBashTimeout`): a finite number is truncated and clamped into [1s, 20 min] — so 0/negative run at the 1s floor and a 25-min ask runs at 20 min — and anything else (absent, NaN, a string) falls back to the 5-min default. Every executor honors it: `LocalExecutor` maps it onto execFile's `timeout` (maxBuffer kept); `E2BExecutor` onto the SDK's `commands.run({timeoutMs})`; `ResidentExecutor` puts it in the `/exec` body **only when the caller asked for one** (an older resident sees the body it always did — attach's readonly/sha convention) and stretches its HTTP wait to `timeoutMs + EXEC_CALL_MARGIN_MS` (30s) so the resident's own streamed exit-124 answer wins the race against the client's transport deadline; `CloudflareSandboxExecutor` does the same body-only-when-asked, and the sandbox Worker sizes its coreutils `timeout` from it. **Servers clamp with the same bounds and never trust the client's number** (resident `handleExec`, sandbox Worker `/exec` — both via the shared `clampBashTimeout`). A deadline kill is exit 124 with a line naming the limit that fired and the `timeoutMs` knob (max included), so the model self-corrects instead of reading a generic abort. The resident's deterministic `/op` path keeps the flat 5-min budget — it has no caller knob. **Back-compat & deploy ordering**: no `timeoutMs` → byte-for-byte today's behavior; a resident Worker predating this change either ignores the field (very old — command still capped at its 5-min budget, answered legibly as exit 124 while the client clamp bounds the wait) or 400-rejects a value above 5 min (the immediate predecessor's `parsePositiveInt`), so the **resident deploys before the bot**, as always. Same for the sandbox Worker + its Dockerfile backstop (container rebuild).
13. **Every Worker reports the commit it was built from, and nothing is hand-bumped** (without it a deploy's receipt has to be assembled from `wrangler versions list` plus a container image digest, because a Worker's `/healthz` cannot say which build is at the edge). The bot always could: `npm run deploy` writes `build.json` from the tree's HEAD (`deploy/cloudflare/write-build.mjs`) — and the release's `publish-image` job writes it from the release commit before building the bot's image, so a container from the published image ([release-and-deploy.md](release-and-deploy.md) items 21, 25) is stamped too — the image COPYs it, `/healthz` serves `build: {commit, builtAt}`, and `deploy all`'s live gate compares it to the commit it deployed ([slack-channel.md](slack-channel.md) item 8). The three Worker SCRIPTS now do the same, and the resident's hand-edited `const BUILD_MARKER = "perf53"` is **gone**: its comment said "bump on every deploy-worthy change", it was bumped three times in total (`gc51` → `head52` → `perf53`) and then went unbumped across five deploys — so it could neither prove a deploy nor expire a stale test override, which is exactly what [resident-repos.md](resident-repos.md) item 49(c) (`ignored:"stale-build <marker>"`) depends on. Mechanism: `deploy/bin/build-stamp.mjs` replaces bare `wrangler deploy` in each Worker-script `npm run deploy`, derives `{commit, builtAt}` from the tree (a `-dirty` suffix when it is not clean, because wrangler bundles the TREE and not the commit), and passes them as `wrangler deploy --define SWITCHBOARD_BUILD_COMMIT/SWITCHBOARD_BUILT_AT`; `src/deploy/buildStamp.ts` is the only reader (`injectedBuildStamp`), gating both reads behind `typeof` because an un-stamped bundle's identifiers do not exist at all — that path answers `commit: "unknown"` instead of throwing inside `/healthz`. `--define` and not `--var`: a CLI `--var` may replace the `vars` a Worker's wrangler.jsonc declares, and the resident's `STATE_WORKER_URL` is load-bearing (the watchdog records firings through it). A generated FILE cannot serve a Worker script either — committed, a deploy would dirty the tree (which `deploy all` refuses); gitignored, `tsc` and a fresh clone break on the missing import. Surfaces: resident `GET /healthz` → `{ok, build}` (was `{ok, u}`); memory `GET /healthz` → `{ok, build, features}`; sandbox gains `GET /healthz` → `{ok, build}` **behind the same `SANDBOX_TOKEN` bearer as every other route** — a build stamp does not justify a new unauthenticated surface on the Worker that proxies command execution.

14. **A full fleet is capacity, not a dead sandbox (`fleet-busy`)**: the per-thread sandbox Worker runs at most `max_instances` container instances (`deploy/cloudflare-sandbox/wrangler.template.jsonc`, 25 — billed per AWAKE instance, with ~15× headroom on the account's containers limits at the item-16 instance size); the thread past that ceiling gets no instance, so `@cloudflare/sandbox` 0.3.x cannot create a session and throws the bare `Failed to create session: 503` (the platform's own wording is "no container instance that can be provided…", and since the 0.13 line also "Maximum number of running container instances exceeded. Try again later, or try configuring a higher value for max_instances" — the phrase the 0.13 SDK's own warm pool matches on — and the platform's start-rate limit, "You are requesting too many containers per second", met by a burst of fresh threads (twenty-four seeded sandboxes started within twenty seconds had a third of their starts refused with it), which is capacity for a moment exactly like a full fleet; the 0.12 and 0.13 lines throw a typed `ContainerUnavailableError`, code `CONTAINER_UNAVAILABLE`, keeping the platform's wording as its message — the Worker takes the type by name first, `isFleetBusyError`, and the text second, every phrasing above). **Nothing ran and nothing is broken.** The contract: (a) the **Worker** recognizes those messages with the shared `isFleetBusy` (`src/execution/sandboxErrors.ts`, node-free, bundled into the Worker) and NAMES the condition — `/exec` finishes its streamed body with `{error: "fleet-busy: <explanation> (<sdk message>)", reason: "fleet-busy", stdout: "", stderr: <same>, exitCode: 127}` (the item-3 dual shape kept for older executors), `/read` and `/write` answer HTTP 503 `{error, reason: "fleet-busy"}` — the same named-reason precedent as the resident's `mirror-busy`; re-sending is safe by construction because session creation fails before any command or file op starts. A container still starting is the sibling token `sandbox-starting` (item 23), waited on by the same executor loop under its own budget. (b) The **executor** (`CloudflareSandboxExecutor.call`) matches the machine token only (`reason === "fleet-busy"`, in-body or on a 503 — an OLDER Worker's bare message stays an ordinary in-body error, so a bot deployed ahead of its Worker changes nothing) and re-sends the IDENTICAL request — same route, body, headers, the envs resolved once per call — after 10 s, 20 s, then 30 s, until the total wait reaches `min(the command's own timeoutMs — the 5-min default for a file op —, FLEET_BUSY_WAIT_MAX_MS = 5 min)`; a hard stop ends the wait at once. Every OTHER in-body error is still `ExecInfraError` after exactly one send. (c) When the wait is spent it throws **`ExecCapacityError`** (`executor.ts`; `extends Error`, NOT `ExecInfraError`) whose message names the wait: `sandbox fleet busy — no free per-thread sandbox after waiting 300s (the fleet's max_instances is reached); try again in a few minutes`. (d) On pi the container is provisioned at the workspace attach, before pi starts, so a full fleet is met there and never inside a turn; a relayed tool that meets it is that one call's failure, named. The `fleet_busy` note kind (parsed by `runEventLines`, a medium `infra_failure` finding in the friction analyzer) was the native loop's — it returned the tool result as `⏳ Sandbox fleet busy — <message>. Retry the command in a minute or finish with what you have.` and went on — and stays a record fact from before the loop's deletion. The shape this closed: a burst of cold runs exhausts `max_instances`, one run gets two identical `Failed to create session: 503` errors seconds apart, the breaker reads them as a wedged sandbox, and the run aborts with a "dead sandbox" diagnosis for a fleet that was merely full. **Deploy order**: the sandbox Worker before the bot — an old Worker's plain in-body error still reads as infra (today's behaviour, unchanged).
15. **Every executor operation is an `exec.*` span** ([tracing.md](tracing.md) item 17). The relay hands each relayed tool call a `TracingExecutor` (`src/execution/tracingExecutor.ts`, a Decorator over the run's executor; [harness-pi.md](harness-pi.md) item 7): `exec` → `exec.exec`, `readFile` → `exec.read_file`, `writeFile` → `exec.write_file`, and `exec.release` / `exec.move_to` only when the wrapped executor has them — each a log-only span under the tool call's span carrying the `backend` and the per-call `timeoutMs`, never the command, the path or the output; a throw ends the span `error` and propagates unchanged. `ExecutorSelection.backend` names where the run's commands execute — `local` (the local executor, and the null executor an agent without a repo gets), `e2b`, `sandbox` (the per-thread Cloudflare sandbox), `resident` — so the spans of a resident-backed run and a cold run tell apart. The wrapper hands each `exec.*` span to the inner executor as `opts.span` (`ExecTraceOptions` on every `Executor` method), so a resident's or a sandbox's HTTP call is that span's `http.client` child ([tracing.md](tracing.md) item 21).
16. **The cold sandbox is the platform's largest predefined instance type**: `deploy/cloudflare-sandbox/wrangler.template.jsonc` sets `instance_type: "standard-4"` — 4 vCPU / 12 GiB / 20 GB; a custom type can be no larger. A cold run clones, installs and checks a whole repository in one container with nothing warm, so the size is decided by the largest single command a cold thread must be able to run, not by the typical one: a large monorepo's typecheck alone needs more than 8 GiB (its own CI notes put the root program near 8 GB and each sub-program near 4 GB), and the previous `standard-3` (2 vCPU / 8 GiB / 16 GB; a live probe read `nproc` 2, 8189 MiB, 15 GB on `/`) could not run it at all. The cost is per AWAKE instance — a sandbox sleeps after 5 idle minutes (item 1) — so the step is paid per active run-hour: 12 GiB against 8 of memory, and vCPU only while the cores are busy; the fleet ceiling (item 14) keeps ~15× headroom on the account's containers limits (6 TiB memory / 1500 vCPU against 25 × 12 GiB and 25 × 4 vCPU — the vCPU line is the tighter one). The resident reaches the same 4 / 12 GiB / 20 GB from the other direction (sixteen threads sharing one container, [resident-repos.md](resident-repos.md) item 55). Both execution images set `NODE_OPTIONS=--max-old-space-size=8192`, so one Node process may take 8 GiB of the 12 for its heap: V8's own default is a quarter of physical memory (~3 GiB here), and the resident image once pinned 1,536 MB — an eighth, meant as a fair share among its sixteen seats — under which this repository's own two `tsc` passes die with "heap out of memory" (they pass at 3,072 MB with ~2 GB of RSS each). A per-process heap limit never shared memory between processes; the cgroup does. The cap is a runaway guard sized for the largest legitimate command, with room, and the same in both images.
17. **Docker in the cold sandbox**: the sandbox image installs Debian's `docker.io` engine and `iptables` in its apt layer, and `docker` on PATH is a wrapper (`deploy/cloudflare-sandbox/docker-wrapper.sh`, installed as `/usr/local/bin/docker` ahead of `/usr/bin/docker`) that starts the engine on first use: when `/var/run/docker.sock` is absent or `docker info` fails it enables `net.ipv4.ip_forward` (the default bridge needs it for containers to reach the network), starts `dockerd` **in its own session** (`setsid -f`, log at `/var/log/dockerd.log`), waits up to 40 s for `docker info` to answer — failing with a message that names the log — and then `exec`s the real client with the caller's arguments. Every later `docker` call in the same warm container finds the engine running and passes straight through. Why in the image: a daemon installed at run time is lost with the container after its idle sleep (item 1) and costs minutes per run. Why its own session: every `/exec` runs under `timeout -k 10 … bash -c` (item 2) and its process group is reaped when the command returns, so a daemon started with `nohup … &` dies with the command that started it; a `setsid` daemon survives to the next one. Why it is safe to ship: the sandbox already runs as root with full capabilities inside its own Firecracker microVM, so a container a run starts lives inside that run's VM and shares its blast radius — the engine adds no privilege the run did not already have, and no `privileged` flag, capability or Worker config changes with it. What it unlocks: `nektos/act` in its default per-job-container mode, and repositories whose tests need containers (testcontainers, compose, image builds), which a cold sandbox could not run before.
18. **Machine classes** ([record 0026](../../decisions/0026-capability-profiles-and-request-routing.md)): `AgentDef.machine` is one of `none | blank | repo-cold | repo-resident`, and the executor factory, repository resolution and the repository gates read the class and nothing else about the agent (`machineNeedsRepo` is the one fact the resolver and the gates take from it). `none`: the null executor (item 7). `blank`: the per-thread backend with an **empty workspace** — no repository is resolved or gated, whatever the context carries — and **no credential** (nothing says a blank run acts as anyone); the resident registry and Worker are never touched. `repo-cold`: the per-thread backend **with the checkout** and the run's credential — scoped like the sandbox token, by the profile's identity (item 5; a `none` identity vets anonymously) — **even when the repository has a serviceable resident**. A bare `owner/name` is vetted against GitHub with that same credential (`githubRepoProbe`: one `GET /repos/{owner}/{name}`; 200 binds, 404 refuses, any other status, a transport failure or a credential that cannot be minted is `unreachable` — could not verify, never a bind and never a refusal; without a credential the vet is anonymous, so a private repository reads as 404, as it would to the run), a bare `in <name>` stays prose (the registry listing is never read), and the resident registry and Worker are never consulted, so an outage there can neither refuse nor delay the run. Its refusals name GitHub, never onboarding: a 404 closes the card `repo not visible` and replies that the repository is outside the Switchboard GitHub App installation or misnamed (`repo_not_visible`, with or without a resident fleet); an unanswered vet is `repo_unverified` with GitHub as the silent party; a visible repository meets the per-repo access gate like every class. `repo-resident`: items 7–8 and [resident-repos.md](resident-repos.md) items 24 and 29 — the onboarded resident when serviceable, else the cold fallback with its note, the registry vet included. Cold is the class for `repo-cold` and a fallback for `repo-resident`: only the fallback carries a note. `explore` is the first preset on `repo-cold` ([agent-explore.md](agent-explore.md) item 3); no preset declares `blank` yet, so that class is proven with a synthetic agent.

19. **A binary read on the seam** (`src/execution/binaryRead.ts`): `Executor.readBytes?(path)` hands back a workspace file whole, as bytes — the screenshot or PDF a run produced, for a tool that needs the bytes rather than `readFile`'s text view. One cap for every implementation, `MAX_READ_BYTES` (10 MiB): a binary cannot be truncated the way text output is, so a larger file is refused by name, never trimmed. `LocalExecutor` stats then reads; the two remote clients ask the same `/read` route for `encoding: "base64"` and decode the answer (`decodeBase64Read`). Both Workers accept the body: no `encoding` is the text read every older client sends, anything but `base64` a 400 by name; both Workers `stat` the file first (as the thread user on the resident) and judge the cap on that size before any read. The sandbox Worker then reads through the SDK's base64 encoding; the resident reads in chunks — `READ_CHUNK_BYTES` (1 MiB less one, a multiple of 3 so the pieces' base64 concatenates without padding) through `tail -c +N | head -c L | base64 -w0` as the thread user — because one command's stdout crosses the sandbox SDK's process log stream, which cuts a stream past a retention limit its typings do not name (about 2.3 MB observed) and reports the cut only as a `truncated` flag on the output; a 12 MB file once came back as 1.7 MB and was handed on as complete. The DO's `run()` now surfaces that flag, a chunk it marks or whose length is not the expected base64 length fails the read as `read-inconsistent` (409, not a 5xx the client would retry for 30 s), and the sandbox Worker refuses the same way when the SDK's read is not `stat`'s size. The answer is `{ encoding: "base64", content, size }` or `{ encoding: "base64", tooLarge: true }` — HTTP 200 either way, because the clients classify a non-2xx or an in-body `error` as a sick Worker and a large file is the model's mistake, not infrastructure. The client decodes and refuses bytes that are not `size` (`read-inconsistent`), and an answer without a size as one that predates the verified read. A Worker that predates binary reads ignores the field and answers text: the client names it as a rollout gap ("redeploy it") rather than decoding text as base64. The method is optional, and the decorators (`ExecHealthTracker`, `TracingExecutor` — `exec.read_bytes`) carry it exactly when the inner executor has it, so a tool that finds it absent tells the truth; the e2b backend has none.
20. **The artifact store** ([record 0033](../../decisions/0033-artifacts-move-by-reference-through-r2.md); `src/artifacts/`): where a run's files move by reference when the `artifacts:` section is configured — one private R2 bucket the bot signs URLs for and never carries a file through. The seam `ArtifactStore { presignPut(key, contentType), presignGet(key), head(key), get(key, { range? }), copyFromUrl({ url, size, key }) }` (`get` opens the object as a stream from a signed GET minted per call — what the run page's proxy route pipes, [live-view.md](live-view.md) item 26; the one place the bot's process touches an artifact's bytes, in transit; with a single byte range — `parseByteRange` admits `bytes=start-end`, `bytes=start-` and `bytes=-suffix`, nothing else — it answers the slice with `part` naming the offsets against the whole `size`, the whole object when the store ignores the range, or `unsatisfiable` with the size when the range starts past the end; the `range` header rides beside the signature, unsigned, as S3 clients send it and `aws4fetch` leaves it) has two implementations: `R2ArtifactStore` over R2's S3 API (`<accountId>.r2.cloudflarestorage.com/<bucket>`, region `auto`, unsigned payload, `aws4fetch` as the signer) and `InMemoryArtifactStore` for tests. Every presigned URL admits a request for `PRESIGN_TTL_SECONDS` (600) and names one key; a PUT URL signs the content type so the object's type is the one the tool derived; `head` answers size and type or null; `copyFromUrl` asks the bot's own Worker (`POST /artifacts/copy`, bearer `ARTIFACTS_COPY_TOKEN`, bounded by `artifacts.inbound.copyTimeoutMs`) and accepts only an answer naming the key and size it asked for. Keys are `runs/<runId>/out/<seq>-<basename>` for a produced file and `threads/<threadKeySafe>/in/<messageTs>/<index>-<basename>` for a received one, the leaf unique per file; `safeBasename` reduces a filename to `[A-Za-z0-9._-]` (runs collapsed, never empty, never a run of dots) so it is shell-, URL- and key-safe by construction; `contentTypeFor` derives a produced file's type from its extension once, `application/octet-stream` otherwise, and `INLINE_IMAGE_TYPES` names the four raster types the run page renders inline. The section (`r2.accountId`, `r2.bucket`, `retentionDays`, `inbound.maxBytesPerMessage`, `inbound.copyTimeoutMs`) is validated at load like every other, an unknown key refused by name; three optional bot secrets (`ARTIFACTS_R2_ACCESS_KEY_ID`, `ARTIFACTS_R2_SECRET_ACCESS_KEY`, `ARTIFACTS_COPY_TOKEN`) travel the three-file secret contract, and a configured store with any of them or `PUBLIC_BASE_URL` missing fails startup by name, never downgrades. `/healthz` carries `artifacts: { bucket }` when configured and no key otherwise; bucket privacy is the operator's `artifacts check`, not a runtime probe (R2's S3 endpoint refuses every unsigned request whether or not the bucket is public). Absent section: no store, and every surface behaves as before it existed. The outbound path (`attach_file` through a presigned PUT and the channel's upload ticket, [agent-coding.md](agent-coding.md) item 10) and the run event and proxy route ([live-view.md](live-view.md) item 26) are built on it. *The inbound half's transport* is the bot Worker's copy route (`deploy/cloudflare/artifactsCopy.ts`, `POST /artifacts/copy { url, size, key }`, what `R2ArtifactStore.copyFromUrl` calls with `ARTIFACTS_COPY_TOKEN`): the Worker holds the two things the copy needs and the bot never does — the `ARTIFACTS` R2 binding and the Slack bot token a `url_private` requires — so the bytes flow Slack → Worker → R2 and the bot only asks. In order, each refusal before the next step: the copy bearer, compared in constant time (401 — an unknown caller learns nothing, not even whether a bucket is bound); the request's shape before any fetch (`url` must be `https:` on `files.slack.com`, `slack.com` or a `*.slack.com` host — 400; `size` a positive integer within the 1 GiB ceiling — 400/413; `key` an inbound key as `inboundKey` builds them, under `threads/`, one character class, no dot segments — 400); no binding or no Slack token → 503; then Slack's answer before any `put` — the token rides in the `Authorization` header and redirects are not followed, so a 200 `text/html` (Slack's login page: the token does not reach the file) is refused as unauthorized (502), any status other than 200 is Slack's word (502), and a `Content-Length` absent or other than `size` is 409 naming both numbers. The body flows through a `FixedLengthStream(size)` into one `put(key, stream, { httpMetadata: { contentType } })` with Slack's content type: a stream that ends short fails the pipe, the put rejects, the bucket keeps nothing and the route answers 502; success answers `{ key, size }`. `GET /artifacts/copy` with the bearer answers `{ bucket }` — the name the Worker binds — so the operator's check can hold it against the config's (`bucketMismatch`, `src/artifacts/agreement.ts`: the same name, or a problem naming both; a Worker binding none says so). *The binding* is rendered only when the deployment profile names a bucket (`deploy/profile.json` `artifacts.bucket`, R2's bucket-name rules, optional and strict): the bot template's `{{#if artifacts}}` block renders `r2_buckets: [{ ARTIFACTS, <bucket> }]` and the `ARTIFACTS_BUCKET_NAME` var together, and a profile without one binds nothing. `npm run deploy` in the bot Worker's directory creates every bucket the rendered config binds before `wrangler deploy` validates the binding (`ensure-bucket.mjs`: `wrangler r2 bucket create`, "already exists" is success, any other refusal is wrangler's and stops the deploy); the plan asks the credential for `wrangler r2 bucket list` (Workers R2 Storage: Edit) on the bot step only when the profile names a bucket. *Inbound staging* rides on that route. With a store configured the Slack adapter leaves the files NEITHER inline pass could carry — a video, a 6 MiB PNG (over the 5 MiB image cap), a PDF over the 10 MiB document cap, a zip — on Slack by reference (`stagedFiles`, `IncomingMessage.staged`: name, size, type, `url_private`, the message's `ts`; metadata only, the inline caps unmoved, a 1 MiB PNG still inline), each within Slack's 1 GiB per-file ceiling, at most ten per message and within `artifacts.inbound.maxBytesPerMessage` (2 GiB by default); the rest stay skipped with the reason in the message's note, and the secret-file denylist refuses staging exactly as it refuses inlining — a `.pem` is never copied anywhere. Without a store nothing is staged and the note reads as before. The reference rides every path a message takes: a follow-up steered into a live run (`FollowUpInput.staged`, the durable inbox row — a few hundred bytes on the base row, so it survives the restart even when the row drops its inline attachments), and a fresh turn (`mergeFollowUps` concatenates them). For a run whose agent has a workspace (`machine` other than `none`) the dispatcher starts the copies right after the run's row exists — `copyFromUrl` into `threads/<threadKeySafe>/in/<messageTs>/<index>-<basename>`, 1-based, so two files of one name take `1-a.png` and `2-a.png` — overlapping the workspace attach, publishes the `artifact` event (`direction: "in"`, naming the message the file arrived with — `messageId`, the id the message's `input` event carries, [live-view.md](live-view.md) item 26) as each copy answers, then after the attach pulls each landed copy over the run's own executor as the thread user (`src/core/dispatch/staging.ts`: `mkdir -p attachments && curl -fsS -o 'attachments/<index>-<basename>' '<presigned GET>'`, both operands single-quoted so `clip"; echo pwned; ".mp4` is a filename; a resident first appends `attachments/` to `.git/info/exclude` so its clean-check keeps reading clean) and appends one line to the request turn: `Attached files are in ./attachments/: 1-clip.mp4 (312 MB, video/mp4)`, a failed copy or pull named with its reason (`clip.mp4 could not be staged: …`, curl's words, no retry). A steer into a live run is staged by the runner before the model reads the drained turn (`RunOptions.stageFollowUps`), the same line closing the follow-up prompt. A workspace-less agent copies nothing and its turn says ``this agent has no workspace for <name> (<size>, <type>); ask `agent:coding` to work with it``. *The thread's files are one catalogue* — the runs' `artifact` events, `in` and `out`; the record is the catalogue and a file no run recorded is not reachable, by design — read one way (`src/core/dispatch/threadAssets.ts` `readThreadAssets`: every run of the thread, listed past one page by cursor, each record paged to its end since a steer's file lands wherever in the log the steer did; one entry per key, oldest run first, owned by the run that first named it; then the store asked by HEAD once per key whether it still holds the object — a list, a page or a HEAD that fails stops only what it was reading, with a logged line) and rendered three ways. (1) *The re-pull*: a new run in a thread that has run before reads the catalogue beside the message's own copies so both overlap the workspace attach, and pulls the `in` entries the store holds into `attachments/` after the message's own files — they number after them on the same counter, are named `from earlier in the thread` in the line and recorded as this run's own `in` event so the run page serves them; a key the store no longer holds is named as expired and takes no index, one the store could not be asked about is named so; nothing is copied again and Slack is never asked; this run's own row is left out of the read (its files are the turn's). (2) *The prompt*: every run in a thread with files carries a FILES OF THIS THREAD block in the session block after memory ([session-log.md](session-log.md) item 10), one line per file — name, size, type, received or produced by which run, and where it is for this run: in the workspace at `./attachments/<index>-<basename>` when this run pulled it (`WorkspaceFiles`, filled by every staging round), on its run's page for a produced file, no longer in the store when its retention passed. (3) *`recall`*: a hit whose turn names a file carries the file's key and the same where-clause, and `recall {assets: true}` lists the catalogue, read fresh, outright ([session-log.md](session-log.md) item 10). A steer into the live run stages only its own files, as before. A resumed run stages nothing (its turn already carried the files). Both coding prompts say the files are already in `./attachments/` when the turn names them.
21. **The container outlives its runtime.** The SDK's container server exits on any uncaught exception (`process.exit(1)` in its `uncaughtException` handler), and the base image runs it as tini's one child (`ENTRYPOINT ["/usr/bin/tini", "--", "/container-server/sandbox"]`; tini exits with its child) — so one such error ended the container: `/workspace` gone, a detached pi with it, and every later command a failure until the platform noticed. The crash class that showed this — a Bun 1.3.12 `fs.watch` race on the server's own session directory, on the 0.12.9 image — is gone on the 0.13 line, whose server is compiled with Bun 1.3.14; the defence stays for the next uncaught exception. The image's `ENTRYPOINT` is tini running `deploy/cloudflare-sandbox/runtime-supervisor.sh`: tini stays PID 1 (it reaps orphans and forwards the platform's signals), the supervisor starts the server as a child and starts it again one second after it exits, so the disk, the detached processes and the platform's view of the container survive, and the next command starts a fresh process on the new server (item 4). A platform stop is SIGTERM, forwarded by tini to the supervisor and by the supervisor to the server, nothing starts again — a stop that lands during the pause between two starts, or before the new pid is known, still ends it — and the server's own status is the container's; a terminal's INT is forwarded as TERM too, since a child started with `&` by a non-interactive shell ignores INT. Five exits in a row inside ten seconds of starting end the container with the last status instead of looping. Invariant: **a runtime crash costs at most the command in flight** — never the workspace, never a detached process, never the thread's container.
22. **The idle deadline is the Durable Object's, and nothing inside the container can extend it.** On the 0.13 SDK line `sleepAfter` is not a deadline: at expiry the SDK asks its runtime whether any tracked process or terminal is still active and renews the timeout if so — or if the probe fails at all. A detached pi holds its command's stdio open, a bot deploy mid-run orphans that pi until the same thread's next run, and the container is awake for good: twenty-five sandboxes were found running 10–16 h after their last request against the 5-minute `sleepAfter`, with three runs in flight, and every new thread was answered with the item-14 refusal. The Worker therefore decides for itself (`src/execution/sandboxIdle.ts`, node-free, bundled into the Worker; `IdleGuard`, wired in `SwitchboardSandbox`): (a) every request the object serves — `/exec`, `/read`, `/write` — runs inside `served()`, which holds the request's start time in flight and records its finish, success or failure alike; the finish reaches storage at most every 10 s (`PERSIST_EVERY_MS`) and is the baseline for the object's next wake. (b) The verdict (`idleVerdict`, pure): **destroy** when nothing is in flight and the last finish is `SANDBOX_SLEEP_AFTER_MS` (5 min, held equal to `SANDBOX_SLEEP_AFTER` by a test) or more ago, or when the oldest request in flight has been there `INFLIGHT_STUCK_MS` (the 20-minute `/exec` maximum plus 10 min) or more — requests are measured one by one, so the pi harness's 750 ms polls beside a 20-minute command never read as one stuck request; **keep** otherwise, and a clock that went backwards keeps. (c) Two clocks ask for the verdict: a sweep the object schedules for itself through the SDK's schedule table (`idleSweep`, every `IDLE_SWEEP_INTERVAL_MS` = 60 s, re-armed after each run while a container is running or a request is in flight, armed again by the first request after a stop) and the SDK's own `onActivityExpired`, overridden to answer with the same verdict — its process probes never decide. (d) A destroy is the SDK's clean `destroy()` (sessions closed, the container SIGKILLed at the end) bounded by `DESTROY_GRACE_MS` (60 s); when that has not finished, threw, or left `ctx.container.running` anything but `false`, the platform's own `ctx.container.destroy()` follows — the guarantee lives in this step. Overlapping deadlines destroy once. (e) On every wake of the object, including the one the SDK's alarm causes on a leaked container, `wake()` reads the stored baseline — or takes now when there is none — and arms the sweep, so a fleet leaked before this code arrived is gone within one window plus one sweep of the deploy. Every stop is one JSON line in Workers Logs (`sandbox.idle-stop` with `why: idle | stuck`, `idleMs`, `source: sweep | sdk-expiry`, the thread; `sandbox.idle-stop.forced` when the platform kill was needed). Invariant: **a per-thread container's life past its last request is at most `SANDBOX_SLEEP_AFTER` plus one sweep interval** — the fleet ceiling (item 14) is reached by concurrent runs only, never by containers no run is using. The item-21 invariant is untouched: a runtime crash still costs at most the command in flight; only a thread nobody has spoken to for five minutes loses its container, as on every SDK line before.
23. **A thread's first request never carries the container's start (`sandbox-starting`).** The SDK's first exec on a container that is not running does the start inside the call — the platform's instance grant (its default wait 30 s), the image pull on a machine that has not seen the image, the microVM boot and the runtime's port (90 s) — so a slow start can outlive the executor's per-send deadline for a 60 s command (90 s, item 11). The deaths that led here — every run that fell to a fresh sandbox dying with "gave no answer within 90s", seven in one hour once the leaked fleet drained — turned out to be item 24's held output pipes, not the start (the Worker's logs showed the container answering commands in 230 ms ten seconds after the ask); the gate stays as the guard for a genuinely slow start, which the SDK's own allowances say can take two minutes. The contract: (a) the **Worker's** Durable Object runs every route through a start gate (`src/execution/sandboxStart.ts`, node-free, bundled; `StartGate`, wired in `SwitchboardSandbox`): when `ctx.container.running` is not `true` and no warm-up is in flight, it begins one — a single `true` through the SDK, which does the start, run inside the idle ledger so the guard (item 22) sees a request in flight for as long as the start takes — and answers at once; `/exec` finishes its streamed body with `{error: "sandbox-starting: <explanation> (<phase>)", reason: "sandbox-starting", stdout: "", stderr: <same>, exitCode: 127}` (the item-3 dual shape), `/read` and `/write` answer HTTP 503 `{error, reason: "sandbox-starting"}`; while the warm-up is in flight every request gets the same answer and no second warm-up begins; once the runtime answered, requests run. A warm-up that fails hands its error to the next request, once, where it is classified as any SDK error is — a full fleet is `fleet-busy` (item 14), a silent control port `runtime-unreachable` (item 9) — and the request after that starts a fresh warm-up if the container is still not running; a container the idle guard destroyed is started again the same way by the thread's next request. Every start is two JSON lines in Workers Logs, `sandbox.starting` and `sandbox.started` with `durationMs` (or `sandbox.start-failed` with the error), so the start time is a measured fact. (b) The **executor** (`CloudflareSandboxExecutor.call`) matches the machine token only (`reason === "sandbox-starting"`, in-body or on a 503 — an older Worker changes nothing) and re-sends the IDENTICAL request after 5 s, 10 s, then 15 s (`SANDBOX_START_BACKOFF_MS`) until the total wait reaches `SANDBOX_START_WAIT_MAX_MS` (10 min — under a midday burst the platform granted containers 2.5 to 5 minutes late, and two threads that died at five minutes had theirs a minute away; the ladder is the token's, restarting at its first step when the answer's token changes, so a start that becomes a refused start is re-sent after the fleet's 10 s, not the 30 s the start's ladder had reached; the Worker asks the SDK for the instance grant with a 10 s limit, `INSTANCE_GET_TIMEOUT_MS`, so a refusal the platform took 63 s to voice under the SDK's 30 s default and retries reaches the gate in seconds) — **whatever the command's own budget**: the start is not the command's time, and a 60 s command survives a two-minute start it did not cause. The two tokens share one wait ledger; the plan (budget, backoff, exhausted message) is the last token's, so a start that turns into a full fleet is judged by the fleet's budget from then on with the time already waited counted. (c) When the wait is spent it throws `ExecCapacityError` — `sandbox not ready — the thread's container did not finish starting within 600s; try again in a few minutes` — never `ExecInfraError`: a slow start is not a dead sandbox. Invariant: **the first command a thread runs on a fresh or restarted container is judged against its own budget from the moment the container is up, never against the start.** Deploy order: the Worker before the bot, as for item 14 — an old Worker's first exec still carries the start, today's behaviour.
24. **A command's output ends when the command does; a detached child never holds the answer.** On the 0.13 line an exec's answer is read from the process's log stream, which the runtime ends only when every holder of the command's stdout and stderr is gone. `setsid -f sh -c '…'` forks a wrapper out of the exec's process group but not out of its file descriptors, so a detached shell that inherited them kept the stream open for its own lifetime — the harness seam's pi start did exactly that, the start command never answered, and every run that fell to a fresh sandbox died at the executor's 90 s deadline while its container, and its pi, ran on (the same shape at 05:08Z that day and after each deploy; run 8aff9e50 is the receipt — the container answered its first commands in 230 ms and the start command was `canceled` 90 s later). Three parts: (a) the **seam's** start script (`startScript`, `src/core/harness/container.ts`) runs the wrapper as `setsid -f sh -c '<inner>' </dev/null >/dev/null 2>&1` (`DETACHED_STDIO`); the process's own stdin is the FIFO and its stdout and stderr the run's files as before, so nothing the process needs changes, and the exec's descriptors close when the exec's shell exits. (b) The **Worker** waits for the output to end only until the command's deadline plus `OUTPUT_AFTER_EXIT_MS` (20 s, `src/execution/sandboxLifecycle.ts`; inside the executor's 30 s per-send margin so this answer always arrives first), then asks the runtime for the process's status: **exited** → the answer is its exit code, empty output and `heldOutputNote` on stderr — what happened, that the output is lost to this call and the job runs on, and how to detach (`DETACH_HINT`); **running** → the process is killed and answered as the shell-level timeout, as before. (c) The exit-124 **hint** (`timeoutNote`) spells the detach with the same redirects, `DETACH_HINT` = `setsid -f sh -c '<command> > /tmp/job.log 2>&1' </dev/null >/dev/null 2>&1`, one string shared by the seam (`DETACHED_STDIO` equals `DETACH_REDIRECTS`, held by a test) and the docs ([agent-explore.md](agent-explore.md) item 5). Invariant: **the Worker answers every `/exec` by the command's deadline plus 20 s, whatever the command left running** — a held pipe costs the call its output, never the run.
25. **A cold sandbox is seeded from the resident's snapshot before its first command (`POST /seed`).** A resident holds a stamped snapshot of its repository in R2 — the checkout without its dependency view ([resident-repos.md](resident-repos.md) item 7) and one archive per lockfile key holding the view (item 61) — and its `/status` publishes the handle, `snapshot.{checkoutBackupId, depsBackupId, ref, sha}`. The bot forwards it as the body's `seed` (`src/execution/seedPlan.ts`: `{slug, checkoutBackupId, depsBackupId?, ref, sha, fetchRef?, fetchSha?}`, every field checked — a backup id is a UUID, a slug is `owner/name`, a ref a branch name, a sha forty hex — and a bad one refused 400 by field name), with the run's env as on every route. The Durable Object, inside the idle ledger and behind the start gate (items 22, 23), restores the checkout into `/workspace/checkout` and the deps entry into `/workspace/.seed-deps` **presigned only** — the container downloads the archive over a URL the object signs, so a gigabyte never crosses the isolate; a Worker without the four presigned values answers `seed-unconfigured`, and `/healthz` says `backupTransfer` — each restore judged by bytes arriving against one shared `SEED_RESTORE_MAX_MS` deadline (8 min) and extracted onto the disk the resident's way (`unsquashfs` from the downloaded archive; the image carries squashfs-tools), then runs the fix-up as one failing-fast script under `SEED_FIXUP_TIMEOUT_MS` (3 min): the tree becomes root's, origin points at GitHub, the deps view replaces `node_modules`, the thread's `fetchRef` is fetched (the credential is the exec env's `GH_TOKEN` through the image's helper, never text in the script) and checked out at `fetchSha` when the fetch holds it else at the tip, and the head is printed last as the answer's `sha`. The marker `/workspace/.switchboard-seed` names the handle the container was seeded from: the same handle again (a retry, a re-attach) answers `cached` at once and never restores over a live tree. Every outcome is named in the streamed body (item 3), so the executor never reads a seed that did not happen as a dead sandbox: `{seeded: true, cached, slug, ref, sha, from, steps: {restore, deps, fixup}, phases: {checkout: {download, extract}, deps: {download, extract} | null}, ms}` — each restore split into the archive's download (judged by bytes) and its extraction onto the disk, logged as `sandbox.seed-restore` too, so a slow seed's owner is read off the answer, or `{seeded: false, reason, detail, step}` with `reason` one of `seed-missing` (the handle's objects are gone — a rotation or an offboard took them; the caller re-reads `/status` once and retries), `seed-failed` (a step failed; the run goes cold with the note), `seed-unconfigured`; a start or a full fleet met at the warm-up keeps its wait token (items 14, 23). A failed seed sweeps what it wrote — mounts, a half tree, the marker — so the cold run that follows clones into an empty workspace. The client is `CloudflareSandboxExecutor.seed`, under `SEED_BUDGET_MS`; the bot's selection of a seeded sandbox is the next item's work. The sandbox Worker binds the RESIDENT's cache bucket (`{{resident.script}}-cache`) and names it in `BACKUP_BUCKET_NAME`, inside an `{{#if resident}}` block of its template — a profile without a resident renders no seed bindings — and the `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` secrets are the resident's own, provisioned to the sandbox too. Deploy: the Worker before the bot, as for items 14 and 23.
26. **A run the resident refuses lands in a seeded sandbox when the probe carried the handle (`ExecutorSelection.seeded`).** In the factory's resident fallback — the probe answered a state that is not serviceable, or the attach failed — a probe body that carried `seed` (item 25) makes the per-thread sandbox call `POST /seed` before the run: `seedForThread` (`src/execution/seedPlan.ts`) puts the thread's own ref and resolved head on the handle as `fetchRef` / `fetchSha` (a thread with no ref stays on the snapshot's branch). A `seeded` answer puts `{slug, ref, sha, workspace: /workspace/checkout, cached, ms}` on the selection, and the card's note reads `<reason> — seeded sandbox · from resident snapshot · owner/name · ref@sha7` (`seededSandboxNote`), so a reader tells the three paths — resident, seeded, cold — apart from Slack alone; the `resident` discriminant stays unset. `seed-missing` re-reads `/status` once (`seedRetryDecision`) and retries with the newer handle when the resident published one; the same handle again, a second miss, `seed-failed`, `seed-unconfigured`, or a Worker with no `/seed` (an older release — the client's infra error) send the run cold with the refusal on the note: `<reason> — using fresh sandbox (seed …)`. Nothing to seed from — an unreachable resident (no body), a repository not onboarded (404), an execution type that is not the cloudflare sandbox — is the cold path unchanged. The dispatcher composes the seeded prompt variant after executor resolution exactly as the resident's ([resident-repos.md](resident-repos.md) item 31): `AgentDef.seededSystem` (`CODING_SYSTEM_SEEDED`, `REVIEW_SYSTEM_SEEDED` — the repository already cloned at the checkout, no clone, no install, `gh` and Docker present unlike the resident's) plus a line naming the repository and the checkout; the REVIEW TARGET block's seeded branch names the checkout, forbids a second clone, pins the first command to the HEAD check there and diffs against the base already present. `resident` and `seeded` never both hold; resident wins.
27. **A restoring resident is a short wait, never a cold fall-through (`/await-restore`).** A resident deploy restores every resident from its snapshot, and every dispatch that probed one mid-restore used to fall to the per-thread fleet at once — under load that burst exhausted `max_instances` and the runs died on capacity while a warm resident was seconds away (the leaked-slot half of the same incident was fixed separately). Event-driven, with **no polling and no retry timer on either side**: (a) the resident Worker gains `POST /await-restore` (operator scope, body `{resource}`; a not-onboarded resource is 404) whose Durable Object registers a waiter on an in-memory ledger (`RestoreWaiters`, `src/execution/restoreWaiters.ts`) BEFORE it reads the state — the read awaits storage, and a transition landing in that gap would otherwise publish to a ledger the request is not on yet and strand it — then answers a non-`restoring` state at once, withdrawing the waiter, and otherwise holds the ONE request until `setResidentState` transitions out of `restoring` — the transition itself publishes the restore event to every held waiter with the state landed on. The answer streams like `/attach` (heartbeat whitespace then one JSON document over HTTP 200) so a restore lasting minutes cannot lose the connection; a DO restart drops the held RPCs with the ledger and the bot's deadline names the fallback. (b) The bot's factory: a `/status` probe that finds the resident `restoring` opens that one held request (`ResidentExecutor.awaitRestore`, bounded by `AWAIT_RESTORE_TIMEOUT_MS` = 10 min — the outer safety net, not a retry timer) instead of falling cold; when the answer is a serviceable state it attaches as any serviceable probe does and the card names the wait (`· after waiting for the resident's restore`); a non-serviceable landing, an attach that fails after the wait, a failed wait, or an older Worker's 404 (a bundle that predates the route) takes item 26's fallback — a sandbox seeded from the probe's handle when it carried one, a fresh one otherwise — with the wait named on the note, never silently. The resumed-run re-attach path is unchanged: a resume never waits, its refusal is typed. Deploy order: the resident Worker before the bot, as always — an old Worker's 404 is the named cold fallback, today's behaviour.
28. **A container that did not accept the connection is a wait, never a dead sandbox (`runtime-busy`).** The container's runtime accepts one control connection per SDK call, and every `/exec`, `/read` and `/write` opens one. When the platform's fetch to the container's port is not accepted inside the platform's own allowance — a few seconds, well under the SDK's 30 s connect timeout — it throws a plain `Error`: `Container is taking too long to accept the connection; the application could be overwhelmed with load`. Nothing ran: the SDK was still connecting, before any process was started. The platform's words name a cause it never measured, and the token names the refusal, never its cause: live, the harness's one-second poll of its transcript log met it three minutes into a whole repository's `verify` saturating every core of the item-16 instance, AND on an idle container — a review thread whose every command was a `git clone`, two `git diff`s, a `grep` and a `sed`, each completing in under 31 ms, the runtime accepting a connection 0.9 s before the refused one and 0.8 s after it, the fleet at 7 of 25 and no deploy rolling. Both times the failure reached the bot as an ordinary in-body error (`answered`, item 9), the harness read it as the run's failure, killed the process and tore the workspace down — and the container accepted the kill a second later. The contract: (a) the **Worker** names the refusal only where no process was started — the spawn (`spawnFailure`) and the gate's warm-up — with the token `runtime-busy` in the item-3 dual shape (`runtimeBusyExecAnswer`); the file routes throw the typed `SandboxRuntimeBusyError` and the fetch handler answers HTTP 503 with the token; the recognizer is the platform's wording alone (`isRuntimeBusySignal`, `RUNTIME_BUSY_WORDING`, `src/execution/sandboxErrors.ts`), since the platform gives no type; a failure of a running command's output is never named so — the process exists, and a re-send would run it twice. (b) The **executor** re-sends the identical request on the token exactly as on `fleet-busy` (item 14) and `sandbox-starting` (item 23), after 3 s, 5 s, then 10 s, inside the operation's own budget capped at `RUNTIME_BUSY_WAIT_MAX_MS` (5 min) — when a command of the thread's own holds the container, it is bounded like every command — and throws `ExecCapacityError` naming the refusal when the wait is spent, never `ExecInfraError`; neither the token's explanation nor that message asserts what held the container or tells the reader to wait for a command that may not be running. (c) An older Worker's bare platform text stays an ordinary in-body error after one send: the token decides, never the words. The instance size is not the fix: an idle container has met the refusal, so no size rules it out, and a poll must survive it whatever the size; the SDK's connect timeout is not the knob either, since the platform's allowance runs out first and the SDK exposes none.

## Validation criteria

| Criterion | Proof |
|---|---|
| 18: `blank` → the per-thread sandbox with no repository and no credential, no resident probe though a resident is configured and a repository is in the context; on local execution, an empty per-thread workspace directory | `[unit]` `src/execution/factory.test.ts::makeExecutor machine classes::blank → a per-thread sandbox with NO repository and NO credential, no resident probe, even with a repo in the context`, `::blank (local) → a per-thread workspace directory, empty` |
| 18: `repo-cold` → the per-thread sandbox with the checkout and the run's credential (write for a `full` toolset, read for `readonly`), no note, no resident probe though one is configured; without a resolved repository, an empty workspace and no note | `[unit]` `src/execution/factory.test.ts::makeExecutor machine classes::repo-cold → a per-thread sandbox WITH the checkout and the run's credential; the resident is never probed though one is configured`, `::repo-cold without a resolved repository → the per-thread sandbox with an empty workspace, no note` |
| 18: the null executor's tool error names the class and the classes that provision a workspace | `[unit]` `src/execution/factory.test.ts::makeExecutor machine classes::the null executor's tool error names the class and the classes that provision a workspace` |
| 19: the `/read` encoding contract — no encoding is the text read, `base64` asks for bytes, anything else is refused by name; the resident reads text with `cat` and never bytes through one command; the base64 cap is exactly what a `MAX_READ_BYTES` file encodes to and the decoded byte count tells a file one byte over apart | `[unit]` `src/execution/binaryRead.test.ts::the /read encoding contract (item 19)::*` |
| 19: the bot decodes a base64 answer to the bytes only when they are exactly the size the Worker measured — a short answer is `read-inconsistent` with nothing handed on, an unsized one predates the verified read; a text answer (a Worker that predates binary reads) is named as a rollout gap and never decoded or counted as infra; a `tooLarge` answer is the cap's one message naming the file | `[unit]` `src/execution/binaryRead.test.ts::decodeBase64Read — the bot side of a Worker's answer (item 19)::*` |
| 19: the resident's chunked read — `stat -c %s` first and only a number parses; the chunk is a multiple of 3 below the SDK's observed stream cut so the pieces' base64 concatenates into the whole; the plan covers the size exactly with a short last chunk and an empty file has none; each chunk command seeks with `tail`, bounds with `head`, encodes unwrapped, and its expected base64 length is known before it runs | `[unit]` `src/execution/binaryRead.test.ts::the resident's chunked byte read (item 19)::*` |
| 19: both remote clients report a short answer as `read-inconsistent` (never infra) and carry a Worker's own 409 refusal with its words | `[unit]` `src/execution/readBytesRemote.test.ts::CloudflareSandboxExecutor.readBytes (item 19)::an answer whose bytes are not the size it names…`, `::the Worker's own read-inconsistent refusal…`, `src/execution/readBytesRemote.test.ts::ResidentExecutor.readBytes (item 19)::bytes that are not the named size are read-inconsistent…` |
| 19: `LocalExecutor.readBytes` hands back the exact bytes a utf8 read would mangle, refuses a path escaping the workspace, and refuses an over-cap file by name with its size before reading it | `[unit]` `src/execution/binaryRead.test.ts::LocalExecutor.readBytes (item 19)::*` |
| 19: the tracing executor carries `readBytes` exactly when the inner executor has it — an `exec.read_bytes` span without the path | `[unit]` `src/execution/tracingExecutor.test.ts::TracingExecutor::*` |
| 19: both remote clients ask `/read` for `encoding: "base64"` (the resident with its resource and thread) and hand back the decoded bytes; a text answer and a `tooLarge` answer are plain errors, never `ExecInfraError`; the resident's missing file is the route's 404 classified like `readFile`'s | `[unit]` `src/execution/readBytesRemote.test.ts::CloudflareSandboxExecutor.readBytes (item 19)::*`, `src/execution/readBytesRemote.test.ts::ResidentExecutor.readBytes (item 19)::*` |
| 20: an R2 PUT URL names one key on the bucket's S3 endpoint, admits requests for 600 s, signs the content type and carries no secret; a GET URL signs the host alone; the same inputs sign identically and another key does not; a key is percent-encoded per segment with the slashes kept | `[unit]` `src/artifacts/store.test.ts::R2ArtifactStore presigned URLs (item 20)::*` |
| 20: `head` answers size and type from a signed HEAD, null on 404, and throws on any other status or on a 2xx without a `content-length` (never reading a missing length as 0); the HEAD and the GET are signed with `accept-encoding: identity`, so the edge never compresses a text object's answer and drops its length (Node's fetch would otherwise advertise br/gzip/deflate/zstd, and a `text/plain` object of 48 bytes or more came back with no `content-length`) | `[unit]` `src/artifacts/store.test.ts::R2ArtifactStore.head (item 20)::*` |
| 20: `copyFromUrl` POSTs to the bot's own Worker with the copy bearer and a timeout and accepts only the size it asked for; a non-2xx or short answer is an error carrying the Worker's words | `[unit]` `src/artifacts/store.test.ts::R2ArtifactStore.copyFromUrl (item 20)::*` |
| 20: both implementations honour one contract (URLs name the key and the window; `head` reads what was stored and null for nothing; a single byte range reads the slice with `part` against the object's size, an open end and a suffix clamp, a start past the end is `unsatisfiable`); the in-memory copy stores exactly `size` bytes with the upstream type and refuses a short body | `[unit]` `src/artifacts/store.test.ts::InMemoryArtifactStore honours the store contract (item 20)::*`, `src/artifacts/store.test.ts::R2ArtifactStore (over a fetch double) honours the store contract (item 20)::*`, `src/artifacts/store.test.ts::InMemoryArtifactStore.copyFromUrl (item 20)::*` |
| 20: `parseByteRange` reads the three single-range forms and nothing else; the R2 `get` forwards a valid range on the wire unsigned, maps a 206 to `part`, a 200 to the whole object, a 416 to `unsatisfiable` with the size — from the 416's `Content-Range` when it names one, else from one HEAD (R2's InvalidRange answer carries no size), an object the HEAD no longer finds being null — and sends no range for a form it refuses | `[unit]` `src/artifacts/store.test.ts::parseByteRange (item 20)::*`, `src/artifacts/store.test.ts::R2ArtifactStore.get with a range (item 20)::*` |
| 20: `safeBasename` keeps the last segment and reduces every other character to `_`, collapsing runs, never empty and never a run of dots; outbound keys live under the run with a per-file sequence and inbound under the thread and message with the file's index | `[unit]` `src/artifacts/keys.test.ts::safeBasename (item 20)::*`, `src/artifacts/keys.test.ts::key builders (item 20)::*` |
| 20: the content type is derived from the extension, case-insensitively, `application/octet-stream` otherwise; the inline image set is exactly the four raster types | `[unit]` `src/artifacts/contentType.test.ts::contentTypeFor (item 20)::*` |
| 20: the `artifacts:` section parses with the documented keys and is absent by default; an unknown key at any level, a missing bucket or account, a bad bucket name and a non-integer knob are refused by name | `[unit]` `src/artifacts/config.test.ts::the artifacts config section (item 20)::*` |
| 20: no section → no store; the section with the three secrets and a public URL → the R2 store on the configured bucket; a missing secret or a missing `PUBLIC_BASE_URL` fails by name | `[unit]` `src/artifacts/buildStore.test.ts::buildArtifactStore (item 20)::*` |
| 20, the copy route before any fetch: a missing or wrong bearer is 401 with nothing fetched or put and `GET` with the bearer names the bound bucket (503 without a binding); a url off Slack's hosts or on `http:`, a non-positive or fractional size, a size over the 1 GiB ceiling (413), a key that is not an inbound key and a body that is not a JSON object are each refused by name with no fetch and no token sent; no binding or no Slack token is 503 after the bearer; a non-POST is 405 | `[unit]` `deploy/cloudflare/artifactsCopy.test.ts::artifacts copy — the request, before any fetch::*` |
| 20, Slack's answer before any put: a 200 `text/html` (the login page) is refused as unauthorized, a non-200 is Slack's status, the token rides the `Authorization` header with redirects not followed; a `Content-Length` absent or other than the declared size is 409 naming both numbers; a fetch that throws is 502 with the reason; nothing is put in any of these | `[unit]` `deploy/cloudflare/artifactsCopy.test.ts::artifacts copy — Slack's answer, before any put::*` |
| 20, the wiring: the Worker hands the copy route the request it rebuilt for tracing, never the inbound one — a rebuilt `Request` takes the body stream with it, so the original reads as an empty body and the route answers 400 "not JSON" while the rebuilt one is parsed and reaches Slack | `[unit]` `deploy/cloudflare/artifactsCopy.test.ts::artifacts copy — the wiring hands the route the request that still has its body::*` |
| 20, the copy: one `put` under the key through the fixed-length pipe with Slack's content type and the answer `{ key, size }`; an upstream body that ends short fails the pipe, the put rejects, the route is 502 and the bucket holds nothing | `[unit]` `deploy/cloudflare/artifactsCopy.test.ts::artifacts copy — the copy itself::*` |
| 20, the bucket step: every distinct `bucket_name` of the rendered config is read (none without `r2_buckets`); exit 0 is created, wrangler's "already exists" (10004) is success, any other refusal is wrangler's verbatim | `[unit]` `deploy/cloudflare/ensure-bucket.test.mjs::ensure-bucket — bucketNamesOf()::*`, `deploy/cloudflare/ensure-bucket.test.mjs::ensure-bucket — decide()::*` |
| 20, the binding: the profile's `artifacts.bucket` is optional and strict (R2 bucket-name rules, refused by field; the committed example names none); the template view carries it; the committed bot template renders `r2_buckets` and `ARTIFACTS_BUCKET_NAME` together exactly when the profile names a bucket, a bot-only profile with a bucket included; the plan asks the bot step for `r2 bucket list` only then | `[unit]` `src/deploy/profile.test.ts::…::\`artifacts.bucket\` is optional…`, `src/deploy/wranglerTemplate.test.ts::…::carries the Access application…`, `src/deploy/wranglerTemplate.test.ts::…::the committed bot template binds the artifacts bucket…`, `src/deploy/plan.test.ts::capabilityProblem::*` |
| 20, the agreement: the config's bucket and the Worker's bound bucket agree, or the problem names both; a Worker binding none says so; the shim routes `/artifacts/copy` to itself | `[unit]` `src/artifacts/agreement.test.ts::bucketMismatch (item 20)::*`, `src/core/trace/workerTrace.test.ts::shimRoute::*` |
| 20, staging — classification: a video, a 6 MiB PNG and an over-cap PDF are staged with their metadata while a 1 MiB PNG the image pass carried is not; store off → nothing staged and the labels unchanged; a 1.2 GB file, an eleventh file and the overflow past the per-message budget are skipped with the reason; the secret denylist wins over staging; no URL/name/size is skipped by name | `[unit]` `src/channels/slack/attachments.test.ts::stagedFiles (by-reference classification)::*` |
| 20, staging — the commands and the line: the pull makes the directory and fetches to `attachments/<index>-<basename>` with both operands single-quoted (a hostile name is a filename); the resident's exclude appends `attachments/` once; sizes read `312 KB`/`1.2 GB`; the line names every landed file with size and type and every failed one with its reason; the workspace-less line points at `agent:coding`; copies land under the thread's inbound key with an `in` event each, pulls follow in order after the exclude on a resident and without it on a sandbox, two files of one name take 1- and 2-, a failed copy is never pulled, a failed pull carries curl's words and is not retried, nothing to stage runs no command | `[unit]` `src/core/dispatch/staging.test.ts::staging — the commands::*`, `src/core/dispatch/staging.test.ts::staging — copy then pull::*` |
| 20, staging — the reference rides every path: a follow-up carries `staged` and `mergeFollowUps` concatenates it into the fresh turn; the durable inbox row carries it on the base row, reads it back, keeps it when the inline attachments are dropped, and drops a malformed entry never fatally | `[unit]` `src/core/threadAdmission.test.ts::mergeFollowUps — unconsumed inputs become ONE fresh request::texts join in order; attachments and staged references concatenate…`, `src/core/runLedger/inboxMessage.test.ts::durable inbox — staged references (record 0033)::*` |
| 20, staging — the dispatcher: a coding run's copy starts after admission and before the workspace attach, the pull runs over the run's executor and the first user turn ends with the line; a `general` run copies nothing and its turn names the missing workspace; without a store no pull and no line | `[unit]` `src/core/dispatcher.test.ts::inbound staging (record 0033)::*` |
| 20, staging — a steer into the live run: copied and pulled over the run's executor before the steer is sent, whose text ends with the line; the record carries the `in` event | `[unit]` `src/core/dispatch/runLoop.test.ts::the pi harness — every preset's runs, in the run's container::a steered follow-up's staged file is copied and pulled before pi reads the steer…`, `…::without a store the pi steer is sent as before…` |
| 20, the thread's files — one catalogue read: the thread's runs are listed past one page by cursor and each record paged to its end (an event on a late page is found); the `artifact` events, `in` and `out`, yield one entry per key, oldest run first, owned by the run that first named it; the store is asked once per key and `held` says whether it still holds the object; a run list, a record page or a HEAD that fails stops only what it was reading, with a warning naming it, and the other files still count; a run list the store could not serve (live rows only, no throw) is the catalogue of those rows, warned, with no further page asked for; `describeAsset` says what a file is and where it is for this run in the words the prompt and `recall` share | `[unit]` `src/core/dispatch/threadAssets.test.ts::readThreadAssets — the thread's files from its runs' records, held or not::*` |
| 20, staging — the thread's earlier files on that read: an `in` entry the store holds takes the run's next index and this run's own `in` event, one the store no longer holds is named as expired with no index and no event, one the store could not be asked about is named so; nothing is copied and the store is not asked again; `WorkspaceFiles` resolves a key to the path THIS run staged it under across every round; a later run in the thread pulls its own file then the earlier one (1- then 2-), re-copies nothing, carries the earlier key on its own record, its turn names the expired one, and its prompt lists the thread's files with where each is — its own message's file left to the turn | `[unit]` `src/core/dispatch/staging.test.ts::staging — the thread's earlier files::*`, `src/core/dispatcher.test.ts::inbound staging (record 0033)::a later run in the thread pulls the files dropped on earlier messages…` |
| 20, staging — both coding prompts say a dropped file is already in `./attachments/` when the turn names it | `[unit]` `src/agents/registry.test.ts::…::both coding prompts route files the person should see through attach_file…` |
| 20, live: a 1 GB Slack file copied through the deployed route — wall time, Slack's egress rate and the response on the unit's PR; a failure sends the record to its multipart fallback (the bot copying in 8 MiB parts) | `[agent]` after the bot Worker deploys with the binding: mint a copy from a switchboard thread's `url_private` with the copy bearer and read `{ key, size }` back; receipts on the receipts issue |
| 20, live — a large file dropped on a thread reaches the run: a 300 MB video (and, at the ceiling, a 1 GiB file) shared on a new message with the ask lands as `./attachments/1-<basename>` with the exact size and sha256 of the source, and `ffmpeg` reads the video; the bot's `/healthz` `process.rssMb` stays within tens of MB of its idle value throughout; the run's summary quotes the turn's line when a file is NOT there rather than inventing it | `[agent]` generate the two files locally (`ffmpeg -f lavfi -i testsrc2 …`, `head -c 1073741824 /dev/urandom`), record their sha256, upload each with a Slack upload URL (`curl --upload-file`) and complete it onto a fresh message asking for path, size, sha256 (and a contact sheet for the video); sample `/healthz` every 20 s meanwhile; compare the run's numbers to the local ones; receipts on the receipts issue |
| 20, live — a deployment without `artifacts:` is unchanged: `/healthz` carries no `artifacts` key, a dropped file over the inline caps gets the "could not be passed through" note as before, and `attach_file` takes the inline path | `[agent]` on a deployment whose config has no `artifacts:` block (or after removing it and `deploy restart`): read `/healthz`, drop an over-cap file on a thread and attach a small screenshot from a coding run; expect the pre-store behaviour on all three; receipts on the receipts issue |
| 20: `/healthz` carries `artifacts: { bucket }` when the store is configured and no key when not | `[unit]` `src/channels/health.test.ts::build identity on /healthz::healthPayload carries \`artifacts: { bucket }\` when the store is configured and no key when not` |
| 20: the three secrets are declared on the bot in the manifest, on the Worker's `Env` and in its forward list | `[unit]` `src/core/secretsManifest.test.ts` |
| 20, the bucket's rules: a retention gives two lifecycle rules over every key — delete after the retention, abort an incomplete multipart upload after one day — described as a person reads them; the read-back matches only when both rules are there as written (a missing, disabled or differently aged rule is named, another id is left alone); a bucket is private exactly when its managed r2.dev domain is disabled and no custom domain is enabled, every open setting named otherwise | `[unit]` `src/deploy/artifactsBucket.test.ts::artifacts bucket — the rules::*`, `src/deploy/artifactsBucket.test.ts::artifacts bucket — privacy::*` |
| 20, the bucket's calls: the four Cloudflare calls ride the operator's `CLOUDFLARE_API_TOKEN` as the bearer and never the bot's token; a missing token is the call's problem naming the variable and the permission with nothing called; a 401/403 names the permission, another status keeps the API's words, `success: false` carries its errors, a thrown fetch its reason, a malformed answer is named | `[unit]` `src/deploy/artifactsBucket.test.ts::artifacts bucket — the host's calls::*` |
| 20, live: `artifacts check` on the deployment's bucket reports `private: true`, and `artifacts lifecycle` applies the configured retention and reads it back equal | `[agent]` with `CLOUDFLARE_API_TOKEN` for the account in the shell: `artifacts check` → the private verdict naming the disabled managed domain; `artifacts lifecycle --dry-run` then `artifacts lifecycle` → `applied 2 lifecycle rule(s) … and read them back equal`; receipts on the receipts issue |
| 19 live: after both Workers deploy, a coding run in a resident thread and one in a cold sandbox thread each `playwright screenshot https://example.com shot.png` then `attach_file shot.png` — the PNG renders inline in the Slack thread; a 12 MB file is refused with the cap's message, not a failed run | `[agent]` `@switchboard agent:coding in <resident repo>: screenshot https://example.com to shot.png, attach it, then `head -c 12000000 /dev/urandom > big.bin` and attach that too; report both tool results verbatim`, and the same in a non-resident thread |
| 18: the GitHub vet — `GET /repos/<owner>/<name>` with the run's credential as a bearer and the scope asked for; 200 binds, 404 refuses, every other status, a transport failure and an unmintable credential are `unreachable`; no credential → an anonymous request; a non-slug is refused before any request | `[unit]` `src/execution/githubRepoProbe.test.ts::githubRepoProbe::*` |
| 18: which vet a class gets, against the production resolver on a deployment with both a sandbox and a resident — `repo-resident`: one registry `GET /status` and no GitHub call; `repo-cold`: `GET /repos` on GitHub with the run's scope and never the registry, a 404 → `rejectedRepo`, silence → `unverifiedRepo`; `blank`: no vet and an empty context | `[unit]` `src/core/dispatch/resolve.test.ts::resolveTarget — the vet a machine class gets::*` |
| 18: a `repo-cold` run's refusals — GitHub's 404 refuses as `repo_not_visible` naming the repository and the installation, with or without residents, never onboarding; GitHub's silence refuses as `repo_unverified` naming GitHub; a visible repository meets the access gate like every class | `[unit]` `src/core/dispatch/authorize.test.ts::authorizeRepo — the repository gates, once the target has landed::a repo-cold agent's repository was vetted against GitHub::*` |
| 13: `resolveBuildStamp` — an injected commit + timestamp survive; a `-dirty` suffix is preserved; a missing/blank/non-string commit is `unknown` and no timestamp is invented; both are trimmed | `[unit]` `src/deploy/buildStamp.test.ts` |
| 13: `injectedBuildStamp` with no `--define` present returns `{commit:"unknown"}` rather than throwing on the undeclared identifiers | `[unit]` `src/deploy/buildStamp.test.ts` |
| 13: `buildStamp`/`defineArgs` — the stamp carries the commit and build time, marks a dirty tree, and emits defines whose values are JS string LITERALS; a value containing a quote is escaped, not injected as code; the identifiers match the reader's | `[unit]` `src/deploy/buildStamp.test.ts` |
| 13: `buildId` — the identity a stored artifact compares against is the commit PLUS the build time, so two builds of one dirty tree and a redeploy of the same clean commit are told apart, while sibling isolates of one deployed version agree; no timestamp → the commit alone | `[unit]` `src/deploy/buildStamp.test.ts` |
| 13: `spawnOutcome` — exit 0/1 pass through; a spawn error and a SIGNAL kill (which reports `status: null`) each become exit 1 with the reason NAMED, so an interrupted deploy is not read as a failed one | `[unit]` `src/deploy/buildStamp.test.ts` |
| 13: memory `/healthz` carries `build` beside `features`, `unknown` in an un-stamped test bundle | `[unit]` `deploy/cloudflare-memory/worker.test.ts` + `runs.test.ts` (workerd) |
| 13: the define reaches all three bundles — `wrangler deploy --dry-run --outdir` per Worker contains the HEAD sha and zero leftover `SWITCHBOARD_*` identifiers (esbuild folds `typeof … === "string"` to `true`), and the resident's dry run still binds `env.STATE_WORKER_URL` (the `--var` hazard avoided) | `[agent]` In each Worker directory run `wrangler deploy --dry-run --outdir <dir>`; grep the bundle for `git rev-parse HEAD` and for leftover `SWITCHBOARD_` identifiers; the resident's dry-run output must still list `STATE_WORKER_URL` under its bindings. |
| 13 live: after deploy each Worker's `/healthz` reports the deployed commit — resident and memory unauthenticated, sandbox with the bearer | `[agent]` After a deploy, `curl` each Worker's `/healthz` (the sandbox with its bearer) and compare `build.commit` to the deployed commit. |
| ≤280s commands succeed; >280s return exit 124 + timeout stderr | `[agent]` In Slack: `@switchboard agent:review run \`sleep 45 && echo OK\` then \`sleep 320 && echo NO\`, report raw results`. Expect `OK` then `exit 124: command timed out in the sandbox after 280s…`. |
| Long commands stream heartbeats (no `fetch failed`) | `[agent]` Covered by the same run — without heartbeats the 320s case surfaces as `fetch failed`. |
| Session recovery after container replacement | `[agent]` Run a command in a thread, `wrangler deploy` the sandbox worker (replaces containers), run a follow-up in the same thread — it must succeed (repo re-clones), not 500 `Session not found`. |
| Thread workspace reuse | `[agent]` `agent:coding create file /workspace/marker.txt`, then follow-up `cat marker.txt` — must print the content (same sandbox), unless the sandbox expired (then a legible re-clone story, not a crash). |
| 1, 2: the idle lifetime is five minutes in the Container class's own grammar | `[unit]` `src/execution/sandboxLifecycle.test.ts::the idle lifetime::is five minutes, in the Container class's own grammar` |
| 2, 4, 9: the sandbox Worker WIRES the seams (static guard; the Worker itself cannot run under vitest) — `sleepAfter = SANDBOX_SLEEP_AFTER`; a command is one supervised process started and collected inside the Durable Object (`createExtensionProcessSandbox(this).exec(`, `proc.output(`) with no session fence and no keepalive of its own; the `/exec` failure path classifies a recycle by type inside the Durable Object and by name after the RPC boundary, a full fleet, and a silent control port; a file route's refusal leaves with the Durable Object's `reason` token (the stat under a binary read on a full fleet included — the executor waits on the token, never the text); the Worker never `destroy()`s a container | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::the Durable Object's sleepAfter is the shared constant, and a command is one supervised process started and collected inside the Durable Object`, `::the /exec failure path names a mid-command recycle by type inside the Durable Object and by name after the RPC boundary, a full fleet, and a silent control port`, `::a file route's refusal carries the Durable Object's reason token`, `::the Worker destroys a container only through the idle guard, and answers the SDK's expiry with the guard's verdict` |
| 9, the typed reason: `ExecInfraError` names why at every construction in both remote executors — a literal from the closed list or a reason-of helper, read as syntax by the TypeScript parser so a parenthesis in a message or a nested call cannot cut the scan short — the waitable set is exactly the transport lost, the deadline passed, the empty failure shape and the Worker unavailable, the executors' real wordings (built by the same helpers they throw with: the one `request failed (…)` sentence for both Workers, the sandbox executor's no-answer and empty-failure shapes) carry those reasons, a request the run's stop aborted is `aborted` by the signal before the error's name, the resident client types an answer by the fields on it (a 5xx carrying a state waits exactly when `isWakeable` says the resident is coming back — the wake path's own decision, so onboarding refuses on both routes — `reason: "unregistered"` refuses, a 5xx with a body and no state is answered, a bare 5xx waits, a body on another status is the words, a bare 4xx is refused), its strike after the wake wait carries the last engine view's decision (a budget spent on a still-restoring resident is unavailable, a definite view refused; a re-attach that fails as infra inside the wait is typed by that same view, the run's own stop — whose signal rides into the re-attach — the call's own `aborted` error), the streamed routes' status is read from the body by one rule, a rejected data-plane call is answered as the Durable Object would by route (the reset word; the replacement's bare 409 on `/exec`, its word on `/read` and `/write`; else the typed 500, its transient the SDK's own predicate and the two remainder sentences) and typed by the client so, a transient attach-failed is unavailable and keeps the wake wait going, the stop rides into the wait's probes, and the resident names the two refusals on its answers as fields and every unnamed-throw 500 through one shape | `[unit]` `src/execution/execInfraReason.test.ts::*`, `src/execution/residentWake.test.ts::wakeDecision: the engine view says whether the container is coming back::isWakeable is the one decision on a state…`, `src/execution/resident.test.ts::ResidentExecutor waits for the wake (item 65: a container rollout is a minute, not a dead sandbox)::the wait respects the command's budget…`, `::a container exit with a definite non-recovering engine view is infra at once, on every call`, `::an unreachable /status is a strike too…`, `::a re-attach that fails as infra inside the wait is a strike typed by the last engine view…`, `::a hard stop while the re-attach inside the wait is in flight aborts that request too…`, `::a hard stop while a /status probe inside the wait is in flight aborts that probe too…`, `::a hard stop during the pause ends the wait at once with the stop's one typed shape…`, `::a re-attach inside the wait whose 500 the Worker typed transient…`, `::a /read whose Durable Object stub rejected with the SDK's stopped-container sentence…`, `::a pending /exec whose Durable Object stub rejected with the SDK's own moved-runtime sentence…`, `::attach: a 500 the Worker typed transient enters the wake wait…`, `::attach: the wait is bounded by the wake budget…`, `::an unreachable /status right after a transient refusal is the same platform blip, waited through…`, `::probes that stay unreachable past the budget after a transient refusal end in the resident unavailable…`, `::the run's stop rides into the first attach request itself…`, `::one wake budget per operation…`, `::the re-attach inside a first attach's wait runs under the attach's own timeout, which the wake ceiling never caps…`, `::the rolling wake's re-attach — the one recovery that always recreates the worktree from the mirror — runs under the attach's own timeout…`, `::one bound for every attach request an operation opens, whatever the command's budget…`, `::a recovery attach after a worktree eviction has the attach default as its floor…`, `::the wake clock starts at the first wait, never at the first call…`, `::after a transient refusal only an unanswered probe is the same blip…`, `src/core/dispatcher.test.ts::*::a hard stop during the first attach's wait ends the request \`stopped\`…`, `::a follow-up queued during the first attach's wait is dropped with the ⛔ note when the stop ends the attach…`, `::a provisioning failure beside a pending stop is the failure, not the stop…`, `src/execution/resident.test.ts::ResidentExecutor.moveTo::carries the run's stop into the move's re-attach…`, `src/core/dispatcher.test.ts::review post-step::head moved during the run (item 12)::the worktree move carries the run's hard stop…`, `src/execution/factory.test.ts::makeExecutor resident selection::warm probe then a TRANSIENT attach failure…`, `::a transient blip the first probe already shows cleared re-attaches at once…`, `::a hard stop during the first attach's wake wait ends it at once…`, `::a /status probe the Worker typed transient is waited through, not a cold fallback…`, `::a probe still transient past the first attach's budget falls cold with the wait named…`, `::a hard stop during the probe's wait ends it at once with the stop's typed error…`, `::probe transport failure → named fallback + negative cache…`, `::a transport failure met on a re-probe inside the blip wait ends the wait at once AND opens the outage breaker…`, `::an attach that fails after the probe waited through a blip names that wait too…`, `::the retry's budget is what the first attach's WAIT left, never what its own latency took…`, `::on a resume (ctx.reattach): re-attach, never re-provision::a resumed run's /status probe the Worker typed transient is waited through, not a re-attach refusal…`, `::a resumed run's probe still transient past the first attach's budget is the re-attach refusal, naming the wait…`, `::a hard stop during a resumed run's probe wait ends it with the stop's typed error…`, `src/core/dispatch/provision.test.ts::reattachWorkspace — the run's recorded workspace re-attached without the gate::the run's own stop ending the re-attach's wait is \`stopped\`…`, `src/core/dispatch/relaunch.test.ts::prepareRelaunch — the relaunch decided and prepared::the run's hard stop rides into the re-attach…`, `src/core/dispatcher.test.ts::run ledger write-through (docs/reference/specs/run-history.md item 35)::a hard stop while the relaunch re-attaches the run's worktree ends the run stopped…`, `::a resumed run hard-stopped while its re-attach waits through a transient refusal ends \`stopped\` and closes its row \`stopped_hard\`…`, `src/execution/tracingExecutor.test.ts::*::wraps release and moveTo only when the inner executor has them…`, `src/execution/resident.test.ts::ResidentExecutor.probeStatus::carries the Worker's \`transient\` typing on a 5xx…`, `src/execution/factory.test.ts::makeExecutor resident selection::a failure beside a pending stop is that failure, not the stop…`, `::the needs-ref retry by default draws on the first attach's one budget and the card names the total wait…`, `::one budget across the needs-ref retry…`, `::on a resume (ctx.reattach): re-attach, never re-provision::a resumed run's re-attach waits through a transient refusal under the first attach's budget…`, `::a hard stop during a resumed run's re-attach wait ends it with the stop's typed error…`, `::a re-attach refusal beside a pending stop is the refusal…`, `src/execution/resident.test.ts::ResidentOperations.run::a rejected op the Worker typed as the platform's transient…`, `deploy/cloudflare-resident/threadErr.test.ts::*`, `::a pending /exec whose Durable Object stub REJECTED…`, `::a pending /exec whose Durable Object stub rejected with the DO's own code-update reset…`, `::a refusal streamed by /exec over HTTP 200 is typed by the status and the lifecycle pair IN the document…`, `deploy/cloudflare-resident/notServiceable.test.ts::*` |
| 9: the recycled-mid-command wording fires only for a recycle-shaped failure more than a minute into the attempt, or at any time for a typed recycle error; an early failure of any text, and a late failure of any other text, is returned unchanged; both the 0.3.x and the 0.12.x recycle texts are recognized | `[unit]` `src/execution/sandboxLifecycle.test.ts::recycledMidCommandMessage::leaves an early failure with any message alone`, `::leaves a recycle-shaped failure alone inside the first minute — that is a real startup failure, not a recycle`, `::names the recycle when a recycle-shaped failure arrives after more than a minute, and tells the model to look at /workspace before assuming either outcome`, `::never rewords an unrelated failure on timing alone — a late transport error keeps its own text`, `::recognizes the 0.12.x recycle texts — a terminated session shell and a container stopped under a pending call`, `::a typed recycle error (certain) is named at any elapsed time — the SDK is stating the runtime went away`; `src/execution/sandboxLifecycle.test.ts::isRecycleError::takes the SDK's typed errors by NAME across the 0.12 and 0.13 lines (the RPC boundary drops the prototype)`, `::falls back to the recycle-shaped texts, and rejects everything else` |
| 5: the credential rides in the exec `env` option and never in the command text — the Worker source carries `env: envVars` and no base64 export | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::the credential goes through the exec env option, never through the command text`; live: after the sandbox Worker deploys, a cold run's `Command executed` lines in the Worker logs carry no `GH_TOKEN` |
| 5: the executor sends the resolved env as `body.env` on every route (`/exec`, `/read`, `/write`) and NO `x-env-*` header — the request carries only `authorization`, `content-type`, `x-thread-key`; an empty map is still `env: {}`; the fleet-busy re-send carries the same body, env included | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor env transport::sends the map in the body and NO x-env-* header on any route`, `::an empty env is still the one body shape: \`env: {}\`, so the Worker reads one field on every route`; `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor fleet-busy wait::re-sends the SAME route, body and headers after 10 s then 20 s and returns the eventual result` |
| 5: `envFromRequest` — reads `body.env` alone (request headers are never read); body keys are upper-cased; names that are not shell identifiers and non-string values are dropped; an `env` that is not a plain object is ignored; a non-object body yields `{}` without throwing; no other body field is read as env | `[unit]` `src/execution/sandboxEnv.test.ts::envFromRequest::reads \`env\` from the body — an object of string values — and returns it as the env map`, `::upper-cases body keys, so \`gh_token\` and \`GH_TOKEN\` are one variable`, `::drops names that are not shell identifiers after upper-casing`, `::drops non-string values — numbers, booleans, null, objects, arrays — and keeps the string ones`, `::an \`env\` that is not a plain object (array, string, null, number, boolean) yields an empty map`, `::a body that is not an object yields an empty map — never a throw`, `::reads only \`env\` — the body's other fields are never env, and there is no header channel` |
| 5: the sandbox Worker WIRES the body path (static) — `envVars` comes from `envFromRequest(` and the Worker source names no `x-env-` channel; the executor resolves the map once (`const envs = await this.opts.resolveEnvs()`), passes it as the body's `env` — under it a caller's own variables when an exec carries some ([harness-pi.md](harness-pi.md) item 4) — in the body, and its source names no `x-env-` either | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::the Worker reads the env map through envFromRequest and names no x-env header channel`, `::the executor sends the env in the body alone — no x-env-* header` |
| 5 live: after the sandbox Worker AND the bot deploy, a cold run's command still sees `GH_TOKEN` (`gh auth status` succeeds) and the `POST /exec` invocation event in Workers Logs lists `authorization`, `content-type`, `x-thread-key` and nothing beginning `x-env-` (the body, which now carries the credential, is not recorded) | `[agent]` After both deploy: run a cold command that includes `gh auth status`, then read the `POST /exec` invocation event in Workers Logs filtered on the thread key and list its request headers. |
| 3: `thrownText` — the SDK's message verbatim (trimmed) when present; a message-less error names the error name and code and says a rollout may be in progress; whitespace-only counts as empty; no name at all still yields a non-empty text | `[unit]` `src/execution/sandboxErrors.test.ts::thrownText::returns the SDK's message verbatim (trimmed) when it has one`, `::a message-less error names the error name and code and says a rollout may be in progress`, `::a whitespace-only message counts as empty`, `::no name at all still yields a non-empty text that says so` |
| 9: the destroy-time disconnect text (`The sandbox was destroyed while the operation was pending.`) is recycle-shaped — reworded after a minute, kept inside it, and recognized by `isRecycleError` | `[unit]` `src/execution/sandboxLifecycle.test.ts::recycledMidCommandMessage::recognizes the destroy-time disconnect text as a recycle`; `src/execution/sandboxLifecycle.test.ts::isRecycleError::falls back to the recycle-shaped texts, and rejects everything else` |
| 9: the `runtime-unreachable` text starts with the token and names the container id, the SDK pin, the platform's running flag (running / stopped / not reported) and the cause (or that there was none); the typed error carries the name and is recognized by name or by token after the RPC boundary, and is neither fleet-busy nor a recycle nor a stale session | `[unit]` `src/execution/sandboxErrors.test.ts::the runtime-unreachable message and error::*` |
| 9: the Worker's answer shapes for it — `/read`/`/write` `{error, reason: "runtime-unreachable"}`, `/exec` the dual form (`error` + exit 127 + `stderr`) plus the reason | `[unit]` `src/execution/sandboxErrors.test.ts::the runtime-unreachable answer shapes the Worker sends::*` |
| 9: the executor turns the in-body `/exec` answer into `ExecInfraError` after exactly one send, the text naming the token and the container; a `/read` 503 is re-sent on the transport retry and the eventual answer returned; a runtime that never comes back is `ExecInfraError` naming the token once the transport retries are spent | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor runtime-unreachable::*` |
| 9 live: a cold thread whose server is killed under a command gets a card naming what happened, never the bare `HTTP error! status: 500` — the recycle wording for the command the kill interrupted (the control connection's peer closed: a typed replacement), and `runtime-unreachable` with the container id if the port then stays silent for 30 s | `[agent]` After the Worker and image deploy, in a NON-resident thread, run `kill -9 $(ps -eo pid,comm \| awk '$2=="sandbox"{print $1}')` — the tool result reads `sandbox recycled mid-command after Ns — the container's runtime was replaced or restarted under the command …`; the next command (`cat /workspace/marker` from item 21's row) runs on the restarted server. A `runtime-unreachable: … (container <64 hex>, sandbox SDK 0.13.0-next.751.1; …)` result means the supervisor gave up or the port is wedged; its container id is the `$containers.container.id` of that thread's rows in the `containers` dataset. |
| 3/9: the executor throws `ExecInfraError` after exactly one fetch on a PRESENT-but-empty in-body `error`; a body without an `error` key and exit 127 with empty output is the normal `exit 127:` result; exit 0 with no output is `(no output)` | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor in-body empty error::an empty-string error is ExecInfraError after exactly one fetch — the Worker's failure shape with its text missing`, `::a body WITHOUT an error key and exit 127 with empty output is the normal \`exit 127:\` result — \`foo 2>/dev/null\` is legitimate`, `::exit 0 with no error key and no output renders (no output)` |
| 3/6: the sandbox Worker WIRES it (static; the Worker cannot run under vitest) — both catches call `thrownText(` and the `.message ?? String(err)` fallthrough is gone; no `this.destroy(` anywhere in the Worker; `wrangler.jsonc` sets `rollout_step_percentage` to 100 | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::every failure text goes through thrownText — the empty-string fallthrough is gone`, `::the Worker destroys a container only through the idle guard, and answers the SDK's expiry with the guard's verdict`, `::wrangler.jsonc rolls the sandbox image out in one wave (rollout_step_percentage 100)` |
| 6 live: across the next sandbox Worker+image rollout, the Worker logs carry no empty-tailed `sandbox.exec error … —` lines; a thread that starts during the rollout either runs normally or sees the named `sandbox exec failed with no message from the SDK (…rollout…)` text, never a silent `exit 127`; any `sandbox.version-skew container=… sdk=…` line is the skew being seen | `[agent]` `wrangler tail <sandbox worker>` through the deploy, start one cold review inside the first two minutes, read its run page |
| 2 live: a command longer than `sleepAfter` completes in a per-thread sandbox and the workspace survives it | `[agent]` After the sandbox Worker deploys, in a NON-resident thread: `run \`mkdir -p /workspace/probe && sleep 330 && echo OK\` with timeoutMs 600000`, then `ls /workspace` → `OK` then `probe`. Re-run after the 0.13 port, since the SDK's busy poll now holds the deadline where the Worker's own keepalive did: the failure signature is a recycle at 300 s (`sandbox recycled mid-command after 300s`) and `probe` gone. |
| Sandbox image Node satisfies the repo's `engines` pin (≥22), so fallback reviews load the full suite | `[agent]` After a sandbox-worker deploy, force the per-thread path (any repo not onboarded as a resident, or a pruned ref) and run: `@switchboard agent:review run \`node --version\` in <owner/repo>, then \`npx vitest run --reporter=basic 2>&1 \| tail -5\``. Expect `v22.x` and 0 unloadable suites (pre-fix: 15 suites failed to load with `webidl.util.markAsUncloneable is not a function`). Build-time backstop: the Dockerfile asserts `node --version` is v22 or the image build fails. |
| Shell quoting through the timeout wrapper | `[unit]` `src/execution/shellQuote.test.ts` — tests the exact `shellQuote` module the worker ships (extracted to `deploy/cloudflare-sandbox/shellQuote.ts`); the `timeout`-binary cases run on CI's Linux and skip on macOS. |
| GH App token minting & caching | `[unit]` `src/execution/githubApp.test.ts` — mocked fetch: JWT shape, cache hit, mint-failure surfacing, GH_TOKEN/null fallbacks. |
| 5: a cached token is reused only while it outlives the longest single command — the margin is ≥ `BASH_TIMEOUT_MAX_MS` + 5 min, a token inside it re-mints, one outside it is reused | `[unit]` `src/execution/githubApp.test.ts::the reuse margin covers the longest single command (BASH_TIMEOUT_MAX_MS) plus slack`, `::re-mints when the cached token has less than the reuse margin left`, `::reuses the cached token while it still has at least the reuse margin left` |
| 5: the sandbox credential is resolved per command, not captured at executor construction — each command carries the token current at its own start | `[unit]` `src/execution/cloudflareSandbox.test.ts::resolves the sandbox env on EVERY call, so each command carries the credential current at its start`, `::resolves nothing at construction — building the executor mints no credential`; `src/execution/e2b.test.ts::each command carries the envs resolved at its start`; `src/execution/factory.test.ts::the sandbox credential is resolved per command, not captured at executor construction` |
| 12: the clamp rule — a finite number truncated and clamped into [1 s, 20 min], anything else the 5-min default — and every executor honoring `timeoutMs` (the local executor kills at it and names the knob) | `[unit]` `src/execution/bashTimeout.test.ts::clampBashTimeout::*`, `src/execution/executor.test.ts::LocalExecutor per-call timeout::*` |
| 5: least-privilege token by identity — the read scope mints exactly `contents`, `pull_requests`, `issues`, `actions`, `checks`, `metadata`, every value `read`; the write scope requests no restriction; the scopes cache separately; a `read` identity gets the read token, a `write` identity the write one, and a `none` identity mints nothing; the factory reads the profile's class and identity, never the preset's own fields | `[unit]` `src/execution/githubApp.test.ts::mints a READ-scoped token whose permissions are exactly contents, pull_requests, issues, actions, checks and metadata — all read`, `::every permission the read scope requests is read — the request never carries write or admin`, `::the default (write) scope requests NO permissions restriction — the full grant`, `::read and write tokens cache in separate slots (one mint per scope)`; `src/execution/factory.test.ts::a readonly agent's sandbox gets a READ-scoped token; a full agent gets WRITE`, `src/execution/factory.test.ts::makeExecutor machine classes::provisions from the profile, never from the preset: the class and identity the context's profile names win over the agent's own fields`; `src/execution/githubRepoProbe.test.ts::githubRepoProbe::a run whose identity mints nothing (no scope) vets anonymously too: the credential resolver is never asked`; `src/core/dispatch/resolve.test.ts::resolveTarget — the vet a machine class gets::repo-cold on a \`read\` identity vets with a read token; on \`none\` it vets anonymously and mints nothing` |
| 5: a review run's sandbox reads CI on the read token — `gh run list -R <owner>/<repo> -L 1` and `gh pr checks <n>` succeed, `gh run rerun <id>` and `gh pr comment` are refused | `[agent]` After the bot deploy, post `agent:review` on an open PR of a repository the installation covers and, from the run page's tool log, confirm the read commands answered 200 and a write returned `403 Resource not accessible by integration`; also runnable from a dev checkout by minting `resolveGithubToken("read")` and driving `gh` with it as `GH_TOKEN`. |
| App installation covers the target repo | `[agent]` `agent:review run \`gh repo view <owner/repo> --json name\`` for each repo agents work on — must return JSON, not 404. Failure signature: "GitHub credentials don't have access" + 404. |
| 7: agents on machine class `none` provision nothing, under every execution type, and a tool call reaching the null executor names the class | `[unit]` `src/execution/factory.test.ts::an agent declaring no repo gets a null executor with cloudflare configured (no sandbox call, no token needed)`, `::an agent declaring no repo gets a null executor with e2b configured (no API key needed)`, `::an agent declaring no repo creates no workspace directory in local mode`; `src/agents/registry.test.ts::machine class declarations: coding and review run on repo-resident; general and research on none` |
| 7: agents on `repo-resident` get the configured backend | `[unit]` `src/execution/factory.test.ts::a repo-requiring agent gets a LocalExecutor with a per-thread workspace (local)`, `::a repo-requiring agent gets the Cloudflare backend when configured`, `::a repo-requiring agent with e2b configured but no API key still fails legibly`; end-to-end: `src/core/dispatcher.test.ts::a coding ask still selects the configured remote backend` |
| Executor selection receives the resolved agent in its context | `[unit]` `src/core/dispatcher.test.ts::passes the resolved agent to executor selection` |
| Resident selection: warm → resident; anything else → named per-thread fallback; outage circuit breaker | `[unit]` `src/execution/factory.test.ts::makeExecutor resident selection` (all six scenarios); details in [resident-repos.md](resident-repos.md) |
| Infra failures throw `ExecInfraError` (distinct from a nonzero exit), so a caller tells a dead sandbox from a command the agent should keep handling | `[unit]` `src/execution/resident.test.ts::ResidentExecutor infra classification::*`, `src/execution/cloudflareSandbox.test.ts::*` |
| A resident `runtime-replaced` on `/exec` (one deploy) is NOT infra — the typed restart the pi harness keys on | `[unit]` `src/execution/resident.test.ts::ResidentExecutor infra classification::a runtime-replaced on /exec (one deploy) is the typed restart, never infra` |
| 14: `isFleetBusy` recognizes the SDK 0.3.x `Failed to create session: 503`, the platform's "no container instance" wording, and `CONTAINER_UNAVAILABLE`; a stale session, "Command execution failed", a file-op failure, a 500, and arbitrary text are NOT fleet-busy | `[unit]` `src/execution/sandboxErrors.test.ts::isFleetBusy::recognizes the SDK 0.3.x client's unparsed 503 from createSession`, `::recognizes the platform's raw no-instance message`, `::recognizes the newer SDKs' CONTAINER_UNAVAILABLE code`, `::is NOT a stale session, a wedged sandbox, or arbitrary text` |
| 14: `isFleetBusyError` takes the 0.12.x `ContainerUnavailableError` by name or code and falls back to the recognized texts; a recycle, a stale session, or an unrelated failure is not fleet-busy; `thrownShape` reads an Error, a plain object, and a string alike | `[unit]` `src/execution/sandboxErrors.test.ts::isFleetBusyError / thrownShape::takes the 0.12.x typed error by name, whatever its text`, `::takes the error code when a client surfaces it, and falls back to the recognized texts`, `::is NOT a recycle, a stale session, or an unrelated failure`, `::thrownShape reads an Error, a plain object, and a string the same way` |
| 10: `check:sandbox-pair` gates on COMMITTED files only — the package.json pin must equal the Dockerfile tag and the lockfile's resolution of that pin (nested workspace entry first, else the root's hoisted one); an INSTALLED copy that disagrees with the lockfile (an install drifted ahead, as the resident's did) is a one-line advisory naming the workspace and both versions with the `npm ci` remedy, never a failure; a fresh clone with nothing installed says nothing | `[unit]` `src/sandboxPairCheck.test.ts::check-sandbox-pair lockfileSdkVersion::prefers the workspace's nested lockfile entry over the root's hoisted one`, `::is null when the lockfile has no entry at all`; `src/sandboxPairCheck.test.ts::check-sandbox-pair pairMismatches::fails a lockfile that resolves the pin to a different version, or has no entry`; `src/sandboxPairCheck.test.ts::check-sandbox-pair installAdvisories::says nothing when the installed SDK equals the lockfile, or nothing is installed (a fresh clone)`, `::emits one line naming the workspace, both versions, and npm ci when the install drifts`, `::fixture: a lockfile pinning one version with another installed is an advisory, not a failure`; `src/sandboxPairCheck.test.ts::the repository's sandbox-image Workers::pin @cloudflare/sandbox to exactly the image tag they build FROM, and the lockfile agrees` |
| 10: every image names Node by one exact `node:<major>.<minor>.<patch>-slim` tag, the three tags are equal and their major is `.nvmrc`'s; each execution image takes it from a `node` stage pinned to `linux/amd64`, removes the base's npm tree, copies the binary, npm's tree and the headers at the base's paths before any global install, and asserts the exact version at build time | `[unit]` `src/deploy/imageNode.test.ts`; `[agent]` `npm run check:image` (CI's `image / build …` legs) green at the PR head, and `node --version` inside each built image prints the tag's version |
| 10: both execution images install one apt set (`python3 make g++ ffmpeg fonts-liberation fonts-noto-color-emoji`, no recommends) in the layer that installs `playwright` globally at one exact version, set `PLAYWRIGHT_BROWSERS_PATH=/opt/ms-playwright` before that install and `NODE_PATH` to the global tree, install chromium's headless shell with `--with-deps --only-shell`, open the browser tree `a+rX`, prove the compilers, the ffmpeg encode → decode round trip, the CLI at the pin and a real headless screenshot at build time, and drop the apt lists, apt's `.deb` archive, npm's cache and the proof artefacts in the same layer; the two images name the same apt set and the same pin; the resident runs the screenshot proof again as `worker1` after the user pool | `[unit]` `src/deploy/imageToolchain.test.ts::deploy/cloudflare-sandbox/Dockerfile::*` (11), `src/deploy/imageToolchain.test.ts::deploy/cloudflare-resident/Dockerfile::*` (11), `src/deploy/imageToolchain.test.ts::the two execution images carry one toolchain::*` (2), `src/deploy/imageToolchain.test.ts::the resident image::runs the screenshot proof a second time as worker1, after the user pool exists — the build proves an unprivileged thread user can drive the browser` |
| 10: `playwright` is one exact global install per execution image, read by the pin parser like pnpm's, and the same version in both | `[unit]` `src/deploy/imagePins.test.ts::the execution images' playwright::is one exact global install per image`, `::names the same version in both images` |
| 10: `bun` is one exact global install per execution image, read by the pin parser like pnpm's, the same version in both, the base's binary removed first and the pin proven on PATH at build | `[unit]` `src/deploy/imagePins.test.ts::the execution images' bun::*` |
| 10: pi is one exact global install in every image (the two execution images and the bot's), proven at build, and the same version in the three | `[unit]` `src/deploy/imagePiHarness.test.ts::the three images carry one pi::*`, `src/deploy/imagePiHarness.test.ts::Dockerfile::*` |
| 10: OpenCode is one exact global install in every image, its install script allowed by name and scoped to it, the version text proven at build, the same version in the three, and the resident proves it again as `worker1` | `[unit]` `src/deploy/imageOpenCodeHarness.test.ts::the three images carry one OpenCode::*`, `src/deploy/imageOpenCodeHarness.test.ts::Dockerfile::*`, `src/deploy/imageOpenCodeHarness.test.ts::the resident image proves OpenCode as a thread user::*` |
| 10: every prompt with a workspace (coding, review, explore, and the resident variants) names Node 24, python3 and g++, ffmpeg, and the headless Chromium through `playwright screenshot` / `playwright pdf` / `require('playwright')` with video; the cold prompts offer Docker and the resident prompts say there is none; the prompts without a workspace name none of it | `[unit]` `src/agents/registry.test.ts::the workspace prompts name the image toolchain::*` (3) |
| 10 live: after the next release, in a cold sandbox thread `agent:explore run: playwright screenshot https://example.com /tmp/a.png && ffmpeg -f lavfi -i testsrc=duration=1:size=320x240:rate=10 /tmp/a.mp4 && ls -l /tmp/a.png /tmp/a.mp4` lists two non-empty files, and the same command in a coding thread on an onboarded repository (a resident, running as `workerN`) does too | `[agent]` Run the command above in a NON-resident thread and read the run page: both files listed with a non-zero size, no `command not found`, no browser launch error. Then the same command through `agent:coding` on an onboarded repository, whose card says the resident attached. |
| 14: the Worker's answer shapes — `reason: "fleet-busy"`, an `error` keeping the SDK's message as the cause, and the `/exec` dual form (`error` + exit 127 + `stderr`) | `[unit]` `src/execution/sandboxErrors.test.ts::the fleet-busy answer shapes the Worker sends::names the reason and the explanation, and keeps the SDK's own message as the cause`, `::the /exec shape carries the dual in-body failure form (error + exit 127 + stderr) like every other exec failure`; the Worker wiring (`streamExec` catch → `fleetBusyExecAnswer`, outer catch → 503 `fleetBusyAnswer`) is `[agent]`: fill the fleet (max_instances threads busy) and run one more cold command — the run page must show `⏳ Sandbox fleet busy` and the run must continue, not `Sandbox exec transport failed 2 times in a row` |
| 14: the executor re-sends the IDENTICAL request (route, body, headers) after 10 s then 20 s and returns the eventual result; a `/read` answered 503 `fleet-busy` waits the same way | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor fleet-busy wait::re-sends the SAME route, body and headers after 10 s then 20 s and returns the eventual result`, `::a /read answered HTTP 503 with reason fleet-busy is the same wait (the default 5-min budget applies)` |
| 14: the wait is bounded by the command's own budget and by the 5-min cap, and then throws `ExecCapacityError` — not `ExecInfraError` — naming the seconds waited | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor fleet-busy wait::gives up once the total wait reaches the command's own budget and throws ExecCapacityError, not ExecInfraError`, `::never waits longer than FLEET_BUSY_WAIT_MAX_MS (5 min) even for a 20-minute command`; constants: `src/execution/sandboxErrors.test.ts::the executor's bounded wait::*` (3) |
| 14: a hard stop during the wait ends it at once with no further send | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor fleet-busy wait::a hard stop during the wait ends it at once with no further send` |
| 14: every OTHER in-body error — including an OLD Worker's bare `Failed to create session: 503` — is still `ExecInfraError` after exactly ONE send (nothing else is replayed) | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor fleet-busy wait::every OTHER in-body error is still ExecInfraError after exactly one send — nothing else is replayed`, `::an OLD Worker's bare Failed-to-create-session 503 (no reason) still reads as infra — one send` |
| 14: `ExecCapacityError` is its own class, never an `ExecInfraError` — a caller that reads infra failures as a dead sandbox never reads a full fleet as one | `[unit]` `src/execution/cloudflareSandbox.test.ts::*` |
| 14: `fleet_busy` notes survive the saved-stream parser and count as a medium `infra_failure` finding | `[unit]` `src/core/runEventLines.test.ts::accepts every declared run_note kind, fleet_busy included (docs/reference/specs/execution.md item 14)`; `src/core/runFriction.test.ts::infra_failure: a fleet_busy note is an infra finding too (capacity is friction), at medium severity — the run went on` |
| 14 live: with the fleet full, one more cold run waits and then proceeds (a slot frees) or ends with `⏳ Sandbox fleet busy — … after waiting 300s` and a normal write-up — never the dead-sandbox abort; `max_instances` 25 is live on the deployed Worker | `[agent]` `wrangler deployments status` / the containers dashboard shows 25; force the condition by running `max_instances`+1 concurrent cold reviews, read the last run's page |
| 11: the shared clamp rule — a 25-min ask runs at 20 min, 0/negative at the 1s floor, NaN/absent/non-numeric at the 5-min default | `[unit]` `src/execution/bashTimeout.test.ts::clampBashTimeout` (every branch) |
| 11: pi's bash is judged at the harness's gate by its explicit `timeout` alone — one reaching past the loop's end is refused before the command runs, one inside it and a call naming none run | `[unit]` `src/core/harness/pi/toolRules.test.ts::judgeToolCall — bash: an explicit timeout against the loop's end::*` |
| 11: the policy module names default (300000), floor (1000) and max (1200000) once, and the note names the limit that fired and the knob that raises it | `[unit]` `src/execution/bashTimeout.test.ts::bash timeout constants::*`, `src/execution/bashTimeout.test.ts::bashTimeoutNote::*` |
| 11: LocalExecutor honors the per-call budget (execFile `timeout`, maxBuffer kept) and a deadline kill is `exit 124` naming the limit + the `timeoutMs` knob — never a generic abort, never the 124 wording for an ordinary nonzero exit | `[unit]` `src/execution/executor.test.ts::LocalExecutor per-call timeout` |
| 11: ResidentExecutor sends `timeoutMs` in the `/exec` body only when asked (older-resident body compat), clamped, kept across the one re-attach retry | `[unit]` `src/execution/resident.test.ts::ResidentExecutor per-call timeout` |
| 11: E2BExecutor maps the budget onto `commands.run({timeoutMs})` and renders the SDK's TimeoutError as `exit 124` naming the limit | `[unit]` `src/execution/e2b.test.ts` |
| 11: CloudflareSandboxExecutor sends `timeoutMs` in the `/exec` body only when asked, clamped | `[unit]` `src/execution/cloudflareSandbox.test.ts` |
| 11: every send to the sandbox Worker has a bot-side deadline of budget + 30 s covering the headers AND the streamed body — a fetch that never answers, and a body that heartbeats but never completes (a gone container under a still-heartbeating Worker), are both abandoned at budget + margin with `ExecInfraError` naming both numbers; no `timeoutMs` → 330 s; a 20-min (or a clamped 25-min) ask → 1230 s; `/read` and `/write` wait the 5-min file-op budget + margin | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor per-send deadline …::a fetch that never answers is abandoned at budget + margin…`, `::a body that heartbeats but never completes…`, `::no timeoutMs → the 5-minute default budget…`, `::a 20-minute timeoutMs → 20 min + 30 s…`, `::/read and /write wait the file-op budget…` |
| 11: the ResidentExecutor bounds the body read too — a heartbeating resident body that never completes is an `ExecInfraError` (`resident worker /exec request failed`) at budget + margin, which fail-fast counts, not a raw `TimeoutError` | `[unit]` `src/execution/resident.test.ts::ResidentExecutor per-send deadline …::a heartbeating body that never completes is an ExecInfraError at the command budget + margin, not a raw TimeoutError or a hang` |
| 11: the hard stop still aborts the send at once (reported as the stop, not as a silent Worker); an answer inside the deadline is unaffected and nothing fires after it; the deadline is per SEND — a fleet-busy wait before the send does not eat into it | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor per-send deadline …::the hard-stop signal aborts at once…`, `::an answer inside the deadline is unaffected…`, `::the deadline is per SEND…` |
| 11: `execDeadline` fires exactly at its timeout under fake timers (a plain timer, not `AbortSignal.timeout`) with a `TimeoutError` reason, and a joined hard stop aborts it at once with the stop's reason, the timer never re-firing | `[unit]` `src/execution/executor.test.ts::execDeadline::*` |
| 11 live: a cold (sandbox) run whose container is destroyed mid-command — `wrangler containers` stop/delete the thread's instance while a long `sleep` runs — gets `sandbox worker /exec gave no answer within …` as an `ExecInfraError` at budget + 30 s instead of hanging; the card reads `running bash (Ns)` meanwhile, and the run ends through fail-fast or the finale, never by an operator stop | `[agent]` human-gated. |
| 11: resident Worker clamps the body's `timeoutMs` server-side with the shared bounds (never trusts the client) and its exit-124 stderr names the limit + knob; `/op` keeps the flat 5-min budget | `[agent]` (Worker wiring; the clamp itself is the shared unit-tested `clampBashTimeout`) On a warm resident: `run \`sleep 330 && echo LONG\` with timeoutMs 360000` → `LONG`; the same command without `timeoutMs` → `exit 124` at 300s with a stderr line naming `timeoutMs` and `1200000`. |
| 11: a >5-min command completes end-to-end through a live resident (HTTP wait = budget + 30s margin, heartbeat keeps hops alive) | `[agent]` The 360s check above must return output, not a bot-side `resident worker /exec request failed` transport error. |
| 11: sandbox Worker sizes coreutils `timeout` from the body's `timeoutMs` (clamped); Dockerfile backstop (1240s) stays above the ceiling — needs a container rebuild to take effect | `[agent]` In a non-resident thread: `run \`sleep 300 && echo OK\` with timeoutMs 360000` → `OK`; without it → `exit 124` at 280s. |
| 15: exec/readFile/writeFile are `exec.*` spans under the tool span with backend and budget, never command/path/output; release/moveTo/readBytes (`exec.read_bytes`) wrap only when present; a throw ends `error` and propagates | `[unit]` `src/execution/tracingExecutor.test.ts::TracingExecutor::*` |
| 15: the factory names the backend on every selection — `local` for the null and local executors, `sandbox` for the per-thread Cloudflare sandbox, `resident` for a warm resident | `[unit]` `src/execution/factory.test.ts::makeExecutor per-agent provisioning::an agent declaring no repo gets a null executor with cloudflare configured (no sandbox call, no token needed)`, `::a repo-requiring agent gets a LocalExecutor with a per-thread workspace (local)`, `::a repo-requiring agent gets the Cloudflare backend when configured`, `src/execution/factory.test.ts::makeExecutor resident selection::warm probe → ResidentExecutor, attached on open, with the resident discriminant set` |
| 16: the sandbox template's container is `standard-4` — 4 vCPU / 12 GiB / 20 GB, the largest predefined type, rendered against the example profile and read back as JSON | `[unit]` `src/deploy/wranglerTemplate.test.ts::the sandbox Worker's container::the cold per-thread sandbox runs on standard-4 …` |
| 16: both execution images set `NODE_OPTIONS=--max-old-space-size` explicitly, to the same 8,192 MiB; the cap covers this repository's own typecheck with room | `[unit]` `src/deploy/imageNode.test.ts::the execution images' Node heap::*` (3); `[agent]` at a checkout of this repository, `NODE_OPTIONS=--max-old-space-size=1536 npx tsc --noEmit` exits 134 with "JavaScript heap out of memory" and the same command exits 0 at `3072` and at `8192` |
| 16 live: after the sandbox Worker deploys, a cold run's `nproc; free -m; df -h /` prints 4 / ~12 GiB (`Mem:` total ≈ 12 000 MiB) / ~20 GB (`/` size ≈ 19–20 GB) | `[agent]` In a NON-resident thread: `@switchboard agent:review run `nproc; free -m; df -h /` in <owner/repo>, report raw output`. The previous standard-3 read 2 / 8189 MiB / 15 GB. Then the shape that motivated the size: the large monorepo's root typecheck in a cold thread completes instead of dying on memory. |
| 17: the sandbox Dockerfile installs `docker.io` and `iptables` in the apt layer without recommends, still drops the apt lists in that layer, and installs the wrapper as `/usr/local/bin/docker` (mode 755) ahead of the engine's own client | `[unit]` `src/deploy/sandboxDocker.test.ts::the cold sandbox image ships a Docker engine::installs docker.io and iptables in the apt layer, without recommends`, `::still drops the apt lists in the same layer`, `::installs the wrapper as /usr/local/bin/docker, ahead of the engine's client on PATH` |
| 17: the wrapper is plain `sh` that parses, starts `dockerd` under `setsid -f` and never as a plain background job, enables IPv4 forwarding before the start, starts only when the socket is missing or `docker info` fails, gives up after 40 s naming `/var/log/dockerd.log`, ends with `exec "$REAL" "$@"`, and stays under 45 lines | `[unit]` `src/deploy/sandboxDocker.test.ts::the docker wrapper::*` (7) |
| 17 live: a cold run pulls and runs a container with working networking, and the engine survives to the next command | `[agent]` After the sandbox Worker+image rollout, in a NON-resident thread: `agent:coding run \`docker run --rm alpine:3 wget -qO- https://registry.npmjs.org/pnpm \| head -c 80\`, then \`docker ps\`` (or the same two commands through a direct `/exec` on the sandbox Worker). Expect the first to print the start of the registry's JSON (`{"_id":"pnpm"…`) after a few extra seconds for the daemon start, and the second to answer at once with the engine already running. Failure signatures: `docker: command not found` = an old image still serving; `docker: the engine did not come up within 40 s — see /var/log/dockerd.log` = the daemon failed to start (read the log in a follow-up); JSON absent with `bad address`/`network is unreachable` = forwarding or the bridge is off. |
| 21: the Dockerfile installs the supervisor as `/usr/local/bin/sandbox-runtime-supervisor` (mode 755) and names tini running it as the image's one `ENTRYPOINT`, with no `CMD` | `[unit]` `src/deploy/sandboxRuntimeSupervisor.test.ts::the sandbox image's PID 1::is tini running the supervisor, installed at mode 755, named by the image's one ENTRYPOINT` |
| 21: the supervisor is plain `sh` that parses, runs `/container-server/sandbox` unless `SANDBOX_RUNTIME` names another, traps TERM and INT and forwards TERM (the background child ignores INT), checks for a stop after the one-second pause, never `exec`s, and carries the ten-second / five-exit give-up | `[unit]` `src/deploy/sandboxRuntimeSupervisor.test.ts::the runtime supervisor::*` (2) |
| 21: driven with a fake runtime — an exit is followed by a second start with the same arguments and a stderr line naming the status; SIGTERM reaches the live runtime and the supervisor exits with the runtime's own status, starting nothing more; SIGINT arrives as TERM; a stop during the pause between two starts ends it with the last status after one start; five exits in a row inside ten seconds end it with the last status after exactly five starts | `[unit]` `src/deploy/sandboxRuntimeSupervisor.test.ts::the runtime supervisor, driven::*` (5) |
| 21 live: a cold thread's workspace and a detached process survive the runtime's death, the supervisor starts the server again, and the next command runs without a re-clone | `[agent]` After the image rollout, in a NON-resident thread: `run \`echo alive > /workspace/marker; setsid -f sleep 600; kill -9 $(ps -eo pid,comm \| awk '$2=="sandbox"{print $1}')\`` (that command's own answer is lost with the server), then `run \`cat /workspace/marker; pgrep -x sleep \| head -1\`` → `alive` and a pid; the thread's container logged `sandbox-runtime-supervisor: the runtime exited with status 137; starting it again` (`containers` dataset). Failure signatures: the second `/exec` answering `HTTP error! status: 500` = the old image (PID 1 was the server); `pgrep` empty = the detached process went with the runtime. |
| 22: the verdict — destroy on a full idle window or a stuck request, keep while served or serving; requests measured one by one | `[unit]` `src/execution/sandboxIdle.test.ts::idleVerdict::keeps a container served inside the window and says when to look again`, `::destroys a container whose last request is SANDBOX_SLEEP_AFTER old, whatever runs inside`, `::a request in flight is service, however long ago the previous one finished`, `::a request in flight longer than any command may run is a stuck request, not service`, `::an overlapping chain of short requests is never one stuck request: the oldest LIVE request decides`, `::a clock that went backwards is not an idle container` |
| 22: the constants agree — the millisecond window equals `SANDBOX_SLEEP_AFTER`, the sweep fits the window, the stuck cap clears the longest command | `[unit]` `src/execution/sandboxIdle.test.ts::the constants agree::SANDBOX_SLEEP_AFTER_MS is the millisecond twin of the SDK grammar string the Worker sets`, `::the sweep runs several times inside one idle window, so a deadline is met within a minute of passing`, `::a request counts as stuck only past the longest command the Worker admits plus its margins` |
| 22: the guard — a wake reads the baseline or takes now and arms one sweep; a leaked container is gone one window later; served() counts in and out and persists on cadence; the sweep re-arms while there is a container; the SDK's expiry is answered by the verdict; a hanging or throwing destroy ends with the platform kill; overlapping deadlines destroy once | `[unit]` `src/execution/sandboxIdle.test.ts::IdleGuard::wake() takes the stored last-served time as the baseline and arms one sweep when none is scheduled`, `::wake() with no record uses now — the leaked fleet's containers are idle from the deploy, and gone one window later`, `::served() counts the request in flight, records the finish, and persists the time at most every PERSIST_EVERY_MS`, `::served() records the finish and releases the count when the operation throws`, `::a sweep during a request in flight keeps the container and re-arms; the deadline restarts from the request's finish`, `::a request in flight past INFLIGHT_STUCK_MS does not hold the container: it is destroyed as stuck`, `::the SDK's own activity expiry is answered by the same verdict — its process probes never decide`, `::a destroy the SDK cannot finish inside DESTROY_GRACE_MS ends with the platform's own kill`, `::a destroy that throws still ends with the platform's kill`, `::two overlapping deadlines destroy once`, `::a sweep on a Durable Object whose container is not running re-arms only while a request is in flight (a container starting)`, `::the first request after a destroy arms the sweep again`, `::a host whose running state is unknown is guarded like a running one` |
| 22 live: after the Worker deploy the sandbox application's live instance count falls to about the bot's `inFlight` within one window plus one sweep, `sandbox.idle-stop` lines appear in Workers Logs for the swept threads, and a follow-up inside five minutes still lands on its warm workspace | `[agent]` `wrangler containers list` before and ~6 min after the deploy against `GET /healthz` `inFlight`; the Worker's logs filtered on `sandbox.idle-stop`; a cold run followed by a `cat` of a marker file at 4 min (same workspace) and at 6 min (re-clone) |
| 23: the start gate — one warm-up per not-running container, the starting answer without running the operation, the same answer while in flight, pass-through once up with the duration logged, a failed warm-up thrown once with the SDK's own error, a stopped container started again, no binding treated as not running | `[unit]` `src/execution/sandboxStart.test.ts::StartGate::a request to a container that is not running begins one warm-up and answers starting at once, without running the operation`, `::while the warm-up is in flight every request answers starting and no second warm-up begins`, `::once the container is up the operation runs, and the start's duration is logged`, `::a running container passes the operation through without any warm-up`, `::a failed warm-up is thrown once, on the next request, with the SDK's own error — a full fleet keeps its name`, `::a container that stopped (the idle guard destroyed it) is started again by the next request`, `::no container binding at all is treated as not running: the warm-up is what says what is wrong` |
| 23: the answer shapes and the wait constants | `[unit]` `src/execution/sandboxErrors.test.ts::the sandbox-starting answer shapes the Worker sends::names the reason, the explanation and the start's phase as the cause; the exec shape is the dual in-body form`, `::the three wait tokens are the only reasons the executor re-sends on; anything else is an ordinary failure`, `::the start budget covers the platform's own start allowances with room, and its poll is denser than the fleet wait's` |
| 23: a token change restarts the backoff ladder — starting, starting, busy re-sends after the fleet's first 10 s step, not the start ladder's 30 s | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor sandbox-starting wait::a token change restarts the backoff ladder: starting → starting → busy re-sends after 10 s, not 30 s` |
| 23: the instance grant is asked for with a ten-second limit | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::the instance grant is asked for with a ten-second limit, so a refused start under a burst reaches the gate in seconds` |
| 23: the executor's start wait — identical re-sends at 5/10/15 s, the start budget over the command's, `ExecCapacityError` at the cap, the file routes' 503, the hand-over to the fleet budget | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor sandbox-starting wait::re-sends the SAME request after 5 s, 10 s, then 15 s while the container starts, and returns the eventual result`, `::a 60 s command waits through a two-minute start: the start budget, not the command's, bounds the wait`, `::gives up at SANDBOX_START_WAIT_MAX_MS with ExecCapacityError naming the start, never ExecInfraError`, `::a /read answered HTTP 503 with reason sandbox-starting is the same wait`, `::a start that turns into a full fleet is judged by the fleet's budget from then on, the time already waited counted` |
| 23: the Worker wires the gate on every route | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::every route goes through the start gate; the warm-up is \`true\` through the SDK inside the idle ledger` |
| 23 live: a run that falls to a fresh sandbox (a resident refusal, or a repository with no resident) reaches its first command instead of dying at 90 s; Workers Logs carry `sandbox.starting` and `sandbox.started` with the start's duration for that thread; the card's elapsed time includes the wait and the run goes on | `[agent]` a review of a pull request in a repository with no resident, or one whose resident refuses on disk pressure; the run page's `run.agent` span carries exec spans after the start; `wrangler tail switchboard-sandbox -c deploy/cloudflare-sandbox/wrangler.jsonc` filtered on `sandbox.start` |
| 24: the detach redirects are one string across the seam, the hint and the docs; the Worker's output wait stays inside the executor's margin; the held-output note names the code, the loss and the detach | `[unit]` `src/execution/sandboxLifecycle.test.ts::held output pipes::the detach redirects are one string, shared by the harness seam's start and the exit-124 hint`, `::the Worker's wait for the output to end past the command's deadline stays inside the executor's per-send margin`, `::the held-output note names the exit code, says the output is lost to this call and the job runs on, and spells the detach` |
| 24: the seam's start script redirects the wrapper's stdio | `[unit]` `src/core/harness/container.test.ts::the container scripts::pi's start is the script the seam wrote before the program, the filter and the layout were inputs, with the wrapper's stdio redirected away from the exec's pipes`, `::starts the process detached behind a FIFO held open for writing, its pid recorded, its stdout through the filter into the log; the environment is not in the script` |
| 24: the Worker wires the bounded wait, the status question and both answers | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::an output that never ends is told apart by the process's status: exited → its code and the held-output note, running → kill and exit 124`, `::the timeout hint tells the model to detach a long job with setsid -f and the wrapper's stdio redirected, and never names nohup` |
| 25: the seed handle — every field checked and refused by name; the fix-up script — ownership, origin, the deps view, the thread's ref at its head or tip, the head last, values quoted, no credential; the constants under /workspace and the budget over both caps; the missing-backup classification by name and text | `[unit]` `src/execution/seedPlan.test.ts::parseSeed::*`; `src/execution/seedPlan.test.ts::seedFixupScript::*`; `src/execution/seedPlan.test.ts::the seed's constants::*`; `src/execution/seedPlan.test.ts::isBackupMissing::*` |
| 25: the Worker wires the seed — `/seed` through the ledger and the gate, presigned only, the marker, the sweep on failure, the streamed answer with its own root; the image carries squashfs-tools; the template's seed block names the resident's bucket exactly when the profile has a resident; the R2 secrets reach the sandbox | `[unit]` `src/execution/seedPlan.test.ts::the seeded sandbox wiring (static)::*`; `src/deploy/wranglerTemplate.test.ts::templateView::*` |
| 25: the client posts the handle and the env under the seed's budget, returns the verdict as it came — a refusal is an answer — and treats an answer without one as infra | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor seed::*` |
| 25 live: `npm run load -- sandbox --seed-from repo:<slug> --threads 1` against a resident with a snapshot answers `seeded: true` with the restore, deps and fix-up timings, and `git -C /workspace/checkout log -1` in that sandbox shows the snapshot's head; the same handle again answers `cached`; a handle from a rotated snapshot answers `seed-missing` | `[agent]` the load receipt and the Worker's `sandbox.seeded` log line, posted on the tracking issue |
| 26: the thread's seed from the handle (the ref and head ride along; no ref stays on the snapshot's branch), the retry-then-cold decision, the card's line | `[unit]` `src/execution/seedPlan.test.ts::seedForThread::*`; `src/execution/seedPlan.test.ts::seedRetryDecision::*`; `src/execution/seedPlan.test.ts::seededSandboxNote::*` |
| 26: the factory — one `/seed` with the thread's ref and head, the seeded facts and the note; the fresh-probe retry on `seed-missing`, the same handle → cold; failed, unconfigured or a Worker without `/seed` → cold with the refusal; nothing to seed from → the cold path unchanged; a local type seeds nothing | `[unit]` `src/execution/factory.test.ts::makeExecutor seeded sandbox::*` |
| 26: the seeded prompt variants, the composer's choice (resident wins; no variant → the agent's own prompt), the REVIEW TARGET block's seeded branch | `[unit]` `src/agents/registry.test.ts::seeded prompt variants::*`; `src/core/reviewRound.test.ts::makeSystemComposer — the seeded sandbox::*`; `src/core/reviewTarget.test.ts::reviewTargetBlock — the seeded sandbox::*` |
| 26 live: a review on a repository whose resident refuses the attach runs on a seeded sandbox — the card's note names the seed, the run's first command finds `/workspace/checkout` at the PR head; `load:resident` at N = 24 on one repository shows 16 warm and 8 seeded with named notes | `[agent]` the run page and the load receipt, posted on the tracking issue |
| 24 live: a run that falls to a fresh sandbox reaches pi's first turn — the start command answers in under a second and the run page's `run.agent` span carries exec spans; a command that detaches a job without the redirects answers by its deadline plus 20 s with the held-output note instead of dying at the executor's | `[agent]` a review of a pull request in a repository with no resident; then, in a cold thread, `setsid -f sleep 300` without redirects under a 10 s `timeoutMs` — the answer carries `heldOutputNote` and the run goes on |
| 27: `RestoreWaiters` — the ledger of held requests: `wait()` holds until `publish`, one publish answers every held waiter with the outcome and empties the ledger, a later waiter is held for the next transition, a publish with nothing held is a no-op; `hold()` registers before the caller's own read and `withdraw()` takes an unanswered waiter off the ledger | `[unit]` `src/execution/restoreWaiters.test.ts::RestoreWaiters — the ledger of held /await-restore requests (item 27)::*` |
| 27: the resident Worker's route — `/await-restore` exists at operator scope, streams through the heartbeat convention, validates then answers a not-onboarded resource 404; `awaitRestore` registers its waiter before it reads the state, withdraws it and answers at once when the state is not `restoring`, and otherwise holds on the ledger with no timer and no poll; `setResidentState` publishes on every transition out of `restoring`; the ledger is the shared `RestoreWaiters` | `[unit]` `deploy/cloudflare-resident/awaitRestore.test.ts::POST /await-restore — the one held request while the resident restores (item 27)::*` |
| 27: the bot's attach path — a restoring probe opens the one held request; a warm answer attaches with the wait named on the card, a serviceable non-warm answer attaches naming the wait and the snapshot, an older Worker's 404 falls back cold with the wait named as unsupported, a non-serviceable landing names the state and the wait, an attach that fails after the wait names the failure and the wait, and a wait that fails in transport is a named cold fallback, never a throw | `[unit]` `src/execution/factory.test.ts::a restoring resident — the one held /await-restore request (item 27)::*` |
| 28: the recognizer takes the platform's accept wording as a plain Error or a bare shape and nothing else; the message starts with the token, says nothing ran and the request is re-sent, and names the container and the platform's words; the typed error is recognized by name or by token after the RPC boundary and is neither fleet-busy nor runtime-unreachable; the platform's bare words are not the token; neither the explanation nor the exhausted message asserts a cause the platform never proved | `[unit]` `src/execution/sandboxErrors.test.ts::the runtime-busy signal, message and error::*` |
| 28: the answer shapes — `/read`/`/write` `{error, reason: "runtime-busy"}`, `/exec` the dual form plus the reason — and the three wait tokens | `[unit]` `src/execution/sandboxErrors.test.ts::the runtime-busy answer shapes the Worker sends::*`; `src/execution/sandboxErrors.test.ts::the sandbox-starting answer shapes the Worker sends::the three wait tokens are the only reasons the executor re-sends on; anything else is an ordinary failure` |
| 28: the executor's wait — identical re-sends at 3/5/10 s and the eventual result, `ExecCapacityError` at the command's own budget and at the 5-minute cap, the file routes' 503, an older Worker's bare text one send as `answered` | `[unit]` `src/execution/cloudflareSandbox.test.ts::CloudflareSandboxExecutor runtime-busy wait::*` |
| 28: the Worker wires the token at the spawn and the warm-up alone, never on a running command's output; the file routes' typed error and the fetch handler's 503; the name after the RPC boundary | `[unit]` `src/execution/sandboxLifecycle.test.ts::sandbox Worker wiring (static)::a refused connect is the runtime-busy token at the spawn and the warm-up alone, the file routes' 503, and named after the RPC boundary` |
| 28 live: a cold thread running a whole-repository gate while the harness polls its transcript keeps the run — the sandbox Worker's logs for that thread carry `runtime-busy` answers followed by the poll's success, never a `sandbox.exec error … taking too long to accept` that ends the run | `[agent]` a coding run in a repository with no resident whose gate is `npm run verify`; the run page's exec spans and `wrangler tail switchboard-sandbox -c deploy/cloudflare-sandbox/wrangler.jsonc` filtered on `runtime-busy` |
