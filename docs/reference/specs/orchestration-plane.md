# Orchestration plane

Switchboard can say **what is happening**: every live and recently ended run, every ship unit and every tracked pull request, each with its owner and its health, as one table read from the stores that already exist. The table is the first surface of the orchestration plane (record 0064, `docs/decisions/0064-the-plane-owns-every-runs-state-a-refusal-becomes-a-queue-position-an-ending-is-judged-by-the-ledger-that-saw-it-and-a-release-is-a-quiet-window-a-person-closes.md`, which merges beside this [unit](../vocabulary.md#unit)): the read-only projection the plane's later units (the queue, the windows, the watches and their moves) grow beside. Nothing in this feature decides, writes or steers.

**Pure table, thin reads (locked design).** One node-free module says what the rows mean, `src/core/plane/table.ts`, so the command's text, the page and the tests agree on every word; the service reads and hands the facts over. A health flag is derived from a fact the row already carries and never from a clock the plane owns: the stall facts are the [runs](../vocabulary.md#run) index's ([live-view.md](live-view.md) item 32), the [pull request](../vocabulary.md#pull-request) facts are the merge door's ([agent-ship.md](agent-ship.md) item 9), a unit's ending is the coordinator's row ([run-history.md](run-history.md) item 50).

- **Code**: [`src/core/plane/table.ts`](../../../src/core/plane/table.ts) (the rows, the health flags, `buildPlaneTable`), [`src/core/plane/decide.ts`](../../../src/core/plane/decide.ts) (the decider: the event, effect and write unions), [`src/core/planeService.ts`](../../../src/core/planeService.ts) (the reads), [`src/core/commands/plane.ts`](../../../src/core/commands/plane.ts) (`plane show`), [`src/channels/planeView.ts`](../../../src/channels/planeView.ts) (`GET /plane`, `/plane.json`), [`web/src/pages/PlanePage.vue`](../../../web/src/pages/PlanePage.vue) and [`web/src/lib/navSections.ts`](../../../web/src/lib/navSections.ts) (the panel and its nav entry), [`deploy/cloudflare-memory/worker.ts`](../../../deploy/cloudflare-memory/worker.ts) (the plane tables, `planeApply`, `POST /plane/outcome`, `POST /plane/ack`, the answers' effects), [`src/core/runLedger/ledger.ts`](../../../src/core/runLedger/ledger.ts), [`src/core/runLedgerWorker.ts`](../../../src/core/runLedgerWorker.ts) and [`src/core/runLedger/writeThrough.ts`](../../../src/core/runLedger/writeThrough.ts) (the client half: the effects field, the ack, the outcome post), [`src/core/dispatch/admission.ts`](../../../src/core/dispatch/admission.ts) (the shadow outcome post and the admission ask), [`src/channels/planeEffects.ts`](../../../src/channels/planeEffects.ts) (the push transport's bot route), [`src/deploy/planeDeploy.ts`](../../../src/deploy/planeDeploy.ts) (the deploy runner's `/plane/deploy` posts), [`src/config.ts`](../../../src/config.ts) (`plane.admission`, `plane.reaskMinutes`, `plane.coldFallback`), [`deploy/cloudflare-resident/levels.ts`](../../../deploy/cloudflare-resident/levels.ts) and [`deploy/cloudflare-resident/worker.ts`](../../../deploy/cloudflare-resident/worker.ts) (the resident's levels, the outbox, the registry's drain posts and its alarm).
- **Tests**: [`src/core/plane/table.test.ts`](../../../src/core/plane/table.test.ts), [`src/core/plane/decide.test.ts`](../../../src/core/plane/decide.test.ts), [`src/core/planeService.test.ts`](../../../src/core/planeService.test.ts), [`src/core/commands/plane.test.ts`](../../../src/core/commands/plane.test.ts), [`src/channels/planeView.test.ts`](../../../src/channels/planeView.test.ts), [`web/src/pages/plane.test.ts`](../../../web/src/pages/plane.test.ts), [`deploy/cloudflare-memory/runs.test.ts`](../../../deploy/cloudflare-memory/runs.test.ts) (inside workerd against the real SQLite DO), [`src/core/runLedger/writeThrough.test.ts`](../../../src/core/runLedger/writeThrough.test.ts), [`src/core/dispatch/admission.test.ts`](../../../src/core/dispatch/admission.test.ts), [`src/channels/planeEffects.test.ts`](../../../src/channels/planeEffects.test.ts), [`src/deploy/planeDeploy.test.ts`](../../../src/deploy/planeDeploy.test.ts), [`src/config.test.ts`](../../../src/config.test.ts), [`deploy/cloudflare-resident/levels.test.ts`](../../../deploy/cloudflare-resident/levels.test.ts).

## Behavior

1. **Runs and their health.** The table lists every run live in this process or on the ledger under another generation, then every run finished within the recent window (one hour by default), each under the viewer's `runs:read` predicate, exactly the rows `runs list` would show. A row's owner is its requester, with the name the adapter resolved when it did, or the generation driving it when the row is live under another container. Its health flags come from the pace facts the live summary carries: `bound-exceeded` when the in-flight call ran past the bound it declared, `stalled` when no tool call landed for a whole pace window, `no-signal` for a row live under another generation, which carries no pace fact and is never read as stalled; a finished row reads `provisional`, `interrupted` or `failed` from its record, or nothing. Live rows sort first, stalled ahead of healthy, then rows with a flag, then the recently ended, newest first within each band.
2. **Pull requests and their health.** Every pull request a unit row or a run's record names is read once through the merge door's own GitHub reads (the facts, the checks at the head, the reviews), up to a bound (20 by default, the units' first), and judged in the merge door's order: `approved` when a review approves the head, `dirty` when GitHub reports a conflict with the base, `red` when a check at the head failed else `pending` when one is still running, `mistitled` when the title fails the repository's title rule, `merged` or `closed` for a closed one, and `unknown` alone when GitHub could not be read or the process holds no GitHub credential. A pull request is owned by the unit whose row names it, else by the run that opened it, else by a person.
3. **Units and the owner gap.** Every instance a listed run belongs to contributes its unit rows, under the same predicate the unit page applies, with the instance's facts beside them. A unit with no ending is `live` while a run of it is live and `waiting` otherwise; an ending record 0051 idles on reads `idle`; `merged` and `merge-ready` read as they are, and a merge-ready unit whose pull request is still open is also an `owner-gap`, the state record 0064 names; a pull request the plane could not read, on GitHub or past the read cap, leans open, so a read failure never hides the gap. A coordinator child is joined to its unit through the spawn's idempotency key, else through the unit whose [thread](../vocabulary.md#thread) it runs in.
4. **`plane show`.** One typed registry command, action `runs:read`, effect read, on every surface by derivation (`plane show` in chat and on the CLI, `GET /api/plane.show`, the `plane_show` MCP tool), answering the table as JSON and rendering it as three sections, aligned columns for a terminal and bullets for chat, each section saying `(none)` when empty; a row's health prints in the user's words — the `owner-gap` flag reads `unmerged` beside `merge-ready` (record 0066, "The collapse"), and the flags join as a phrase, never the raw flag join. The router is offered it with its three fixtures.
5. **The panel.** `GET /plane` serves the shared web shell with the table as its seed and `GET /plane.json` answers the same table, one view in two encodings (record 0056), Access-gated beside `/runs` and `/costs`; a viewer the gate resolved to nobody sees nothing, a table that cannot be built is a 502 with the reason, never a 500. The page paints the three sections with the same words and links each run as the runs index does: a live run of this process through its capability token, every other run tokenless. **Plane** is a nav section beside Runs, always present because its rows are run facts under the same predicate.
6. **The decider.** One pure, node-free function says what the plane does: `decide(state, event) → { state, effects, writes }` (`src/core/plane/decide.ts`), over a closed event union (`ask`, `sealed`, `withdraw`, `window`, `level`, `observation`, `reask`) and a closed effect union (`admit`, `probe`), never throwing — an event about a run or thread the state does not know is a no-op. An ask on a free thread proceeds: state unchanged, no writes, no effects (`planeAskWordOf` reads `proceed`). An ask on a live thread queues with a `thread_free` condition and a position counted among the thread's waiting rows. A seal flips the condition and the queue walk admits oldest first, re-evaluating after each admission — the admitted run's thread is live again, so a second row on the same thread keeps waiting. A withdraw marks the waiting row `withdrawn`, and a withdrawn row is never admitted. Effects carry stable ids derived from the run (`admit:<runId>`), so a duplicate offer after a roll is the same effect. The ledger object applies it inside ONE `transactionSync` (`planeApply`): the state read from its own tables, the decider's writes and the offered effects committed together or not at all.
7. **The tables and the answers' effects.** The ledger object holds the plane's six tables from this unit on — `plane_queue(run_id, requester, thread_key, stage, request_json, conditions_json, position_at, queued_at, state)`, `plane_reservations(kind, key, run_id, at)`, `plane_windows(kind, key, phase, opened_at, reason_json)`, `plane_findings(id, watch, subject, timeline_json, filed_at)`, `plane_effects(id, body_json, offered_at, acked_at)` and `plane_levels(resident, name, side, reported_at, generation)` — so an earlier bot and a later Worker agree on the schema; only the queue and the effects are written yet. The endings unit adds the seventh, `plane_endings(run_id, kind, cause, at)` ([run-history.md](run-history.md) item 33). Every answer the object gives the bot's heartbeat and reclaim-sweep calls carries `effects: PlaneEffect[]` — the unacknowledged offers, oldest first, at most 32 — always present, empty until a unit writes one; the client reads a field-less answer from an older Worker as none offered. The bot acknowledges each effect by id on `POST /plane/ack`: `done` and `skipped` close the offer, `deferred` leaves it riding the next answer; an unknown id is a no-op, never an error. The write-through acks everything it receives `deferred` today — nothing executes an effect until the transport unit lands.
8. **Shadow.** `plane.admission: off | shadow | on` is the plane's gate ([routing-and-config.md](routing-and-config.md) item 31; default `off` — `planeAdmissionOf`). Under `shadow` the dispatch's admission stage posts its own outcome per dispatch to `POST /plane/outcome`, fire and forget through the write-through (`planeOutcome` — a failed or missing route is one warning, never a wait or a throw): `proceeded` for a claim, `refused:thread-live` for every thread-in-flight refusal, `refused:allowlist` for the [agent](../vocabulary.md#agent) gate's; a steer, a supersede and a redispatch are not asks for a new run and post nothing, and the executor's `fell_cold:<token>` arrives with the resident unit. The object judges the same ask with the decider inside one transaction and logs the decider's word beside the bot's; nothing runs — and nothing queues — from the decider under shadow, and a disagreement (a `proceeded` the decider would queue, a thread-live refusal it would admit) bumps a per-condition counter in `meta` (`plane_disagreements`) for the later `plane disagreements` line; a pair the decider does not model yet is logged uncounted. `off` posts nothing.
9. **The resident conditions (record 0064, "The queue").** Each resident computes two levels — `seat` (the worker-user pool, threads and ops both: `above` exactly when no user is free, the next attach's `user-pool-exhausted`) and `memory` (the gate's soft side, [resident-repos.md](resident-repos.md) item 70) — in a pure module (`deploy/cloudflare-resident/levels.ts`), stamped with its generation (the container's boot id where memoized, else the DO incarnation). Every `/attach`, `/exec` and `/status` answer carries the `levels` document; a crossing lands in a per-name outbox re-offered on every answer until a newer crossing supersedes it, and a boot or a replaced runtime re-states both names. The registry posts the fleet drain the same way (`name: "drain"` on `/drain`, `/undrain` and its answers): the set posts `above` and arms ONE alarm at the record's `until`, a clear posts `below`, and the alarm posts `below` for a drain nobody lifted. The bot forwards a post to `POST /plane/level`; on the object, `seat` and `memory` land as level events — a `below` side walks the queue — and `drain` as the `resident-drain` window's open and lift. A resident-stage ask (`stage: "resident"`, the resident named) queues on a level reported `above`, holds no reservation (its thread is already this run's), and proceeds — for the bot to fall cold — when the resident is `unknown` (`residentSideOf`: no report, or one from another generation); a `restartOf` ask passes the windows and the memory line. A refusal-by-name the bot meets at attach or exec (`POST /plane/observe`: `user-pool-exhausted` → seat, the gate's `memory-pressure` → memory, `draining` → the drain window, `runtime-unreachable`/`runtime-replaced` → seat) re-enters an admitted run's queue row `waiting` at its old position and writes the level `above` — the refusal is evidence. While a waiting row waits on a resident that has said nothing within the re-ask cadence (`plane.reaskMinutes`, default 2, stored from the admit post's `reaskMs`; `PLANE.reaskMinutes` in `src/core/budgets.ts`), the object's alarm — pulled forward to the cadence, never pushed back — emits one `probe(resident)` effect per resident (`probe:<resident>`: at most one open per resident, re-offered after an ack), which the heartbeat loop answers by probing `/status` and forwarding the levels; a probe is never deferred by a drain and a process without the seam skips it. `plane.coldFallback` (default false — `planeColdFallbackOf`) is the rollback lever: write presets fall cold instead of queueing. The bot's stage-three ask and its forwarding are the executor's half, still to land: `[gap]` a coding ask on a resident above a line queues at stage three, a read preset falls cold, and the bot forwards levels and refusals.

## Validation criteria

| Criterion | Evidence |
|-----------|----------|
| A fresh tool call is no flag; a whole window without one is `stalled`; a call past its bound is `bound-exceeded` ahead of `stalled` | `[unit]` `src/core/plane/table.test.ts::runHealthOf — a live run's flags from the pace facts and its status::a live run of this process with a fresh tool call has no flag`, `::a live run with no tool call for a whole window is stalled`, `::a call past its own declared bound is bound-exceeded, and outranks stalled` |
| A row live under another generation is `no-signal`, never stalled; finished rows read provisional, interrupted or failed | `[unit]` `::a row live under another generation carries no pace fact and reads no-signal, never stalled`, `::a provisional record and an interrupted or failed ending are named` |
| Pull request flags in the merge door's order; unknown alone when unread | `[unit]` `src/core/plane/table.test.ts::prHealthOf — a tracked pull request's flags from the merge door's facts::an open pull request with green checks and an approval at its head is merge-ready`, `::a red check at an approved head is red, a conflict is dirty, a failing title is mistitled`, `::pending checks are named; a merged or closed pull request carries its state alone`, `::a pull request GitHub could not be read is unknown and nothing else` |
| Unit flags from the ending and the live run; the owner gap | `[unit]` `src/core/plane/table.test.ts::unitHealthOf — a unit's flags from its ending and its runs::a unit with no ending and a live run is live; with no ending and no run it is waiting`, `::an idling ending kind (record 0051) reads idle with its kind; merged and merge-ready read as they are`, `::a merge-ready unit whose pull request is still open is an owner gap` , `src/core/plane/table.test.ts::buildPlaneTable — the owner gap when the pull request could not be read::an unread pull request and one past the read cap both lean open: the gap is flagged either way` |
| A child is joined to its unit by key, else by thread | `[unit]` `src/core/plane/table.test.ts::unitIdOfRun — which unit a coordinator child belongs to::reads the unit off the spawn's idempotency key, else off the unit whose thread the run is in` |
| Row order and owners; units joined to instances and pull requests to their owners; empty windows and findings | `[unit]` `src/core/plane/table.test.ts::buildPlaneTable — the rows, their owners and their order::lists live runs first, stalled before healthy, then recently ended; names each owner`, `::joins units to their instance and their runs, and pull requests to the unit that owns them`, `::a pull request a plain run opened is owned by that run; one nobody names is a person's`, `::carries empty windows and findings until the plane has them` |
| The service reads live and recent runs under the predicate, joins units, reads each pull request once, bounds the reads, and never throws for an unreadable GitHub | `[unit]` `src/core/planeService.test.ts::createPlaneService — the table over the stores that exist::lists the live runs and the runs finished within the recent window, under the viewer's predicate`, `::joins a coordinator child to its instance's units and reads the units' pull requests once each`, `::an instance the viewer may see nothing of contributes no units and no pull requests`, `::a pull request GitHub cannot read is unknown; without a GitHub reader every pull request is`, `::reads at most the configured number of pull requests, the units' first` |
| `plane show` is one read under `runs:read`, refused before the service is asked, rendered as three sections | `[unit]` `src/core/commands/plane.test.ts::plane.show::registers one read under runs:read on every surface`, `::answers the table under the caller's own predicate: an all-channels reader gets every row`, `::a caller without runs:read is refused before the service is asked`, `::renders three sections for a terminal, the header counting live, recent, units and pull requests`, `::renders bullets for chat, with the sections in bold`, `::an empty table says so in every section` |
| The health words on the text surfaces: a unit whose health is `owner-gap` prints `merge-ready, unmerged` on chat, the CLI and MCP output | `[unit]` `src/core/commands/plane.test.ts::plane.show::a unit whose health is owner-gap prints \`merge-ready, unmerged\` on chat, the CLI and MCP output — never the raw flag join (record 0066)` |
| The panel's routes, the seed with tokens for this process's live rows, the twin, the nobody viewer, the 502 | `[unit]` `src/channels/planeView.test.ts::createPlaneViewHandler::falls through for other paths and refuses non-GET`, `::serves the shell with the table as the seed, read under the viewer's predicate, with tokens for this process's live rows only`, `::answers the twin as the same table in JSON`, `::a viewer the gate resolved to nobody sees nothing`, `::a table that cannot be built is a 502 with the reason, never a 500` |
| The page paints the counts, the rows, the owners and the health words, links live runs through their tokens, and says so when empty | `[unit]` `web/src/pages/plane.test.ts::PlanePage::paints the header counts and one row per run, unit and pull request with their health words`, `::links a live run of this process through its token and every other run tokenless`, `::says so when a section is empty` |
| The router is offered `plane show` with its three fixtures | `[unit]` `src/core/commandConformance.test.ts::command conformance — catalogue fences::every offered command has its three routing fixtures — happy path, paraphrase, decoy (load:route's command half)` |
| The decider: a free thread is admitted with a reservation written in the same decision; a live or reserved thread queues with `thread_free` and a position ranked per shared condition; an open window queues on `window_open` and a pending deploy on `deploy_settled`, each admitted on its lift; the seal deletes the reservation and admits oldest first, re-evaluating after each admission; a no-transition event returns the state unchanged; a withdraw closes the row; effects carry stable ids | `[unit]` `src/core/plane/decide.test.ts::decide — the plane's pure decider (orchestration-plane, record 0064)::an ask on a free thread is admitted: no queue row, a reservation on the thread written in the same decision, no effects`, `::two asks a second apart on one thread: the first is admitted with a reservation, the second queues with position 1 behind it`, `::an ask that meets an open window queues on window_open and is admitted by the window's lift`, `::an ask that meets a pending deploy queues on deploy_settled and deploy.landed flips it`, `::a reserved thread queues a second ask until the seal deletes the reservation and admits it`, `::position counts waiting rows sharing the unmet condition, per condition`, `::an ask on a live thread queues with a thread_free condition and position 1`, `::a second ask on the same live thread queues behind the first, position 2`, `::a thread_free condition flips on a seal and the oldest waiting row is admitted, with an effect carrying its id`, `::a queue walk admits oldest first and re-evaluates after each admission: the admitted run's thread is live again, so its follower keeps waiting`, `::two waiting rows on different threads are both admitted on their seals, oldest first`, `::an event with no transition returns the state unchanged: a seal nothing waits on`, `::a withdraw marks the waiting row withdrawn and a withdraw of an unknown run changes nothing`, `::a withdrawn row is never admitted by a later seal`, `::effects carry stable ids derived from the run they admit` |
| Inside workerd: the plane's tables exist, and `planeApply` commits the state flip and the offered effect in one `transactionSync` | `[unit]` `deploy/cloudflare-memory/runs.test.ts::orchestration plane — the tables, the decider, shadow and the effects::a fresh object holds the plane's seven tables`, `::planeApply commits the queue row, then the seal's admitted state and its effect, together in transactionSync` |
| Shadow on the object: the decider's word is logged beside the bot's outcome, nothing queues, and a disagreement bumps the per-condition count; a malformed post is 400 before any object call | `[unit]` `deploy/cloudflare-memory/runs.test.ts::orchestration plane — the tables, the decider, shadow and the effects::shadow logs refused:thread-live beside queued for a second ask on a live thread, and persists nothing`, `::an agreeing proceeded post counts nothing; a proceeded post on a live thread bumps the per-condition disagreement count`, `::a bad stage, outcome word or ack word is 400 before any object call` |
| The effects field: present and empty on every heartbeat and reclaim answer, an offer rides until acked `done` or `skipped`, `deferred` leaves it, an unknown ack id is a no-op | `[unit]` `deploy/cloudflare-memory/runs.test.ts::orchestration plane — the tables, the decider, shadow and the effects::a heartbeat answer and a reclaim answer carry effects: [], present even with nothing offered`, `::an offered effect rides the heartbeat answer; deferred leaves it offered, done closes it, an unknown id is a no-op` |
| The client half: the write-through acks received effects `deferred` when nothing is wired, runs an `admit` through the wired executor — a draining generation defers it — and the outcome post fires and forgets: a failure is one warning | `[unit]` `src/core/runLedger/writeThrough.test.ts::events, state, heartbeat::plane effects on a heartbeat answer are each acked deferred — this generation executes none yet (orchestration-plane item 7)`, `::an admit effect runs through the wired executor and its word is the ack; a draining generation defers it instead (record 0064)`, `::planeOutcome fires the post and swallows a failure with one warning — the dispatch never waits on the plane (orchestration-plane item 8)` |
| The admission stage under `plane.admission: on`: two asks a second apart answer `admitted` then `queued` at position 1; the claim promotes the reservation; the seal admits the queued run with its admit effect and its attaching row under the plane's id; a withdrawn row is never admitted; `deploy.landed` flips `deploy_settled`; a sealed run's open effects are dropped | `[unit]` `deploy/cloudflare-memory/runLedger.test.ts::the plane's admission stage — /plane/admit, reservations, the seal's walk (orchestration-plane; record 0064)::two asks a second apart on one thread: the first is admitted with a reservation, the second queued at position 1; the first's claim promotes the reservation; the seal admits the queued run with its attaching row`, `::a duplicate admit decision after a roll keeps the effect's first offer and the attaching row (INSERT OR IGNORE)`, `::runs stop on a queued id withdraws it: the row goes withdrawn, a second withdraw answers false, and the seal admits nothing`, `::a pending deploy queues an ask on deploy_settled and deploy.landed flips it, admitting the queued run`, `::effects for a sealed run are dropped at the seal: an admitted-then-finished run's open admit goes with its finish`, `::validates: a missing threadKey, requester or request is 400; a malformed withdraw run id is 400` |
| The bot's door under `plane.admission: on`: a queued ask holds nothing — the slot released, no row reserved or adopted, no heartbeat — the thread hears the position and the withdraw lever; an admitted ask proceeds; `off`, a resume and a restart never ask | `[unit]` `src/core/dispatch/admission.test.ts::admit — the plane's admission ask under plane.admission: on (record 0064, "The queue")::an ask the plane queues holds nothing: the slot is released, no row is reserved or adopted — no heartbeat, nothing in the drain's held set — and the thread hears the position`, `::an admitted ask proceeds — asked once, the reservation is the object's to promote`, `::off — the default — never asks; a resume and a restart re-enter decided work and never ask either` |
| The push transport: the state Worker POSTs committed effects to the bot's `/plane/effects`, which runs each through the SAME executor the heartbeat answers use and acks its word; draining or unwired defers; a wrong bearer or a malformed body runs nothing; a push that fails leaves the effect riding the next heartbeat answer | `[unit]` `src/channels/planeEffects.test.ts::POST /plane/effects — the push transport (record 0064)::runs each pushed effect through the wired executor and acks its word on the object`, `::a draining generation defers, and no wired executor defers too — the offer stays for a bot that can run it`, `::an effect whose execution or ack throws is one warning and stays offered — it rides the next heartbeat answer; the rest of the push still acks`, `::refuses a wrong or missing bearer, and everything when the token is unset (fail closed); refuses non-POST`, `::a non-JSON body, a body without an effects array and a malformed effect are 400 — nothing runs`, `deploy/cloudflare-memory/runLedger.test.ts::the plane's admission stage — /plane/admit, reservations, the seal's walk (orchestration-plane; record 0064)::a push that fails leaves the effect on the next heartbeat answer: the bot answered 404, nothing was acked, and the offer rides the heartbeat` |
| A queued id has a page and a lever: `getRun` answers the stored ask's view (the requester, the thread's channel, the position, the waiting words — the queued reply's own), the run page heads with them, and `runs stop` on the id withdraws the waiting row for either mode; a row the queue holds not-waiting is a conflict | `[unit]` `src/core/runsService.test.ts::RunsService — a queued ask on the plane (record 0064)::getRun answers a queued id's view: the requester, the thread's channel, the position and the waiting words — so the id a person was told has a page`, `::stopRun withdraws a queued id — the reply's runs stop lever — for either mode; the queue knows the id but not as waiting is a conflict`, `web/src/pages/runPage.test.ts::RunPage — history mode::a queued ask's page heads with the position and the waiting words, amber` |
| The deploy runner posts the window: `deploy all` with a bot step posts `pending` before the first Worker uploads and `landed` after the run — landed or failed alike — so `deploy_settled` flips end-to-end; no state Worker, a missing bearer, an older Worker or an unreachable one is one line each and never fails the deploy | `[unit]` `src/deploy/planeDeploy.test.ts::postPlaneDeploy — the /plane/deploy post from the deploy runner::posts pending and landed to the state Worker's /plane/deploy with the bearer, the store key and the commit; landed logs how many asks were admitted`, `::no state Worker posts nothing; a missing bearer is one line and nothing is sent`, `::an older state Worker (404) and an unreachable one (throw) are one line each — the deploy is never failed by the plane` |
| The resident stage's decider: a write ask above the soft memory line or an exhausted seat queues at stage three holding nothing; a `below` report admits it and another resident's admits nothing; below both lines it proceeds with no writes; a `restartOf` ask passes windows and the memory line; an unknown resident queues nothing; an observed refusal re-enters an admitted run at its old position, maps every named refusal, and a no-op otherwise; the drain window opens and lifts on the registry's posts; a reask probes each silent resident once and none that reported within the cadence | `[unit]` `src/core/plane/decide.test.ts::decide — the resident stage's conditions (record 0064)::*` |
| The resident's levels: the seat and memory sides, the crossing posts, the boot's re-statement, the outbox's supersession, and the Worker wiring — levels on every `/status`, `/attach` and `/exec` answer, the registry's drain posts and its one alarm at `until` | `[unit]` `deploy/cloudflare-resident/levels.test.ts::the sides::*`, `deploy/cloudflare-resident/levels.test.ts::the crossing posts and the boot's re-statement (record 0064)::*`, `deploy/cloudflare-resident/levels.test.ts::the Worker wiring (static): levels on every answer, the registry's drain posts and its alarm::*` |
| The resident stage on the object: `/plane/level` lands in `plane_levels` and a `below` side admits the queued resident ask (which held no reservation) with its attaching row; a drain post opens and lifts the `resident-drain` window; `/plane/observe` re-enters an admitted run and the next `below` report re-offers the SAME admit despite its acked row; the re-ask alarm is pulled forward and never pushed back, probes a silent resident and re-offers the probe after its ack; a malformed level or observe post is 400 | `[unit]` `deploy/cloudflare-memory/runLedger.test.ts::the plane's resident stage — /plane/level, /plane/observe, the re-ask alarm (orchestration-plane item 9; record 0064)::*` |
| `plane.coldFallback` parses as a boolean, defaults false, and a non-boolean is refused by name | `[unit]` `src/config.test.ts::plane block (routing-and-config item 31, record 0064)::coldFallback parses as a boolean, defaults false, and a non-boolean is refused by name (record 0064)` |
| The shadow outcome post from the admission stage: `proceeded` on a claim, `refused:thread-live` on a thread-in-flight refusal, nothing for a steer, nothing under `off` | `[unit]` `src/core/dispatch/admission.test.ts::admit — the plane's shadow outcome post (orchestration-plane item 8)::under shadow a claim posts proceeded with the requester, the thread and the admission stage`, `::under shadow a second ask on a live thread that asks for another agent posts refused:thread-live`, `::a steered follow-up is not an ask for a new run: nothing is posted for it`, `::off — the default — posts nothing, for a claim and for a refusal alike` |

## Roadmap

The plane's later units (record 0064) grow the decider into the plane: the runner conditions, the executor's resident-stage ask (item 9's gap), the quiet and pressure windows, the watches and their moves, the findings, and the panels beside the table. Each lands with its own items here.
