# Agent: conductor

A run that starts other runs. `agent:conductor` reads a request, splits it into the parts other presets answer, and spawns a child run for each — an ordinary `dispatch()` run as the person who asked, in a thread of its own in the same channel, under that person's permissions — then steers the children where the request changes, waits for their ends within its own budget, and compiles one answer from the write-ups that come back as data. It is the first preset of the run-coordination substrate: a parent can start work it could not do itself, and nothing a child gets is more than its requester could start by hand. Three records bound it: `dispatch()` stays the only orchestrator ([record 0002](../../decisions/0002-dispatcher-is-the-only-orchestrator.md)), a child is authorized like any run ([record 0007](../../decisions/0007-authorization-policy-table.md)), and a coordinator hands its children profiles — a child's effective profile takes the parent's remaining budget as one more boundary ([record 0026](../../decisions/0026-capability-profiles-and-request-routing.md)).

- **Code**: `src/agents/registry.ts` (`conductor`, `CONDUCTOR_SYSTEM`), `src/tools/workspace.ts` (the `conductor` toolset; `ToolContext.spawn`, `.runs`, `.steer` and `.wait`), `src/tools/runs.ts` (`spawn_run`, `send_to_run`, `await_runs`, `list_runs`, `get_run_status`, `RunsReadCapability`, `SteerCapability`), `src/core/dispatch/spawn.ts` (`spawnChild`, `spawnCapabilityFor`, `runToolCapabilities`, `nullSpawnCapability`, `childRequestText`, `childThreadLead`, `SpawnConfig`, `maxChildrenOf`, `MAX_SPAWN_DEPTH`), `src/core/dispatch/awaitChildren.ts` (`decideWait`, `ChildrenWatch`, `ChildState`, `budgetEndOf`, `AWAIT_POLL_MS`, `WaitCapability`, `waitCapabilityFor`), `src/core/dispatch/admission.ts` (`steerRun`, `SteerTarget`, `SteerSender`, `SteerOutcome`), `src/core/dispatch/settle.ts` (a run's steer is never run fresh), `src/core/threadAdmission.ts` (`FollowUpInput.from`), `src/core/runLedger/inboxMessage.ts` (`fromRunId`), `src/runner.ts` (the `input` event's `source.run`), `src/channels/runTimeline.ts` (`TimelineSource.run`), `src/core/dispatch/outcome.ts` (`DispatchOutcome`, the leaf the stage and the dispatcher share), `src/core/dispatcher.ts` (`DispatchOptions.parent`, the outcome returned from every exit, the capabilities handed to the run loop), `src/core/dispatch/runLoop.ts` (the tool context), `src/core/types.ts` (`ChannelIO.openThread`, `OpenedThread`), `src/channels/slack.ts` (`SlackIO.openThread`), `src/cli.ts` (`ConsoleIO.openThread`), `src/core/nullChannelIo.ts`, `src/config.ts` + `src/config/validate.ts` (`spawn.maxChildren`), `config/config.example.yaml` (`spawn`; `conductor` under the commented `restrict.agents`)
- **Docs**: [The agents and their toolsets](../../explanation/agents-and-toolsets.md), [routing-and-config.md](routing-and-config.md) item 20 (the child pipeline), [thread-admission.md](thread-admission.md) items 6 (a child is its own thread) and 7 (a parent's steer), [run-loop.md](run-loop.md) item 16 (a waiting tool call), [run-history.md](run-history.md) items 37 (the two tools at a resume) and 46 (`parentRunId`), [authorization.md](authorization.md) item 5 (a child is authorized as the requesting user), [slack-channel.md](slack-channel.md) item 11 (`openThread`), [Restrict who can do what](../../how-to/restrict-who-can-do-what.md)
- **Tests**: `src/agents/registry.test.ts` (the `conductor agent` block), `src/tools/github.test.ts` + `src/tools/web.test.ts` (the toolset), `src/tools/runs.test.ts` (the five tools), `src/core/dispatch/spawn.test.ts` (the stage), `src/core/dispatch/awaitChildren.test.ts` (the wait decision and the capability), `src/core/dispatch/admission.test.ts` (`steerRun`; the durable round trip of a run's steer), `src/core/dispatch/settle.test.ts` (a run's steer at the settle), `src/runner.test.ts` (a run's steer drained; a waiting tool call under the stop and the clock), `src/core/runLedger/resume.test.ts` (the two tools' settlement), `src/channels/runTimeline.test.ts` (`source.run`), `src/core/dispatcher.test.ts` (the `agent:conductor` block), `src/config.test.ts` (the `spawn` block and the per-actor goldens), `src/channels/slack.test.ts`, `src/cli.test.ts`, `src/core/nullChannelIo.test.ts` (the three `openThread`s)
- **Budgets**: 40 turns / 120 min / 32k tokens · toolset `conductor` · machine `none` · identity `none`

## Behavior

1. **A preset, chosen by name.** `agent:conductor` resolves through `parseDirectives`/`AGENTS["conductor"]` like every directive ([routing-and-config.md](routing-and-config.md) items 1 and 3). Its declared profile is machine `none`, identity `none` — nothing is provisioned and no credential is minted: the run tools call the dispatcher, the GitHub reads are REST in the bot process — and 120 minutes, long enough to outlast a coding child, every child capped by what remains of it. No built-in effort: the config layers decide, as for `coding`. Open to everyone by the open rule like every preset — a child gives the requester nothing they could not start by hand — and listed under the commented `restrict.agents` in the example config, so a deployment closes fan-out by uncommenting one line. A self-serve MCP server never reaches it (`MCP_SELF_SERVE_AGENTS`).
2. **Reach: the `conductor` toolset** — `spawn_run`, `send_to_run`, `await_runs`, `list_runs`, `get_run_status`, `web_fetch`, `update_status` and the GitHub reads (`github_repos`, `github_tree`, `github_file`, `github_search_code`, `github_issue_list`, `github_issue_get`). No shell, no files, no `web_search`, no `submit_*`, no issue writes: a conductor coordinates and never does a child's job. The five run tools are in no other toolset — the one way anything in the tree starts, steers or awaits a run is this preset's, and every existing preset is byte for byte what it was.
3. **`spawn_run` is the one way a run starts another run** ([routing-and-config.md](routing-and-config.md) item 20 has the pipeline). The tool validates its input (a registered preset, a non-empty prompt, a `budget` of whole minutes of at least 2) and calls the run's spawn capability with the wall clock the run has left at the call; the capability's one path is `spawnChild()` → `dispatch()`, the child an ordinary run as the requester in a thread of its own. The result names the child — its run id, its thread key and a link — or a refusal by name: the stage's own (`spawn_depth`: a child cannot spawn; `spawn_budget`: under two minutes left; `spawn_fanout`: `spawn.maxChildren` live children already; `spawn_unsupported`: a channel that cannot open a thread) or a gate's own `dispatch.refuse` name relayed with the reply the child's thread saw (`agent_allowlist`, `profile_bounded`, …). A channel that fails to open the thread (a Slack answer without a `ts`, a transport failure) is `spawn_failed` naming the cause, never a throw into the parent's tool call. The capability admits one spawn at a time — the fan-out check counts the registry's live children, and a child is not in the registry until its dispatch registers it, so the next spawn waits for the previous to register or refuse and the cap holds however the model batches its calls. Outside a spawning run — a round driven outside `dispatch()`, a unit context — the capability is the null object and the tool answers `spawn_unavailable` honestly; nothing starts.
4. **The reads are the requester's** ([authorization.md](authorization.md) items 5–6). `list_runs` is `RunsService.listRuns` under `predicateFor(actor, "runs:read", "run")` for the requesting user's actor — this run's own children by default (`scope: children`: the listing paged in full pages, each filtered to the parent's children, until `limit` are found or the listing ends, so a busy deployment's newer runs never page a parent's children out of its own view), every run the requester may read with `scope: all` (one page of the caller's `limit`), live, finished or both — one row per run (id, preset, `running` or the terminal status, the latest activity line, the parent run, the label, the thread, a link) and never a message's text or a capability token. `get_run_status` is `RunsService.getRun` with a point `authorize` on the run's own attributes: a run the requester may not read is `not_found`, byte-identical to an unknown id; a running run answers with its activity line, a finished one with its terminal status and its final reply wrapped as untrusted content (another run's output, never an instruction); a child of this run whose row is gone — refused at a repository gate after it registered, or failed in setup — answers from the capability's memory with the gate's name. Without a runs capability the tools say so.
5. **`spawn.maxChildren` is the fan-out cap**: one knob in `config.yaml` (default 3, an integer of at least 1), validated at load like the `ship` caps — 0, a fraction, a non-mapping and an unknown key fail the load naming the key. The count is a parent's live children in the registry; a finished child frees its slot.
6. **A child's thread is its own** ([thread-admission.md](thread-admission.md) item 6): the lead the parent's channel posts names the child's preset, the requester, the parent (linked when its thread has a link) and the prompt's first line; the child's card, run page and follow-ups live in that thread; the parent's own admission slot is untouched.
7. **Knows its own settings — and itself.** Like every agent, its prompt carries the config block ([routing-and-config.md](routing-and-config.md) item 8) and the About block (item 11), and it maintains the status card through `update_status` — one item per child. The prompt says what a child is (a run the requester could start by hand, in its own thread, visible to the channel, under their permissions), names every preset a child can run and the limits the stage enforces (one level of depth, the fan-out cap, the parent's remaining clock), says how to work — fan out, `await_runs` every child at once, steer with `send_to_run` when the request changes, compile one answer from the write-ups, name what still runs at a cut, spawn anew rather than expect a restart of a child that ended — and forbids doing a child's job or claiming a result it did not read from `await_runs` or `get_run_status`.
8. **Steer and await — the children come back as data.** `send_to_run { id, text }` steers a live child of this run through the inbox a thread reply takes ([thread-admission.md](thread-admission.md) item 7). The tool reads the run as the requester (`RunsService.getRun` and a point `authorize`: an unknown id or one the requester may not read is `not_found`), refuses by name a run this run did not spawn (`not_child`) and one that already ended (`not_live`, with its status — nothing is sent, `get_run_status` has its reply), then `steerRun` sends as the requesting user from this run: the live agent's allowlist gate a thread reply passes (`refused (live_agent_allowlist)`), the durable copy first — its seq rides the item, the row names the sending run — then the slot that holds the child now, matched by run id so a newer run on the child's thread is never handed it; with no slot here and the ledger's copy taken, the child's durable inbox on the generation that drives it (`steered … live on another bot generation`); no slot and a refused push is `not_live`. The child drains it at its next step boundary like any follow-up, under the follow-up header, and its stream's `input` event names the requester, the parent's thread link and — as `source.run` — the parent's id; no ack is posted in the child's thread (nothing was said there to answer), and a child that finishes before its next step never reads it: a run's steer left unconsumed at the child's end is set aside, never run fresh and told to nobody. `await_runs { ids, timeoutMinutes? }` waits for the named runs — the requester's reads, so an id outside their predicate is `not_found` and waited on no further — and hands each end back as data: `ended` says what ended the wait, `note` what to do, `runs` one row per id with `status` (`completed`, `failed`, `refused`, `stopped_soft`, `stopped_hard`, `interrupted`; `running` for one still live at the cut, `elsewhere: true` when another generation drives it; `not_found`), the identity fields `list_runs` shows, the last activity line, `finalReply` wrapped as untrusted content for a finished run, and `reason` (the gate's name) for a child of this run refused after it registered, read from the spawn capability's memory once its row is gone. The decision is pure (`decideWait`): every named run terminal ends it `all_ended`; else a stop of this run, soft or hard, ends it at once (`stop`); else a follow-up in this run's own inbox (`follow_up` — the parent's next step reads it, then awaits again); else the parent's remaining wall clock less the wrap-up reserve, `RUN_DEADLINE_RESERVE_MS` — the same minute the bash tool keeps back — ends it `budget`, the children still running named in the note and left running; else the caller's `timeoutMinutes` (`timeout`), the budget named when both have passed. The wait is paced by `AWAIT_POLL_MS` (5 s): each tick re-reads the children still live through the one runs service — the registry for a child in this process, the ledger for one another generation drives, the store for one that finished — and the registry's index feed wakes it the moment a child here finishes, is sealed, discarded or evicted, so an in-process end never waits on the poll; the run's hard-stop signal ends the sleep at once, and one signal ends every early tick, so no timer outlives it. A terminal state, once seen, never changes (`ChildrenWatch`): a duplicate end frame, a stale `running` read or a row gone after a record was seen change nothing. An interrupted child is reported as `interrupted` and never restarted — a parent that still wants the work spawns a new child. Fan-in is the model's next turn — the prompt says to compile — and no new primitive. `await_runs` is side-effect free (it runs beside a turn's reads, and a resume runs it again); `send_to_run` is not, and gets the restart result at a resume, never a second push ([run-history.md](run-history.md) item 37). The runner treats a waiting call like any tool ([run-loop.md](run-loop.md) item 16).

## Roadmap (gaps)

- `[gap]` Thread stickiness in a spawned thread: the child's directive lives in no user turn of its thread, so a follow-up there after the child finished resolves to the channel's default agent; the routed preset's sticky card label ([record 0026](../../decisions/0026-capability-profiles-and-request-routing.md), slice two) would cover it.
- `[gap]` The board: `parentRunId` is on the record and the live summary; the `/runs` index does not yet draw the tree.

## Validation criteria

| Criterion | Proof |
|---|---|
| 1: `conductor` declares machine `none`, identity `none`, toolset `conductor`, 120 minutes, a turn backstop of at least 30, no built-in effort and no resident prompt; `getAgent("conductor")` resolves it | `[unit]` `src/agents/registry.test.ts::conductor agent (docs/reference/specs/agent-conductor.md)::conductor: machine none, identity none…` |
| 1: every preset names a valid identity; `conductor` is `none` beside `general` and `research` | `[unit]` `src/agents/registry.test.ts::agent registry matches the feature specs::identity declarations…` |
| 1: open by the open rule — with no boundary set every actor kind admits `conductor` exactly as `canRunAgent` decides, and it resolves its declared profile | `[unit]` `src/config.test.ts::boundaries (Scope.boundary): a scope caps, never grants::per-actor goldens…` |
| 2: the `conductor` toolset is exactly the five run tools, `web_fetch`, `update_status` and the GitHub reads; no other toolset holds a run tool; the reads are in every toolset with a tool loop; `web_search` is not in it | `[unit]` `src/tools/github.test.ts::toolset wiring::conductor holds spawn_run…`, `::reads are in every toolset with a tool loop…`, `src/tools/web.test.ts::toolset + agent wiring::gates web_search to the research and explore toolsets…` |
| 3: `spawnChild()` builds the child's message as the requesting user on the opened thread and calls `dispatch()` with `parent` set; the child's replies land in its own thread; depth, budget, fan-out (the default cap, a finished child, a configured cap, another parent's children) and an adapter without `openThread` refuse by name before anything is opened; a gate's refusal is relayed by name with the child thread's reply; a failed or throwing dispatch is `spawn_failed`; the capability spawns with the remaining clock and remembers how a registered child ended; the null capability refuses honestly | `[unit]` `src/core/dispatch/spawn.test.ts::spawnChild — the one path a child run is born through::*`, `::spawnCapabilityFor — the capability a spawning run's tools hold::*`, `::childRequestText / maxChildrenOf::*` |
| 3: end to end, the factory spied — a conductor spawns a `research` child as the requester in a thread of its own: the child runs the full pipeline with its budget clipped to the parent's remaining minutes as `parent` (the factory, the runner's def, the card, the config block), its record and live summary carry `parentRunId`, the parent's tool result names the child, and the parent's admission slot is untouched while the child holds its own; a child for a requester without `agent:run:coding` ends at the agent gate with the allowlist refusal in the child's thread and never reaches the factory; a `write` preset in a channel bounded to `read` ends at the profile gate; a conductor child's own `spawn_run` is refused `spawn_depth` and no grandchild exists | `[unit]` `src/core/dispatcher.test.ts::agent:conductor — a run that spawns child runs through dispatch()::*` |
| 3: `spawn_run` passes the request and the remaining budget to the capability and reports the child's id, thread and link; relays a refusal by name with the child thread's text; validates an unknown preset, a short budget and an empty prompt before calling anything; answers `spawn_unavailable` outside a spawning run; `RUN_TOOLS` is exactly the five and only `await_runs` is side-effect free | `[unit]` `src/tools/runs.test.ts::spawn_run — the capability, called with the run's remaining wall clock::*` |
| 4: `list_runs` lists this run's live children by default and every visible run with `scope: all`; a run in a private channel the requester is not a member of is absent for them and present for an admin; the children scope pages in full pages past newer runs until `limit` children are found; finished runs come from the store with their terminal status; no output carries a token; without a runs capability the tool says so | `[unit]` `src/tools/runs.test.ts::list_runs — the runs the requester may see::*` |
| 3: an `openThread` that throws is `spawn_failed` naming the cause with nothing dispatched; spawns issued at once are admitted one at a time, so under a cap of 1 the second is refused `spawn_fanout` | `[unit]` `src/core/dispatch/spawn.test.ts::spawnChild — the one path a child run is born through::a channel that cannot open the thread…`, `::spawnCapabilityFor — the capability a spawning run's tools hold::spawns issued at once are admitted one at a time…` |
| 3: a child that takes the boot-gap redispatch stays its parent's child — the fresh run carries `parentRunId` and the parent's clock as its boundary | `[unit]` `src/core/dispatcher.test.ts::run ledger write-through (docs/reference/specs/run-history.md item 35)::a spawned child whose thread the boot-gap map names…` |
| 4: `get_run_status` answers a live child as `running` with its activity line and a finished one with its terminal status and its final reply wrapped as untrusted content; an unknown id and a run the requester may not read are both `not_found`; a child refused at a gate after it registered answers from the spawn capability by the gate's name | `[unit]` `src/tools/runs.test.ts::get_run_status — one run as the requester may see it::*` |
| 5: `spawn.maxChildren` parses, defaults to 3, and refuses 0, a fraction, a non-mapping and an unknown key at load naming the key | `[unit]` `src/config.test.ts::spawn block (spawn.maxChildren)::*` |
| 6: `openThread` on the three channels — Slack posts the lead top-level and keys the thread by the posted `ts` with its permalink; the CLI derives `<thread>/child-<n>` and prefixes the child's output; the null channel derives the same key and logs the lead's length | `[unit]` `src/channels/slack.test.ts::SlackIO.openThread (docs/reference/specs/slack-channel.md item 11)::*`, `src/cli.test.ts::askExitCode — what the \`ask\` process exits with (the ConsoleIO channel's receipt)::ConsoleIO.openThread derives the child's key…`, `src/core/nullChannelIo.test.ts::nullChannelIO::openThread hands back a derived key…` |
| 7: the prompt names its tools, says what a child is, names every preset a child can run and the limits, says to fan out, await and compile, that an ended child is never restarted, and forbids doing a child's job or claiming an unread result | `[unit]` `src/agents/registry.test.ts::conductor agent (docs/reference/specs/agent-conductor.md)::conductor's prompt…` |
| 8: the wait decision — a terminal state, once seen, never changes (a duplicate end frame, a stale `running` read, a row gone after a record change nothing; an id the watch was not built with is dropped); every run terminal ends the wait `all_ended` ahead of anything else; a stop ends it at once; a follow-up in the parent's inbox ends it; the budget's edge is the deadline less the bash tool's reserve and ends it `budget`, named over `timeout` when both have passed; otherwise the wait runs to the earlier bound | `[unit]` `src/core/dispatch/awaitChildren.test.ts::ChildrenWatch — the children's states as the wait accumulates them::*`, `::decideWait — what ends the wait::*` |
| 8: the wait capability reads the run's own stop control and inbox and wakes on a watched child's finish, seal or removal in the registry — never on another run's end or the child's mere activity; a child already ended wakes it at once; the default sleep ends on its delay or its signal, never rejecting | `[unit]` `src/core/dispatch/awaitChildren.test.ts::waitCapabilityFor — what a waiting tool watches::*` |
| 8: `steerRun` — into a child live here: the durable copy first (its seq on the item, `fromRunId` on the row), then the child's inbox with the requester as the sender, the parent run as `from`, the parent's thread as the link, no channel handle, no reply anywhere; no slot here and the ledger's copy taken: `elsewhere`; a slot holding a different run is not the target; no slot and a refused push: `not_live`; a sender the live agent's allowlist excludes is refused before anything is pushed | `[unit]` `src/core/dispatch/admission.test.ts::steerRun — a run steers a live run through the inbox a thread reply takes::*` |
| 8: a run's steer survives the durable round trip — the row names the sending run and the follow-up read back carries `from`, so a resumed child never runs it fresh; a person's row reads back with none | `[unit]` `src/core/dispatch/admission.test.ts::followUpFromInbox — a durable inbox item back as a follow-up::a steer a run sent survives the round trip…` |
| 8: a run's steer unconsumed at the child's end is neither handed on as a fresh turn nor told after a stop — alone it settles quiet; a person's follow-up beside it settles as before | `[unit]` `src/core/dispatch/settle.test.ts::settleThread — the thread when the request is over::a steer a run sent…` |
| 8: the runner drains a run's steer like any follow-up and the `input` event's source names the sending run; the timeline reads `source.run` | `[unit]` `src/runner.test.ts::follow-up inbox (docs/reference/specs/thread-admission.md)::a follow-up a run sent…`, `src/channels/runTimeline.test.ts::createRunTimeline — grouping::passes input / answer / run_note through…` |
| 8: `send_to_run` on a live child pushes the text into its inbox as the requesting user with the parent run as `from`, the durable copy first, and says it is read at the child's next step; a child live on another generation is steered through its durable inbox; a finished child is `not_live` with its status and nothing is pushed; a run this run did not spawn is `not_child`; an unknown or unreadable id is `not_found`; a requester the live agent's allowlist excludes is refused as a thread reply would be; a child that ended during the push is `not_live`; an empty text is a string error; without the capability the tool says so | `[unit]` `src/tools/runs.test.ts::send_to_run — a parent steers a live child through the inbox a thread reply takes::*` |
| 8: `await_runs` — every named run ended returns each end with its status, a completed child's reply wrapped as untrusted content, an interrupted child as `interrupted` with nothing restarted, an unreadable id as `not_found`, with no sleep; a child ending in this process wakes the wait through the registry's end frame; the budget's edge ends it `budget` with the children still running named and left running, the sleeps poll-sized and the last clipped to the edge; `timeoutMinutes` ends it `timeout`, a budget inside the reserve ends it at once; a stop ends it `stop`, a follow-up in the parent's inbox ends it `follow_up` and leaves the follow-up for the runner; a child another generation drives is `running` elsewhere at the cut and ends from the store once its record is there; a child refused after it registered ends by the gate's name; the sleep's signal follows the run's hard stop; `ids` and `timeoutMinutes` are validated, duplicates folded; without the capability the tool says so | `[unit]` `src/tools/runs.test.ts::await_runs — the children's ends, as data, within the parent's budget::*` |
| 8: at a resume `await_runs` runs again and `send_to_run` (like `spawn_run`) gets the restart result | `[unit]` `src/core/runLedger/resume.test.ts::settlementFor — D4::the run tools settle by the same rule…` |
| 8: the runner bounds a waiting call like any tool — a hard stop mid-wait aborts it at once through the context's signal with no finale; the wait hands back at the run's wrap-up reserve with the `budget` report and the model still takes a turn to write up | `[unit]` `src/runner.test.ts::a waiting tool call (await_runs) under the runner's stop and clock::*` |
| 8: end to end, the real runner and inbox — a conductor spawns a research child, steers it and awaits it: the child reads the steer at its next step as the requester's follow-up, its record's second `input` names the parent run, and the await returns when the child's end frame lands with its reply as data; a parent awaiting a child another generation finished (a store record, no registry frame) returns from the record, an `interrupted` child comes back as `interrupted`, and nothing restarts either | `[unit]` `src/core/dispatcher.test.ts::agent:conductor — a run that spawns child runs through dispatch()::send_to_run steers a live child…`, `::a parent awaiting a child that another generation finished…` |
| 3, 6: live — a conductor spawns two `research` children as the requester into two threads in the channel, each with its own card and run page, both records carrying the parent's id | `[agent]` In Slack, in a channel the bot is in: `@switchboard agent:conductor research two things for me, each in its own child: (1) what a Durable Object is, (2) what Cloudflare Workflows are` — expect two new top-level `↳ *research* run for <you>…` messages in the channel, each with a card and a run page of its own, the conductor's card listing both children, and `runs get <child id>` (or the run page) showing `parentRunId` = the conductor's run id on both. Human-gated. |
| 3: live — the same request by a requester without `agent:run:research` (the preset restricted for the test) shows the refusal in the child's thread and the parent's tool result naming it | `[agent]` As a `config:write` holder: add `research` to `restrict.agents` in the deployed config, then as a user without the grant: `@switchboard agent:conductor research what a Durable Object is` — expect the child thread's lead followed by `🚫 You're not on the allowlist for the \`research\` agent…`, no card in the child thread, and the conductor's reply naming `agent_allowlist`; the runs index shows no research run. Clean up the restriction. Human-gated. |
| 8: live — a conductor spawns three `research` children, steers one with `send_to_run`, awaits all three and posts one compiled answer | `[agent]` In Slack: `@switchboard agent:conductor research three things for me, one child each, then steer the first to focus on pricing before compiling: (1) Durable Objects, (2) Cloudflare Workflows, (3) Cloudflare Queues` — expect three `↳ *research* run…` threads; on the conductor's run page a `send_to_run` call answered `steered: folded into the research run …` and one `await_runs` call answered `"ended":"all_ended"` with three rows; on the steered child's run page a second `↪ FOLLOW-UP` block reading the steer, attributed to the requester and the conductor's run; the conductor's one reply compiles the three write-ups. Human-gated. |
| 8: live — a bot kill during the wait resumes the parent and the await completes from the ledger | `[agent]` Start the request above, then while the conductor's card shows `await_runs` in flight: `POST /admin/crash` — expect the conductor's run page to show a `resumed` note followed by a fresh `await_runs` call (the settlement re-ran the read), the children resumed in their own threads, and the same compiled answer once they end; `runs get <conductor id>` shows one run, `completed`. Human-gated. |
| 8: live — an `interrupted` child comes back as that status and nothing restarts it | `[agent]` With `spawn.maxChildren: 1` set for the test, start `@switchboard agent:conductor research what a Durable Object is in one child, then wait for it`; while the child's card spins, kill the bot twice inside one lease (`POST /admin/crash`, wait 5 s, `POST /admin/crash`) so the child's transcript is incomplete and closes `interrupted` at the reclaim — expect the conductor's `await_runs` result to carry `"status":"interrupted"` for the child, the runs index to show no second research run, and the conductor's reply to say the child was interrupted rather than a research answer. Restore the cap. Human-gated. |
