# Load harness

The repeatable measurement of Switchboard under many concurrent runs, and the receipt every capacity change is judged by: each change is a before/after number from these commands, never an estimate. The harness measures infrastructure — the bot process, the resident, the cold sandbox fleet, the Slack card path — never a model: the end-to-end command drives the bot with a scripted provider that never thinks. One command is the exception by design: the pi driver (items 13–15) measures an alternative agent harness on real coding tasks, so it is the one command that spends model tokens — and its dry run uses the same scripted provider.

- **Code**: `src/load/aggregate.ts` (samples → per-op percentiles, refusals by reason, D10 checks, the markdown receipt), `src/load/history.ts` (run-store paging + the concurrency sweep-line), `src/load/runLoop.ts` (the staggered N-thread driver with guaranteed teardown), `src/load/reasons.ts` (refusal tokens from client errors), `src/load/residentLoad.ts`, `src/load/sandboxLoad.ts`, `src/load/e2eLoad.ts`, `src/load/cardsLoad.ts` (the four drivers), `src/load/scriptedProvider.ts` (the OpenAI-compatible scripted model, plain and streamed, + the review/coding/pi-coding/pi-review profile scripts), `src/load/piRpc.ts` (the event → run-stream home table, the per-task accumulator, the driver over the abstract transport; the framing and the transport seam are `src/core/harness/pi/protocol.ts`), `src/load/piExtension.ts` (the extension pi loads: the terminal tools and the `tool_call` hook), `src/core/harness/pi/toolRules.ts` (the tool rules by identity — the refusal preview of each spike call, the write identity's for the coding tasks and the read identity's for the review suite), `src/load/piTasks.ts` (the five representative coding tasks), `src/load/piReviewTasks.ts` (item 18: the review tasks — merged public pull requests pinned to their head), `src/load/piReview.ts` (item 18: the review framing from the registry's composer, the outcome judged as the post-step judges it, the receipt's rows), `src/load/piProcess.ts` (spawning pi: arguments, environment allowlist, config directory; the review checkout at a pull request's head; `SYSTEM.md`), `src/load/fakeSlack.ts` (the rate-limited Slack Web API stand-in), `src/execution/bindingPurge.ts` (the resident's `purge-bindings` decision), `src/channels/processMetrics.ts` (`/healthz.process`), `src/load/routeReplay.ts` (item 17: the labelled requests off the run store's records, the replay over the dispatcher's own `RouteModel` seam, the confusion table and its rendering; the compound half — `compoundExamples`, `historyCompounds`, `replayCompound`, `compoundScore`, `renderCompound`; the imperative half — `replayImperative`, `imperativeScore`, `renderImperative`), `src/load/routeCompoundFixtures.ts` (item 17: the checked-in compound set — twenty compounds with their expected part presets, five decoys), `src/load/routeImperativeFixtures.ts` (item 17: the checked-in imperative set — twenty terse imperatives, five read-only decoys, five review-shaped asks, each with the presets that count as right), `scripts/load.ts` (the entrypoint behind `npm run load`)
- **Tests**: `src/load/aggregate.test.ts`, `src/load/history.test.ts`, `src/load/runLoop.test.ts`, `src/load/reasons.test.ts`, `src/load/residentLoad.test.ts`, `src/load/sandboxLoad.test.ts`, `src/load/e2eLoad.test.ts`, `src/load/cardsLoad.test.ts`, `src/load/scriptedProvider.test.ts`, `src/load/piRpc.test.ts`, `src/core/harness/pi/protocol.test.ts`, `src/load/piExtension.test.ts`, `src/core/harness/pi/toolRules.test.ts`, `src/load/piTasks.test.ts`, `src/load/piReviewTasks.test.ts`, `src/load/piReview.test.ts`, `src/load/piProcess.test.ts`, `src/load/fakeSlack.test.ts`, `src/execution/bindingPurge.test.ts`, `src/channels/processMetrics.test.ts`, `src/load/routeReplay.test.ts` (item 17, the compound set's shape included)
- **Docs**: [how-to: run a load test](../../how-to/run-a-load-test.md)

## Behavior

1. **One sample per operation, one summary per run.** Every command records a `Sample` per operation it performs (`op`, start, duration, ok, and on failure the refusal's machine token as `reason`) and reduces them with `summarize`: per operation the count, successes, failures and p50/p95/p99/max latency — **over the successful samples only**, because a refusal answers in milliseconds and would flatter every percentile it joined; an operation with no success reports NaN, never 0. Failed samples are counted by `reason` (`unnamed` when the client threw without one). Percentiles are nearest-rank.
2. **Checks are the plan's D10 lines.** `evaluateSlo(summary, spec)` turns `<op> p<p> ≤ N ms`, `zero <reason>` and `zero failed <op>` rules into pass/fail lines carrying the actual number; a latency or zero-failures rule on an operation with no samples FAILS with `no samples` rather than passing vacuously. `zero failed <op>` is the structural form for a command whose failure reasons are open-ended (`load -- e2e`: a run's terminal status or an HTTP status), so an unanticipated reason can never pass by omission. Each command has its own spec in `scripts/load.ts`; the exit code is non-zero when any check fails.
3. **The receipt is markdown.** `renderMarkdown` writes the parameters, the per-op table, refusals by reason, each check as ✅/❌ with the actual and the limit, a `Verdict: PASS|FAIL|no checks` line, and free-text notes. Every command writes `load-results/<command>-<runId>.json` (the samples, the summary, the checks) and `.md` beside it; `load-results/` is gitignored — receipts go on the tracking issue.
4. **`load:history` reproduces the baseline.** It pages the state Worker's `POST /runs/list` newest-first with the `(finishedAt, id)` cursor, drops the provisional start-of-run tombstones (`finishedAt === startedAt`, [run-history.md](run-history.md) item 27), and sweeps the half-open `[startedAt, finishedAt)` intervals for the peak number of simultaneously live runs (overall and per UTC day) plus the duration mean and percentiles. A run finishing exactly when another starts does not overlap it.
5. **One driver, guaranteed teardown.** `runThreads` starts N threads spread evenly over the stagger window (the first at once, the last at the window's end), runs each thread's `setup → iterate… → teardown` until the hold ends or the run is aborted, and ALWAYS runs teardown — with the setup or iteration error that ended the thread — so whatever setup acquired (a pool seat, a sandbox, a card) is released. A setup failure counts and never iterates; an iteration error ends its thread and counts; a teardown that throws counts and never stops the other teardowns; an abort signal ends every thread at its next iteration boundary.
6. **The scripted provider never thinks.** `startScriptedProvider` serves `POST /chat/completions` in the OpenAI wire shape the `openaiCompat` provider parses; the answer is the script step at index = the number of tool results already in the conversation, so one server serves fifty conversations with no per-conversation state. A tool step answers one `tool_calls` entry (`call_<index>`, JSON arguments) with `finish_reason: tool_calls`; a text step answers content with `stop`; past the script's end the answer is its trailing text (`Done.` when none); a tool the request did not declare is removed from the script for that request (the step index stays the tool-result count), so each declared tool is called exactly once. The `coding` profile reads, inspects, burns CPU (`cpuBurnCommand`: one core, pure compute, `--cpu-seconds`), writes under `.load-harness/` in the worktree and answers; the `review` profile reads, inspects, burns and answers. **Neither ends with a terminal tool by default** (`submit_pr_description`, `submit_verdict`): a load run has no GitHub side effect; `terminal: true` opts in for a fixture repo.
7. **The fake Slack enforces the published limits.** `FakeSlack` answers `chat.postMessage`, `chat.update` and `conversations.replies` in Slack's shapes, refuses `chat.update` with `ratelimited` + `retryAfterS` when either budget is spent — a per-app token bucket at the Tier 3 rate (default 50 a minute) and a per-channel bucket at the coalescer's cited ~1 edit a second — and records for every accepted edit the lag from the frame's production time (`frameAt`) to acceptance, so a refused frame's lag keeps counting through its retries. `stats()` reports posts, updates, refusals, distinct cards edited, and lag p50/p95/p99/max.
8. **`load:resident` drives the bot's own client.** N `ResidentExecutor`s with thread keys `load:<runId>:<i>`, each attaching (timed as `attach`) then looping the profile: `read` README.md, `exec` a short git command, `exec-cpu` the CPU burn, `write` under `.load-harness/` (coding only; the review profile attaches read-only and never writes), a pause; teardown `release("always")` timed as `detach`. Refusals count by token (`user-pool-exhausted`, `mirror-busy`, `disk-pressure`). Guards (plan D9): the run refuses to start unless the operator `GET /status` reports exactly 0 in flight (unknown is refused); more than `RESIDENT_LOAD_MAX_THREADS` (16, one resident's pool) needs `--override`; and after the run the admin `purge-bindings` op removes the run's evicted bindings under `load:<runId>:` — a purge failure is reported in the receipt, never thrown, so the measurements always come back.
9. **`load:cards` is a simulation over the real coalescer.** The HTTP ingress `status()` handle is a no-op ([http-ingress.md](http-ingress.md) item 2), so an end-to-end run exercises zero card traffic; `simulateCards` instead runs N cards through `coalesceStatus` (the production floor, 3 s) against `FakeSlack` in **virtual time**: each card produces the dispatcher's 5 s heartbeat frames and a tool event every 7 s for the hold, then a terminal frame; the edit path mirrors the Slack adapter — one `chat.update`, on `ratelimited` wait Retry-After and re-send the same frame up to the WebClient's retry budget (10), give up after it; a retry whose frame has since been superseded is dropped as `staleDropped` (a real client would have spent it on a stale card). Deterministic and instant. **What it shows at the published limits, fifty cards, ten minutes:** edits refused, retries served first-come so most cards never get an edit at all (lag is measured over accepted frames only, so starvation hides in it — the surviving frames wait past the coalescer floor while the starved cards contribute nothing), and **terminal frames lost** — the adapter's `done` swallows the failed edit the same way, so a card past the retry budget stays a spinner forever. Lifting the limits gives every card sub-floor lag and every terminal frame: the limit, not the coalescer, is the ceiling. This is the number the plan's status budget (D3) has to fix.
10. **`load:e2e` runs the whole bot.** N threads each post `POST /ingress` synchronously (`{text, channel: "load", thread: <runId>-<i>}`, the bearer from `SWITCHBOARD_LOAD_INGRESS_TOKEN`) and record one `run` sample per request from the response's run receipt (`ok` iff `completed`; the terminal status is the reason otherwise; a transport failure is `transport`, a client timeout `timeout`). Between a thread's runs it pauses `--pause`; **after a failed request it pauses at least `failurePauseMs` (5 s)** so a dead endpoint is never hammered at full speed. While threads run, `GET /healthz` is sampled every 15 s for `inFlight` and the process metrics (item 12). The model is the scripted provider (`npm run load -- provider`, item 6) configured on the bot as an `openai-compatible` provider; the resident and sandbox Workers are whatever that bot is configured with. The receipt's check is `zero failed run` (item 2), and the run ends when the last thread does: the health sampler's sleep is raced against completion, never adding its interval to the tail.
11. **`load:sandbox` proves the cold path and measures the fleet ceiling.** N `CloudflareSandboxExecutor`s with keys `load:<runId>:sandbox:<i>`; the first command per thread (`first-exec`) is what creates the container, so its latency is the cold start or the `fleet-busy` wait ([execution.md](execution.md) item 14); then `exec` and `exec-cpu` per iteration. More than `SANDBOX_LOAD_MAX_THREADS` (25, the fleet's `max_instances` when written) needs `--override`. Sandboxes are not released explicitly; the Worker sleeps them after their idle window.
12. **`/healthz.process`.** The bot samples `process.memoryUsage()` and a `monitorEventLoopDelay` histogram (`startProcessMetrics`): `{rssMb, heapUsedMb, eventLoopLagP99Ms}`, the lag being the p99 over the window since the previous read (the histogram resets on each read, so consecutive polls read consecutive windows). Always present on the live `/healthz`; the harness reads it, and it is the only off-box view of the bot process under load.
13. **`load:pi` runs the five representative coding tasks on pi's harness.** For each task the driver puts the checkout on a branch it names (`load-pi/<task>-<runId>`, as the ship pipeline owns its children's branch), starts one `pi --mode rpc` in the checkout with every discovery off (`--no-extensions --no-skills --no-prompt-templates --no-themes`, a config directory of its own beside the receipt — `load-results/pi-<runId>-agent`, with `defaultProjectTrust: never`, kept after the run because pi's session files there are the run's own record — so neither the operator's `~/.pi/agent` nor a `.pi/` in the checkout is read) and exactly one extension loaded by path, pi's built-in tools allowlisted (`read, bash, edit, write, grep, find, ls`), and a session directory so the session file exists; then it sends `get_state` and the task's prompt and reads the stream until pi settles (`agent_settled`), the budget aborts it (`abort`, then a grace, then stdin closed), pi refuses the prompt, or the stream ends. The record per task: wall time, turns (assistant messages), pi's automatic retries, tokens and cost summed from pi's per-message `usage` (pi's catalog rates, an estimate), every tool call with its one-line summary, outcome, whether the extension's hook saw it and the refusal preview's verdict (item 14), the PR-shaped outcome — a `submit_pr_description` object that passes `parsePrDescription`, with the problems when it does not — the final text, the errors, a count per event kind, and the event kinds with no home on the run stream (`PI_EVENT_HOME`: each type pi documents maps to a `RunEvent` type or span name, or to `null`; an unknown type is reported as `unknown:<type>`). Each call is judged against the gate the hook stands for once the stream has said what became of it: `vetted` when the hook saw it before it ran; `rejected-by-pi` when pi answered the call by itself, before the hook and without running the tool — pi validates the arguments against the tool's schema before its `tool_call` hook fires (pi-agent-core `prepareToolCall`), and answers a call whose arguments fail, whose tool is not on its list, or whose assistant message the output token limit cut, with `tool_execution_start`, an error `tool_execution_end` in its own words and no hook (`piAnsweredWithoutRunning` reads those words; the record carries pi's reason as `piRejection`); `bypassed` when the hook never saw it and pi did not say it never ran — it ran, or was still open when the stream ended. The receipt's checks: every task settled, every task PR-shaped, every call pi ran also seen by the hook — none `bypassed`; the calls pi answered by itself are listed with pi's reason and never fail the check, since nothing ran. **The model key is read from the environment variable `--key-env` names (default: the variable pi itself reads for `--provider`), never from a file; without it the command refuses to start and prints exactly that variable's name.** The child's environment is an allowlist — `PATH`, `HOME`, the key under pi's own variable, pi's config directory, and the switches that keep pi off the network for everything but the model — and the key is never on the command line. pi runs in the checkout, not where the operator typed the command, and looks a relative path up there: every path the driver hands it — the config directory, the session directory, the extension — is absolute, whichever directory the driver ran from (a relative config directory is looked up in the checkout, where no models.json is, and pi knows no custom provider). Through the proxy the model's entry carries what pi's catalog says of the Claude family it fronts — `reasoning: true`, the 1M window, the 64k output ceiling — so the arm thinks at the same level and compacts at the same point as the direct arm; a bare custom entry pi runs with thinking forced off. On the Anthropic shape the entry also says which thinking payload the model takes — `compat.forceAdaptiveThinking`, from the production harness's `takesAdaptiveThinking` ([harness-pi.md](harness-pi.md) item 4): a Claude model from the 4.6 generation on takes adaptive thinking (`thinking.type: "adaptive"` plus `output_config.effort`) and the 5 family refuses the legacy budget with a 400, a model before 4.6 refuses adaptive and keeps the budget — because pi's built-in catalog says that per model and a model behind the proxy is not in it: without the flag pi sends the budget and a Claude 5 model answers 400 on its first thinking turn, every task settling in one turn with no tokens. The completions shape carries no such switch. A task pi left before its first turn (`exited`, no turn) has no stream to explain it: the receipt's notes carry pi's stderr for that task — redacted, folded onto one line, its first 300 characters — so the reason is on the receipt and not only in the JSON. Every string the record carries passes `redactSecrets`. `--print-prompt --task <name>` prints the task's prompt (with a branch of its own) and exits without starting pi or needing a key: the same words go to today's coding agent through `npm run cli -- ask` for the other arm of the comparison.
14. **The extension and the refusal preview.** The extension (`piExtension.ts`) has no imports so pi's own loader runs it as written: it registers `submit_pr_description` and `submit_verdict` with the bot's field names as plain JSON Schema (pi validates without TypeBox), and a `tool_call` hook that observes and never blocks. Its one channel back is pi's `notify` UI request, which RPC mode writes to stdout as an `extension_ui_request` prefixed `switchboard-pi `, so a notice rides the same stream as pi's events and pairs with them by `toolCallId`; a dialog request (`select`, `confirm`, `input`, `editor`) is answered `cancelled` so no extension can hang an unattended run. The refusal preview (the coding preset's tool rules, `src/core/harness/pi/toolRules.ts`) stands in for the tool-level gate the authorization spec still lists as a gap and is not the policy table: each pi tool maps to a capability-profile bundle (`bash` → `shell`; `read`/`grep`/`find`/`ls` → `files`; `write`/`edit` → `write-files`; `submit_pr_description` → `pr`; `submit_verdict` → `verdict`); a bundle outside the coding preset's reach (`verdict`, or an unknown tool) is `outside-profile`; a `bash` that pushes to a remote other than `origin` or to a branch other than the run's (`HEAD` is the run's branch, since the driver checked it out), that merges or approves a pull request by CLI or API, that reads the executor's credential store, dumps the whole environment (`env`/`printenv` alone, `export -p`, `declare -p`, a bare `set`, `/proc/*/environ`, `process.env` whole — never `env VAR=x cmd` or one variable) or expands a credential-named variable, is `refused` with the rule named; a file tool whose path resolves outside the checkout, or names the credential file, is `refused`; everything else is `allowed`.
15. **The scripted provider streams, and speaks pi's tool names.** A `stream: true` request (pi's client always streams) is answered as `text/event-stream`: per step, one chunk carrying the whole call or text as a delta, one carrying the finish reason, one usage-only chunk with no choices, then `data: [DONE]`; a request without `stream` is answered as before. The `pi-coding` profile is the coding mix in pi's vocabulary (`read`, `bash`, `bash`, `write`) and always ends by submitting a description that passes `parsePrDescription` before its text — with pi the terminal tool reaches the driver and never GitHub, so there is no side effect to opt out of. `npm run load -- provider --profile pi-coding` plus `npm run load -- pi --provider scripted --model any --base-url http://127.0.0.1:8089 --key-env <any set variable>` is the dry run: a real pi process, the whole protocol, no model spend.
16. **A refusal's token is recovered from the error a client threw, never counted by message text.** `reasonOf` (`src/load/reasons.ts`) scans the message for one of the named tokens the Workers fold into their errors — `mirror-busy`, `user-pool-exhausted`, `disk-pressure`, `fleet-busy`, `runtime-replaced`, `not-onboarded`, `needs-ref`, `not-attached`, `rate-limited` — wherever the token sits in the message; when no token is present, message fallbacks recover `fleet-busy` from the sandbox capacity error, `not-onboarded` from the resident's 404 answer, and `rate-limited` from an HTTP 429 or a "rate limit" phrase, checked before the timeout rule so a throttled request that also mentions a timeout counts as rate limiting. A timeout-shaped error (`TimeoutError`, "timed out", "timeout") is `timeout`; anything else is `unknown` — never a message fragment.
17. **`load:route` scores the request router against the requests people already typed** ([routing-and-config.md](routing-and-config.md) item 21; [record 0026](../../decisions/0026-capability-profiles-and-request-routing.md) asks for under 5 percent error before slice two). It pages the run store newest-first (`--since <date>`; `--limit N` labelled requests, default 200) and reads each record (`labelledRequests`): a run is a labelled example when a person chose its preset — `run_meta.agentSource` `directive` or `sticky` (the stamped labels: the confusion table and every bar are theirs) — or, for a record from before that stamp, on a preset other than `--default-agent` (default `general`), `unstamped`: the requester may have typed it, the thread may have carried it, or a channel or user scope's `agent` may have set it, and the record cannot say which (its `input` is directive-stripped), so these are replayed and reported apart — their count and how often the router agreed — and never enter the table or its bar; a stampless run on the default is unlabelled; never a run the router chose (its `route` event — the router is not graded against itself), a spawned or coordinator child, a schedule's run, an unknown preset, an empty text. The label is the preset the run ran on; the text is the record's `input` with every directive token hidden (`stripDirectiveTokens`), so the router sees exactly what a plain message would have been. Each request goes through the dispatcher's own `route` over `providerRouteModel` on `--provider`/`--model` (the key from the environment as for pi; `--concurrency` at a time, 4 by default; the thread's earlier directives are not on a record and so none; the allowlist is every routable preset), and the receipt (`load-results/route-<runId>.md` and `.json`) is the per-preset confusion table with accuracy (`confusionTable`, `renderConfusion`), the misroutes (run id, label, routed, reason, the text), record 0026's read-only-to-write clause as a line of its own (`readToWriteRoutes`: every request whose label's identity is `none` or `read` — `general`, `research`, `review`, `explore` — that the router sent to a `write` preset, `coding` or `ship`; a compound is not one, its parts meeting the clause as children), the unstamped line, the label sources and the skipped counts. The verdict's rows (`routeChecks`): accuracy at or above 95 percent, every request answered, read-only-to-write routes exactly 0 — a row that fails the verdict by itself, without anyone reading the table — and the two compound rows below. **The compound half**, on the same run of the command and under the same prompt — the compound form offered under `--max-parts` (default `spawn.maxChildren`'s 3), as production offers it to a requester who may run the conductor, so a labelled single the router splits is a misroute in the table: the checked-in set (`src/load/routeCompoundFixtures.ts`: twenty compound requests written in the house's voice with the presets a right split names, five decoys — one ask with several steps that reads as compound) is replayed (`replayCompound`) and scored (`compoundScore`): a compound is detected when the answer is the conductor with parts, a decoy must stay a single route, and the parts are matched to the expected presets as multisets (an undetected compound matches none of its parts; a detected one with extra or wrong parts is a miss). The checks are detection on at least 90 percent of the compounds and no decoy split; the part-preset accuracy is printed beside them with every miss (id, kind, expected, answered, reason, text). The history's `conductor` requests — every labelled request whose requester typed `agent:conductor`, left out of the singles table since the conductor is never a single route — are replayed the same way and scored on detection alone (their parts are not on the record), their count printed honestly, `no conductor request in the window` when there is none. **The imperative half**, on the same run and under the same prompt: the checked-in set (`src/load/routeImperativeFixtures.ts`: twenty terse imperatives — an order to change code or repair a failure with no file, cause or repository named, "looks like the ci failed, fix it" among them verbatim, the one real coding misroute the replay at the flip found — expecting `coding`; five read-only decoys about the same failures — "check whether ci is red", "list the failing tests" — expecting `research` or `general`; five review-shaped asks naming a pull request expecting `review`) is replayed (`replayImperative`) and scored (`imperativeScore`): an answer counts as a hit when it is one of the example's expected presets, and a look-alike (a decoy or a review-shaped ask) that reaches a write preset (`identity: write`) is counted apart. The checks are the imperatives reaching `coding` on at least 90 percent of the set and look-alikes reaching a write preset exactly 0; each kind's hits are printed beside them with every miss (id, kind, expected, routed, reason, text). The set exists because the history has almost no such requests: the router's one real coding misroute at the flip was one, so the case is scored on every run of the command. The one command besides pi that spends model tokens — one small call per request; the tests run it with a scripted decision.

18. **`load:pi --suite review` runs the review tasks on pi's harness under the read identity, and posts nothing.** The tasks (`piReviewTasks.ts`) are merged pull requests of the repository the checkout clones — three to five shapes a review is routinely handed — each carrying its number, its head at merge, its head branch and its title, and never the repository's name: the checkout's `origin` names it (`originSlug`), the receipt renders each pull request's URL from that, and a checkout whose origin does not hold a pinned head refuses to start. The suite is repeatable because GitHub keeps `refs/pull/<n>/head` after a merge. For each task the driver fetches that ref beside the base branch into a ref of its own, detaches the checkout at it and refuses to start when `HEAD` is not the pinned head — the resident attaches a review's worktree at the PR head the same way, and a review of the wrong head is the one thing the post-step never posts; then it writes pi's `SYSTEM.md` — the registry's resident review prompt composed by the run loop's own `makeSystemComposer` with the REVIEW TARGET block naming the task's pull request, head and base and the checkout as the worktree, the read identity's harness note (the five read tools, no `edit`, no `write`, `submit_verdict` the one relay), and a driver note naming the relays this driver has no bot to serve (`diff_digest`, `update_status`, `web_fetch`, the skill tools) — and starts pi with the read identity's allowlist (`PI_REVIEW_TOOLS`: pi's tools less `edit` and `write`, plus `submit_verdict`; no `submit_pr_description`), the same extension, the same budget rules with 25 minutes as the default, and the prompt a person would type (the pull request by URL). The extension's `submit_verdict` records the verdict on the stream and the driver keeps it in the receipt in place of the post: nothing reaches GitHub. Each call is previewed against the tool rules under `identity: read` (item 14's rules with the read reach: `edit` and `write` outside the profile, any push refused). The score is the post-step's judgement without the post: the verdict parsed by the same parser (`parseVerdictInput`), its head held against the pinned one by the same guard (`checkReviewedHead`), the body the post-step would have posted rendered by the same `buildReviewPostBody` — its first line on the receipt — and the read-only facts: no write tool ran (a write pi refused before the hook is listed and does not fail the row; one that ran does), the checkout's tree clean and its `HEAD` still the pinned head after the task. The receipt's checks: every task settled, every task's verdict in the house shape naming the reviewed head, every call pi ran seen by the hook, no write tool ran, the checkout untouched, no credential persisted; the notes carry one row per task with the verdict, its finding count, the head check and the write calls. Both arms run as for coding — a built-in provider with its own key, or `--through-proxy` with a run bearer. The dry run is `npm run load -- provider --profile pi-review` plus `--suite review --provider scripted …`: the `pi-review` profile reads the head and the diff with pi's `bash` and submits an `approve` whose `head` is a placeholder the scripted provider resolves off the REVIEW TARGET block in the request (`REVIEW_HEAD_PLACEHOLDER`), the way a model reads it; a request without the block leaves the placeholder and the parser drops it.

## Validation criteria

| Criterion | Evidence |
|-----------|----------|
| 1: nearest-rank percentiles; per-op stats over successful samples only; NaN with no success; refusals by reason with `unnamed` | `[unit]` `src/load/aggregate.test.ts::percentile — nearest-rank over a sorted array::*`, `::summarize — per-operation latency and refusals by reason::*` |
| 2: latency and zero-reason checks pass/fail with the actual number; no samples fails | `[unit]` `src/load/aggregate.test.ts::evaluateSlo — the D10 lines::*` |
| 3: the receipt carries parameters, the op table, refusals, checks and the verdict line | `[unit]` `src/load/aggregate.test.ts::renderMarkdown — the receipt::*` |
| 4: tombstones dropped; sweep-line peak, first instant, per-day; half-open intervals; cursor paging bounded by maxPages | `[unit]` `src/load/history.test.ts::*` |
| 5: staggered starts; teardown always runs with the ending error; setup/iteration/teardown failures counted; abort ends threads | `[unit]` `src/load/runLoop.test.ts::runThreads::*` |
| 6: step = count of tool results; the OpenAI wire shape; undeclared tools skipped; profiles end in text and carry no terminal tool unless asked; the server serves `/chat/completions` and 404s the rest | `[unit]` `src/load/scriptedProvider.test.ts::*` |
| 7: token buckets; per-channel and per-app refusals with Retry-After; lag counted from the frame's production; replies oldest-first; stats | `[unit]` `src/load/fakeSlack.test.ts::*` |
| 8: the coding mix, read-only review, named refusals past the pool, the 0-in-flight preflight, the 16-thread guard, the purge (and a failed purge reported) | `[unit]` `src/load/residentLoad.test.ts::runResidentLoad::*` |
| 8: the purge decision — evicted bindings under a whole non-production namespace only | `[unit]` `src/execution/bindingPurge.test.ts::selectBindingsToPurge::*` |
| 9: one card is well-behaved; fifty on the pre-budget client lose terminal frames and starve; fifty on the budgeted client see no refusal, paint every card, land every terminal frame, and send the same edits whether or not the limits are lifted; today's seven-card shape is clean; deterministic | `[unit]` `src/load/cardsLoad.test.ts::simulateCards::*` |
| 10: one synchronous ingress request per iteration with bearer, text and thread; run status as the outcome; transport errors; the failure pause | `[unit]` `src/load/e2eLoad.test.ts::runE2eLoad::*` |
| 11: cold start as `first-exec`, `fleet-busy` named past the seats, the 25-thread guard | `[unit]` `src/load/sandboxLoad.test.ts::runSandboxLoad::*` |
| 12: `/healthz` carries `process` when sampled; the sampler reads whole MiB and a fresh lag window | `[unit]` `src/channels/health.test.ts::healthPayload — process metrics::*`, `src/channels/processMetrics.test.ts::startProcessMetrics::*` |
| 13: LF-only framing; records and notices parsed; turns, usage and cost summed once per assistant message; tool calls with outcome, hook pairing and preview; the PR-shaped judgment through the validator; the answer, event counts and the unmapped list; provider errors, retries and unknown kinds; settle only on `agent_settled`; dialogs cancelled | `[unit]` `src/core/harness/pi/protocol.test.ts::splitJsonl…::*`, `::parsePiLine…::*`; `src/load/piRpc.test.ts::hookNotice…::*`, `::PiTaskAccumulator…::*` |
| 13: the gate's judgment per call — `vetted` when the hook saw it, `rejected-by-pi` with pi's reason when pi answered the call without running it, `bypassed` when it ran (or was still open) unseen; pi's three pre-hook answers read by name and nothing else | `[unit]` `src/load/piRpc.test.ts::PiTaskAccumulator…::judges each call against the gate…`; `src/core/harness/pi/protocol.test.ts::piAnsweredWithoutRunning…::*` |
| 13: every documented pi event type has a home or a `null` | `[unit]` `src/load/piRpc.test.ts::PI_EVENT_HOME…::*` |
| 13: the driver asks state and sends the prompt, answers dialogs, closes stdin on settle; a refused prompt is `error`; an ended stream is `exited`; the budget aborts, waits the grace and is `budget`; timers cancelled | `[unit]` `src/load/piRpc.test.ts::drivePiTask…::*` |
| 13: RPC mode with every discovery off and one extension by path, the tools allowlist, the model with its thinking suffix, a session directory, never `--api-key`; the environment allowlist with the key under pi's name; the provider → variable map; the config directory (never-trust settings, a models.json for a custom endpoint) | `[unit]` `src/load/piProcess.test.ts::*`; the line reader `src/core/harness/pi/protocol.test.ts::jsonlLines::*` |
| 13: five tasks of five shapes; each prompt carries the branch, the proof, the terminal tool, never merge, do not push | `[unit]` `src/load/piTasks.test.ts::*` |
| 14: two tools with the bot's field names as plain JSON Schema (no TypeBox symbol); the hook reports and never blocks; the submissions and the session start reach `notify` with the prefix | `[unit]` `src/load/piExtension.test.ts::piExtension::*` |
| 14: bundles and the coding reach; `outside-profile` for `verdict` and unknown tools; bash pushes, merge/approve, credentials, environment dumps; paths outside the checkout; malformed inputs refused | `[unit]` `src/core/harness/pi/toolRules.test.ts::*` |
| 15: a `stream: true` request streams three chunks per step and ends in `[DONE]`; the pi-coding profile uses pi's names and submits a valid description | `[unit]` `src/load/scriptedProvider.test.ts::chatCompletionChunks…::*`, `::the pi coding profile::*`, `::startScriptedProvider — the server::answers a stream: true request…` |
| 16: the named tokens found anywhere in the message; the `fleet-busy`, `not-onboarded` and `rate-limited` fallbacks, rate limiting before the timeout rule; `timeout`; `unknown`, never a fragment | `[unit]` `src/load/reasons.test.ts::reasonOf — the machine token inside a client's error::*` |
| Live: `load:pi` without the key refuses to start naming the variable | `[agent]` `env -u ANTHROPIC_API_KEY npm run load -- pi --checkout . --task test-gap --model x` → exit 1, stderr `the model key must be in the environment variable ANTHROPIC_API_KEY`. |
| 13: pi is handed its config directory, session directory and extension as absolute paths whatever the driver knew them as — it runs in the checkout, where a relative path would be looked up | `[unit]` `src/load/piProcess.test.ts::piEnv::hands pi an absolute config directory…`, `src/load/piProcess.test.ts::piArgs::hands pi an absolute session directory and extension path…` |
| 13: the proxy's model entry carries `reasoning: true`, the 1M window and the 64k output ceiling; the dry run's scripted model stays a bare entry | `[unit]` `src/load/piProcess.test.ts::writeAgentDir::the proxy's model entry says the model can think…`, `src/load/piProcess.test.ts::writeAgentDir::the dry run's scripted model stays a bare entry…` |
| 13: on the Anthropic shape the proxy's model entry asks a Claude 5 model for adaptive thinking and a Claude model before 4.6 for the budget payload; the completions shape carries no Anthropic compat | `[unit]` `src/load/piProcess.test.ts::writeAgentDir::the proxy's model entry asks a Claude 5 model for adaptive thinking…`, `src/load/piProcess.test.ts::writeAgentDir::the proxy's model entry asks a Claude model before 4.6 for the budget payload…`, `src/load/piProcess.test.ts::writeAgentDir::the completions shape through the proxy carries no Anthropic compat…` |
| 13: a task pi left before its first turn carries pi's stderr in the receipt's notes — redacted, one line, its first 300 characters, `(empty)` when there was none; a task that reached a turn or ended any other way adds no note | `[unit]` `src/load/piProcess.test.ts::earlyExitNote::*` |
| 13: `--through-proxy <bot base url>` makes the bot's model proxy pi's one provider (`switchboard`, the Anthropic messages shape, or the completions shape under `<base>/v1` with `--shape openai`) and reads a run bearer from the harness's own variable; the models file names the shape asked | `[unit]` `src/load/piProcess.test.ts::writeAgentDir::names the Anthropic messages shape for the bot's model proxy when asked…` |
| Live: `load:pi` through the bot's model proxy — the five tasks with a run bearer as the only key, every call metered on the borrowed run's page ([harness-pi.md](harness-pi.md)) | `[agent]` Start a long read-only run to borrow a bearer from (`agent:explore` on any repository: a 120-minute wall clock and a 720-turn guard), read its id from the card's Live run link; `POST $SWITCHBOARD_BASE_URL/admin/model-proxy/bearer` with `authorization: Bearer $SWITCHBOARD_INGRESS_TOKEN` and `{"runId":"<id>"}` → `201` with `bearer` and `model`; `export SWITCHBOARD_PI_MODEL_KEY=<bearer>`; in a scratch clone of this repository `npm run load -- pi --checkout <clone> --task all --through-proxy "$SWITCHBOARD_BASE_URL" --model <the bearer's bare model id>` → `load-results/pi-*.md` with five `task` rows `settled` and `PR-shaped: yes`, the borrowed run's page showing one `model.turn` per pi call with token counts, the bot's log carrying `[model-proxy] run=<id> turn=…` lines and no body; compare tokens and wall time against the direct-key arm's receipt. |
| Live: `load:pi` dry run — a real pi over the whole protocol with no model spend | `[agent]` `npm run load -- provider --profile pi-coding --cpu-seconds 2` in one shell; in another, in a scratch clone, `SWITCHBOARD_PI_MODEL_KEY=dry npm run load -- pi --checkout <clone> --task test-gap --provider scripted --model any --base-url http://127.0.0.1:8089 --pi <pi bin>` → `load-results/pi-*.md` with one `task` row `settled`, `PR-shaped: yes`, tool calls `read, bash, bash, write, submit_pr_description` all hook-paired, no refusal, verdict PASS. |
| Live: `load:resident` at N = 16 against a quiet resident produces a receipt, leaves no bindings behind, and refuses to start while the resident has work in flight | `[agent]` In a quiet window: `npm run load -- resident --resource repo:<slug> --threads 16 --hold 120 --cpu-seconds 20` → `load-results/resident-*.md` with attach/read/exec/exec-cpu/write/detach rows and `purge: 16 binding(s) removed`; `/debug threads` shows no `load:` keys afterwards. Start a real run on the same repo and re-run → `refusing to start: … op(s) in flight`. |
| Live: `load:sandbox` at N = 1 proves the current cold path | `[agent]` `npm run load -- sandbox --threads 1 --hold 30 --cpu-seconds 5` → `first-exec` ok, no refusals. |
| Live: `load:e2e` against a local bot on the scripted provider completes runs with no model spend | `[agent]` `npm run load -- provider --profile coding --cpu-seconds 10` in one shell; a bot started with a config naming `providers.scripted: {type: openai-compatible, baseUrl: http://127.0.0.1:8089}` and `defaults.models.coding: scripted/any` in another; `npm run load -- e2e --ingress-url http://127.0.0.1:8080/ingress --healthz-url http://127.0.0.1:8080/healthz --text "agent:coding in <slug>: load harness" --threads 8 --hold 120` → every `run` sample `completed`, `healthz samples` with rss and lag in the notes. |
| 17: which records are labelled and how — a directive or sticky stamp, `unstamped` for a record without one on a non-default preset, the default unlabelled, never a routed run, a child, a schedule's run, an unknown preset or an empty text; the text loses every directive token | `[unit]` `src/load/routeReplay.test.ts::labelledRequests — which finished runs are labelled examples, and the label hidden from the text::*` |
| 17: the replay marks each answer correct or not, keeps the order, times the decision; no route is never correct; no requests → no call | `[unit]` `src/load/routeReplay.test.ts::replayRoutes — the router asked about each request, in order::*` |
| 17: the confusion table — one row per preset in table order, per-label and overall accuracy, no route as `(none)`, misroutes in order, a label outside the list still rowed; the rendering with the accuracy line and each misroute | `[unit]` `src/load/routeReplay.test.ts::confusionTable + renderConfusion — the arithmetic and the misroutes::*` |
| 17: `readToWriteRoutes` counts a read-only label (identity `none` or `read`) routed to a write preset and nothing else — not a write label to write, a read label to read or to the conductor, or no route; `routeChecks` names the seven rows and a clean run passes them all; one read-only label routed to `coding` fails the read-only-to-write row and the verdict even when the accuracy bar still passes | `[unit]` `src/load/routeReplay.test.ts::readToWriteRoutes + routeChecks — the verdict's rows::*` |
| 17: the checked-in imperative set is twenty terse imperatives expecting the write preset, five decoys expecting `research` or `general`, five review-shaped asks expecting `review`, every preset a row of the table, every id unique, every text short, the replay's one real coding misroute on it verbatim | `[unit]` `src/load/routeReplay.test.ts::the checked-in imperative set (src/load/routeImperativeFixtures.ts)::*` |
| 17: the imperative replay and score through the real `route()` parse — a router that reads the rule hits every imperative, sends no look-alike to a write preset and both rows pass; a keyword router hits the imperatives but sends decoys to `coding` and the look-alike row fails; a miss is an answer outside the expected presets, a write answer on a look-alike is counted apart, no route is a miss and never a write, order kept and decisions timed, an empty set is NaN; the rendering names the rates and every miss | `[unit]` `src/load/routeReplay.test.ts::the imperative set through route() over a scripted model::*` |
| 17: the checked-in compound set is twenty compounds of two or more parts and five decoys of one, every preset a row of the router's table and never `ship` or `conductor`, every id unique; the fixtures are tagged as the checked-in source and the history's conductor requests taken with their parts unknown | `[unit]` `src/load/routeReplay.test.ts::the checked-in compound set (src/load/routeCompoundFixtures.ts)::*` |
| 17: the compound replay and score — a perfect router detects every compound with its parts and splits no decoy; a compound left single, a decoy split and a part on the wrong preset are misses and count against detection and the parts, in replay order and timed; a history request scores detection alone; no route is not detected, extra parts match only the expected count, an empty set is NaN; the rendering names the rates and every miss | `[unit]` `src/load/routeReplay.test.ts::replayCompound + compoundScore + renderCompound — the split scored::*` |
| Live: `load:route` over the production run store on the deployment's fast model — the singles table, the compound set and the imperative set in one receipt | `[agent]` `SWITCHBOARD_STATE_WORKER_URL=… MEMORY_TOKEN=… ANTHROPIC_API_KEY=… npm run load -- route --since <date> --limit 200 --provider anthropic --model <the deployment's defaults.models.general id>` → the receipt's compound lines: `compound: detected N/20 (…), decoys split 0/5, part presets …` and the history line with the conductor count, the two compound checks passing; its imperative line: `imperatives: N/20 to coding (…); look-alikes to a write preset 0/10 (…)`, the two imperative checks passing; posted on the unit's board item. Human-gated. |
| Live: `load:route` over the production run store on the deployment's fast model | `[agent]` `SWITCHBOARD_STATE_WORKER_URL=… MEMORY_TOKEN=… ANTHROPIC_API_KEY=… npm run load -- route --since <date> --limit 200 --provider anthropic --model <the deployment's defaults.models.general id>` → the receipt's table and misroutes posted on the U30 board item; the accuracy check against 95 percent is the go/no-go for `routing.auto` in production. |
| 18: the review tasks are three to five pull requests, distinct by name and number, each pinned to a full head sha on `main` with its head branch and carrying no repository name; a task is found by name; the URL is rendered in whichever repository the checkout's origin is | `[unit]` `src/load/piReviewTasks.test.ts::PI_REVIEW_TASKS::*` |
| 18: the repository is read off the checkout's origin — the https and ssh forms with or without `.git`; anything else is refused | `[unit]` `src/load/piProcess.test.ts::parseGithubSlug::*` |
| 18: the system prompt is the registry's resident review prompt composed as a review run composes it — the REVIEW TARGET block with the task's coordinates in the checkout's repository, the read identity's harness note naming the relayed verdict tool, the driver's note on the absent relays; the prompt is the pull request by URL | `[unit]` `src/load/piReview.test.ts::reviewSystemPrompt::*` |
| 18: a task is judged as the post-step would judge it — a house-shaped verdict naming the reviewed head renders the `LGTM:` body with its findings, `request_changes` the `Changes requested:` line, no verdict the no-verdict line and a problem; another head or none fails the head check the guard would fail; a rejected verdict is not the house shape; every write tool asked for is listed by call with the gate's judgement | `[unit]` `src/load/piReview.test.ts::reviewOutcome — one task judged as the post-step would judge it::*` |
| 18: the receipt's rows — every row passes for a settled task with a house-shaped verdict on the reviewed head, every call vetted, nothing written and the checkout untouched; an unsettled task, a verdict off the shape or the head, a bypassed call, a write that ran, a dirtied or moved checkout each fail their row naming the task; a write pi refused before the hook does not fail the write row | `[unit]` `src/load/piReview.test.ts::reviewChecks — the receipt's verdict rows::*` |
| 18: the review child's allowlist is the read identity's plus the verdict tool alone; the checkout fetches the pull request's head into a ref of the driver's own beside the base branch and detaches at it | `[unit]` `src/load/piProcess.test.ts::piArgs::the review child's allowlist is the read identity's — pi's tools less edit and write — plus the verdict tool alone`, `src/load/piProcess.test.ts::the review suite's checkout::*` |
| 18: the `pi-review` profile reads the head and the diff with pi's bash, submits a house-shaped verdict whose head is the placeholder, and ends with text; the placeholder is resolved off the REVIEW TARGET block in the request, plain and streamed, and left — then dropped by the parser — without one | `[unit]` `src/load/scriptedProvider.test.ts::the pi review profile::*` |
| Live: `load:pi --suite review` dry run — a real pi over the whole protocol under the read allowlist with no model spend | `[agent]` `npm run load -- provider --profile pi-review --cpu-seconds 1 --port 8091` in one shell; in another, in a scratch full clone, `SWITCHBOARD_PI_MODEL_KEY=dry npm run load -- pi --suite review --checkout <clone> --task docs-line --provider scripted --model any --base-url http://127.0.0.1:8091` → `load-results/pi-review-*.md` with one `task` row `settled`, verdict `approve, 1 finding(s)`, head `reviewed`, write calls `none`, checkout `untouched`, tool calls `bash, bash, submit_verdict` all vetted, verdict PASS. |
| Live: `load:pi --suite review` through the bot's model proxy — the review tasks with a run bearer as the only key, verdicts recorded and posted nowhere ([harness-pi.md](harness-pi.md) item 10) | `[agent]` Borrow a bearer as the through-proxy row above does; `SWITCHBOARD_PI_MODEL_KEY=<bearer> npm run load -- pi --suite review --checkout <clone> --task all --provider anthropic --model <id> --through-proxy <bot base url>` → every task `settled`, every verdict in the house shape naming the reviewed head, every call vetted, `no write tool ran`, the checkout untouched, no credential persisted, verdict PASS; the pull requests carry no new comment; the borrowed run's page carries the `model.turn` spans. The direct-key arm is the same command without `--through-proxy` and with `--key-env`. |
