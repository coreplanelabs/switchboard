# Temporary PR image validation receipt

This is a temporary validation receipt for the [published PR image live acceptance procedure](specs/pr-images.md#live-acceptance--deployment-gate) and is not intended to merge.

The test PR description records the results for the existing synthetic light and dark captures. Both thread attachments succeeded, but public publication returned HTTP 400 for each, so no public URLs were available to embed. This docs-only probe makes no production behavior change.
